# 视频号加热链接(微信推送数据接收服务)

> 2026 Q1 接的一个"看起来简单,其实有不少坑"的需求。
> 表面上是"写个 HTTPS POST 接口接收数据",实际要考虑:**微信 3s 超时 + 百万级数据洪流 + 去重 + 对账 + 多客户隔离 + 实时看板**。
> 我是这个需求的**主力**,从接口设计到上线 2 周。

---

## 一、一句话讲清楚是什么

视频号的"加热"是官方的广告投放能力,投放之后微信会产生**曝光/扣费/广告付费**三类数据事件。

官方不提供主动查询接口,而是**"微信推给你"**:
- 我方开发一个 HTTPS POST 监测链接
- 填到视频号加热后台
- 微信把实时数据往这个链接推,我方**3 秒内必须 200 响应**
- 数据接收 → 去重 → 入库 → 看板 → 推送代投方

相当于做一个**面向微信的 Webhook 接收站**。

---

## 二、需求背景(面试开场)

### 业务诉求
- 阅文漫剧在视频号投放加热,要看**实时曝光 / 扣费 / 广告付费**
- 外部代投方(瓯源等)要能看到自己的投放数据 → 决定是否追投
- 以前靠运营每天手动下载后台 Excel,**T+1 滞后**、**数据量大时漏数**

### 技术挑战
- 微信推送**要求严格**:HTTPS / 3s 响应 / 域名备案 / 必须 POST
- 数据量**虽不算大但不稳定**:单客户日均几十万条事件,大促期间峰值 TPS 几百,需要为**突增 5-10x** 预留设计
- **多客户隔离**:目前只有瓯源 1 家,但产品明确要求可扩展成 N 家,每家看自己的数据
- **金额精度**:`action_value` 是 10⁻⁶ 元(微分),换算要严谨,不能丢精度
- **去重**:微信可能重推,`unique_id` 必须强去重

### 我的定位
后端主力。协作:
- 👆 对接 **视频号官方推送协议**(看文档 + 实测字段)
- 👇 对接 **大数据看板**(曝光/消耗/ROI 实时展示)
- ↔️ 协作 **产品**(多客户模型)、**运营**(字段口径)、**运维**(HTTPS 证书 + 域名备案)

---

## 三、核心架构设计

### 总体链路
```
[微信服务器]
   │ HTTPS POST (日常几十 TPS,大促峰值 ~500 TPS)
   ↓
[Nginx 入口 / TLS 终止]
   │
   ↓
[Spring Boot 接收服务 /wechat/drama/heat]
   │ ① 极简校验(timestamp/签名/客户)
   │ ② 立即进内存队列 + 返回 200
   ↓
[后台异步消费线程]
   │ ③ 批量去重(Redis BloomFilter + SETNX)
   │ ④ 批量写 MQ(TDMQ)
   ↓
[消费者 Worker]
   │ ⑤ 落 MySQL 明细 + 聚合表
   ↓
[看板 API / 推送服务]
   │ ⑥ 实时大盘 / 企微消息通知代投
```

**核心思想**:**快进快出**,接口层**只做接收**,不做业务。
即使当前量不大,也按"为未来峰值 5-10x 预留"的标准设计,避免上线后量一涨就得推倒重写。

---

## 四、模块拆解(面试能讲深的几块)

### 模块 1:3 秒超时下的"极速返回"设计

#### 微信硬性要求
微信推送:**3 秒没收到 200 就判定你挂了,会重试 + 降低推送频率**,严重的会**停止推送**。

**重点**:虽然当前日常流量只有几十 TPS,但如果在接口里做"解析 → 去重 → 写 DB",**任何一次 Redis 抖动 / DB 慢查 / GC 停顿**都可能超 3s。再加上大促峰值可能飙 5-10 倍,必须从一开始就按"最坏情况"设计。

可能超时的场景:
- Redis 抖动(2ms → 20ms)
- DB 慢查(5ms → 500ms)
- 高峰期线程池被打满,新请求排队
- Full GC 停顿几百毫秒

#### 解决方案:Receive-then-Process
```java
@PostMapping(value = "/wechat/drama/heat", consumes = "application/json")
public ResponseEntity<?> receive(@RequestBody byte[] raw, HttpServletRequest req) {
    long ts = System.currentTimeMillis();
    // ① 最快校验:来源 IP 白名单 / 签名头(极少 CPU 消耗)
    if (!verifySignature(req)) {
        return ResponseEntity.ok(RESP_OK);  // 故意也返回 200,让微信别重试
    }
    // ② 原始字节直接进本地 Disruptor 环形队列(无锁、微秒级)
    boolean ok = ringBuffer.offer(new RawEvent(raw, req.getHeader("X-Client"), ts));
    if (!ok) {
        // 队列满(极端情况):落磁盘兜底文件,后台补偿任务读取
        fallbackFileWriter.append(raw);
    }
    // ③ 立即返回,整个处理 < 1ms
    return ResponseEntity.ok(RESP_OK);
}
```

- 用 **Disruptor 无锁环形队列**,P99 < 500μs,入队几乎零失败
- 不解析 JSON(解析放到后台),减少反序列化开销
- **队列满兜底**:顺序写磁盘文件,比 MQ 更快,事后补偿入库
- 接口**总是返回 200**(哪怕签名错),避免微信认为我方挂了重推,靠后台审计异常请求

**实测效果**:日常流量下 P99 约 **30-50ms**;大促峰值 ~500 TPS 下 P99 稳定在 **100ms 以内**,远低于 3s 红线。压测模拟 10 倍峰值也能撑住。

---

### 模块 2:去重设计(Redis BloomFilter + SETNX 双层)

#### 为什么要严格去重
微信推送**不保证 exactly-once**,会:
- 网络抖动时重推
- 我方 200 延迟时再推一次
- 实测偶发重复,虽然占比不高(~1%),但累计一段时间数据也会失真

重复入库 = 曝光数虚高、扣费金额重算,直接给运营决策造成误导。

#### 两级去重

**第一层:Bloom Filter(快速过滤 99%)**
```java
// 日 BF,每天 0 点滚动新 key(过期 48h)
String bfKey = "heat:bf:" + LocalDate.now();
boolean mayExist = bloomFilter.mightContain(bfKey, uniqueId);
if (!mayExist) {
    bloomFilter.put(bfKey, uniqueId);
    return false;  // 一定没重复
}
```
- 使用 Redis 4.0+ `RedisBloom` 模块
- **滚动淘汰**:每天一个新 BF,保留 2 天,既能去重又不会无限增长

**第二层:Redis SETNX(精确兜底 0.1% 疑似)**
```java
if (mayExist) {
    // 疑似重复,用 SETNX 精确判断(TTL 48h)
    String lockKey = "heat:dup:" + uniqueId;
    Boolean newSet = redisTemplate.opsForValue()
        .setIfAbsent(lockKey, "1", Duration.ofHours(48));
    if (Boolean.FALSE.equals(newSet)) {
        return true;  // 确认重复
    }
}
```

**第三层:DB 唯一索引兜底**
```sql
CREATE UNIQUE INDEX uk_unique_id ON wechat_heat_event(unique_id);
```
Redis 故障时,DB 层抛 `DuplicateKeyException`,Worker 捕获后只记日志不报错。

> **卡点**:一开始我只用 DB 唯一索引,上线后发现偶发 `IntegrityException`,虽然量不大但日志很吵,也占 DB 连接。后来补了 BF 和 SETNX 前置拦截,DB 层重复插入基本降到 0。

---

### 模块 3:金额精度处理(10⁻⁶ 元踩的坑)

#### 微信的金额字段
```json
{ "action_value": 1234567 }  // 单位: 微分(10⁻⁶ 元)
// 实际金额 = 1234567 / 1000000 = 1.234567 元
```

#### 常见错误写法
```java
// ❌ 踩过坑的版本:用 double
double amount = data.getLong("action_value") / 1000000.0;  // 精度丢失

// ❌ 第二版:入库前 round 2 位
BigDecimal amt = BigDecimal.valueOf(action_value).divide(BigDecimal.valueOf(1000000), 2, RoundingMode.HALF_UP);
// 看似 OK,但一天百万条数据求和后,累计误差能差好几块钱
```

#### 正确做法:全程用微分,展示时才换算
```java
// DB 字段直接存 long(单位:微分)
@Column(name = "action_value_micro") private Long actionValueMicro;

// 聚合查询用 SUM(long) —— 精确
SELECT SUM(action_value_micro) FROM wechat_heat_event WHERE client_id = ?;

// 前端展示层才除 1000000,带 6 位小数,最后 round 到 2 位
public String toYuan(long micro) {
    return new BigDecimal(micro)
        .divide(BigDecimal.valueOf(1_000_000), 6, RoundingMode.HALF_UP)
        .setScale(2, RoundingMode.HALF_UP)
        .toPlainString();
}
```

> 财务对账时,**一分不差**。这个点 PM 和财务都很认可。

---

### 模块 4:多客户隔离 + 灰度能力

#### 需求
- 当前只有瓯源 1 家代投,产品明确以后会有 N 家
- 每家的加热后台配置的监测链接**必须不同**,不能让 A 家的数据流到 B 家
- 灰度阶段,某些客户**只接收不入库**(观察数据),不影响线上

#### 设计

**方案 A(放弃)**:每个客户一个独立 URL `/wechat/drama/heat/oy`、`/wechat/drama/heat/xx`
→ 灵活但难维护,新增客户要改 Controller

**方案 B(采用)**:URL 带 `clientKey` 参数 + 签名验证
```
https://our.domain.com/wechat/drama/heat?c=oyuan&sig=xxx
```
- `c`:客户 key(建表映射到 `client_id`)
- `sig`:我方发给客户的独立密钥,防止随便往这个接口打垃圾数据

```java
String clientKey = req.getParameter("c");
ClientConfig cfg = clientConfigCache.get(clientKey);  // 本地 Caffeine 缓存,5min
if (cfg == null || !cfg.isEnabled()) {
    return RESP_OK;  // 静默拒绝,不告诉对方配置错了
}
if (cfg.isShadow()) {
    // 灰度模式:只记审计日志不入业务库
    shadowLogger.log(raw, clientKey);
    return RESP_OK;
}
ringBuffer.offer(new RawEvent(raw, clientKey, ts));
```

**灰度开关**:通过 Apollo 配置中心动态改,不用重启。某个客户出问题立刻切回 shadow。

---

### 模块 5:实时看板 + 企微推送

#### 看板分层
| 时效 | 引擎 | 典型查询 |
|------|------|---------|
| 秒级 | Redis ZSet | 过去 1 小时每分钟曝光/消耗(客户实时追投判断) |
| 分钟级 | MySQL 聚合表 | 今日/昨日聚合,按剧、按时段 |
| T+1 | MySQL 明细 | 月度财务对账 |

写入链路:
```
Worker 消费 MQ → 并行写:
    ├── MySQL 明细(用于对账)
    ├── MySQL 聚合表(定时任务每分钟 rollup)
    └── Redis ZSet 实时桶(score=timestamp, value=event_id)
```

#### 企微推送触发
和产品确认的触发规则:
- **每小时**:聚合一次发到代投方企微群("瓯源-视频号加热"),包含本小时曝光/消耗/ROI
- **异常**:消耗突增 > 200%(相比前小时平均) → 立即推送
- **每日**:早 9 点推送昨日日报

实现用**XXL-Job 定时任务 + 企微机器人 Webhook**:
```java
@XxlJob("heatHourlyReport")
public void hourlyReport() {
    List<ClientConfig> clients = clientCfgService.listEnabled();
    for (ClientConfig c : clients) {
        ReportData d = metricsService.lastHour(c.getClientId());
        wecomBot.send(c.getWebhookUrl(), buildMsg(d));
    }
}
```

---

## 五、三个最坑的技术点

### 坑 1:微信推送 IP 段未文档化
官方文档没给推送的 IP 白名单。我第一版用 `X-WeChat-Signature` 签名验证,但:
- 微信那头**没有"约定签名算法"的规范**,实测根本没这个 Header
- 最后用**请求参数里的 `timestamp` + 客户密钥 HMAC-SHA256 自验**,每个客户发自己的密钥
- IP 白名单从线上采样统计出来,加到 Nginx 层做软过滤(不严格拦截,避免 IP 变动导致漏数)

### 坑 2:首次上线当天,推送被微信临时降频
上线当天流量其实不大(日常几十 TPS),但我第一版接口直接做"接收 → 解析 → Redis 去重 → 写 DB",个别请求因 Redis 抖动走到了 1.5s+,微信侧判定响应慢,**临时把我们的推送频率降到了原来的 30%**,部分事件出现几分钟延迟。

应急处理:
1. 临时把 Nginx `proxy_read_timeout` 调到 5s(缓解微信超时判定)
2. 接口内去掉所有非必要操作,只做"接收 + 入队 + 返回"
3. 把原来同步调 Redis 改成 Disruptor 本地入队,Redis 操作挪到后台线程

第二天推送频率恢复正常。**教训**:接口 SLA 要按"最坏情况"设计,不能因为日常量小就贴着 3s 红线写代码,任何一次抖动都可能触发降级。

### 坑 3:`unique_id` 偶发 null
微信个别事件推过来 `unique_id` 为空,导致 BF/SETNX 都失效,数据重复入库。

修复:对于 `unique_id` 为 null 的事件,用 `md5(action_ts + drama_id + action_type + action_value)` 自算一个代理 ID。加到 DB 字段和索引里,不影响统计。

---

## 六、亮点提炼(面试数字)

1. **接口稳定性**:日常 P99 30-50ms,大促峰值 ~500 TPS 下 P99 < 100ms,远低于微信 3s 要求;压测模拟 10x 峰值仍稳定
2. **去重准确率**:三层去重(BF + SETNX + DB 唯一键),DB 层重复插入降到 0
3. **运营效率**:运营从"每天 T+1 下载 Excel"升级为"实时看板",数据滞后从 24h → 1min
4. **可扩展性**:多客户隔离设计 + Apollo 动态配置,**新增客户 0 改代码,5 分钟上线**

---

## 七、技术栈

- **接入层**:Nginx(TLS 终止 + IP 软白名单 + 限流)
- **应用层**:Spring Boot + Disruptor 环形队列 + Caffeine 本地缓存
- **去重**:Redis BloomFilter + SETNX + DB 唯一索引
- **消息**:TDMQ(异步削峰)
- **存储**:MySQL(明细 + 聚合 + 对账)+ Redis ZSet(秒级实时)
- **监控**:Prometheus 埋点(QPS/P99/去重命中率/MQ 堆积)+ 企微告警

代码量约 **2500 行**(含压测脚本和 mock 推送工具)。

---

## 八、面试追问 + 准备好的答案

### Q1:为什么不直接用 MQ 接收,用 Disruptor 干啥?
MQ 写入本身有网络 IO(~1-3ms),高峰期偶发 >10ms,贴 3s 超时容易危险。Disruptor 是**进程内无锁队列**,入队 P99 < 500μs,几乎 0 失败。MQ 放到后台消费环节,天然削峰。

### Q2:如果 Disruptor 队列满了怎么办?
`ringBuffer.offer()` 返回 false 时,走**本地顺序追加写磁盘文件**(比 MQ 更快),后台有个补偿任务每分钟扫这些文件补录到 MQ。文件丢了就丢了,是**容灾兜底**而不是主路径。

### Q3:Redis BloomFilter 怎么用?Java 侧怎么调?
公司 Redis 装了 `RedisBloom` 模块(Redis 4.0+),通过 `BF.ADD` / `BF.EXISTS` 命令操作。Java 侧用 JedisCluster + 自定义命令封装。每天凌晨新建一个 BF key,保留 2 天轮转,避免无限膨胀。

### Q4:金额用 long 存微分,聚合超过 Long.MAX_VALUE 吗?
微信加热单条最大金额也就几百元,$10^6$ 微分/元,单条 ~$10^8$ 微分。Long 最大 $9.2 \times 10^{18}$,远不会溢出。监控里仍加了"单日消耗 > 1e16 告警"兜底,防御性编程。

### Q5:灰度怎么回滚?
两套机制:
- Apollo 动态开关:把 `client.shadow=true` 改一下 5s 生效,线上停写业务库
- **流量录制回放**:shadow 模式下虽然不入库,但所有原始请求都存 COS,回滚后可以重放进新版本验证

### Q6:微信推过来的数据假了怎么办?(有人伪造请求)
- HMAC 签名验证(每个客户独立密钥)
- Nginx 软 IP 白名单
- 对单一 `client_id` 的 TPS 限流(突增 > 10x 历史基线 → 报警人工介入)
- 审计日志留痕所有原始请求,事后可追溯

### Q7:当前 TPS 其实不高,为什么要上 Disruptor 这么重的方案?
诚实讲:当前流量不大,确实**同步写 Redis + DB 也能跑**。但我有两个考虑:
1. **微信 3s 超时是硬约束**,任何一次 Redis/DB 抖动都可能触发降频,上线后复盘也印证了这点
2. **未来客户会增加**(产品口径 N 家),今年做好解耦,明年上量不用重写。按未来 5-10x 流量预留设计,成本就是多写两三百行代码

如果面试官继续追问"过度设计?",我会承认:磁盘兜底这块确实偏保守,真实场景下队列满的概率极低。这是**为最坏情况买的保险**。

---

## 九、如果让我重做

1. **接入 Pulsar / Kafka 的 Webhook 模式**:直接让 MQ 做接入,跳过 Spring Boot 层,延迟更低
2. **推送协议校验更严**:和视频号官方推动签名标准化(目前靠自协商)
3. **量上来后**:把 Disruptor 换成 Flink,实时窗口计算替代聚合表定时 rollup,延迟能到秒级
4. **多租户 SaaS 化**:把这个服务抽出来做成平台,视频号/抖音/快手的推送都能接

---

## 十、一句话总结(开场用)

> "这个是 2026 年初我做的视频号加热数据接收服务,本质是一个面向微信的 Webhook 接收站。
> 日常量不大(几十 TPS),但微信侧 3s 超时是硬约束,一次抖动就可能被降频。
> 技术含量的点是 **3 秒超时下的极速返回设计**(Disruptor + 异步处理)、**三层去重**(BF + SETNX + DB)、**金额精度**(全程 long 存微分)。
> 日常 P99 30-50ms,峰值 ~500 TPS 下也稳定在 100ms 以内,运营数据滞后从 T+1 降到 1 分钟内。"

# 分销后台 C 端作品管理(新业务线 + 异步下载)

> 2026 Q1 做的一个纯 C 端业务改造。接入抖音端原生/快手端原生两条新业务线,重构作品管理页面,加**异步批量下载**和**任务中心**。
> 涉及面不算大,但**异步任务 + 大文件打包 + 权限分发 + 防盗**这套组合拳,是面试能讲细节的好素材。
> 我是这个需求的**主力**,1.5 周上线。

---

## 一、一句话讲清楚是什么

分销平台原本只支持"微信小程序免费链路"一条业务线,现在要接入**抖音端原生**和**快手端原生**。
每个业务线都有:
- 作品列表(带筛选排序)
- 剧集详情 + 分页播放器
- **批量下载**(素材下载,最多下 70% 集数,打包 zip)
- **任务中心**(追踪异步下载任务)

核心是让分销商能在我方后台**挑剧 → 试看 → 下载素材 → 去投流推广**,闭环起来。

---

## 二、需求背景(面试开场)

### 业务诉求
- **业务扩张**:从"微信小程序"单条线扩到"抖音原生"、"快手原生"三条线,每条线规则不完全一样
- **素材获取**:以前分销商要素材只能找运营要,人工发,**效率极低**;要自助式下载
- **防盗版**:素材不能被**全集搬运**,只给 70% 集数 + 法律声明
- **母子账号**:企业号下挂多个子账号,有些子账号直接看父账号的全部作品(不用分配渠道)

### 技术诉求
- **多业务线共用一套代码**,不能每条线写一份 Controller/Service
- **批量下载大文件**:一部剧 70 集,单集 200MB,一次下载可能 **14GB+**,不能同步打包(会超时、OOM)
- **任务中心**要能实时刷状态(进行中 → 完成/失败),**失败要可重试**
- 播放器要支持**分页**(单剧 100+ 集不能一次查),且每集能单独下载

### 我的定位
后端主力。对接:
- 👆 **内容中心**(剧集元数据的上游)、**COS**(视频文件存储)
- 👇 **前端**(作品列表 + 播放器 + 任务中心)
- ↔️ **产品**(三条业务线字段差异)、**运营**(分类枚举、置顶规则)

---

## 三、核心设计思路

### 3.1 多业务线抽象:策略模式 + 配置驱动

原代码里微信小程序这条线是硬编码的,新加抖音/快手不能再 copy-paste。我重构成:

```java
public enum BizLine {
    WX_MINI("wx_mini", "微信小程序"),
    DOUYIN_NATIVE("dy_native", "抖音端原生"),
    KUAISHOU_NATIVE("ks_native", "快手端原生");
}

public interface WorkManageHandler {
    BizLine line();
    /** 作品列表查询:每条业务线字段/规则不一样 */
    PageResult<WorkVO> listWorks(Long orgId, WorkQueryDTO q);
    /** 剧集详情 */
    DramaDetailVO detail(Long orgId, Long dramaId);
    /** 下载权限校验(有些剧不允许下载) */
    boolean canDownload(Long orgId, Long dramaId);
}
```

三个实现类:
- `WxMiniWorkHandler`(老代码迁过来)
- `DouyinNativeWorkHandler`(新增)
- `KuaishouNativeWorkHandler`(新增)

Controller 只做路由:
```java
@GetMapping("/works")
public Result<?> list(@RequestParam BizLine line, @RequestParam Long orgId, WorkQueryDTO q) {
    WorkManageHandler handler = handlerFactory.get(line);
    return Result.ok(handler.listWorks(orgId, q));
}
```

**好处**:
- 新加一条业务线 = 写一个新 Handler,主干代码不动
- 每条线的差异(字段、筛选、权限)在 Handler 内部收敛,不散落
- 单测好写,mock Handler 直接测

---

### 3.2 新增字段"分发漫剧分类"的兼容设计

产品新加了一个字段:**分发漫剧分类**(6 种枚举:沙雕漫、解说漫、AI真人、简笔画、动态漫、图漫)。

#### 挑战
- 上游**内容中心**里剧集有"分类"字段(拟真/动画短剧/简易动画/...),和我们的"分发分类"**不是一一对应**
- 历史数据几万条剧集,没有分发分类,要有**合理默认值**的映射规则
- 运营希望能手动改,改了之后不能被上游同步覆盖

#### 方案

**数据模型**:不改原 `drama.category` 字段,**加一列**:
```sql
ALTER TABLE drama ADD COLUMN distribute_category VARCHAR(32) COMMENT '分发漫剧分类';
CREATE INDEX idx_dist_cat ON drama(distribute_category);
```

**初始化规则**(跟产品对齐后):
| 上游 category | distribute_category 默认值 |
|--------------|---------------------------|
| 拟真 | ai真人 |
| 动画短剧 | 动态漫 |
| 简易动画 | (空)需要运营手动配置 |
| 其他/未知 | (空) |

用一次性刷数脚本跑历史数据:
```sql
UPDATE drama SET distribute_category = 'ai真人' WHERE category = '拟真' AND distribute_category IS NULL;
UPDATE drama SET distribute_category = '动态漫' WHERE category = '动画短剧' AND distribute_category IS NULL;
```

**防覆盖**:加一个 `distribute_category_manual` 字段,运营手动配置过的置 1,上游同步流程跳过。

> **卡点**:一开始没做 manual 标记,运营改完第二天 cronjob 同步又给覆盖回去了,被吐槽。立刻加了这个标记位,并且把历史被覆盖的记录从操作日志里 diff 出来补回。

---

### 3.3 批量异步下载(重点讲这块)

#### 需求描述
- 分销商在剧集详情页选中若干集(最多选到 70%)
- 点"批量下载",**后台异步打包成 zip** → 存 COS → 返回下载链接
- 任务中心能看到进度(进行中/完成/失败)

#### 链路设计
```
[用户] 点批量下载
   ↓
[下载服务]
   ① 校验:剧集权限 / 集数上限(≤70%) / 法律声明勾选
   ② 创建 download_task 记录,状态 PENDING
   ③ 发 MQ 消息 (taskId)
   ④ 立即返回任务 ID → 前端跳任务中心
   ↓
[Worker 消费]
   ① 更新 task 状态 RUNNING
   ② 从 COS 下载所有选中的视频到本地临时目录
   ③ ZipOutputStream 流式打包(不全量加载进内存!)
   ④ 上传 zip 到 COS,记录 URL
   ⑤ 更新 task 状态 DONE + COS URL + 文件大小
   ↓
[前端]
   任务中心每 5 秒轮询任务状态,RUNNING → DONE 时显示下载按钮
```

#### 关键实现 1:流式打包(防 OOM)

单部剧 70 集,单集 200MB,**一把塞进内存直接 OOM**。用流式:

```java
public void buildZip(Long taskId, List<String> episodeCosKeys, String outputCosKey) {
    // 本地临时 zip 文件
    Path tmpZip = Files.createTempFile("download-" + taskId + "-", ".zip");
    try (OutputStream fos = Files.newOutputStream(tmpZip);
         ZipOutputStream zos = new ZipOutputStream(new BufferedOutputStream(fos))) {
        int idx = 0;
        for (String key : episodeCosKeys) {
            ZipEntry entry = new ZipEntry(String.format("%02d.mp4", ++idx));
            zos.putNextEntry(entry);
            // 从 COS 流式下载,边下边写 zip
            try (InputStream in = cosClient.getObjectStream(key)) {
                IOUtils.copy(in, zos);  // 8KB buffer,恒定内存
            }
            zos.closeEntry();
            // 更新进度
            taskService.updateProgress(taskId, idx, episodeCosKeys.size());
        }
    }
    // zip 完成后再上传到 COS(分片上传大文件)
    cosClient.uploadMultipart(outputCosKey, tmpZip);
    Files.deleteIfExists(tmpZip);
}
```

- **恒定内存**:不管多少集,JVM 堆只用 8KB buffer
- **增量进度**:每处理完一集更新 `progress`,前端展示"打包中 12/70"
- **本地磁盘临时存**:避免网络中断导致 zip 半残;打包完整再传 COS

#### 关键实现 2:任务表设计

```sql
CREATE TABLE download_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    org_id BIGINT NOT NULL,
    task_name VARCHAR(200) NOT NULL COMMENT '剧名+集数区间',
    drama_id BIGINT NOT NULL,
    episodes JSON NOT NULL COMMENT '选中集数列表',
    status TINYINT NOT NULL COMMENT '0待执行 1执行中 2完成 3失败',
    progress INT DEFAULT 0 COMMENT '已处理集数',
    total INT DEFAULT 0,
    cos_url VARCHAR(500) COMMENT '打包后 zip 的下载链接',
    error_msg VARCHAR(1000),
    retry_count INT DEFAULT 0,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    INDEX idx_org_status (org_id, status),
    INDEX idx_created (created_at)
);
```

- `episodes` 用 JSON 存选中集数 `[1,2,5,6,7,...]`
- `progress` / `total` 用来渲染进度条
- `retry_count` 限制最多重试 3 次,超过人工介入

#### 关键实现 3:重试机制

下载过程可能失败(COS 偶发 500、网络中断、磁盘满)。用 **MQ 重试 + 指数退避**:

```java
@Consumer(topic = "download-task", maxRedelivery = 3)
public void handle(DownloadTaskMsg msg) {
    try {
        zipBuilder.buildZip(msg.getTaskId(), ...);
        taskService.markDone(msg.getTaskId());
    } catch (RetryableException e) {
        int retry = taskService.incrRetry(msg.getTaskId());
        if (retry > 3) {
            taskService.markFailed(msg.getTaskId(), e.getMessage());
            alertService.notify("下载任务失败 taskId=" + msg.getTaskId());
            return;
        }
        // 延迟重试(TDMQ 延迟消息)
        long delaySec = (long) Math.pow(2, retry) * 60;  // 1,2,4 分钟
        mqProducer.sendDelayed(msg, Duration.ofSeconds(delaySec));
    } catch (Exception e) {
        // 不可重试的错误(权限/参数问题)直接失败
        taskService.markFailed(msg.getTaskId(), e.getMessage());
    }
}
```

---

### 3.4 防盗版 + 下载限制

#### 70% 集数上限
总 70 集的剧,最多能选 49 集下载。前后端双校验:
```java
public void validateEpisodeCount(Long dramaId, List<Integer> selected) {
    int total = dramaMapper.selectEpisodeCount(dramaId);
    int limit = (int) (total * 0.7);
    if (selected.size() > limit) {
        throw new BizException("最多可下载 " + limit + " 集(总集数的 70%)");
    }
}
```

#### 法律声明强制勾选
前端显示声明,用户必须勾"我已阅读并同意"。后端接口接收一个 `agreementSigned=true`,没勾不给下。签名 + 下载记录入库审计,出事能追到具体账号。

#### 水印(预研未落地)
PM 提过加动态水印(下载账号 ID),但要改视频编码流水线,工期拉不过来,这期先搁置。后面再单独做一期。

---

### 3.5 分页播放器 + 剧集切换

单部剧 100+ 集,一次查全量耗时 + 前端渲染卡。改成**50 集/页**:
```java
public EpisodePageVO listEpisodes(Long dramaId, int page) {
    int start = (page - 1) * 50 + 1;
    int end = page * 50;
    return mapper.selectByRange(dramaId, start, end);
}
```
前端默认选 1-50,切到 51-100 时再次请求。

**单集下载**:直接返回带签名的 COS 预签名 URL(15 分钟有效),前端直接触发浏览器下载。不走异步打包链路(小文件没必要)。

---

### 3.6 母子账号权限:无需分配渠道

企业号下挂子账号,产品要求:**子账号端渠道下直接看到父账号的全部作品**,不用走"分配渠道"那套逻辑。

我在 `WorkManageHandler` 里加了判断:
```java
public PageResult<WorkVO> listWorks(Long orgId, WorkQueryDTO q) {
    OrgInfo org = orgService.get(orgId);
    Long queryOrgId = orgId;
    // 端渠道 + 子账号:查父账号的作品
    if (isEndChannel(this.line()) && org.isSubAccount()) {
        queryOrgId = org.getParentOrgId();
    }
    return workMapper.selectWorks(queryOrgId, q);
}
```

> **卡点**:第一版我把"使用父账号数据"做成通用逻辑,结果微小免费链路那边测试出问题——他们的子账号是**独立渠道**,不能串用父账号。后来加了 `isEndChannel(line)` 判断,只有端渠道场景才走父账号数据。**业务规则不能一刀切**。

---

## 四、三个最坑的点

### 坑 1:zip 打包完上传 COS 超时
早期用 `cosClient.putObject(key, file)`,单次上传一个 14GB 的 zip,COS 网关返回 504。

解决:改用**分片上传**(`initiateMultipartUpload` → 循环 `uploadPart` → `completeMultipartUpload`),每片 10MB,单个 part 失败可重传。上传耗时从"超时失败"变成稳定 3-5 分钟。

### 坑 2:同一用户狂发下载任务,Worker 被单账号占满
有个测试账号一小时发了 50 个下载任务,把 Worker 全占了,其他用户的任务排队超过 30 分钟。

解决:
- DB 层加 **每用户同时 RUNNING 任务数 ≤ 2** 的校验
- MQ 消费层加 **公平调度**(根据 `org_id` hash 到不同 Worker,避免一个账号把一个 Worker 霸占)
- 新任务提交时,如果该用户已有 ≥ 2 个 RUNNING,直接返回"请等上一个任务完成"

### 坑 3:任务中心轮询压力大
100 个用户同时打开任务中心,每 5 秒轮询一次,DB `SELECT status FROM download_task WHERE org_id = ?` QPS 嗖一下上来。

优化:
- 任务状态**写入 Redis**(`download:task:{taskId}` → JSON),TTL 1 天
- 轮询接口优先读 Redis,miss 再查 DB
- 进一步可以用 **SSE(Server-Sent Events)** 推状态变更,但一期没做

---

## 五、亮点提炼(面试数字)

1. **业务线扩展**:从 1 条扩到 3 条,**新加一条业务线代码改动 < 200 行**(Handler 模式)
2. **素材自助率**:上线后分销商**找运营要素材的工单数降了 90%**(原来 100 单/月,现在 10 单/月)
3. **大文件稳定性**:70 集打包下载任务**成功率 99.3%**(失败主要是用户主动取消)
4. **打包内存占用**:流式 zip + 分片上传,**打包一个 14GB 的包只吃 50MB 堆内存**(不做流式分分钟 OOM)

---

## 六、技术栈

- **框架**:Spring Boot + MyBatis
- **存储**:MySQL(元数据 + 任务)+ COS(视频文件 + zip)+ Redis(任务状态缓存)
- **异步**:TDMQ(重试)+ XXL-Job(清理超过 7 天的过期 zip)
- **打包**:`java.util.zip.ZipOutputStream` 流式 + COS SDK 分片上传
- **代码量**:约 3000 行(含 Handler 重构 + 下载服务 + 任务中心)

---

## 七、面试追问 + 准备答案

### Q1:为什么用策略模式,不用继承?
策略模式组合优于继承:
- 每条业务线的字段/规则差异大,继承树会很深
- Handler 可以独立测试 / 独立迭代
- Spring 里 `@Component` + `List<WorkManageHandler>` 注入,加新的 Handler 不改老代码,符合开闭原则

### Q2:为什么不用 Stream API 做 zip?用 IO 流 copy 性能好吗?
`IOUtils.copy(in, zos)` 内部就是 8KB buffer 循环读写,**底层是 JVM native 的 write system call**,性能和 Stream API 没差。用 IO 流更直观、内存可预测。Stream API 如果处理不当反而可能持有中间集合。

### Q3:COS 分片上传失败一个 part 怎么办?
SDK 支持 per-part 重试。如果 3 次重试都失败,`abortMultipartUpload` 清理已上传的 part(否则按 COS 存储计费会持续收钱),然后整个任务标记失败,MQ 走任务级重试。

### Q4:下载任务超过 1 小时没完成怎么办?
XXL-Job 每 10 分钟扫一次 RUNNING 超过 1 小时的任务,标记为 STALLED,触发人工介入。很多情况下是 Worker 崩溃导致任务卡住,扫到后重新入 MQ。

### Q5:COS 预签名 URL 被泄露怎么办?
- 短 TTL(15 分钟)
- 加签名里包含客户端 IP(`x-cos-security-token` 绑定来源)
- 下载日志埋点:同一 URL 多次下载 / 异常 IP 报警
- 更严的方案是走**我方后端代理下载**(流式转发),但性能/带宽成本高,这期没做

### Q6:"分发分类"的映射规则万一产品改了怎么办?
我把映射规则做成**配置表**(或 Apollo 配置),不是硬编码:
```sql
CREATE TABLE category_mapping (
    source_category VARCHAR(32),
    distribute_category VARCHAR(32),
    PRIMARY KEY (source_category)
);
```
产品改规则时修数据就行,代码不用改。

---

## 八、如果让我重做

1. **分片下载**:对于超大 zip,前端支持断点续传(Range 请求),不用每次全下
2. **下载带水印**:视频处理流水线加 FFmpeg 水印(账号 ID + 时间戳),真正防盗
3. **任务中心用 SSE 或 WebSocket**:替代轮询,服务端主动推状态变更
4. **多级权限控制**:子账号能看但不能下载 / 能下载但水印更显眼 / 管理员全权限

---

## 九、一句话总结(开场用)

> "这个是 2026 年做的分销后台 C 端扩展,新增抖音/快手两条业务线,最有技术含量的点是**异步批量下载**——流式 zip 打包 + COS 分片上传,处理 14GB 的包只吃 50MB 内存,加任务中心 + MQ 重试 + 每用户并发限制,上线后分销商自助下载,运营工单降了 90%。"

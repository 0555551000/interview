# 辅助素材库(可选讲点,按需引用)

> 核心主项目是**阅文漫剧分销平台**(见"阅文漫剧_真实面经.md")。
> 这份是面试时如果被问到**更细的技术点**或者**前面主线讲完了还想再抛点料**,可以顺手拿出来的补充素材。

## 结构

- **第一部分:HMAC-SHA256 内部接口签名**(剧集分类互通那条链路里我做的)
- **第二部分:操作日志(before/after)+ 状态机**(合同 / 审核场景)
- **第三部分:纯 Python AES(跳板机无依赖场景)**
- **第四部分:6 步 Orchestrator 编排(机构创建)**
- **第五部分:跨系统数据刷入(SQL 脚本 + 抽检验证)**
- **第六部分:DCL 单例 + MyBatis TypeHandler(敏感数据加密)**

---

## 一、HMAC-SHA256 内部接口签名

### 背景

阅文漫剧平台里有个场景:**vega 运营后台(外部服务)要回写 dramaCategory 到分销平台**(我负责的模块)。这是"服务对服务"的内网调用,不走用户登录态,但必须:

- 身份验证(不是外部随便发请求都能接受)
- 防重放
- 防中间人篡改

### 方案

**HMAC-SHA256 签名 + 时间戳窗口 + Nonce 去重**。

三件套 Header:

```
X-Timestamp: 1704067200000        (毫秒时间戳)
X-Nonce: abc123def456              (随机 UUID)
X-Sign: <HMAC-SHA256 签名的十六进制>
```

签名串构造:

```
payload = Method + "\n" + Path + "\n" + canonical(QueryParams) + "\n" + MD5(Body) + "\n" + Timestamp + "\n" + Nonce
signature = HMAC_SHA256(payload, secret)
```

**关键实现**:

```java
public static String sign(String secret, String method, String path,
                          Map<String, String> query, String body,
                          String ts, String nonce) {
    String canonical = canonicalQuery(query);          // key 字典序 + URL 编码
    String bodyMd5   = body == null || body.isEmpty() ? "" : DigestUtils.md5Hex(body);
    String payload   = String.join("\n",
            method.toUpperCase(), path, canonical, bodyMd5, ts, nonce);
    return hmacSha256Hex(secret, payload);
}

// 过滤器里校验
public void doFilter(ServletRequest req, ServletResponse resp, FilterChain chain) {
    HttpServletRequest r = (HttpServletRequest) req;
    String ts    = r.getHeader("X-Timestamp");
    String nonce = r.getHeader("X-Nonce");
    String sign  = r.getHeader("X-Sign");

    // 1. 时间戳窗口校验(±5min)
    long now = System.currentTimeMillis();
    long tsLong = Long.parseLong(ts);
    if (Math.abs(now - tsLong) > 5 * 60 * 1000) {
        resp.setStatus(401); return;
    }

    // 2. Nonce 防重放(Redis SETNX,TTL 5min 和时间戳窗口一致)
    if (!redisLockService.setNxWithExpire("nonce:" + nonce, "1", 5 * 60)) {
        resp.setStatus(401); return;
    }

    // 3. 重算签名 + 常量时间比较
    String expected = InternalSignUtil.signFromRequest(secret, r, readBody(r));
    if (!MessageDigest.isEqual(expected.getBytes(), sign.getBytes())) {
        resp.setStatus(401); return;
    }

    chain.doFilter(req, resp);
}
```

### 为什么这么做的解释

- **时间戳窗口 ±5 分钟**:容忍 NTP 时钟偏差,同时不给重放太大窗口
- **Nonce Redis `SETNX`**:同一 Nonce 5 分钟内只能用一次
- **`MessageDigest.isEqual` 常量时间比较**:防时序侧信道攻击(`String.equals` 短路会泄露前缀长度)
- **签名串按字典序规范化**:两端按相同规则拼接,不会因为参数顺序不同算出不同签名

### 面试追问应答

**Q: HMAC 和 JWT 怎么选?**
- JWT 适合用户登录态(有 subject、有 exp、需要无状态)
- HMAC 适合服务对服务调用(每请求独立签名,不存 token)

**Q: 为什么不用 OAuth?**
- OAuth 是授权协议,适合开放平台场景(第三方调你的 API)。内网服务互调太重了

**Q: 密钥怎么管理?**
- Apollo 配置中心下发,轮换时双密钥过渡

---

## 二、操作日志(before/after) + 状态机

### 背景

vega 运营后台有个场景:运营对内容做**热度干预**(手动调点赞数 / 浏览数)。
- 必须留痕(谁、什么时候、改了什么、改前改后值多少)
- 后续审计或者用户投诉时能回溯

### 方案

**JSON 字段存变更前后值**:

```sql
CREATE TABLE post_operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    post_id VARCHAR(64),
    operation_type INT COMMENT '1审核 2驳回 3屏蔽 4解除屏蔽 5删除 6热度干预',
    operator VARCHAR(255),
    remark TEXT,
    extra_data JSON,                 -- 变更前后值
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

`extra_data` 结构:
```json
{
  "before": {"likeCount": 156, "viewCount": 2340},
  "after":  {"likeCount": 500, "viewCount": 10000}
}
```

查询时:`SELECT extra_data->'$.before.likeCount' FROM post_operation_log` 直接取。

### 状态机设计

帖子状态 5 种:0 待审 / 1 通过 / 2 驳回 / 3 屏蔽 / 4 删除。

不是任意两个状态都能转换。用 enum 硬编码转移规则:

```java
public enum PostAction {
    AUDIT_PASS   (Set.of(0),       1),
    AUDIT_REJECT (Set.of(0),       2),
    BLOCK        (Set.of(1),       3),    // 通过 → 屏蔽
    UNBLOCK      (Set.of(3),       1),    // 屏蔽 → 通过
    DELETE       (Set.of(1, 3),    4);    // 通过/屏蔽 → 删除

    final Set<Integer> fromStatus;
    final int toStatus;

    public void check(int currentStatus) {
        if (!fromStatus.contains(currentStatus))
            throw new BizException("状态非法,不能从 " + currentStatus + " 转到 " + toStatus);
    }
}
```

**并发保护**:用 CAS 乐观锁:

```sql
UPDATE post SET audit_status = ? WHERE id = ? AND audit_status = ?
```

影响行数为 0 就抛"状态已变",两个运营同时改同一条帖子只有一个成功。

### 面试追问应答

**Q: 操作日志量大怎么办?**
> 按月分表 `post_operation_log_202604`,Service 层拼表名。超 1 年归档 HDFS

**Q: 状态机加新状态怎么办?**
> enum 加一个值,加一条转移规则。改一处影响全局

---

## 三、纯 Python AES(跳板机无依赖场景)

### 背景

运维在跳板机上抽检手机号加解密结果,**沙箱环境装不了第三方库**(`pycryptodome` 这种都不行)。

### 方案

**纯 Python 手写 AES-128-CBC**,单文件无依赖,单 300+ 行。

核心实现:

```python
# AES S-Box / Inverse S-Box / Rcon
_SBOX = (0x63, 0x7C, 0x77, 0x7B, ...)   # 256 字节
_INV_SBOX = tuple(_SBOX.index(i) for i in range(256))
_RCON = (0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1B, 0x36)

def _key_expansion(key: bytes) -> list:
    """AES-128 密钥扩展,返回 44 个 32-bit word"""
    # ... 标准 FIPS-197 算法

def _aes_decrypt_block(block: bytes, round_keys: list) -> bytes:
    """解密 16 字节块"""
    # AddRoundKey → (ShiftRowsInv → SubBytesInv → AddRoundKey → MixColumnsInv) × 9 → ShiftRowsInv → SubBytesInv → AddRoundKey

def decrypt(cipher_text: str) -> str:
    """Base64 密文 → AES-128-CBC 解密 → PKCS5 去填充"""
    encrypted = base64.b64decode(cipher_text)
    plaintext = b""
    prev_block = IV
    for i in range(0, len(encrypted), 16):
        block = encrypted[i:i + 16]
        decrypted = _aes_decrypt_block(block, _ROUND_KEYS)
        plaintext += bytes(x ^ y for x, y in zip(decrypted, prev_block))
        prev_block = block
    return _pkcs5_unpad(plaintext).decode("utf-8")
```

验证方法:

1. 准备已知明文/密文对(Java 实现生成)
2. Python 解密,结果必须和明文完全一致
3. Python 加密,密文必须和 Java 完全一致

### 用途

```bash
python3 phone_decrypt.py xD6HGz13BIz0XqeRiodpuw==        # 解密单个
python3 phone_decrypt.py -e 15601791952                   # 加密单个
python3 phone_decrypt.py -f ciphers.txt                   # 批量
python3 phone_decrypt.py -i                               # 交互
```

### 面试追问应答

**Q: 为什么不用标准库?**
> 跳板机沙箱装不了第三方库。纯 Python 实现单文件无依赖

**Q: 性能怎么样?**
> 手写 AES 单次解密微秒级,跳板机抽检用途够用。真要生产级性能用 Java / Go 调 openssl

**Q: 怎么保证和 Java 实现一致?**
> 交叉验证 — 取 Java 生产数据,Python 解密对比明文;Python 加密结果,Java 解密对比明文

---

## 四、6 步 Orchestrator 编排(机构创建)

### 背景

阅文创作者入驻端(nova 平台,辅助项目)有个复杂流程:机构入驻要过 6 步。

### 方案

**Orchestrator 主编排 + 策略校验器插拔 + 工厂派发业务类型**:

```java
public void createOrganization(OrgAddParam param, BusinessTypeEnum businessType) {
    // ① 策略校验(多个 Validator 按 @Order 顺序跑)
    executeValidation(param);

    // ② OCR 识别营业执照
    BizLicenseOcrDTO ocr = getBizLicenseOcrOrNull(param);

    // ③ 机审(身份证 OCR + 法人匹配 + 黑名单 + 三要素)
    OrganizationStatus audit = executeAudit(param, ocr);

    // ④ 落库
    String orgId = orgManager.createOrganization(param, ocr, businessType, audit);

    // ⑤ 用户-机构绑定(落库里一起做了)
    // ⑥ 业务派发
    executeBusinessTypeHandler(businessType, orgId);
}

// 策略校验器(插拔)
public interface OrgValidator {
    int getOrder();
    void validate(OrgAddParam param);
}

@Component @Order(1)
class OrgBindingValidator implements OrgValidator { ... }

@Component @Order(2)
class BusinessLicenseValidator implements OrgValidator { ... }

// 编排时:
@Autowired private List<OrgValidator> validators;
private void executeValidation(OrgAddParam param) {
    validators.stream()
            .sorted(Comparator.comparingInt(OrgValidator::getOrder))
            .forEach(v -> v.validate(param));
}
```

**加新校验规则**:新增 `@Component implements OrgValidator`,编排代码**一行不改**。

### 面试追问应答

**Q: 为什么不用 Spring StateMachine?**
> 我这里是**流水线**不是状态机(没分支没回退)。状态机杀鸡用牛刀

**Q: 6 步能并行吗?**
> OCR 和校验理论上可并行,但串行便于日志追踪和排查。当前业务延迟可接受就没做

---

## 五、跨系统数据刷入(SQL 脚本 + 抽检验证)

### 背景

商务在一个系统搜不到另一个系统的老用户(两套独立系统,手机号都加密)。要做**一次性数据迁移** — 把系统 A 的 24 条用户数据刷到系统 B。

### 方案要点

**1. 先摸清两边加密体系**

写 Java 工具类交叉验证:

```java
// 从系统 B 抓一条真实密文,用系统 A 的 KEY/IV 试解
String plain = decryptAsA("rmRKy/sqW77TmEil0EGnrg==");
if (isValidPhone(plain)) {
    // 两边密钥一致!密文可直接平移
}
```

**2. 密文直接 INSERT,不解不加**

```sql
INSERT INTO org_contact (org_id, name, phone, is_primary, status) VALUES
  ('COMIC_813000824354', 'COMIC商户-13026', 'xD6HGz13BIz0XqeRiodpuw==', 1, 1),
  -- ... 24 条
```

**3. user_id 重复的加 guid 后缀避重**

```
name = "COMIC商户-" + user_id                           (默认)
name = "COMIC商户-" + user_id + "-" + guid              (本批内 user_id 重复时)
```

**4. 抽检验证**

上线前抽 3 条做端到端验证:真实手机号 → 前端加密 → 查系统 B → 能查到用户。

### 面试追问应答

**Q: 为什么不建机构主表(organization)?**
> 商户没有营业执照、法人等必填字段,硬建会污染机构体系。虚拟 org_id = "COMIC_{guid}" 占位,将来要批量清理用 WHERE org_id LIKE 'COMIC_%' 就行

**Q: 脚本执行风险?**
> 备份 → UAT 演练 → 生产按合同号分批执行。每批单独事务

---

## 六、DCL 单例 + MyBatis TypeHandler(敏感数据加密)

### 背景

DO 字段里的身份证、银行卡、手机号要加密存 DB。业务代码手动加解密容易漏 — 有人存的时候忘加密(明文落库)、有人查的时候忘解密(直接用密文当明文)。

### 方案

**MyBatis TypeHandler 透明加解密 + DCL 单例从 Spring 拿密钥**。

**TypeHandler 实现**:

```java
@MappedJdbcTypes(JdbcType.VARCHAR)
@MappedTypes(String.class)
public class AESStringTypeHandler extends BaseTypeHandler<String> {
    @Override
    public void setNonNullParameter(PreparedStatement ps, int i,
                                    String v, JdbcType jdbcType) throws SQLException {
        ps.setString(i, SensitiveDataCryptoUtil.encrypt(v));    // 存时加密
    }

    @Override
    public String getNullableResult(ResultSet rs, String col) throws SQLException {
        return SensitiveDataCryptoUtil.decrypt(rs.getString(col));   // 查时解密
    }
}
```

Mapper XML 声明:

```xml
<result column="id_card_no" property="idCardNo"
        typeHandler="com.xxx.util.AESStringTypeHandler"/>
```

**难点**:TypeHandler 是 MyBatis `new` 出来的,`@Autowired` 不生效。用 **DCL 单例 + 静态方法** 桥接:

```java
@Component
public class SensitiveDataCryptoUtil {
    @Value("${sensitiveData.secretKey:3kIkegOubUjifSvL}")
    private String secretKey;

    private static volatile SensitiveDataCryptoUtil INSTANCE;

    private static SensitiveDataCryptoUtil getInstance() {
        if (INSTANCE == null) {
            synchronized (SensitiveDataCryptoUtil.class) {
                if (INSTANCE == null) {
                    INSTANCE = Global.getBean(SensitiveDataCryptoUtil.class);
                }
            }
        }
        return INSTANCE;
    }

    public static String encrypt(String data) {
        if (StringUtils.isEmpty(data)) return data;
        return getInstance().encryptInternal(data);
    }
}
```

### 面试追问应答

**Q: 为什么 DCL 不用饿汉式?**
> TypeHandler 加载可能在 Spring 初始化前。DCL 延迟到第一次加解密时才去拿 Bean,那时 Spring 必然就绪

**Q: `volatile` 为什么必须?**
> 防指令重排。没 volatile 可能看到"对象非 null 但构造函数没跑完"的部分初始化状态

**Q: WHERE 查不了加密字段吗?**
> **这是最大痛点**。DB 存密文,`WHERE phone = '138xxx'` 查不到。Service 层先加密再查:`mapper.selectByPhone(encrypt(phone))`。模糊查询干脆做不了,要做得存一个"前缀哈希"字段做索引

**Q: AES 固定 IV 不安全?**
> 知道,是技术债。改进方向:AES-GCM + 随机 IV。迁移策略 — 新字段直接 GCM,老字段双读(GCM 失败试 CBC),跑批重加密,最后下线 CBC

---

## 使用建议

**这份文档是"补充弹药"**,不是主线。

**主线**是`阅文漫剧_真实面经.md`里的 6 个核心需求 + 4 个线上治理(签约框架、动态过期、N+1 优化、新旧 ID 映射、工厂模式、batchCreate,加合同号重复、回调状态、双合同、批量任务)。

**这份弹药**是在以下场景用:

- 面试官追问到某个细节(比如 HMAC 签名、DCL 单例),主线没展开的话,从这里拿答法
- 主线讲完了面试官还想听,抛一个"哦对了,我还做过 X"
- 被问"你懂 XX 吗"(比如 Python、Orchestrator 模式、操作日志),有素材可答

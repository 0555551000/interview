# Java 后端工程师简历(5 年经验)

> 基于阅文漫剧分销平台真实工作内容整理。
> 核心项目:阅文漫剧分销与签约平台,集团统一技术栈 Peacock 脚手架。

---

## 个人技能

- **语言与框架**:Java 8 / Java 21、Spring Boot 2.3/2.7、Spring Security、MyBatis、Lombok
- **RPC / 通信**:TRPC-Java(腾讯)、TAF/JCE(旧协议兼容)、Polaris(北极星)服务注册
- **存储 / 缓存**:MySQL(分区表 / 组合索引 / 双 ID 映射)、Redis(分布式锁 + 方法内 HashMap 本地缓存)、腾讯云 COS
- **消息 / 调度**:TDMQ-CMQ(腾讯)、XXL-Job、Spring @Scheduled
- **设计模式**:策略 + 模板方法、工厂模式、编排器、校验器链、泛型上下文容器
- **安全 / 加密**:AES-128-CBC、HMAC-SHA256(内部接口签名)、PBKDF2 密码加盐、MyBatis TypeHandler 透明加解密
- **配置 / 观测**:Apollo / Rainbow(七彩石) 配置中心、MDC 链路追踪、moonwalker / sunwalker Agent
- **工程能力**:分布式事务补偿、N+1 查询优化、新旧系统兼容迁移、双 ID 映射、规则引擎(QLExpress)、EasyExcel 流式处理

---

# 项目一:阅文漫剧分销与签约平台(主要项目)

**时间**:2023.xx - 至今 · **角色**:核心开发 / 3 个模块 Owner · **技术栈**:Spring Boot、MyBatis、TRPC、腾讯云 COS、Apollo

## 项目介绍

阅文集团漫剧业务线的核心后端系统,基于集团统一技术栈(**Peacock 脚手架 + 开放平台组件**)开发,深度对接集团 6+ 中台服务(合同中心、分润系统、机构主数据、敏感数据中心、文件服务、内容中心)。服务**万级 UP 主 + 千级机构 MCN**,**月均签约 300+ 单,年结算金额千万级**,管理上万部漫剧内容资产。

我负责**分销签约、结算分润、IP 中台内容管理**三大核心模块的开发与维护,支撑从入驻、签约、结算到内容上架的全流程业务闭环。

## 核心产出

### 1. 分销签约模块(Owner)· 签约框架 5→1 天扩展

**问题**:新增合同类型需改动核心流程代码,每次新增要改 3-5 处代码,开发周期 3-5 天,易引入回归 bug。

**方案**:
- 基于**策略模式**抽象 `ContractInvoker` 统一签约接口
- **模板方法模式**封装 `CreateContractHandler` 标准流程(幂等校验 → 参数验证 → 合同创建 → 后置回调)
- 设计 `CreateContractContext` 泛型上下文容器,根据签约主体类型自动装配身份、实名、银行卡、企业资质等全量数据(个人 8 项 / 机构 15 项)

**效果**:
- 新合同类型开发周期从 **5 天 → 1 天**,核心代码零改动
- 已支撑 **5 种合同类型**稳定运行(个人签约、机构合作、补充协议、版权授权、委托制作)
- 签约接口代码量减少 **40%**

### 2. 分销签约模块 · 合同"已过期"状态动态计算

**问题**:合同过期状态依赖定时任务更新,最长延迟 1 小时,存在一致性风险。

**方案**:数据库仅存"已生效、已终止"两种基础状态,"已过期"通过 `cooperation_end_time < NOW()` 实时动态计算。列表查询时 SQL 条件自动适配 `CASE WHEN` 支持前端过期筛选。

**效果**:**消除定时任务依赖,状态延迟从最长 1 小时降至 0**,万级合作记录状态查询实时准确。

### 3. 结算分润模块(Owner)· N+1 查询优化

**问题**:结算列表循环调用用户信息 RPC 接口,单次 50-200 条记录最差 200 次 RPC 调用,接口耗时 3-5 秒。

**方案**:方法内使用 HashMap 实现轻量级本地缓存,同一次请求内相同 userId 只查询一次,结合 `computeIfAbsent` 做去重。

**效果**:**RPC 从 200 次降到实际用户数(通常 10-20 次),接口响应从 3-5 秒降至 500ms 以内**。

### 4. 结算分润模块 · 新旧机构 ID 双向转换

**问题**:阅文多业务线遗留新旧两套机构 ID 体系(老分润系统上线多年,千万级历史数据,几十个业务线在用,无法切换),分销结算需同时适配。

**方案**:设计 `organization_mapping` 映射表,Service 层统一做 ID 翻译 — 前端传新 ID → `newToOld()` 转老 ID → RPC 调分润系统 → `batchOldToNew()` 批量反向转换 → 查新 ID 对应机构名 → 组装 VO。

**效果**:**千级机构新旧 ID 平滑过渡,零侵入新业务代码,分润结算零差错**。

### 5. 结算分润模块 · 自定义分页组件

**问题**:15+ 个后台查询接口存在大量重复分页代码,COUNT 和 LIST 查询逻辑散乱。

**方案**:自定义 MyBatis 多条件分页组件,封装 COUNT + LIST 双查询逻辑,统一处理动态条件拼接。

**效果**:**15+ 个接口复用,查询相关代码量减少 60%**。

### 6. 结算分润模块 · OrgManager 数据聚合层

设计 `OrgManager` 机构数据聚合层,通过 6 个 Driver 封装 **8 张表 + 3 个中台服务**的查询,对外提供统一接口,隐藏底层数据组装复杂度。实现 **DO/VO 分层转换、日期格式化、枚举翻译、空值安全处理**,覆盖 20+ 个结算相关接口。

### 7. IP 中台内容模块(核心开发者)· 成品处理器工厂

**问题**:成品处理逻辑随内容类型硬编码,已有 3 种类型(剧本/剧集/成品),每新增一种需改 4-6 处代码。

**方案**:基于**工厂模式**设计成品处理器工厂 `ArtifactHandlerFactory`,定义统一 `ArtifactHandler` 接口规范业务行为(`create` / `bindCams` / `syncStatus`),通过 List 注入 + Map 自动注册,本地缓存优化实例复用。

**效果**:**新增内容形态零改动核心流程,扩展开发周期从 3 天降至 0.5 天**,已支撑 4 种内容类型稳定运行。

### 8. IP 中台内容模块 · 机构批量创建剧本

**问题**:机构批量创建剧本需处理跨服务双 ID 映射、数据去重、分批调用等复杂逻辑,单次批量请求 50-500 条。

**方案**:实现 `batchCreateContentScriptForOrg` 五步流程 — 解析 JSON 扩展字段 → 按 **名称/书籍ID/作品类型** 三维度精准去重 → 分批调用内容中心(每批 50 条)→ 反向组装 ID 关联 Map 完成跨服务 ID 映射(临时 ID → 正式 ID)→ 本地落库 + 幂等处理。

**效果**:**支撑单次最高 500 条剧本的批量上线,去重准确率 100%,跨服务 ID 映射零错乱**。

### 9. IP 中台内容模块 · CAMS 批量绑定

实现 CAMS 批量绑定逻辑,过滤已绑定数据、封装统一参数、区分不同业务绑定策略(走工厂拿对应 handler),单次绑定支持 100+ 条记录。对接**天网 IP 项目中台**,实现剧本/剧集成品与天网项目的绑定、任务 ID 同步与状态管理。

### 10. 集成腾讯云 COS + 结算附件管理

集成腾讯云 COS 实现结算凭证、发票、确认函等附件上传与签名 URL 管理,**月均上传附件 500+ 份**。附件类型分类、文件大小校验、类型白名单、过期 URL 自动签名。

## 线上问题治理

### 1. 合同号重复导致分账统计错乱

机构合作场景下出现**合同号重复、多剧本异常关联同一合同**,影响 10+ 笔分账记录。
- **方案**:SQL 多表联查定位脏数据 → 接口前置幂等校验 → 数据库新增唯一索引 → 业务层关联计数管控(`bind_script_cnt` 字段)
- **效果**:上线后 **3 个月零次重复创建**,分账统计准确率 100%,修复 10+ 笔历史脏数据

### 2. 数据库写入异常导致流程中断

**金额数值超长溢出、CAMS 绑定核心字段(CBID/VDID)为空**等导致流程卡死,月均触发 2-3 次线上工单。
- **方案**:参数校验规则(`@DecimalMax`)+ 字段非空约束 + DECIMAL 精度从 `(10,2)` 调到 `(16,4)`
- **效果**:修复后 **2 个月零相关工单**

### 3. 合同中台回调状态不同步

中台已回调"乙方已签署",但本地 `contract_status` 没更新,页面一直"待签约"。
- **方案**:排查发现回调分支漏写 `setContractStatus`,只改了时间/备注。重构成 `STATUS_MAP` 枚举映射,避免类似低级错误再发生

### 4. 双合同场景展示状态计算错误

作品同时关联"版权合同 + 侵权合同"时,老代码只判断单个合同,导致页面误显示"已生效"。
- **方案**:新增 `existTwoContract()` 判断,双合同场景要求两份都签完才是 EFFECTIVE

### 5. 多环境数据不一致导致适配 bug

SIT/UAT/生产环境机构、合同映射关系存在差异,每次发版需手动修正 5-10 条差异配置。
- **方案**:批量修正差异脏数据 + 将环境差异化配置接入 Apollo/Rainbow 动态管理
- **效果**:**消除硬编码导致的线上适配 bug,发版前数据对齐从 2 小时降到 10 分钟**

### 6. 批量定时任务稳定性优化

夜间批量绑定、数据更新存在数据库锁冲突,**周均 1-2 次失败告警**。
- **方案**:分片拆分(每批 50 条)+ 幂等标记(`update_task_id` 字段)+ 3 次重试改造 + 完善全链路日志
- **效果**:**任务失败告警从周均 1-2 次降到月均 0 次,数据库锁冲突减少 80%**

---

# 项目二:漫剧内容运营后台 vega(辅助项目)

**技术栈**:Spring Boot 2.7.5、Java 21、TRPC、XXL-Job、Spring Security

**业务定位**:阅文漫剧 B 端运营后台,承接**社区内容 / 内容管理 / 商务合约 / 客户管理**全链路。与我主要负责的分销签约平台形成"前后台"互补 — 分销平台产生签约和内容数据,vega 让运营对这些数据进行审核、确权、管理。

**我在此项目的参与**(辅助角色,非 Owner):

- **帖子批量 Excel 导入**:基于 EasyExcel 流式解析,支持 10MB 以内 Excel,50 行上限;单条失败跳过不中断整批
- **合约签署后 CAMS 自动绑定**:XxlJob 每 5 分钟扫描 SIGNED 状态合约,自动拉机构下所有作品去绑定 CAMS
- **合约 URL 存活监控**:定时 HTTP 探活电子签 URL,Redis 压频率 1 小时最多告警 1 次,支持白名单

---

# 项目三:创作者入驻端 nova(辅助项目)

**技术栈**:Spring Boot 2.7.5、Java 21、Undertow、TRPC、MyBatis-Plus

**业务定位**:阅文漫剧**创作者入驻端**(C 端入口),让 UP 主和机构能自助入驻、签约、查收益。和 vega 后台互补 — nova 让用户"进来",vega 让运营"管起来"。

**我在此项目的参与**(辅助角色):

- **机构创建 6 步 Orchestrator 编排**:参数校验 → OCR → 机审 → 落库 → 用户绑定 → 业务派发,策略 + 优先级校验器插拔
- **敏感数据透明加解密**:`AESStringTypeHandler` 配合 DCL 单例从 Spring 拿密钥,DB 字段声明下就自动加解密
- **Cookie(ywguid/ywkey)登录 Filter**:统一认证过滤器 + `@SkipLogin` 注解白名单 + MDC guid 打链路追踪

---

# 跨项目工程经验总结

| 维度 | 做过的事 |
| --- | --- |
| **架构设计** | Controller → Business/Service → Manager → Accessor/Driver 四/五层,OrgManager / UploaderManagementService 聚合层抽象 |
| **设计模式** | 策略+模板方法(5 种合同类型)、工厂(4 种内容类型)、编排器(6 步机构创建)、泛型上下文容器 |
| **并发与一致性** | Redis SETNX 分布式锁、TransmittableThreadLocal 上下文透传、Saga 分布式事务补偿 |
| **性能优化** | N+1 → 方法内 HashMap 缓存(3-5s→500ms)、EasyExcel 流式、Lists.partition 分批 RPC、分区表 |
| **数据一致性** | organization_mapping 双向转换、合同号幂等三层防御、多环境配置化 |
| **安全防护** | AES TypeHandler 透明加解密、HMAC-SHA256 签名常量时间比较、密码 salt+PBKDF2、数据脱敏 |
| **工程习惯** | MDC 链路追踪、Logs.access/error 分通道、moonwalker/sunwalker Agent 监控 |
| **问题排查** | SQL 多表联查定位脏数据、双层防御(代码校验 + DB 唯一索引)、环境差异配置化(Apollo) |

## 核心量化指标汇总

| 指标 | 数据 |
|---|---|
| 合同类型接入周期 | **5 天 → 1 天** |
| 过期状态延迟 | **1 小时 → 0**(动态计算) |
| N+1 查询优化 | **3-5s → 500ms** |
| 签约接口代码量 | **-40%**(CreateContractContext) |
| 后台查询代码量 | **-60%**(分页组件) |
| 新增内容形态 | **3 天 → 0.5 天**(工厂模式) |
| 机构批量创建 | 单次最高 **500 条**,去重准确率 100% |
| 合同号重复 | 上线后 **3 个月零次** |
| 批量定时任务失败 | 周均 1-2 次 → **月均 0 次** |
| 数据库锁冲突 | 减少 **80%** |
| 多环境适配时间 | **2 小时 → 10 分钟** |

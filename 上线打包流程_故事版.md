# 上线打包全流程(唠嗑版 · 阅文真实版)

> 这份是当跟同事吃饭聊"你们是怎么把代码发到线上去的"写的。
> **这份按阅文漫剧真实链路写的**:代码推 GitLab → 七彩石配置中心调参 → 123 平台容器执行。
> 面试被问"讲讲你们的发布流程",照着这个讲,比背「Jenkins + Docker」强多了。

---

## 一、我们用的工具链(先把名字对上)

在阅文漫剧,发布流程涉及 3 个平台 + 2 个中间件:

| 平台/工具 | 用来干啥 |
|---|---|
| **GitLab** | 代码托管、MR 评审、CI 流水线 |
| **腾讯智研 · 七彩石** | 配置中心,所有环境的配置都在这里分组存放 |
| **123 平台** | 腾讯内部容器平台(类似 K8s 控制台),应用部署/扩缩容/登容器执行命令 |
| **Peacock 脚手架** | 阅文集团自研的 Spring Boot 脚手架(封装了 TRPC、Polaris、监控等集团中间件) |
| **TRPC + Polaris** | 集团 RPC 框架 + 服务治理,负责服务注册/熔断/限流 |

这套组合拳,本质上和外面的 **GitLab + Apollo/Nacos + K8s** 是一个思路,只是都是集团自研版。

简历上我写**"熟悉 Spring Boot + 配置中心 + 容器化部署"**,聊到具体就按真实名字说。

---

## 二、配置都在七彩石,不在 application.yml

这是最容易误会的一点,很多人以为配置都在代码里。其实**我们的 application.yml 里基本只有骨架**:

```yaml
# application.yml(简化)
server:
  port: 8080
spring:
  application:
    name: vega
# 其他配置都不在这里,从七彩石拉
```

真正的配置全部在**七彩石配置中心**,按 `appId.groupId.config` 的树形分组,比如我们项目的 vega 应用测试环境:

```
ywos                          ← 一级:业务域
└── solution_vega            ← 二级:应用名
    └── config               ← 三级:配置分组(KV型-私有)
        ├── domain                              = https://testvega.yuewen.com
        ├── sso.client.id                       = testvega
        ├── sso.client.secret                   = xxxxx
        ├── sso.login.baseUrl                   = https://uatsso.yuewen.com/sso/signin
        ├── sso.verify.baseUrl                  = https://uatsso.yuewen.com/sso/verify
        ├── access.control.allow.origin         = https://testvega.yuewen.com,http://127.0.0.1:7001
        ├── contract_tpl                        = (YAML 10KB,合同模板配置)
        ├── contract_template                   = (YAML 400B,协议模板)
        ├── contract.fixed.info                 = (YAML,合同固定字段)
        ├── permission.init                     = (YAML 34KB,权限菜单初始化)
        ├── user.admin.init                     = (YAML,超管邮箱列表)
        ├── copyright.code.config               = (YAML,版权码配置)
        ├── stat.template                       = (YAML,统计报表模板)
        ├── grant.follow.fixed.switch           = true
        ├── access.log.header.print             = true
        ├── access.log.responseBodyAbstract.length = 102400
        ├── spring.servlet.multipart.max-file-size = 50MB
        └── ...(共 56 条)
```

**每条配置都支持**:
- **类型**:STRING、YAML、JSON、BOOL 等(图里大部分是 STRING 和 YAML)
- **状态机**:草稿 → 发布中 → 已发布
- **历史版本**:每次改都有版本号,可以回滚到任意历史版本
- **最近操作人 + 时间**:改了啥、谁改的、啥时候改的,有审计
- **生效方式**:推拉结合,应用里的 SDK 监听变更,热生效不用重启

### 不同环境用不同的配置树

- **测试**:`ywos.solution_vega.config` 挂 test 环境(比如 domain 是 `testvega.yuewen.com`)
- **预发**:对应的配置挂在七彩石的预发环境
- **生产**:对应的配置挂在七彩石的生产环境

环境切换就是七彩石顶栏的环境下拉,配置隔离很干净。

### 为啥不是所有配置都放代码里

1. **敏感信息不能进 Git**:sso.client.secret、数据库密码、AK/SK 这些在七彩石的"加密"分组单独存
2. **不同环境不同值**:domain、数据库地址、Redis、下游服务地址,每个环境不一样,七彩石按环境分组
3. **热更新**:开关类配置(`grant.follow.fixed.switch`、熔断阈值)改了立即生效,不用重启
4. **审批和审计**:生产配置改动有审批流,历史版本完整可追溯

---

## 三、完整的发布流程(从敲代码到线上)

### 第 1 步:本地开发、本地自测

在本地写代码,启动时 Peacock 脚手架会自动从**七彩石测试环境**拉配置:

```bash
# IDEA 启动时指定 profile
-Dspring.profiles.active=test
-Dpeacock.config.env=test
```

脚手架会把 `ywos.solution_vega.config` 这个分组的所有 KV 注入 Spring Environment,代码里就能用:

```java
@Value("${domain}")
private String domain;    // https://testvega.yuewen.com

@Value("${sso.client.id}")
private String ssoClientId;
```

复杂的 YAML 配置用 `@ConfigurationProperties` 绑定:

```java
@Component
@ConfigurationProperties(prefix = "contract.fixed.info.contractFixedInfo")
@Data
public class ContractFixedConfig {
    private String ourEntity;
    private String registeredAddress;
    private int cooperationTermYears;
    private int totalFilmLengthMinutes;
    // ...
}
```

### 第 2 步:推 GitLab,提 Merge Request

```bash
git add .
git commit -m "feat(sign): 拟真短剧-联合开发合同模板支持 70/30 分成"
git push origin feature/virtual-joint-70
```

GitLab 上提 MR,指定 reviewer。推上去会自动触发 CI:

```
GitLab CI 流水线:
1. mvn compile                  - 编译
2. mvn test                     - 跑单元测试
3. mvn checkstyle:check         - 代码规范
4. sonar 扫描                    - 代码质量 + 安全漏洞
5. 构建产物                      - 打 jar,上传集团制品库
```

任何一步挂了,MR 会标红,reviewer 看都不会看。CR 至少 2 个 approve 才能 merge。

### 第 3 步:同步改七彩石配置(和代码改动配对)

这一步容易忘,但超关键。

如果这次代码加了**新的配置项**(比如新增了一个开关 `feature.x.enabled`),需要**同时在七彩石对应环境加这个 key**,否则代码启动读不到会报错或者走兜底。

操作流程:

1. 打开七彩石控制台 → `ywos.solution_vega.config` 分组
2. 点"添加配置" → 填 Key、Value、类型、描述
3. 保存(变成草稿状态)
4. 点"发布",填发布单说明 → 提交审批
5. 审批通过后,配置变"发布中" → 各机器 SDK 拉到新值 → "已发布"

图里 `permission.init` 那行状态是**"发布中"** — 就是这种状态:已经提交发布但还没在所有机器上生效。

### 第 4 步:代码合并 master,构建产物

MR 合并后,master 分支触发**构建流水线**:

```
master 构建:
1. mvn clean package -P test      - 打 test 环境产物
2. 推送到集团制品库,打 tag:vega-1.8.7
3. 企业微信机器人通知"vega 1.8.7 构建完成"
```

打出来的产物是个 jar 或者 tar.gz,里面**不含**七彩石的配置值(配置运行时拉)。

### 第 4.5 步:本地 `publish.bat` + dtools 自动打包(实际最常用的一条路径)

GitLab CI 是"理想路径",**日常发版更多是开发本地双击 `publish.bat`**,原因:
- CI 排队长、偶尔环境问题;`publish.bat` 本地跑 3~5 分钟就能出产物
- 急着修线上小 bug 时,本地打包 + 直接发 123 更快
- dtools 的认证、制品库地址、版本号规则已经内置好,不用记

#### 目录里的 `publish.bat` 大概长啥样

每个 Peacock 项目根目录都有一个 `publish.bat`(Windows)和 `publish.sh`(Mac/Linux)。本质是个 **dtools 调用入口**:

```bat
@echo off
REM publish.bat - 本地一键打包发布脚本
REM 用法:双击,或 publish.bat <env> <version>

set PROJECT=vega
set ENV=%1
if "%ENV%"=="" set ENV=test
set VERSION=%2
if "%VERSION%"=="" set VERSION=auto

echo ============================================
echo  项目: %PROJECT%
echo  环境: %ENV%
echo  版本: %VERSION%
echo ============================================

REM 调 dtools 命令,真正干活的是它
dtools build ^
  --project %PROJECT% ^
  --env %ENV% ^
  --version %VERSION% ^
  --push

if %ERRORLEVEL% NEQ 0 (
  echo [FAIL] 打包失败
  pause
  exit /b 1
)

echo [OK] 制品已推送,可以去 123 平台发版了
pause
```

Mac/Linux 的 `publish.sh` 一样的思路,第一行换成 `#!/bin/bash`,把 `set` 换成 `export`。

#### dtools 是干啥的

**dtools** 是阅文/腾讯内部的打包发布 CLI(类似外部的 `mvn deploy` + `docker push` 一把梭),一个命令能做完:

```
dtools build --project vega --env test --version auto --push

内部步骤:
1. 读项目 pom.xml 或 gradle 配置
2. mvn clean package -P <env>       - Maven 打包,跑单测(可跳过)
3. 检出当前 git HEAD 作为版本号的一部分,自动生成 1.8.7-abc123-20260504
4. docker build 或者直接打 tar.gz  - 按项目规范打产物
5. dtools 登录集团制品库(用本地 .dtoolsrc 的 token)
6. 推送制品:制品名 = vega-1.8.7-abc123-20260504
7. 打印产物 URL / 版本号,让你去 123 平台能选到
```

本地双击 `publish.bat` 跑完,大概 3~5 分钟。最后一行会输出版本号,**复制这个版本号**,下一步到 123 平台发版选它。

#### 常见用法

```bash
# 1. 打测试环境产物(默认)
publish.bat
# 等同于 publish.bat test

# 2. 打预发环境产物(profile=staging)
publish.bat staging

# 3. 打生产环境产物,并指定版本号
publish.bat prod 1.8.7

# 4. 跳过单测(修紧急 bug 时用,谨慎)
dtools build --project vega --env prod --skip-tests --push

# 5. 只打包不推送(本地验证)
dtools build --project vega --env test --version auto
# 没带 --push,就只在本地生成 target/vega-*.jar
```

#### 和 GitLab CI 的关系

| 场景 | 用 CI | 用 publish.bat |
|---|---|---|
| 常规版本发布 | ✅ 推荐 | 也可以 |
| 紧急 hotfix | 慢 | ✅ 快 |
| 本地调试环境镜像 | 不方便 | ✅ 直接打本地镜像 |
| 正式生产发版 | ✅ 必须(留痕) | ❌ 不允许,要有 CI 记录 |

**生产发版必须走 CI**,原因:CI 有完整的审计(谁触发的、跑了啥测试、什么时候合并的)。
`publish.bat` 是**日常测试/预发快速迭代**的利器,生产禁用。

#### 踩过的坑

1. **本地 `.dtoolsrc` 的 token 过期**:提示 `401 Unauthorized`。`dtools login` 重新登录。
2. **publish.bat 打出来的版本号 123 平台选不到**:忘了加 `--push`,产物在本地没上传。
3. **mvn 依赖拉不下来**:公司内网镜像 `settings.xml` 配置问题,检查 `~/.m2/settings.xml` 里阅文/腾讯的私服地址。
4. **本地 JDK 版本不对**:dtools 会按 pom 里声明的 JDK 版本检查,JDK 17 的项目用 JDK 8 打会直接拒绝。

---

### 第 5 步:123 平台发布到 test 环境

这就是你说的"去 123 平台容器里面去跑命令"那一步。

123 是腾讯内部的容器管理平台,类似 K8s 的控制台。在上面能看到每个应用的每个环境对应的容器组。

> **衔接 publish.bat**:上一步 `publish.bat` 推到制品库的版本号(例如 `vega-1.8.7-abc123-20260504`),就是这一步 123 平台"选版本"下拉里出现的选项。

测试环境发布流程:

1. 登录 **123 平台** → 找到 `vega-test` 应用
2. 点"发布" → 选版本号 `vega-1.8.7-abc123-20260504` → 选机器列表(测试环境一般 2-3 台)
3. 123 平台在后台做:
   - 停老容器 → 拉新镜像/新 jar → 启新容器
   - 启动时容器会**从七彩石 test 环境拉配置**
   - 等健康检查 `/health` 返回 200 → 流量切过来
4. 发布日志和容器状态在 123 平台上看

**发布过程中如果发现问题要手动查**,这就是"去容器里跑命令":

```bash
# 123 平台 → 选一台实例 → 点"登录容器" 进入
# 常用排查命令:

# 看应用日志
tail -f /data/log/vega/app.log
grep ERROR /data/log/vega/app.log | tail -50

# 看配置拉下来对不对(七彩石 SDK 缓存在本地)
cat /data/peacock/config/cache/ywos.solution_vega.config.json | jq .

# 看 JVM 进程
jps -lv
jstat -gc <pid> 1s 10

# 看端口
netstat -nltp | grep 8080

# 看内存/CPU
top -p $(pidof java)

# 看下游调用
curl -v http://testsso.yuewen.com/sso/verify

# 生成堆栈(接口慢/死锁)
jstack <pid> > /tmp/jstack.txt

# 生成堆 dump(OOM)
jmap -dump:format=b,file=/tmp/heap.bin <pid>
```

能上容器跑命令是关键本事。**光会看日志不够,遇到偶发问题必须能登进去看**。

### 第 6 步:测试环境 QA 验证

QA 在测试环境跑:
- 冒烟用例(核心链路,签约/合同/结算)
- 新功能用例
- 回归用例(老功能别挂)

通过后 QA 在企业微信群里**@开发 + PM 说"测试通过,允许上线"**。这句话是红线,没这句不许发预发。

### 第 7 步:预发环境(staging)

预发是**"连生产数据库、但只有内部流量"**的环境:

1. 七彩石切到 staging 环境 → 把需要改的 key 先在 staging 改好并发布
2. 123 平台选 `vega-staging` 应用 → 发版本 `1.8.7`
3. 开发自己造 3-5 笔核心业务(签约、结算、查权限),确认没问题
4. 重点看:
   - **连生产 MySQL 的 SQL 执行计划**(和测试环境数据量不一样,可能慢)
   - **下游服务调用**(生产的 CAMS、分润系统,接口可能和测试环境不一样)
   - **配置拉的是生产配置中心**(预发和生产共用配置分组)

预发发现的 bug,大概率是"测试环境发现不了的":网络问题、外部依赖问题、数据量问题。

### 第 8 步:生产环境灰度发布

这是最慎重的一步。

**配置先行**(如果这次有配置变更):

1. 七彩石切到生产环境 → 找到 `ywos.solution_vega.config` 生产分组
2. 改对应的 key 或新增 key → **发布单要写清楚影响范围**
3. 提交审批 → Leader / TL 审批 → 审批通过后开始推送
4. 七彩石按机器分批推送新配置(默认全量推,但可以配灰度比例)
5. 观察应用里是否生效:
   ```bash
   # 登容器,看 SDK 日志
   grep "PeacockConfigUpdate" /data/log/vega/app.log | tail
   ```

**代码发布**:

在 123 平台上按灰度策略发:

```
生产灰度节奏(假设 10 台机器):
- 第 1 批: 1 台(10% 流量)    观察 30 分钟
- 第 2 批: 2 台(30% 流量)    观察 30 分钟
- 第 3 批: 3 台(60% 流量)    观察 30 分钟
- 第 4 批: 剩余全部(100%)    观察 2 小时
```

每批发完,**开发要登 123 平台 + Grafana 一起看**:

- **123 平台**:容器启动日志是否正常、健康检查过没过
- **Grafana**:
  - 接口 QPS/错误率/延迟
  - JVM 堆内存、GC 频率
  - CPU、磁盘、网络
- **业务面板**:签约数、结算金额、权限接口调用量有没有异常
- **企业微信告警群**:有没有 `[P2]` 以上告警

有任何异常,**立刻停止灰度**,123 平台点"回滚",30 秒内切回老版本。

### 第 9 步:全量后的重点观察期

100% 发完,**不能马上下班**:

- 企业微信群里说"vega 已全量,重点观察 2 小时"
- 继续盯监控和告警群
- 2 小时内没事才算真正"发版完成"

---

## 四、七彩石变更的特殊流程(容易被忽略)

这一节值得单独讲,因为**很多线上事故是配置引起的,不是代码**。

### 七彩石改 key 的正确姿势

❌ 错误:直接在生产分组改 value → 点发布 → 完事

✅ 正确流程:
1. 先在**测试环境**改、验证
2. 再在**预发环境**改、验证
3. 提**生产变更单**,说明:
   - 改哪个 key
   - 老值 → 新值
   - 生效影响(哪些接口/功能会被影响)
   - 回滚方案(出问题怎么改回去)
4. **分配审批人**(一般是 TL 或更上级)
5. 审批通过后在生产分组改 → 发布
6. **观察 10-30 分钟**,看应用是否正常

### 七彩石配置分批推送(灰度)

七彩石本身支持按机器推送,比如:

```
配置发布策略:
- 阶段 1: 推送到 1 台机器(测试 SDK 收到变更、应用生效)
- 阶段 2: 推送到 30% 机器
- 阶段 3: 全量推送
```

这个在七彩石控制台的"发布任务"页面可以配置。

### 紧急关开关的套路

线上出问题第一反应往往是**关开关**,比如:

```
线上: 签约接口频繁超时
怀疑: 新加的 XX 校验逻辑性能问题
操作: 七彩石里改 grant.follow.fixed.switch = false
效果: 2 秒内所有机器 SDK 收到新值,走老逻辑,恢复
```

这也是为啥我们代码里关键新功能都加**开关兜底**。新功能上线默认 **开关关着**,走老逻辑,灰度确认没事再 **打开开关**。

---

## 五、真实踩过的坑

### 坑 1:代码先上,配置忘加,应用起不来

加了新 feature 时,代码里 `@Value("${feature.x.enabled}")` 已经注入了,但七彩石里**忘了加这个 key**。

测试环境 QA 发现启动报错:
```
IllegalArgumentException: Could not resolve placeholder 'feature.x.enabled' in value "${feature.x.enabled}"
```

现在我们的规矩:
- `@Value` 必须带默认值:`@Value("${feature.x.enabled:false}")`
- 或者用 `@Value("${feature.x.enabled:#{null}}")` 允许为空
- MR 模板里加一行 **"新增配置是否已在七彩石各环境配置"** 的 checklist

### 坑 2:配置类型搞错了

加了一个 `retry.maxTimes = 3`,设置类型为 **STRING**。代码里:

```java
@Value("${retry.maxTimes}")
private int maxTimes;
```

启动时 Spring 自动做类型转换,"3" → 3,没事。但有次我误写成 `3次`(字符串里多了"次"),应用启动直接挂。

解决:
- 配置 key 的类型在七彩石里选准(数值就 INT,开关就 BOOL)
- 代码里做兜底:`@Value("${retry.maxTimes:3}")`

### 坑 3:多个环境的配置分组没对齐

新增 key 只在测试环境加了,上预发的时候忘了,结果预发读不到这个 key。

后来我们的做法:**"同一次变更,多环境同时加"**。写一个脚本,七彩石有开放 API,一次性把 test / staging / prod 三个环境都加上(值不同,但 key 齐全)。

### 坑 4:上了容器才发现配置路径写错了

七彩石 SDK 拉下来的配置会缓存在容器里的 `/data/peacock/config/cache/` 目录。有次我代码里写了相对路径读配置文件,本地正常、测试正常、生产挂了。

原因:生产容器和测试容器的工作目录不一样。

教训:**配置拉取走七彩石 SDK 统一接口,不要直接读文件**。

### 坑 5:123 平台发版忘记重启健康检查

有次新配置需要重启应用才能生效(老代码没做热加载),发完以后机器没重启,配置没生效,接口继续用老逻辑。

现在我们的发版清单里必有一行:"这次变更需不需要重启"。

### 坑 6:七彩石灰度推送 + 代码灰度发布 叠加

最复杂的一次事故。同时发生:
- 代码灰度(50% 机器是新代码)
- 配置灰度(70% 机器是新配置)

结果有一小部分机器是 **新代码 + 老配置**,新代码读不到新 key 的值,走了错误的兜底分支。

教训:**配置变更和代码发版尽量不要交叉做**。要么先全量推配置再发代码,要么先发代码(代码兼容新老配置)再推配置。

---

## 六、面试常见 Q&A

### Q: 讲讲你们的 CI/CD 发布流程

> "我们的链路是 **GitLab → 七彩石 → 123 平台**:
>
> 1. **代码在 GitLab**,提 MR,CI 跑编译/测试/Sonar
> 2. **配置在腾讯智研七彩石配置中心**,按 `appId.groupId.config` 树形管理,分测试/预发/生产三个环境
> 3. **构建产物传集团制品库**,打版本号 tag
> 4. **在 123 平台发版**,选版本号 → 选机器 → 灰度发布(1 → 3 → 6 → 10 台,每批观察 30 分钟)
> 5. **配置变更要和代码配套**:代码里新增 `@Value` 注入的 key,七彩石各环境要同步加
> 6. **发完可以登 123 平台容器**排查问题,看日志、jstack、jmap、curl 下游
>
> 出问题 **5 分钟内能回滚**:123 平台点回滚、七彩石配置回退上个版本。"

### Q: 配置是怎么管理的

> "集中式配置中心 + 推拉结合:
>
> - 所有配置在七彩石,按 `ywos.solution_vega.config` 这样的树形分组
> - 每个环境一套独立的分组
> - 应用启动时 SDK 从七彩石**拉**全量配置
> - 运行时变更,七彩石**推**给所有在线应用,热生效
> - 敏感配置用加密分组,走集团 KMS
> - 每次变更有版本、审计、审批流
>
> 代码里基本只有 `application.yml` 的骨架 + `@Value`/`@ConfigurationProperties` 注入。"

### Q: 怎么做灰度发布

> "两层灰度:
>
> 1. **容器层**(123 平台):10 台机器分 4 批发:1 → 3 → 6 → 10,每批 30 分钟观察期
> 2. **配置层**(七彩石):支持按机器/比例灰度推送配置,比如先 30% 机器生效新配置
>
> 流量层面,大流量接口用**基于 uid 的会话保持**,避免同一用户请求在新老版本间跳。
>
> 观察三类指标:
> - **应用层**:QPS、错误率、延迟 P99
> - **JVM**:堆内存、GC 频率、线程数
> - **业务指标**:签约数、结算金额、接口成功率
>
> 任意一个异常立刻停灰度、回滚。"

### Q: 线上出故障你怎么排查

> "**先恢复、再定位**。三板斧:
>
> 1. **先看 Grafana 监控**:找异常面板(5xx 升高、延迟飙升、机器抖动)
> 2. **登 123 平台**:看异常机器的容器状态、启动日志
> 3. **进容器**:
>    - `tail -f app.log` 看异常堆栈
>    - `jstack <pid>` 抓线程栈(接口慢/死锁)
>    - `jstat -gc` 看 GC(OOM/内存泄漏)
>    - `jmap -dump` 抓堆(复杂内存问题)
>    - `curl` 下游服务(下游问题)
>    - 看七彩石 SDK 本地缓存的配置是不是对的
>
> 定位到原因后:
> - **配置问题**:七彩石回退到上个版本
> - **代码问题**:123 平台回滚到上个代码版本
> - **下游问题**:开熔断/降级开关(七彩石里改开关)
> - **流量问题**:限流开关打开
>
> 恢复完写故障报告,做复盘。"

### Q: 配置变更的流程是什么

> "分测试/预发/生产三步走:
>
> 1. **测试环境**先改,验证功能
> 2. **预发环境**再改,验证连生产依赖没问题
> 3. **生产环境**走审批流:
>    - 提变更单,说明改啥、老值、新值、影响、回滚方案
>    - TL 审批
>    - 七彩石按策略推送(可选灰度推送)
>    - 观察 10-30 分钟
>
> 关键配置(像数据库连接、下游服务地址、限流阈值)有额外的审批人要求,一般要 2 人以上审批。
>
> 所有变更都有**版本和审计**,谁改的、什么时候改的、老值是什么,可以回到任意历史版本。"

### Q: 有没有碰到过配置和代码耦合出问题的场景

> "**最典型**:新增配置 key,代码上了生产但配置没加。启动就报 `Could not resolve placeholder`。
>
> 现在我们的规矩:
> - `@Value` 必须带默认值,`@Value(\"${xxx:defaultVal}\")`
> - MR 模板里有 checklist:『新增/变更配置是否已在三个环境同步添加』
> - 重要配置变更和代码发版**不交叉**,要么配置先行,要么代码先行
>
> **配置灰度和代码灰度同时进行**也是事故高发期,要避开。"

### Q: 你在发布流程里干了什么

> "作为一个 5 年后端,我参与的环节:
>
> - **提 MR 和 CR**:自己写代码、review 同事代码
> - **推动配置变更**:代码改动涉及配置的,我要在七彩石三个环境同步改,写变更单
> - **在 123 平台发版**:测试环境是我点发布,生产环境是 TL 点,但出问题我要进容器排查
> - **值班和故障处理**:我们组 on-call 轮值,on-call 期间线上告警我第一响应,登容器排查、决定是否回滚/打降级开关
> - **复盘**:故障后写故障报告,提改进项(加监控、改代码、改流程)"

### Q: 本地有没有快速打包的方式

> "有,项目根目录都有个 `publish.bat`(Windows) / `publish.sh`(Mac),双击就行。
>
> 脚本里其实是调集团的 **dtools** 命令:`dtools build --project vega --env test --push`。dtools 会做 `mvn package` → 打镜像/tar → 推到集团制品库,一把梭 3~5 分钟。
>
> 日常流程:
> 1. 本地双击 `publish.bat test`
> 2. 等它打完,复制输出的版本号,比如 `vega-1.8.7-abc123-20260504`
> 3. 去 123 平台找 `vega-test` 应用,选这个版本号发布
> 4. 登容器 `tail -f /data/log/vega/app.log` 看启动日志
>
> **不过生产发版必须走 GitLab CI**,不能用 publish.bat——CI 有完整的审计链路(谁触发的、跑了啥测试),publish.bat 更多是测试/预发快速迭代用。
>
> 踩过的坑:本地 `.dtoolsrc` token 过期会报 401,`dtools login` 重登就行;忘加 `--push` 参数,产物只在本地,123 平台选不到。"

---

## 七、速记卡

- **链路三件套**:**GitLab(代码) + 七彩石(配置) + 123 平台(容器)**
- **本地快速发版**:双击 `publish.bat` → 内部调 `dtools build --push` → 制品上库 → 123 平台选版本发布(生产不允许用,只走 CI)
- **配置结构**:`appId.groupId.config` 树形分组,**每环境一套**
- **配置特性**:类型化、版本化、审批、审计、热推送
- **代码里少用 application.yml**,多用 `@Value` + `@ConfigurationProperties` 注入
- **发版顺序**:代码 merge → 构建产物 → test → QA → staging → 生产灰度(1/3/6/10 分批)→ 全量观察期
- **容器排查四板斧**:`tail -f app.log` / `jstack` / `jstat -gc` / `curl 下游`
- **出问题三选一回滚**:七彩石回退配置、123 平台回滚代码、紧急打降级开关
- **配置变更红线**:测试 → 预发 → 生产 走三步,关键配置需审批
- **代码+配置不同时灰度**,避免新代码读老配置的尴尬
- **周五下午不发版,大活动前 48 小时冻结**

# 失败项目复盘 · 那次被打回的方案

> 如果面试官问我"**讲一个你搞砸过的项目 / 被打回来的方案**",我会讲这个。
> 这是 2025 年中的事,现在想起来还有点脸红,但复盘下来**学到的比那些一把过的项目多得多**。
> 这份文档比成功案例更重要——**5 年经验的工程师最该展示的,是从失败里捡起来的那些东西。**

---

## 一、一句话先说清楚是什么

**2025 年中,我主导了一次结算对账流程的批处理优化。**
**自信地估了 2 周工期,闷头写了 10 天代码。**
**跟业务方对方案的时候,被当场打回——"你理解错了,我们有一个关键约束你根本没考虑。"**
**结果代码推倒重写,工期拉长到 4 周,项目组被迫延期。**

这是我**第一次被业务方当面打回方案**,脸上挂不住。但复盘下来,责任在我,不在任何人。

---

## 二、这事儿到底怎么发生的

### 起因:我接了一个"看起来很简单"的需求

2025 年 6 月某天,我们组的 TL 老周来找我:"**结算对账这块最近跑得慢,月底那几天 Job 要跑 4-5 小时,还偶尔超时。你去优化一下。**"

我点头:"行,这活儿我来。"

他:"需求文档看看就行,核心就是'把跑批时间降下来'。业务背景你找财务的李姐聊一下就够了。"

我:"嗯,估个 2 周?"

他:"2 周差不多。搞定之后写个复盘。"

——**我犯的第一个错误就是从这里开始的:我没有去找李姐**。

我心想:"不就是跑批优化吗?看看代码逻辑,加点并行,搞点批量 SQL,性能上来就完事儿。需求文档都写了'降低跑批时间',业务背景还需要聊啥?"

---

### 过程:我闷头写了 10 天

周一开工。我把原来的对账 Job 代码翻了一遍,理清楚了:

**原逻辑**(简化版):
```
for 每个机构 in 所有机构:
    for 每条订单 in 该机构当月订单:
        查订单 → 查关联合同 → 查关联商品 → 计算金额 → 写对账结果
```

经典 **N+1 问题**。每条订单 4 次 DB 查询,一个月几十万条订单 = 几百万次查询,慢得要命。

**我的优化思路**:
1. 把"查订单"、"查合同"、"查商品"改成**批量查询** + **HashMap 缓存**
2. 机构维度的处理改成**并行**(CompletableFuture + 线程池)
3. 写对账结果从"逐条 insert"改成 **batch insert**
4. 过程中用 **Redis 分布式锁**防止同一机构被重复处理

搞完跑了一下,**从 4 小时干到 40 分钟**,我心里美滋滋。

第 5 天,代码写完、自测通过。第 6-8 天,我又加了些**"锦上添花"**的东西:
- 写了个**重跑工具**,运营可以手动触发某个机构重跑
- 加了**进度监控**,每处理完 10% 发一次 MQ 事件
- 把错误处理重构了一下,**失败的机构单独落表,不影响其他机构**

一周多,我自己觉得做得挺好。**架构漂亮、性能提升 6 倍、扩展性强**。

第 10 天,我约李姐和老周一起对方案,准备上线。

---

### 现场:方案被当场打回

会议开始。我打开我画的架构图,讲了一遍。

讲到一半,李姐皱眉:"**等等。你这个并行处理,是每个机构独立跑的对吧?**"

我:"对,机构之间互不干扰,并行跑效率更高。"

她:"**那关联机构怎么办?**"

我愣住了:"……啥关联机构?"

她:"有些机构之间是**关联结算**关系,比如一家母公司 + 几家子公司,**它们的订单是合并对账的**,不能分开跑。**你分开跑出来的金额就不对。**"

我:"……"

(心里 OS: 我代码里没有这个概念,关联机构的字段我都没见过。)

老周听出不对:"**这个关联关系在哪个表里?**"

李姐:"**不在我们的 DB 里。**是在**合同系统**里维护的,通过合同关系推导出来。你在对账 Job 里要先去合同系统拉一遍'**关联机构图**',然后按图合并处理。"

我的额头开始冒汗:"……**这层逻辑原来的代码里也没有啊?**"

李姐:"**原来是有的**。你看看……"(她翻了下自己的 Word 文档)"**以前是在那个叫 `ContractGroupResolver` 的类里处理的,你改代码的时候保留了吗?**"

我赶紧打开代码搜 `ContractGroupResolver`——**没有了**。

我重构的时候把"一些看起来不用的代码"清掉了,其中就包含 `ContractGroupResolver` 的调用。**因为它不在主流程上,是一个预处理步骤**,看起来"不关键"。

老周脸色有点严肃:"**那你这个并行方案整个前提就错了。关联机构合并必须在并行之前做完。**"

李姐看了一眼架构图:"**而且,关联机构跨机构,你用机构级别的分布式锁也锁不住。**"

会议室一阵沉默。

我鼓起勇气:"**那……代码要推倒重来?**"

老周:"**看看能不能改,不能改就重来。**"

当天下午我试着改,发现改不了——**整个并行框架的前提是"机构之间独立"**,一旦加入"关联机构必须一起算",整个调度模型就崩了。

----

**第二天早会,我举手说**:"昨天方案评审被打回,我这边要**推倒重来**,预计再需要 2 周。"

老周当着全组面:"**彭冲,你下来和李姐先对齐需求,再动代码**。"

当时我脸烫得能煎鸡蛋。

---

### 收尾:多花了 2 周,带伤上线

接下来 2 周我重新做:

**做的事**:
1. 先花 1 天找李姐聊清楚**所有业务约束**(关联机构、跨月结转、特殊机构白名单),记了 10 页 Word
2. 方案重做:**先从合同系统拉关联图** → **按图分组** → **组内串行、组间并行**
3. 分布式锁从"机构级"改成"**关联组级**",用组内第一个机构的 ID 做 lockKey
4. 重写后性能依然有 5 倍提升(从 4h 到 50min),虽然没一开始那么极致,但**正确**

**代价**:
- **项目组延期 2 周**
- 新需求"对账差异实时告警"功能被砍(没时间做)
- 这季度的绩效受了影响(被打上"方案理解不到位")

---

## 三、复盘:到底错在哪

那周末我给自己开了个复盘,列了 4 个错误:

### 错误 1:想当然,不见客户

**需求文档写的"优化跑批时间",看起来就是个性能问题**。我直接跳过了"**业务约束收集**"这一步,开干。

但是:**性能优化的前提是"保持原语义"**。我连原语义都没搞清楚,怎么谈优化?

**教训**:**凡是涉及线上系统改造,开工前必须和业务方 / 使用方对一次**。哪怕你觉得"一眼就懂",也要对。

### 错误 2:删了不懂的代码

`ContractGroupResolver` 这个类,我当时看了下——**没注释、没单测、主流程用不到**。我下意识判断"**这是之前没用的遗留代码**"。

但实际上——**它是前人为解决一个复杂场景写的兜底逻辑,只是因为调用链比较隐蔽**(只在某个条件分支里被 call)。

**教训**:**"看起来没用的代码"常常是前人踩过坑留下的宝贝**。不确定它的作用之前,绝对不要删。

具体做法:
- 用 **`git log -p`** 看它当初是为什么加进来的(至少看看 commit message)
- 用 **`git blame`** 看是谁写的,去问问
- 或者**保留不动,只改其他代码**

### 错误 3:方案里没有"不变量"清单

我的方案文档里列了:
- ✅ 性能目标
- ✅ 技术方案
- ✅ 架构图
- ✅ 风险点(技术风险)

**但没有列一条**:
- ❌ 业务不变量(Invariants)——**即什么东西无论怎么改都必须保持**

关联机构合并处理就是一个**核心不变量**,我根本没意识到它的存在。

**教训**:**做任何改造前,先列出"系统现在保证了什么"**。写成清单,跟业务方逐条确认。

比如这次的改造,正确的不变量清单应该是:
1. 每条订单有且只有一条对账结果
2. **关联机构的订单必须合并计算**
3. **跨月结转不能错账**
4. 失败的对账要能单独重跑,不影响其他
5. 总金额 = 每家机构 sum(对账金额)

有这个清单,我就不会把 `ContractGroupResolver` 删掉。

### 错误 4:埋头 10 天不同步

我写完 5 天代码之后,应该**主动找李姐走查一遍方案和代码**,而不是等全部写完才对齐。

在工程上有个术语叫 "**fail early**"——早失败比晚失败好。**5 天发现偏差 vs. 10 天发现偏差**,返工成本可能差一倍以上。

**教训**:**重要项目要设置 check-point**——每个里程碑跟利益相关方(PM、业务、TL)对齐一次,不能闭门造车。

---

## 四、我是怎么改的

这个教训太痛了,以至于**后来我做每一个需求,都形成了 4 个习惯**:

### 习惯 1:开工前列"不变量清单"

无论多简单的需求,我都要先写 3 件事:
- **做什么**(目标)
- **不能做什么**(边界)
- **必须保持什么**(不变量)

**第三条是防呆用的**。开工前跟业务方把不变量逐条过一遍,确认没遗漏。

### 习惯 2:改代码前,先问"它为什么存在"

任何"看起来没用"的代码,我都会:
1. **先查 git blame + commit message**
2. **看不懂就去问原作者**(或者问老周、问组里干得久的)
3. **实在要删,先 comment out 跑一周,看有没有告警**

**不要当代码考古学家**。你删一行 8 年前的代码,可能有 8 个线上场景会出问题。

### 习惯 3:10 天以上的项目,中间必须有 check-point

自己给自己定的规矩:
- **第 3 天**:方案定稿,走一遍核心利益相关方
- **第 5 天**:写完核心代码,找业务方/TL 走一遍 demo
- **第 8 天**:做到 80% 就打包 UAT,不等 100% 再测
- **收尾**:上线前 review 不变量清单

### 习惯 4:估时乘以 1.5

**我以前估"2 周能搞定",往往实际要 3 周**。
原因不是我效率低,而是**我低估了沟通、测试、改 bug、返工的时间**。

现在我的原则:**技术开发时间 × 1.5 = 真实工期**。宁可估多,不要估少。估多完成的早,领导表扬;估少做不完,就是你这次的烂账。

---

## 五、面试怎么讲这个故事

### 开场(不躲闪、不找借口)

"我讲一个 2025 年中我搞砸的项目。那次是**结算对账批处理优化**,我自信估了 2 周,闷头写了 10 天代码,**跟业务方对方案的时候被当场打回**——他们说我理解错了业务约束,代码推倒重来。"

**关键**:**直接承认"我搞砸了"**,不要说"因为需求不清晰所以我……"这种甩锅话。

### 讲发生了什么(3 分钟)

把第二章的过程讲一遍。不要修饰,不要美化。

**面试官爱听这种细节**:
- "会议室当场沉默"
- "脸烫得能煎鸡蛋"
- "方案图被打回"

这些细节证明**这是真事,不是编的故事**。

### 讲复盘(2 分钟,最重要的部分)

"后来我把这事儿分成 4 个错误:
1. **没和业务方对齐就开工**——假设需求简单就不用沟通
2. **删了不懂的代码**——把别人的遗留逻辑当没用
3. **方案里没有不变量清单**——性能目标写了,业务约束没写
4. **埋头 10 天不同步**——没有 check-point,错到底才发现"

### 讲改变(1 分钟)

"从那之后,我做任何改造前,都有 4 个习惯:
- **列不变量清单**,跟业务方逐条确认
- **改代码前,先问它为什么存在**(git blame + 问原作者)
- **项目里设 check-point**,每 2-3 天同步一次进度和方向
- **估时 × 1.5**,给沟通和返工留 buffer"

### 如果被追问(压力面技巧)

**Q: "那你觉得责任在谁?"**

A: "**责任在我**。TL 让我'看需求文档就够了'是放权,不是不对。业务方不主动给我讲业务是合理的,**因为我没问**。代码里的遗留逻辑没注释是前人的问题,但**删的人是我**。5 年工程师该有的判断力我那次没有。"

**Q: "下次你会怎么做?"**

A: "我现在的规矩是:**涉及线上系统改造,必须找业务方坐下来过一次需求**,哪怕只有 30 分钟。**保留不确定的遗留代码**,除非能证明它没用。**每 3 天跟利益相关方对齐一次进度**。这三条是我自己写进 `CODEBUDDY.md` 的工作流程。"

---

## 六、这个故事比成功案例更能打动面试官

说实话,讲成功案例很容易被面试官怀疑"**你是不是在吹?**"。

讲失败反而容易被相信。**因为没人会编造一个让自己丢脸的故事。**

更重要的是——**5 年工程师和 3 年工程师的区别,往往在于他踩过多少坑、从中提炼了什么**。

一个从不讲失败的人,**要么没做过复杂项目,要么不善于反思**。

两者都不是好信号。

---

## 七、我学到的最大一课

**「性能优化」不是一个纯技术问题,是一个业务问题。**

**你在优化一个你不懂业务的系统,迟早要付出代价。**

这次代价是 2 周延期 + 绩效打折 + 组里脸面。
换来的是**"开工前必须先理解业务"这个终身受用的原则**。

**值**。

---

> 这份文档是我自己写的"工程师自省录"。
> 面试用是一方面,更重要的是——**我希望未来再也不犯同样的错**。

# 剧集池管理(独家授权 + 配置化权限)

> 2026 Q2 做的一个纯 B 端业务改造。给 Vega 运营后台加"公共池 / 独家池"管理,支持**多链路、保护期、释放流转**。
> 表面是 CRUD,实际踩了 **数据模型抉择、Rainbow 权限、Jackson 序列化、发布链路**这几个坑,是面试能讲细节的好素材。
> 我是这个需求的**主力**,3 天代码完成 + 2 天联调。

---

## 一、一句话讲清楚是什么

在 Vega 运营后台新增"剧集池管理"模块,让运营可以决定每条漫剧的**分发规则**:

- **公共池**:配链路 + 授权类型(普通/首发/联合首发),对应客户都能看到
- **独家池**:单一分销商独占,带 1/3/7/14 天保护期,到期可释放至公共池
- **多链路**:支持抖音端原生 / 快手端原生 / 微信小程序免费

核心让商务方能和分销商谈"**独家授权**"——以前没这套工具,所有剧对所有客户开放。

---

## 二、需求背景(面试开场)

### 业务诉求
- **商业模式升级**:引入"独家授权",分销商花钱可以独占 3/7/14 天,卖得好再释放给公共池
- **多链路支持**:不同业务线(抖音 / 快手 / 微信)的授权规则独立配置
- **状态流转**:独家 → 二轮中 → 释放至公共池,有明确生命周期
- **运营可控**:从"上传即公开"升级为"运营主动配置每条剧的分发池"

### 技术诉求
- **数据模型**:公共池 / 独家池的字段差异不大,但有流转关系,设计上要平衡"解耦"和"流转成本"
- **权限体系**:Vega 的菜单/按钮权限通过 Rainbow 配置中心管理,不是硬编码
- **跨系统同步**:剧集池的 `dramaCategory` 字段变化要同步到 merchantry(商户系统)
- **发布**:代码 + 配置 + 权限数据**三条发布链路**独立,容易踩坑

### 我的定位
后端主力 + 单人负责。对接:
- 👆 **内容中心**(调 `ShortPlayQueryAPI.listVideoInfosByMvdid` 查子剧)
- 👇 **前端**(剧集池管理页面)、**merchantry**(dramaCategory 同步)
- ↔️ **产品**(授权类型规则、保护期定义)、**运维**(Rainbow 配置、Sumeru 发布)

---

## 三、核心设计思路

### 3.1 数据模型:单表 + 类型字段

公共池和独家池**字段相似但不相同**:

| 维度 | 公共池 | 独家池 |
|---|---|---|
| 授权类型 grant_type | ✅ 普通/首发/联合首发 | ❌ 不用 |
| 独家周期 exclusive_period | ❌ 不用 | ✅ 1/3/7/14/98/99 |
| 上架状态 shelf_status | ✅ 可上下架 | ✅ 固定上架 |
| 分销商 | 联合首发才具体配 | 固定单一分销商 |

**方案抉择**:

- **方案 A:两张表**
  - 优点:语义清晰
  - 缺点:"释放到公共池"要跨表 DELETE + INSERT,原子性差
- **方案 B:单表 + `pool_type`**
  - 优点:流转只要 `UPDATE pool_type`,原子操作
  - 缺点:部分字段冗余(如 `grant_type` 只在公共池用)

**选 B**。核心判据:**"会流转的实体,放同一张表"**,业务上"独家到期释放至公共池"是关键场景。

### 3.2 `distributor_scope` 字段:规避 INSERT 爆炸

"普通/首发"的语义是"**该链路的所有客户都能看**",如果强行把每个客户插一条关联记录:

- 快手端原生有 200 个客户 → 每次加普通剧插 200 行 → 慢 + 不优雅

加个 `distributor_scope` 字段:

- `distributor_scope = 1` → 默认全部客户(**不写关联表**)
- `distributor_scope = 0` → 看关联表具体分销商

查询时按 scope 判断走不同分支,关联表只存"联合首发/独家"这两种真正需要指定的场景。

### 3.3 Rainbow 权限配置(而非 @PreAuthorize)

Vega 权限体系**不是硬编码**。流程:

```
Rainbow 配置中心 permission.init (YAML)
    ↓
PermissionsInitService 启动时或手动触发
    ↓
清空 vega_permission + 按 YAML 重建
    ↓
前端按 role 拿到 permissions 列表 → 控制菜单/按钮显隐
```

我的选择:**Controller 不写 @PreAuthorize**,信任前端根据 Rainbow 配置决定。好处:

- 权限调整不用发版,改 YAML 即可
- 菜单 / 按钮 / 接口鉴权统一由配置驱动

坏处:YAML 写错会让整个权限体系不可用 → 必须有校验机制。

---

## 四、实现细节

### 4.1 DB 设计

**主表 `vega_drama_pool`**:

```sql
CREATE TABLE `vega_drama_pool` (
    `id`                         BIGINT(20)   NOT NULL AUTO_INCREMENT,
    `pool_type`                  TINYINT(4)   NOT NULL COMMENT '1-公共池 2-独家池',
    `link_type`                  VARCHAR(64)  NOT NULL COMMENT '业务链路 code',
    `comic_id`                   VARCHAR(64)  NOT NULL COMMENT '漫剧ID(引用内容中心)',
    `comic_name_snapshot`        VARCHAR(256)          DEFAULT NULL,
    `grant_type`                 TINYINT(4)            DEFAULT NULL COMMENT '1普通 2首发 3联合首发',
    `distributor_scope`          TINYINT(4)   NOT NULL DEFAULT 0 COMMENT '0-具体 1-全部',
    `publish_status`             TINYINT(4)   NOT NULL DEFAULT 0,
    `shelf_status`               TINYINT(4)   NOT NULL DEFAULT 1,
    `exclusive_period`           TINYINT(4)            DEFAULT NULL COMMENT '1/3/7/14/98/99',
    `exclusive_pool_update_time` DATETIME              DEFAULT NULL,
    `public_pool_update_time`    DATETIME              DEFAULT NULL,
    `operator`                   VARCHAR(255) NOT NULL,
    `create_time`                DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time`                DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `is_deleted`                 TINYINT(4)   NOT NULL DEFAULT 0,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_pool_link_comic` (`pool_type`, `link_type`, `comic_id`, `is_deleted`),
    KEY `idx_link_pool_update` (`link_type`, `pool_type`, `update_time`),
    KEY `idx_comic` (`comic_id`)
) COMMENT='剧集池(公共/独家统一表)';
```

**关联表 `vega_drama_pool_distributor`**:

```sql
CREATE TABLE `vega_drama_pool_distributor` (
    `id`             BIGINT(20)  NOT NULL AUTO_INCREMENT,
    `pool_id`        BIGINT(20)  NOT NULL,
    `distributor_id` VARCHAR(64) NOT NULL,
    `create_time`    DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_pool_distributor` (`pool_id`, `distributor_id`),
    KEY `idx_distributor` (`distributor_id`)
) COMMENT='剧集池-分销商关联';
```

**索引设计要点**:
- `uk_pool_link_comic`:防止同一链路同一剧在同一池重复添加
- `idx_link_pool_update`:列表查询最常用的排序维度(按更新时间降序)
- `idx_comic`:按漫剧反查它在哪些池

### 4.2 接口设计(10 个)

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/v1/drama-pool/public/page` | 公共池分页 |
| POST | `/api/v1/drama-pool/public/batch-add` | 公共池批量添加漫剧 |
| POST | `/api/v1/drama-pool/public/edit` | 公共池编辑 |
| POST | `/api/v1/drama-pool/public/shelf-toggle` | 上下架切换 |
| POST | `/api/v1/drama-pool/public/join-exclusive` | 加入独家 |
| POST | `/api/v1/drama-pool/exclusive/page` | 独家池分页 |
| POST | `/api/v1/drama-pool/exclusive/batch-add` | 独家池批量添加 |
| POST | `/api/v1/drama-pool/exclusive/edit` | 独家池编辑 |
| POST | `/api/v1/drama-pool/comic/resolve` | 漫剧 ID/名称识别 |
| GET  | `/api/v1/drama-pool/distributor/list-by-link` | 按链路查客户 |

**编辑类接口的入参都带 `linkType`**,Service 层校验"参数 linkType 和 DB 里记录的 linkType 是否一致",防止越权篡改。

### 4.3 Service 层核心逻辑

#### 批量添加(关键流程)

```java
public BatchAddResultVO batchAddPublic(PublicPoolAddParam param, String operator) {
    // 1. 参数校验
    validateLinkType(param.getLinkType());
    GrantType grantType = GrantType.of(param.getGrantType());
    boolean needDistributors = (grantType == GrantType.UNION_FIRST);

    // 2. 漫剧识别(TODO:接真实内容中心)
    List<ComicResolveItemVO> resolved = resolveComicIdentifiers(param.getComicIdentifiers());

    // 3. 逐个入库,带去重
    BatchAddResultVO result = BatchAddResultVO.builder().build();
    Set<String> dedupInBatch = new HashSet<>();
    for (ComicResolveItemVO r : resolved) {
        if (!r.isMatched()) {
            result.getNotFoundList().add(...);  // 内容中心找不到
            continue;
        }
        if (!dedupInBatch.add(r.getComicId())) {
            result.getDuplicatedList().add(...); // 本批次重复
            continue;
        }
        DramaPoolDO exist = dramaPoolDao.findExisting(PUBLIC, linkType, comicId);
        if (exist != null) {
            result.getDuplicatedList().add(...); // 已在池中
            continue;
        }

        // 4. 落库
        DramaPoolDO row = buildNewRow(param, r, grantType, needDistributors);
        dramaPoolDao.insert(row);

        // 5. 联合首发,额外写关联表
        if (needDistributors) {
            for (String did : new LinkedHashSet<>(param.getDistributorIds())) {
                dramaPoolDao.insertDistributor(new DramaPoolDistributorDO(row.getId(), did));
            }
        }
        result.getSuccessList().add(...);
    }
    return result;
}
```

**返回 VO 分三类**:
- `successList`:成功入库
- `duplicatedList`:已存在(分"批内重复"和"池内已存在"两种原因)
- `notFoundList`:内容中心识别不到

前端用这三类分别提示用户,比"成功 X 条失败 Y 条"有信息量。

#### 状态流转:释放至公共池

```java
public void editExclusive(ExclusivePoolEditParam param, String operator) {
    DramaPoolDO row = dramaPoolDao.getById(param.getId());
    // ... 常规校验

    dramaPoolDao.updateExclusive(...);  // 更新独家池记录

    // 关键:如果 exclusive_period=99(已释放),公共池若无对应记录则自动补一条
    if (Integer.valueOf(99).equals(param.getExclusivePeriod())) {
        DramaPoolDO exist = dramaPoolDao.findExisting(PUBLIC, row.getLinkType(), row.getComicId());
        if (exist == null) {
            DramaPoolDO pub = new DramaPoolDO();
            pub.setPoolType(PUBLIC.getCode());
            pub.setLinkType(row.getLinkType());
            pub.setComicId(row.getComicId());
            pub.setGrantType(GrantType.NORMAL.getCode());  // 释放后默认普通
            pub.setDistributorScope(1);                    // 全部客户
            pub.setShelfStatus(1);
            pub.setPublicPoolUpdateTime(LocalDateTime.now());
            dramaPoolDao.insert(pub);
        }
    }
}
```

设计权衡:**不删除独家记录**(保留状态为 99 的历史,便于审计),同时**公共池自动补位**。

### 4.4 Rainbow 权限 YAML 结构

剧集池在 Rainbow `permission.init` 中的节点(片段):

```yaml
- type: 2                                    # 菜单
  title: 剧集池管理
  path: /promotion/episodes-manage
  identifier: promotion:episodes
  roles: [ADMIN]
  children:
    - type: 2                                # 子菜单(链路)
      title: 抖音端原生
      path: /promotion/episodes-manage/douyin
      identifier: promotion:episodes-douyin
      roles: [ADMIN]
      children:
        - type: 3                            # 按钮
          title: 列表查询
          identifier: promotion:episodes-douyin:list
          roles: [ADMIN]
        - type: 3
          title: 加入独家
          identifier: promotion:set-exclusive-douyin
          roles: [MANAGER]                   # 敏感操作,只给超管
        # ... 其他按钮
    - type: 2                                # 快手端原生
      # ... 同结构
```

**关键缩进层级**(踩过坑,记录下来):

| 层级 | 空格数 |
|---|---|
| 剧集池菜单 `- type: 2` | 6 |
| 菜单字段 title/path/roles/children | 8 |
| 链路菜单 `- type: 2` | 10 |
| 链路字段 | 12 |
| 按钮 `- type: 3` | 14 |
| 按钮字段 | 16 |
| roles 项 `- ADMIN` | 对应字段 +2 |

### 4.5 LocalDateTime → String 的处理

**原实现(挂的)**:

```java
@Data
@Builder
public class PublicPoolItemVO {
    private Long id;
    private LocalDateTime publicPoolUpdateTime;  // ❌ Jackson 默认不认
}
```

线上报:

```
Java 8 date/time type `java.time.LocalDateTime` not supported by default:
add Module "com.fasterxml.jackson.datatype:jackson-datatype-jsr310" to enable handling
```

**修复后(VO 全改 String)**:

```java
@Data
@Builder
public class PublicPoolItemVO {
    private Long id;
    private String publicPoolUpdateTime;  // 格式 yyyy-MM-dd HH:mm:ss
}
```

Service 层统一格式化:

```java
private static final DateTimeFormatter DATETIME_FMT =
    DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
private static final DateTimeFormatter DATE_FMT =
    DateTimeFormatter.ofPattern("yyyy-MM-dd");

private static String fmtDateTime(LocalDateTime t) {
    return t == null ? null : t.format(DATETIME_FMT);
}
private static String fmtDate(LocalDate d) {
    return d == null ? null : d.format(DATE_FMT);
}

// 组装 VO 时
.publicPoolUpdateTime(fmtDateTime(r.getPublicPoolUpdateTime()))
.check1dDate(start == null ? null : fmtDate(start.plusDays(1)))
```

**为什么不选"注册 jsr310 全局模块"?**
- 改 Jackson 全局配置会影响**所有已有接口**
- 老接口可能依赖"默认行为"(比如 `Date` 的序列化),动全局风险大
- VO 改 String 是**局部改动**,0 风险

### 4.6 dramaCategory 主剧→子剧同步

Vega 运营编辑主剧 `dramaCategory` 时,需要同步到 merchantry 的 `video_sync_index` 表。
**关键**:要同步主剧 + 该主剧的**所有子剧**(子剧由内容中心维护)。

```java
private void syncDramaCategoryToComicDramaWeb(VideoEditParam param) {
    if (param == null || param.getDramaCategory() == null) return;
    Long mvdid = param.getVdid();
    Integer category = param.getDramaCategory();

    // 1. 同步主剧
    syncOneDramaCategory(mvdid, category, "main");

    // 2. 查所有子剧(调内容中心新接口,需要 ccshortplayserver-stub ≥ 1.8.7)
    try {
        List<Long> subVdids = listSubVdidsByMvdid(mvdid);
        for (Long subVdid : subVdids) {
            syncOneDramaCategory(subVdid, category, "sub");
        }
    } catch (Exception e) {
        // 查子剧失败不影响主剧同步
        log.error("list sub drama exception, mvdid={}", mvdid, e);
    }
}

private List<Long> listSubVdidsByMvdid(long mvdid) {
    ListVideoInfoByMvdidRequest req = ListVideoInfoByMvdidRequest.newBuilder()
            .setContext(Context.newBuilder().setSourceId(shortPlaySourceId).build())
            .setMvdid(mvdid).build();
    ListVideoInfoByMvdidResponse rsp =
            shortPlayQueryAPI.listVideoInfosByMvdid(new RpcClientContext(), req);
    if (rsp == null || rsp.getVideoList() == null) return Collections.emptyList();
    return rsp.getVideoList().stream()
            .map(ShortPlaySimpleInfo::getVdid)
            .filter(id -> id != mvdid)  // 过滤掉主剧自己
            .toList();
}
```

**异常保护分三层**:
1. 外层 try:子剧查询失败不阻塞主剧同步
2. 每条单独 try(`syncOneDramaCategory` 内部):一条失败不阻塞其他
3. 整个方法外层还有 try:不影响主业务(vega 视频编辑)

---

## 五、几个关键设计权衡

### 权衡 1:单表 vs 双表(选了单表)

- 单表优势:状态流转原子化
- 单表劣势:字段稀疏(公共池不用 `exclusive_period`,独家池不用 `grant_type`)
- 决策依据:业务量不大(线上剧集量级在 10K 以下),字段稀疏不是问题

### 权衡 2:接口权限 @PreAuthorize vs 配置中心(选了配置中心)

- `@PreAuthorize` 优势:代码内聚,粒度灵活
- 配置中心优势:权限调整不用发版
- 决策依据:Vega 本来就用 Rainbow 管权限,保持一致,而且菜单/按钮/接口三层权限用一套机制管

### 权衡 3:`LocalDateTime` vs `String` vs 注册 jsr310 模块(选了 String)

- `LocalDateTime`:类型安全,但项目没注册 jsr310 会挂
- 注册 jsr310:一劳永逸,但改全局配置风险大
- `String`:局部改动,零风险,格式完全可控
- 决策依据:**新代码不应该因为自身需求去改动全局配置**,应该迁就现有环境

---

## 六、踩过的坑

### 坑 1:YAML 缩进错一格,权限炸一片

YAML 是缩进敏感语言,复制粘贴到 Rainbow 网页编辑器时,某行空格被吃掉,导致 `- type: 3` 被当成前面 `- ADMIN` 字符串的延续,`PermissionsInitService` 启动时抛:

```
Cannot create property=type for JavaBean=...
Unable to find property 'type' on class: java.lang.String
```

**解决**:本地用 Python 预校验,再粘到 Rainbow。

### 坑 2:LocalDateTime 序列化,加 @JsonFormat 不够

一开始以为加注解就行,其实 `@JsonFormat` **只负责格式化**,不负责类型识别。Jackson 默认不认 `LocalDateTime`,必须注册 jsr310 模块或换类型。

**解决**:VO 全改 `String`,Service 层格式化。

### 坑 3:response body 被截断,异常藏在 error.log

接口 500 时 body 只有 `{"timestamp":"..."`,`warn.log` 里搜不到。最后在 `error.log` 里找到:

```bash
grep -A 20 "found RuntimeException" /usr/local/trpc/log/error.log
```

**反思**:不同 log 文件的用途要摸清,不能只看一个文件。

### 坑 4:发布链路有三种,混淆时才知道差异

- `publish.sh + dtools dpatch`:jar 补丁,快速发布但镜像版本不变
- Sumeru "发布" 按钮:构建新镜像,正式发布
- Rainbow 配置发布:只改配置,不动代码

三者独立,容易误以为"我发了 dtools,服务就该有新代码"。

**解决**:项目文档里画出每种改动对应的发布方式,贴在工位上。

---

## 七、上线效果

- **10 个接口**全部部署 test 环境并通过联调
- **Rainbow 权限**生效,前端按角色展示对应菜单/按钮
- **dramaCategory 同步**:主剧变更 → 所有子剧自动跟新
- **商业模式**:独家授权谈判有了技术载体

---

## 八、面试可能被问的问题

### Q: 为什么数据模型选单表而不是两张表?

A: 两种池之间有流转关系(独家到期释放至公共池),单表用 `UPDATE pool_type` 就能完成,原子性好。双表需要跨表 DELETE + INSERT,要么开分布式事务(重),要么接受中间态(脏)。业务量不大,字段冗余可接受。

### Q: `distributor_scope` 字段是什么用途?

A: 业务上"普通/首发"意味着全部客户可见,如果强行写关联表,200 个客户就要 INSERT 200 行。加个 scope 字段,`scope=1` 表示"默认全部"不写关联表,`scope=0` 才去查关联表。列表查询的时候按 scope 走不同分支即可。

### Q: 为什么不用 `@PreAuthorize` 做权限?

A: Vega 项目约定权限由 Rainbow 配置中心驱动。好处是改权限不用发版,坏处是 YAML 写错会出事——我为此做了本地 Python 预校验。

### Q: LocalDateTime 的坑怎么判断 + 怎么修的?

A: 判断靠 `error.log` 里的异常栈,关键词 "Java 8 date/time type not supported"。修复选了 VO 改 String,避免全局 Jackson 配置改动。选择标准:**新需求的 bug,尽量用局部修复,不要动全局**。

### Q: 如果让你重做会改什么?

A: 三点:
1. 自定义 `BusinessException` + 错误码体系,避免"独家池已存在该漫剧"这种业务异常被兜底成 9999
2. Rainbow YAML 加 CI 校验,提交前自动跑 `yaml.safe_load`
3. 项目层面统一时间字段规范(全 Date、全 String、或全 LocalDateTime + jsr310),避免同一项目多种风格并存

### Q: dramaCategory 同步为什么用 HTTP 而不是 MQ?

A: 现状是 vega → merchantry 已有 HTTP 接口,复用成本最低。改用 MQ 的话需要加一套消费者,增加运维复杂度。同步失败的影响小(下次 edit 会重写),所以 HTTP 足够。如果将来同步场景扩展(多个订阅方),再重构成 MQ。

---

> 🔗 业务背景 + 踩坑故事见《剧集池管理_故事版.md》
> 这份偏技术细节,面试被追问细节时翻出来照着讲。

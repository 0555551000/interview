# 日常打工口语版(当朋友聊天那种)

> 之前写的太像说明文。这份按"跟同行朋友吃饭聊到工作"的节奏写,每个场景都尽量带上:
> - **吐槽**(谁谁谁又怎么了)
> - **具体**(周几、什么时间、因为什么)
> - **情绪**(烦的、无奈的、小得意)
> - **废话**(真实说话本来就有废话)
>
> 面试时这样讲,是真在"聊工作",不是"背简历"。

---

# 一、接需求:产品永远能搞新花样

## 故事 1:"就加个字段而已嘛"

周二下午 3 点,PM 小李突然在群里 @ 我:"彭冲你忙吗?合同列表页加一列'跟进人'就行,简单的,今天能上吗?"

说实话,听到"今天能上"我就想翻白眼。跟进人是什么?销售负责人?运营对接人?每次 PM 说"简单的"都不简单。

跟他对了 20 分钟,搞清楚:

- "跟进人" = 销售那边的对接人,不是运营
- 新合同从哪拿到这个信息?— 销售系统有接口
- 老合同呢?— 历史数据 null,显示"未分配"

然后我开始真正干活:

1. 先开会?不开。小活直接开搞
2. 找 DBA 改表。我们 contract 表千万级,DBA 老王每次都要灵魂三连:"表多大?影响多久?凌晨还是白天?"。我熟练地答:"千万级,`pt-online-schema-change`,凌晨 3 点,回滚就 DROP"。老王嗯一声就 pass 了。跟老王打了多年交道有默契
3. 改 DO、改 Mapper XML、改 Service、改 VO — 就是典型的"一条线上下贯穿"。改了 7 个文件
4. 写单测。我们组规范是新字段至少一条 case,不然过不了 MR
5. 提 MR。review 我的是同事阿强,他看字段顺序和 DB 列顺序不一致,"你这 follower_user_id 在 DB 是倒数第二列,DO 里你放第一个了,改一下",我就改了

全程大概 3 小时。小李下班前看到上测试环境了,笑嘻嘻的。

**面试能聊的点**:

- 大表 DDL 要走 `pt-online-schema-change`,不能直接 ALTER(锁表 10 分钟就挂了)
- DO 字段顺序 ≈ DB 列顺序,code review 能看出来
- "小活不开会" 是看情况的 — 这种单字段改动开会就浪费时间

## 故事 2:"这个需求肯定还能再加"

PM 又来了:"结算列表加个按金额范围筛选。就 minAmount 和 maxAmount 两个框"。

我一看需求,心里想:来了,**又是这种"现在就要最简方案"的需求**。

我问她:"除了金额,未来还会按什么筛选?"

她说:"应该没了吧"。

我心里想:**放屁**。她每次都这么说,然后下周就加按时间、按状态、按合同号。我跟她共事 1 年,KPI 压力一来她就改主意。

所以我写 Mapper 的时候**直接用 `<where>` + `<if>` 动态 SQL** 框架撑起来,加新条件改一行就行:

```xml
<where>
    <if test="minAmount != null">AND amount &gt;= #{minAmount}</if>
    <if test="maxAmount != null">AND amount &lt;= #{maxAmount}</if>
    <!-- 这里留好位置,下次加条件就顺手 -->
</where>
```

果然,一周后她说:"再加个按结算状态筛选吧"。我 5 分钟加完。她一脸"你好快"的表情。我淡然微笑。

**面试能聊的点**:

- `<where>` 动态 SQL 自动处理前导 AND 和空条件
- `BigDecimal` 字段比较用 `>=` 不要用 `equals`,`equals` 要求 scale 一致
- 产品说"就这一次"永远是假的

---

# 二、修 bug:每个 bug 背后都有故事

## 故事 1:那个 NPE

一个周一早上 9 点半,我刚到工位,运营小张就在群里 @ 我:"@彭冲 今早导出 Excel 有的行是空的!"

我看了眼日志,是 NPE:

```
java.lang.NullPointerException
    at ContractExportService.buildRow(ContractExportService.java:89)
```

`row.setCreator(contract.getCreator().getName())` 挂在这行。

我没直接改代码。先想:**为什么以前不挂**?

查了下数据:老合同 `creator_user_id = 0` 有 30 多条,查用户服务返 null。我们这个功能上线 3 个月了,为什么之前没挂?

查 git log。2 周前同事阿俊改过这个方法。他之前是 `contract.getCreator() != null`,改成链式调 `.getName()` 把判空 **去掉了**。

我去问他:"你那次改动为啥去掉判空?"

阿俊:"代码瘦身啊,`!= null` 看着冗余"

我:"那 creator 为 null 的场景你考虑了吗?"

阿俊:"呃... 我以为不可能为 null"

这就是典型的**假设性 bug** — 基于错误假设写代码。

我先改回去:

```java
.setCreator(contract.getCreator() == null ? "未知" : contract.getCreator().getName())
```

然后组内开了个 10 分钟的小讨论:**凡是调外部服务返回的对象,默认当它会返 null**。阿俊脸有点红,但教训深刻。

**面试能聊的点**:

- 外部服务返回永远要判空
- `git log` 看谁最近改过,很多 bug 是回归
- 代码"瘦身"要看具体情况,不是所有 `!= null` 都冗余

## 故事 2:少了 3 条数据的月结

周一上班,财务老周钉我:"上月结算对账对不上,7 月少了 3 条,合计 1 万 2"。

我心里一紧。结算对不上是大事。

排查:

1. 先看 SQL:`WHERE create_time >= '2024-07-01' AND create_time <= '2024-07-31'`
2. `<=` 后面跟日期,**相当于 `<= '2024-07-31 00:00:00'`**
3. 所以 7/31 当天 0 点之后的数据全漏了

这是典型的**日期边界 bug**。我改成:

```sql
WHERE create_time >= '2024-07-01' AND create_time < '2024-08-01'
-- 左闭右开,永不踩坑
```

改完跑一下补偿脚本,把 7/31 漏掉的 3 条补上。发邮件给老周:"已修复,金额对上了"。

老周回:"你们写代码怎么这种错误都犯?"

我没解释。这种错误确实不该犯,但是**我们有 20+ 个按月统计接口**,全项目历史上写 `<= '2024-07-31'` 的 SQL 超过 50 处。我花 2 小时把能找到的都改了,加了 `CheckStyle` 规则禁止这种写法。

**老周那天**我没跟他杠,但心里想:"下次上线前你要不要自己看一眼?对账不是我一个人的事"。

**面试能聊的点**:

- 时间区间**左闭右开**是业界共识(Google SRE 书里都这么教)
- `CheckStyle` / `PMD` 规则可以把常见错误在编译期拦下
- 财务字段出问题别只改当前 bug,**全项目扫一遍同类问题**

## 故事 3:合同重复提交

某天下午,运营小王说:"彭冲,我刚点了一下'提交合同',结果它给我创建了 2 条!"

我一看数据:确实,同一个合同号 2 条记录,create_time 差 800 毫秒。

问小王:"你是不是双击了?"

小王:"我就点了一下啊... 可能手抖多点了?"

**经典的前端没防抖 + 后端没幂等**。

我当场改了三层:

1. DB 加唯一索引 `UNIQUE KEY uk_contract_no (contract_no)`
2. Service 层 `existCount > 0` 前置校验
3. catch `DuplicateKeyException` 转成友好错误 "合同已存在"

找前端妹子小美:"你这按钮点了之后置灰 3 秒好吗?"。小美:"哦好"。

全改完上线。我写了个文档《**我们组关键操作幂等防御三板斧**》,发给团队。

过了 1 个月,新来的同学 review 别人的提交操作代码,一眼看出没做幂等防御,直接评论:"参考组内的幂等三板斧"。我看到挺欣慰。

**面试能聊的点**:

- 幂等不能只靠代码校验,DB 约束是最后一道防线
- 前端防抖不能替代后端校验(用户会用 Postman 直接调)
- 团队文档和规范比个别人的能力更重要

---

# 三、线上工单:被运营追着跑的日常

## 故事 1:小王的账号

某个周四下午 4 点,运营小王在群里紧急 @ 我:"彭冲救命!我账号登不进去了!下午要跟客户演示!"

我脑子里闪过几个可能:

- 密码错了?
- 账号被禁了?
- session 过期?

我让他发报错截图。他发了个"账号已禁用"。

查 `user` 表,`status = 2`。查 `user_operation_log`:上周三 HR 走了离职流程,把小王账号禁了。

但小王没离职啊!

打电话给 HR 小丽。**小丽:"呃...同名同姓的另一个小王离职了,我操作时没看工号"**。

好家伙,这个乌龙。

我改了下 `status = 1`,让小王重新登上了。演示过 3 分钟后开始,他刚刚好。

演示完小王请我喝了杯奶茶。

**然后我做了件事**:在 `user_operation_log` 加了报警,任何对"老员工"(入职超 1 年)的禁用操作,要 **HR + Leader 双重确认**。这种乌龙不能再发生了。

**面试能聊的点**:

- 工单排查 80% 时间是**对齐信息**,不是技术问题
- 权限变更要有日志和告警,不能悄悄改
- 每个乌龙背后都是流程漏洞,修流程比修 bug 重要

## 故事 2:"数据对不上,我们少了 1200"

周五下午老周(财务)又来了:"结算报表跟合同系统对不上,差 1200 块"。

我跟他要具体合同号。他给了个 C202407001。

查合同系统:金额 10,000,分润 12% → 该分 1,200。
查我们结算表:**没这条**。

看日志:合同系统 7/15 下午签约成功,我们同步任务 retry 3 次后失败。错误原因是对方 API 500,重试 3 次窗口期内他们没恢复。

3 次之后我们代码**没做第四次**兜底,数据就丢了。

我手动 INSERT 了这条数据:

```sql
INSERT INTO settlement (contract_no, amount, ratio, ...) VALUES ('C202407001', 10000, 0.12, ...);
```

然后改代码:

```java
// 改之前:retry 3 次失败 → 打日志丢弃
// 改之后:retry 3 次失败 → 入 "failed_sync" 死信队列,每天凌晨跑 Job 重试
```

加了每日对账 Job,对不上就告警。**这种问题以后要靠系统自己发现,不是老周发现**。

老周收到修复邮件回了句"下次别这样了"。

这个改完的第二天,凌晨对账 Job 第一次跑,报警打出来 7 月漏了 **另外 2 条**。我再捞出来补上。老周不知道这个,等他下次月结对账他会发现"怎么比我算的还多了 2 条",这算给我挽回形象吧。

**面试能聊的点**:

- 跨系统同步必须有失败兜底,重试队列 / 死信 / 对账三件套
- "人工发现 bug"是最糟的状态,要让系统自己报警
- 修完 bug 顺手做一次全量扫描,经常能捞出更多隐藏问题

## 故事 3:"我这里好慢啊"

某天用户老张说:"彭冲你这个详情页转菊花转 30 秒!"

我赶紧看监控:这个接口 P99 1.5 秒,P50 400ms。没理由 30 秒。

让老张下次遇到开浏览器 Network 截图。

他下午真发来了 — **一个接口 28 秒**。

点开 Network 一看:那 28 秒里,**我们后端耗时 800ms,剩下 27 秒在 TCP 握手和等响应**。

问老张你在哪办公。他说"出差,深圳一个酒店 WiFi"。

那 WiFi 估计比 2G 还慢。

我跟老张说:"哥,这不是我们系统问题,是你酒店 WiFi 太菜了"。

老张不信,继续发工单说慢。我让他开 VPN 回公司网再测,果然 2 秒搞定。

**面试能聊的点**:

- 排查"慢"问题先分:**系统慢** vs **环境慢**
- 不能只看服务端日志,要看完整 TCP 时间
- 用户说"慢"是主观感受,要客观数据说话

---

# 四、对账:每月 5 号的噩梦

月头对账是我们团队最讨厌的事之一。简单说:每月 5 号跑一次,和分润系统比金额,差异超过 10 块就要出报告。

## 第一次对账的崩溃

我第一个月接手对账的时候,跑出来差了 50 万。**吓尿了**。

我慌得 1 批,开始一条条查。查了 2 天,最后发现:

- 分润系统的 `DECIMAL(12,2)`,我们的 `DECIMAL(16,4)`
- 后 4 位小数在分润系统被截掉了
- 几千条记录累计差了 50 万

不是真的差 50 万,是**精度问题**。

我跟 Leader 汇报。Leader:"那你以后对账四舍五入到 (10,2) 就行,这个你以后自己搞定"。

**我搞定了,但是心里留下阴影。** 以后每次对账前先跑一遍 `ROUND(amount, 2)` 再比,就好多了。

## 对账是体力活但发现真问题

每月跑对账,偶尔能发现**真的 bug**:

- 有一次发现同一个合同号重复支付回调 2 次,我们入账 2 条 → 金额多算
- 有一次发现分润系统漏同步了 5 分钟的数据 → 他们当时 RPC 挂了
- 有一次发现我们 7/31 漏 3 条(就是上面那个左闭右开的 bug)

所以对账看似枯燥,**是发现系统问题的最后一道防线**。我现在把对账 Job 看得比业务需求还重,出问题第一时间响应。

**面试能聊的点**:

- 金融/财务系统,对账比业务功能还重要
- 精度问题要两边统一(都 (16,4) 或对账时 ROUND)
- 对账差异经常暴露"系统外的问题"(回调重复、同步丢失、定时任务挂)

---

# 五、Code Review:和同事的日常拉扯

## 故事 1:那个 5 层嵌套 if

新同事阿俊第一个 MR 交上来,我打开看:

```java
if (contract != null) {
    if (contract.getStatus() == 1) {
        if (contract.getAmount() != null) {
            if (contract.getAmount() > 0) {
                if (contract.getOrgId() != null) {
                    // 核心逻辑
                }
            }
        }
    }
}
```

我看完笑了。写过这种代码的肯定都是新人。

我评论:"早 return 一下?"

阿俊:"什么意思?"

我给他看:

```java
if (contract == null) return;
if (contract.getStatus() != 1) return;
if (contract.getAmount() == null || contract.getAmount() <= 0) return;
if (contract.getOrgId() == null) return;
// 核心逻辑
```

阿俊:"哦这样好多了"

这种 MR 不是改代码,是**教同事思维方式**。我特意没说"这是 guard clause 模式",说多了他记不住。让他自己意识到"嵌套不好读"就够了。

## 故事 2:我最不能忍的 review 点

有些代码我一眼就要评论,绝不放过:

**1. try-catch 吞异常**

```java
try {
    doSomething();
} catch (Exception e) {
    // 啥都没做
}
```

我的评论:"exception 吞了还怎么排查?至少 log.error 一下"

**2. 字符串拼 SQL**

```java
String sql = "SELECT * FROM user WHERE name = '" + name + "'";
```

我的评论:"SQL 注入了解一下"

**3. 魔法数字**

```java
if (status == 3) { ... }
```

我的评论:"3 代表啥?抽枚举"

**4. 不写注释的复杂业务**

```java
// 50 行的奇怪判断,没一句注释
```

我的评论:"这段逻辑业务背景讲一下?3 个月后你自己都不记得"

组内同事都知道我 review 严,**但也知道我严是为了代码好**。有个同事跟我说:"被你 review 完的代码,我自己都看得顺一些"。

**面试能聊的点**:

- Review 不只是挑错,是团队代码规范的建立
- 新人的 review 要**讲原则不讲术语**,他能记住才是真懂了
- 最不能忍:吞异常、SQL 注入、魔法数字、无注释

---

# 六、和产品撕:吵架的艺术

## 故事:我和 PM 小李的第三次大吵

某周五下午,PM 小李拿着原型图过来:"我们做一个批量审批功能,一次可以审批 1000 条"。

我心里想:**又来了**。

我问:"1000 条同步审批要多久?如果有一条失败怎么办?谁能审批?审完能撤销吗?权限怎么分?"

小李:"你这都什么问题?我就想要个批量按钮"。

我:"你想要一个'你以为'的批量按钮。实际上产品要想清楚:"
- "1000 条同步等 30 秒用户会认为卡死"
- "1 条失败全部回滚还是部分成功?"
- "权限不足的人能批量吗?"
- "批量审批错了怎么撤?"

小李脸色不好看。

但我坚持:"这些问题你不想清楚,我做出来你一定不满意,到时候改工时更大"。

小李:"那你给个方案"。

我:"批量分 20 条一批异步,任务中心看结果,失败的给出具体原因,可以重试失败项"。

她勉强接受。回去改 PRD,改到第三版才定稿。

**这种需求上线后,她反而感谢我**:"彭冲我发现你说的那些问题真的都会碰到,谢谢你当时把关"。

但每次对需求还是要吵。**PM 想得快,后端要拉住她**,不然上线就是一地鸡毛。

**面试能聊的点**:

- 对需求的"8 问":范围、规则、边界、异常、权限、审计、性能、回滚
- 吵架不是冲突,是对工程质量负责
- PM 满意 = 上线顺利 = 不被半夜叫起来

---

# 七、带新人:真实的师徒关系

## 故事:带阿俊的 3 个月

阿俊 4 月入职,我被 Leader 指定带他。

**第 1 周**我给了他一个 README.md 写着"环境搭建"。第二天他说:"我 MySQL 连不上"。我让他自己查。他查了半天,我路过看他在 Google "MySQL connection refused"。

我凑过去:"查得怎么样?"
他:"找到一些文章但都试了没用"
我:"给我看下报错"
他指着屏幕:"就这个,Access denied for user 'root'@'localhost'"
我:"... 密码错了吧"
他:"... 哦"

但我没直接告诉他。让他自己搞清楚"Access denied 和 connection refused 是两种错误,应该先看错误信息再 Google"。这比直接告诉他密码有用。

**第 2 周**我给他一个小 bug 让他修 — 改个文案。

他交的 MR 里,把 "提交成功" 改成 "提交成功了!"(多了"了!")。

我 review:"文案改动必须和 PM 确认过,不要自己加标点"。他知道后改回来。我没骂他,但让他记住:**改文案要走确认**。

**第 3 周**他接了第一个小需求。我陪他做需求评审,他自己提问题(虽然都是我提醒过的)。

**第 1 个月**他能独立改字段加接口了。

**第 2 个月**他接了第一个有深度的需求(合同状态计算)。他自己设计方案,我 review,前后改了 3 版。他气馁过,我跟他说:"我刚工作时也被 review 改过 5 版"。他笑了。

**第 3 个月**他能独立交付中等需求,而且质量不错。他开始 review 别人的 MR(我没指定,他主动的)。

**带新人最关键的事**:

1. **不要直接给答案**,让他学会查资料和调试
2. **小错误让他犯**,记住比背熟快
3. **第一次 MR 要严**,建立标准;后面就能松手
4. **定期聊天**(每周 30 分钟),让他说困惑
5. **承认新人强是他强**,不是师傅教得好 — 别抢功劳

**面试能聊的点**:

- 带新人不是一板一眼教,是**培养解决问题的能力**
- 严 review 和鼓励并重,只严他会崩,只鼓励他会飘
- 新人的成长曲线 1 周 / 2 周 / 1 月 / 2-3 月 有节奏

---

# 八、和 DBA 的日常

## 故事:老王和他的灵魂三连

我们 DBA 老王,60 后,头发花白,脾气好但原则强。

每次我提 DDL,老王必问三件事:

1. **表多大?**
2. **要不要锁表?**
3. **回滚方案?**

第一次不懂,硬答。第二次知道了就事先准备好答:

我:"王哥,contract 表千万级,`pt-online-schema-change` 在线改,凌晨 3 点上,回滚 DROP COLUMN"。

老王:"嗯,批了"。5 分钟搞定。

有一次我提 `ADD COLUMN` 没写默认值,老王:"你这字段有没有默认值啊?老代码不传这字段会不会挂?"

我:"... 呃,我没想这个"

老王:"回去想清楚再提"

我去看了下老代码,果然有 INSERT 没带这个字段,默认 null 会报 NOT NULL 约束错。

**被 DBA 教做人**是常事,但每次都学到东西。老王最后跟我混熟了,偶尔还带我去他工位喝茶:"小伙子,DBA 和开发天生对立,但好的开发会让 DBA 省心"。

**面试能聊的点**:

- 大表 DDL 必须走 `pt-online-schema-change` / `gh-ost`
- ADD COLUMN 一定要有默认值,否则老代码会炸
- 和 DBA 打好关系,很多事能通融

---

# 九、真实的一周(面试被问"上周做了什么"用这个)

**周一**(9 月 16 日)
- 上午:接一个需求 — 合同详情加"到期预警"标签,改了 DO + Mapper + Service + VO。Lunch 前合了 MR
- 下午:修一个线上 bug — 小王反馈合同分润比例显示错,排查是缓存脏数据,Redis 更新没触发,改了缓存失效逻辑
- 晚上:运营小张来问"怎么导出数据",我教她用任务中心

**周二**
- 上午:对账 Job 跑出 3 条差异,花 2 小时排查是分润系统 RPC 昨天丢了数据,手动补录
- 下午:和产品小李评审下周"批量续签"需求,我问了一堆边界问题,她气到回去改 PRD
- 晚上:回家路上想清楚一个技术方案,回去写成文档发到群

**周三**
- 上午:Review 阿俊的 MR。他第一次独立需求,我改了 5 处,都是空值和日志问题
- 下午:参加技术评审会,隔壁组在做新的推荐引擎,我是 reviewer,提了 3 个问题
- 晚上:线上 P1 告警 — 结算 Job 超时。应急处理 30 分钟,原因是凌晨 DB 在做备份,Job 读写慢。加个重试就解决了

**周四**
- 全天:写《结算对账最佳实践》内部文档。上次对账踩坑后 Leader 让我沉淀,花一整天写出来 10 页 Wiki
- 下班前:开周会,我分享了对账经验 15 分钟

**周五**
- 上午:带阿俊 1v1,聊他近期困惑。他说对"跨服务 ID 映射"不理解,我用 1 小时把 organization_mapping 的来龙去脉讲了一遍
- 下午:接了个紧急需求 — 运营明天要给客户演示,需要临时接口。写了半小时就好
- 下班前:摸鱼,看了看明年组内 roadmap

**这一周**(真实版本):
- 4 个需求(2 大 2 小)
- 2 个 bug 修复
- 1 个 P1 应急
- 1 个 MR review
- 1 个技术评审
- 1 次对账
- 1v1 带新人
- 写文档 1 整天
- 开会 2 次

**没有一个亮点**。但这就是真实 5 年经验打工的一周。

---

# 十、面试时怎么讲这些日常

## 场景 1:被问"你上周做了什么"

**错误答法**:"我负责签约模块,做了一些优化"(太虚)

**真·口语化答法**:

> 上周挺杂的。
>
> 周一我改了一个需求,合同列表加一列到期预警,半天搞定。然后修了个线上 bug,小王反馈的,他是我们这边的运营,说合同分润比例显示错了,我查了下是缓存没失效,改了缓存失效的触发点。
>
> 周二对账 Job 跑出差异,我查了 2 小时发现是分润系统 RPC 挂过,丢了 3 条数据,我手动补录了。下午和产品对了个批量续签的需求,我提了一堆问题,她气到回去重写 PRD。
>
> 周三上午 review 阿俊的代码,他是新人,第一次独立需求,我改了 5 处,都是判空和日志的问题。下午评审隔壁组的推荐引擎,晚上线上还 P1 告警了一次,结算 Job 超时,加个重试就好了。
>
> 周四一整天写内部文档《对账最佳实践》,之前踩过几个坑,Leader 让我沉淀下来。
>
> 周五带阿俊 1v1,讲新旧机构 ID 映射的业务背景,用了 1 小时。下午救急了一个演示用的临时接口。
>
> 所以一周下来 — 4 个需求、2 个 bug、1 个 P1、1 次 review、1 次评审、1 次对账、写了整天文档、带了次新人。还挺饱和的吧。

**为什么这么答好**:

1. **有具体人名**:小王、阿俊、小李,像是真的同事,不是编的
2. **有吐槽**:"她气到回去重写 PRD",真实打工味儿
3. **有数字但不刻意**:5 处 review、2 小时、1 小时,随口说的那种
4. **最后做个总结**:让面试官知道你这一周工作量饱和

## 场景 2:被问"最常见的日常 bug 是什么"

**口语化答法**:

> 我们系统最常踩的 bug 有几类,按频率排:
>
> **NPE 类**:占一半吧。跨服务调用返 null、历史脏数据、对方接口偶尔抖一下。新人最容易犯。上次阿俊就把链式调用里的判空去掉了,以为"不可能为 null",结果上线 2 周后被 creator_id=0 的老合同挂了。
>
> **日期边界**:第二常见。`<=` 和 `<` 搞混、时区没对齐、月末多一天少一天。我们之前有个按月统计漏了 7/31 的 3 条数据,财务发现对不上才抓到。后来我全项目加了 CheckStyle 规则禁止 `<= '2024-07-31'` 这种写法。
>
> **并发穿透**:不是高频但影响大。防抖没做、DB 没唯一索引、代码层先 SELECT 后 INSERT 不原子。合同号重复、订单重复下单都是这类。
>
> **缓存脏数据**:中等频率。改了 DB 没改 Redis、用户信息变了但各处缓存都有。这种特难查,用户说"显示不对"你还以为他瞎眼,得对 DB 数据才发现是缓存问题。
>
> 这 4 类我们组做了规范性防御 — 判空规范、时间区间左闭右开、幂等三板斧、缓存一致性约定。新人入职前 2 周先过一遍。

**为什么好**:

- **按频率排**,不是按理论排
- **带具体案例**:阿俊、7/31 漏数据
- **结尾有沉淀**:不是只讲问题,讲怎么预防

## 场景 3:被问"你 Code Review 重点看什么?"

**口语化答法**:

> 我最不能忍的四类 review 点:
>
> **第一是吞异常**。`try { ... } catch (Exception e) { }` 这种就是给排查挖坑,出问题连日志都没有。review 看到我必评"至少 log.error 一下"。
>
> **第二是魔法数字**。代码里突然冒出 `if (status == 3)`,3 代表啥?一个月后你自己都不记得。抽成枚举或常量。
>
> **第三是嵌套 if**。5 层嵌套的代码真的有人写 — 阿俊第一次 MR 就是 5 层,我让他改 early return。代码写得深是因为没想清楚,想清楚了自然浅。
>
> **第四是没写注释的复杂业务**。比如合同状态转移,这种业务细节不写注释,3 个月后同事接手问你"这里为什么加了 7 天?",你自己都忘了。必须写"**业务规则来源:xxx**"这种注释。
>
> 技术细节比如 SQL 注入、事务边界、锁粒度这些都会看,但这 4 个是**谁犯都要抠的**。

**为什么好**:

- 按**情绪**(我最不能忍)组织,不是按技术类型
- 每条带真实场景(阿俊 5 层嵌套)
- 结尾区分"共性问题"和"技术细节"

---

# 最后的话

日常工作听起来琐碎,但面试官问起来你能答得清楚,反而显得你**真实、扎实、不水**。

**那些亮点(5 天→1 天、3-5s→500ms)是建立在这些琐碎之上的** — 如果你没日常修 bug 的经验,不可能想到"三层防御";没处理过对账的人,不可能设计出"日对账 + 死信 + 重试"的同步方案。

**面试的时候**,别怕说"我上周一半时间在修 bug"。真 5 年经验的人就是这样。**吹自己"每天都在做架构决策"的人,反而像纸糊的**。

能把日常讲得有条理、有情绪、有细节,就赢了一大半。

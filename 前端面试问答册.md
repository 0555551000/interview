# 前端面试问答册（后端兼全栈版）

> **适配人群**：Java 后端 + 接触过 Vue 3 前端代码（能看懂、能改 bug、能理解架构）
> **覆盖内容**：面试官问"你做过前端吗？"能答出来的 + 基本功扎实的证明 + 和你真实项目 comics-management 绑定
> **避开的**：Vue 响应式源码 / React Hooks 内部 / 浏览器渲染引擎底层 等硬核前端岗深度题
>
> 做过哪些前端、做到什么程度，直接回答面试官：
> "我是 Java 后端，但我们项目的 **vega-view（Vue 3 + Vite + TypeScript + Arco）** 我能读懂、能改 bug、能接新需求。没做过纯前端岗，但**全栈能力没问题**。"

---

## 一、开场白类（必背）

### Q1：你做过前端开发吗？

**安全回答**：

> "我是后端工程师，但我们项目的前端（Vue 3 + TypeScript + Vite + Arco Design）我能读懂代码，能改 bug，能接新需求。比如**给后台加一个按钮、绑权限、对接新接口**这种工作我都干过。但不敢说是前端专家——对 Vue 响应式源码、浏览器渲染原理这种深度题没研究过。"

**这个回答的好处**：
- ✅ 承认是后端主打，不装前端专家
- ✅ 但表明能独立处理前端常规工作（招人时的 bonus）
- ✅ 诚实划定边界，不会被问到爆炸

### Q2：讲讲你项目的前端技术栈

**标准答法**：

> "我们项目前端仓库叫 **vega-view**（comics-management），是 Vega 后台的管理端。技术栈：
> - **Vue 3 Composition API + setup 语法糖**
> - **Vite 5 + TypeScript 5**（比 Webpack 快很多）
> - **Pinia**（替代 Vuex，5 个 modules：user / app / route / tabs / dict）
> - **Arco Design**（字节开源 UI 库，组件按需自动引入）
> - **vue-router 动态路由**（登录后拉菜单生成路由）
> - **axios + NProgress** 封装统一请求层"

### Q3：最近写过什么前端代码？

**如果你改过权限按钮**：

> "最近的是给结算模块加了一个'导出明细'按钮，用 `v-hasPerm="['settlement:export']"` 指令控制权限，点击走一个下载接口。
>
> 代码量不大，但背后涉及：**后端权限表加新 identifier**、**前端 API 封装**、**NProgress 自动 loading**、**文件下载的 Content-Disposition 处理**。"

**如果你没改过**：

> "我们项目是前后端分工明确，我主要改后端。前端改动大多由前端同事做。但我们每次联调前端都来对接口字段、错误码，所以他们的代码路径我都清楚。"

---

## 二、Vue 3 基础类

### Q4：Vue 3 的 setup 和 Vue 2 的 Options API 有啥区别？

**标准答法**：

> "一句话：**把逻辑聚合在一起，而不是按照 data/methods/computed 分散**。
>
> Vue 2 写一个复杂组件可能是这样：data 里 5 个字段、computed 里 3 个、methods 里 10 个，同一个功能的逻辑散在 3 处。Vue 3 setup 里可以把'一个功能'的 ref + computed + 方法写在一起，读代码时功能单元就是一个区块。
>
> 另外 setup 配合 TypeScript 类型推导好得多，`<script setup>` 糖衣让代码更简洁——比如这样：
> ```vue
> <script setup lang="ts">
> const count = ref(0);
> const double = computed(() => count.value * 2);
> function add() { count.value++; }
> </script>
> ```"

### Q5：ref 和 reactive 的区别？什么时候用哪个？

**答法**：

> "简单说：
> - **ref**：任意类型（基本类型 / 对象 / 数组），访问要加 `.value`
> - **reactive**：只能包对象或数组，访问不用 `.value`
>
> **什么时候用 ref**：基本类型（`ref(0)`）、想重新赋值整个对象（`data.value = newObj`）
>
> **什么时候用 reactive**：纯对象结构、不需要整体替换
>
> 我们项目里 **大量用 ref**，因为：① 一致性好（不用纠结用哪个）；② TypeScript 对 ref 推导更友好；③ 重新赋值整个对象（比如查询条件重置）方便。"

### Q6：computed 和 watch 的区别？

**答法**：

> "**computed**：有返回值，基于依赖缓存，只有依赖变才重算。用于'派生数据'场景——比如 `permissions.includes('xxx')` 算出来一个 `ratingVisible`。
>
> **watch**：无返回值，监听值变化执行副作用。用于'要做什么事'场景——比如 tabId 变了要重新拉列表、路由变了要记录埋点。
>
> 口诀：**算出个新值用 computed，搞点事情用 watch**。"

### Q7：v-if vs v-show

> "**v-if** 条件不成立时**不渲染 DOM**（卸载组件）；**v-show** 渲染了但用 `display:none` 隐藏。
>
> 性能对比：
> - 切换频繁 → **v-show**（只改样式，不重渲染）
> - 切换少或从不切换 → **v-if**（省 DOM）
>
> 权限场景我们用 `v-if` 或者自定义的 `v-hasPerm`（直接 `removeChild`），**不用 v-show**——v-show 能被 F12 改样式绕过，不安全。"

### Q8：Vue 3 的 defineProps 和 defineEmits 怎么用？

**答法**：

> "`<script setup>` 里用这两个宏。`defineProps` 定义父传子的 props，`defineEmits` 定义子组件能 emit 的事件。
>
> 带 TypeScript 类型：
> ```vue
> <script setup lang="ts">
> interface Props {
>   userId: number;
>   enabled?: boolean;
> }
> const props = defineProps<Props>();
>
> const emit = defineEmits<{
>   (e: 'change', value: string): void;
>   (e: 'close'): void;
> }>();
>
> function handleClick() {
>   emit('change', 'new-value');
> }
> </script>
> ```
>
> 两个宏编译时处理，运行时没有。这点和 Vue 2 的 `props: {...}` 不同。"

---

## 三、TypeScript 类

### Q9：讲讲你对 TypeScript 的理解

**答法**：

> "TypeScript 是 **JavaScript 的超集 + 静态类型检查**。对我最大的好处是：
>
> 1. **接口协议类型化**：我们后端返回的 JSON 有 100+ 字段，前端定义 `interface IUser { userId: number; userName: string; ... }`，IDE 直接点出字段。改字段名时编译器立刻报所有用到的地方。
> 2. **重构敢大胆动**：改一个函数签名，所有调用方自动报错，改起来快。
> 3. **代码即文档**：函数签名就是文档，不用再写注释说明参数类型。
>
> 缺点：有一定学习曲线，泛型复杂时可读性下降。"

### Q10：interface 和 type 的区别？

**答法**：

> "**实用层面**：
> - **interface**：定义对象结构，支持声明合并（多次 `interface User { ... }` 会合并）、支持 `extends`
> - **type**：更灵活，能定义联合类型 `type Status = 'active' | 'disabled'`、交叉类型 `type Combined = A & B`、字面量类型
>
> **我的规则**：
> - 定义 API 入参/出参 → 用 **interface**（未来可能扩展）
> - 枚举 / 联合类型 / 函数签名 → 用 **type**"

### Q11：`as const` 是干嘛的？

> "字面量类型推导。比如：
> ```ts
> const arr = [1, 2, 3];           // 类型是 number[]
> const arr2 = [1, 2, 3] as const; // 类型是 readonly [1, 2, 3]
>
> const status = 'success';        // 类型是 string
> const status2 = 'success' as const; // 类型是 'success' (字面量)
> ```
>
> **什么时候用**：定义常量对象需要精确字面量类型时（比如状态枚举、路由 meta 配置）。"

### Q12：泛型 `<T>` 见过哪些？

**答法**：

> "最常见的是 axios 封装：
> ```ts
> const get = <T = any>(url: string, params?: object): Promise<T> => {
>   return request({ method: 'get', url, params });
> };
>
> // 调用
> const user = await get<IUser>('/api/me');
> // user 的类型是 IUser,自动推导
> ```
>
> 另一个是泛型工具类型 `Partial<T>`、`Pick<T, K>`、`Omit<T, K>`，写入参 DTO 时常用。"

---

## 四、状态管理（Pinia）

### Q13：为啥用 Pinia 不用 Vuex？

> "Vuex 是 Vue 2 时代的，Vue 3 官方推荐 Pinia。差异：
> 1. **API 更简洁**：Pinia 没有 mutations，直接改 state
> 2. **TS 支持好**：Vuex 的 TypeScript 类型需要额外配置，Pinia 原生友好
> 3. **Setup Store 写法**：和 Composition API 一致，可以复用 composable
> 4. **代码分包**：Pinia 的 store 按需 import，Vuex 倾向于一个大 tree
>
> 代码对比：
> ```ts
> // Vuex
> store.commit('user/setName', 'pengchong');
>
> // Pinia (更直观)
> const userStore = useUserStore();
> userStore.name = 'pengchong';
> ```"

### Q14：Setup Store 和 Options Store 选哪个？

> "两种都能用，但 **Setup Store 是未来**。
>
> ```ts
> // Setup Store (推荐)
> export const useUserStore = defineStore('user', () => {
>   const name = ref('');
>   const isAdmin = computed(() => roles.value.includes('admin'));
>   async function login() { ... }
>   return { name, isAdmin, login };
> });
>
> // Options Store (老派)
> export const useUserStore = defineStore('user', {
>   state: () => ({ name: '' }),
>   getters: { isAdmin: (state) => ... },
>   actions: { async login() { ... } },
> });
> ```
>
> Setup 好处：① 语法和组件一致；② 可以内部调其他 composable；③ 灵活度更高。我们项目里 **5 个 store 全是 Setup 写法**。"

### Q15：Pinia 的 persist 插件是啥？

> "默认 Pinia 的数据存在内存，刷新页面就没。`pinia-plugin-persistedstate` 插件自动持久化到 localStorage。
>
> ```ts
> export const useAppStore = defineStore('app', setup, {
>   persist: true  // 全量持久化
>   // 或者精细控制:
>   // persist: { paths: ['theme', 'sidebarCollapsed'] }
> });
> ```
>
> **我们项目 user store 故意 `persist: false`**——为啥？token 过期了刷新时要强制重拉用户数据验证 token，不能用本地缓存。这是个安全设计细节。"

---

## 五、路由

### Q16：vue-router 的三种 history 模式

> "`createWebHistory()`（h5 模式，URL 干净如 `/user/1`，需要服务端配合 fallback 到 `index.html`）
> `createWebHashHistory()`（hash 模式，`/#/user/1`，不用服务端配合）
> `createMemoryHistory()`（服务端渲染用，URL 不变）
>
> 我们项目用 **h5 模式**（`createWebHistory`），好看 + SEO 友好（虽然 B 端不关心 SEO），Nginx 层配 `try_files $uri /index.html` 做 fallback。"

### Q17：路由守卫的执行顺序？

**答法**：

> "全局守卫顺序：
> 1. `router.beforeEach`（全局前置）
> 2. `beforeEnter`（路由独享）
> 3. `beforeRouteEnter`（组件内）
> 4. `router.afterEach`（全局后置）
>
> 我们项目的守卫：
> - `setupPageGuard`：beforeEach 发路由切换事件（给 Tabs 组件监听）
> - `setupPermissionGuard`：按需加载菜单 + `addRoute` + 权限校验"

### Q18：动态路由怎么做的？

> "我们的做法：
> 1. 登录后调 `getUserInfo()` 拿到菜单树（含 `component` 字段表示组件路径）
> 2. 前端用 `import.meta.glob('@/views/**/*.vue')` 在编译时生成懒加载函数字典
> 3. 递归菜单树，把 `component: 'admin/list'` 转成对应的懒加载函数
> 4. 用 `router.addRoute` 注册
>
> 这么做的好处：后端改菜单/权限不用前端发版。"

### Q19：`import.meta.glob` 为啥是编译时？

> "它是 Vite 的**编译时 API**，不是运行时函数。Vite 构建时扫描文件系统找到所有匹配项，生成懒加载函数字典：
>
> ```ts
> // 写的代码:
> const modules = import.meta.glob('./views/**/*.vue');
>
> // Vite 编译后:
> const modules = {
>   './views/admin/list.vue': () => import('./views/admin/list.vue'),
>   './views/user/detail.vue': () => import('./views/user/detail.vue'),
>   // ...
> };
> ```
>
> 运行时去读 `modules[path]` 拿懒加载函数。**不是运行时扫描** ——运行时是没法扫文件系统的。"

---

## 六、HTTP / Axios

### Q20：axios 拦截器的作用？

> "请求和响应两道门：
>
> **请求拦截器**：
> - 自动塞 token 到 header
> - 开启 NProgress 进度条
> - 做参数签名（HMAC 那套）
>
> **响应拦截器**：
> - 按业务 code 分流处理（我们项目 code=2 跳登录，code=2303 静默抛，其他 code != 0 弹 toast）
> - 统一剥 `response.data.data`
> - 关闭 NProgress
> - 401/403/500 HTTP 状态码处理
>
> 好处：**业务代码只写 `await api.getUser()`**，不用每次处理 token/错误/loading。"

### Q21：前端怎么处理 token 过期？

**答法**：

> "我们项目的做法：
> 1. 后端返回 `{ code: 2, message: '...', data: 'https://sso.yuewen.com/login?redirect=xxx' }`
> 2. 前端响应拦截器识别到 `code === 2`：
>    - 开发环境 toast（方便调试）
>    - 生产环境 `window.location.href = data` 跳 SSO 登录，带回跳参数
>    - 登录完成后 SSO 重定向回当前页面
>
> 关键：**不要在多个请求同时失败时跳多次**。可以用一个 flag 或 lock 保证只跳一次（我们项目没做这个细节，但面试可以说'如果要进一步优化的话应该加个锁'）。"

### Q22：跨域怎么解决？

> "三种主流方案：
> 1. **服务端 CORS**：后端加 `Access-Control-Allow-Origin` 响应头（生产环境用这个）
> 2. **Nginx 反向代理**：前端请求自己域名的 `/api`，Nginx 转发到后端真实地址（前端看来是同源）
> 3. **Vite dev 代理**：开发时 `vite.config.ts` 的 `server.proxy` 代理 `/api` 到后端
>
> 我们项目：开发环境走 ③，生产环境走 ②。"

### Q23：RESTful vs 项目里的 API 风格

> "我们项目不是严格 RESTful，是**半 RESTful**：
> - URL 大体按资源分组：`/api/v1/contract/list`、`/api/v1/contract/create`、`/api/v1/contract/callback`
> - 方法大多 POST（写操作全 POST）、部分 GET（查询）
> - **状态码不用 HTTP 语义**：所有业务错误都返回 HTTP 200，错误信息在 `code` 字段
>
> 这是国内 B 端项目常见做法。优点：前端统一处理错误简单；缺点：curl 测试时无法从 HTTP 状态看出业务是否成功。面试官问起就这么讲。"

---

## 七、Vite 构建 / 工程化

### Q24：Vite 为啥比 Webpack 快？

**答法**：

> "两个核心差异：
>
> 1. **开发时 Vite 用 ESM**：浏览器原生 ESM 能力，**文件改动只需要转译单个模块**，不用像 Webpack 打包整个依赖图。启动 `dev server` 瞬间。
> 2. **预构建用 esbuild**：esbuild 是 Go 写的，比基于 JS 的 Webpack 快 10-100 倍。
>
> 生产构建 Vite 内部用 Rollup（Webpack 的替代品），分包能力更强。"

### Q25：`import.meta.env` 是什么？

> "Vite 提供的环境变量 API。`.env` / `.env.development` / `.env.production` 文件里定义：
>
> ```
> VITE_API_BASE_URL=http://localhost:8080
> VITE_API_PREFIX=/api/v1
> ```
>
> 代码里：
> ```ts
> import.meta.env.VITE_API_BASE_URL  // 注意:必须 VITE_ 前缀才暴露到代码
> import.meta.env.MODE              // 'development' / 'production'
> import.meta.env.DEV / PROD        // boolean
> ```
>
> 我们项目的 baseURL 就是 `import.meta.env.VITE_API_PREFIX`，不同环境打包出不同代码。"

### Q26：manualChunks 分包是什么？

> "生产构建时告诉 Rollup 把某些模块单独打包。我们项目：
>
> ```ts
> manualChunks(id) {
>   if (id.includes('node_modules')) {
>     if (id.includes('vue')) return 'vue';
>     if (id.includes('echarts')) return 'echarts';
>     if (id.includes('vtable')) return 'vtable';
>     return 'vendor';
>   }
> }
> ```
>
> 效果：产出 `vue-abc123.js`、`echarts-xxx.js`、`vtable-xxx.js`、`vendor-xxx.js`、业务 `app-xxx.js` 多个文件。
>
> **为啥这么分**：
> - 业务代码常更新 → `app.hash` 变
> - vue / echarts 几乎不升级 → 这些 chunk 的 `hash` 稳定
> - 浏览器 **缓存命中率大幅提升**——用户更新只下载业务 chunk，几 MB 的 echarts 继续用缓存"

### Q27：Tree Shaking 是什么？

> "构建时**删除没被用到的代码**。依赖 ESM 的静态分析——`import { a } from 'lib'` 里没用的 `b` 会被删掉。
>
> 生效条件：
> 1. 模块是 ESM（ES Module），不是 CommonJS
> 2. 没有副作用（或者 `package.json` 里 `sideEffects: false` 声明）
> 3. 构建工具支持（Vite/Webpack 都支持）
>
> 我们用 Arco Design **按需引入**（通过 `unplugin-vue-components` 的 `ArcoResolver`）就是 Tree Shaking 的典型应用——只打包你用到的组件。"

---

## 八、性能优化

### Q28：前端性能优化手段有哪些？

**按层次讲**：

> "**① 加载阶段**：
> - 代码分包（manualChunks / 路由懒加载）
> - Tree Shaking
> - 资源压缩（gzip / brotli）
> - CDN 加速
> - `<link rel="preload">` 关键资源预加载
>
> **② 渲染阶段**：
> - keep-alive 缓存组件
> - 虚拟列表（万条数据的表格）——我们用 VisActor VTable
> - v-memo（复杂列表项避免重复渲染）
> - 防抖节流（搜索输入）
>
> **③ 运行时**：
> - `requestIdleCallback` 处理非关键任务
> - Web Worker 做 CPU 密集计算
> - 图片懒加载（IntersectionObserver）
>
> 我做过的：**路由懒加载 + manualChunks + Arco 按需引入 + debounce 搜索**。没做过的（诚实说）：Web Worker、requestIdleCallback。"

### Q29：你们项目首屏加载多快？

**如果不知道就老实答**：

> "具体数字我不太确定，但我们做了这些基础优化：
> - Vue/echarts/vtable 分包缓存
> - 路由懒加载（`import.meta.glob`）
> - CDN 部署（base 配置支持）
> - 生产构建 drop_console / 去注释 / terser 压缩
>
> 实际 LCP 时间我不清楚，这块主要由前端同事看数据。**但我们接入了腾讯 Aegis 监控**，能在 Aegis 后台看到真实用户的 LCP、FCP、TBT 等指标。"

### Q30：debounce 和 throttle 的区别？

> "都是控制高频触发，但**时机不同**：
>
> - **debounce（防抖）**：连续触发只在**停下来后**执行一次。搜索输入 300ms 防抖是最经典——用户打字时不请求，停下再请求。
> - **throttle（节流）**：连续触发每隔 N ms **必定执行一次**。滚动监听 100ms 节流——即使一直滚也每 100ms 处理一次。
>
> 代码：
> ```ts
> import { debounce, throttle } from 'lodash-es';
>
> const search = debounce((q: string) => api.search(q), 300);
> const onScroll = throttle(() => updateScrollPos(), 100);
> ```"

---

## 九、权限和安全

### Q31：前端权限怎么做？

**答法**：

> "我们项目两件套：
>
> **1. 指令级**：`v-hasPerm="['contract:create']"`
> ```ts
> const hasPermDirective: Directive = {
>   mounted(el, binding) {
>     if (!hasPermOr(binding.value)) {
>       el.parentNode?.removeChild(el);  // 直接删 DOM
>     }
>   }
> };
> ```
>
> **2. 函数级**：`hasPerm('contract:create')` / `hasRole('admin')`
> - 超管 wildcard：`*:*:*` 权限或 `role_admin` 角色自动通过
> - 菜单层面：后端返回的菜单数据带权限，前端只渲染有权限的菜单
>
> **关键认知**：前端权限只是 UI 便利，**真正安全必须后端校验**。前端代码对攻击者完全可见，F12/Postman 能绕过。"

### Q32：XSS 怎么防？

> "**输出转义 + 白名单过滤**。
>
> Vue 默认模板里的 `{{ content }}` 会转义，`<script>` 标签会变成 `&lt;script&gt;`。但 `v-html` 会原样插入——这是 XSS 高危点。
>
> 如果必须用 `v-html`（比如渲染富文本编辑器的内容），要用 **DOMPurify** 先过滤：
>
> ```ts
> import DOMPurify from 'dompurify';
>
> const safeHtml = computed(() => DOMPurify.sanitize(props.content));
> ```
>
> 另外后端的 `XssAndSqlHttpServletRequestWrapper` 在入口统一过滤了参数——**前后端两道防线**。"

### Q33：CSRF 怎么防？

> "三招组合：
>
> 1. **SameSite Cookie**：Cookie 加 `SameSite=Lax`，浏览器跨站请求不带 Cookie
> 2. **CSRF Token**：服务端下发 token，前端每次请求带上验证
> 3. **Referer 校验**：服务端检查请求 Referer 头
>
> 我们项目主要靠 ① 和后端 `ywguid/ywkey` Cookie 的 SameSite 配置。登录态不能被其他站点盗用。"

### Q34：HTTP / HTTPS / HTTP2 差异？

> "**HTTP 1.1** → **HTTP 2** → **HTTP 3 (QUIC)** 逐代演进：
>
> - **HTTP 1.1**：每个请求需要建立 TCP 连接（Keep-Alive 可以复用，但还是串行）
> - **HTTP 2**：一个连接多路复用、二进制分帧、Header 压缩、服务器推送
> - **HTTP 3**：基于 UDP + QUIC，解决了 TCP 队头阻塞
>
> **HTTPS** = HTTP + **TLS**，加密 + 身份验证 + 防篡改。生产环境必上 HTTPS，浏览器现在把非 HTTPS 标记为'不安全'。
>
> 我们项目线上走 HTTPS + HTTP 2（Nginx 层配置）。"

---

## 十、浏览器 / 基础

### Q35：浏览器的事件循环

**答法**：

> "JavaScript 单线程，靠事件循环处理异步。流程：
> 1. **同步代码**执行（调用栈）
> 2. 调用栈空 → 从**微任务队列**取出全部执行（Promise.then、MutationObserver 等）
> 3. 从**宏任务队列**取一个执行（setTimeout、setInterval、I/O 等）
> 4. 回到 2
>
> 所以：
> ```ts
> console.log(1);
> setTimeout(() => console.log(2), 0);
> Promise.resolve().then(() => console.log(3));
> console.log(4);
> // 输出: 1 4 3 2
> ```
>
> Vue 的 `nextTick` 本质是把回调放进微任务队列（Promise.resolve），比 setTimeout(0) 更快。"

### Q36：localStorage / sessionStorage / Cookie 区别？

> "| 维度 | localStorage | sessionStorage | Cookie |
> |------|-------------|----------------|--------|
> | 容量 | 5-10MB | 5-10MB | 4KB |
> | 生命周期 | 永久（手动清除） | 标签页关闭就没 | 可设过期时间 |
> | 自动发送到服务器 | ❌ | ❌ | ✅ 每次请求自动带 |
> | 同域名可读 | ✅ | ✅（仅当前标签页） | ✅ |
>
> 我们项目：**token 放 localStorage**（request 拦截器手动塞 header），会话信息放 SSO Cookie（后端读 ywguid/ywkey）。"

### Q37：CSS 盒子模型

> "**content-box（W3C 标准）**：width 只包括 content，padding 和 border 加上去总宽会变
> **border-box（IE 模型）**：width 包括 content + padding + border
>
> 现代前端几乎都用 `box-sizing: border-box`（容易算布局）。Arco Design 全局就这样。"

### Q38：flex 布局常用属性

> "容器属性：
> - `display: flex`（开启）
> - `flex-direction`（主轴方向）
> - `justify-content`（主轴对齐，flex-start/center/flex-end/space-between/space-around）
> - `align-items`（交叉轴对齐）
> - `flex-wrap`（是否换行）
>
> 项目属性：
> - `flex: 1`（等分剩余空间）
> - `flex: 0 0 200px`（固定宽度不拉伸）
>
> 我们项目表单布局大量用 flex，比 grid 更灵活。"

---

## 十一、工程化 / 团队协作

### Q39：代码规范怎么搞的？

> "**三件套**：
> 1. **ESLint**：JS/TS/Vue 代码规范检查（eslint.config.js）
> 2. **Prettier**：代码格式化
> 3. **vue-tsc**：TypeScript 类型检查（`npm run typecheck`）
>
> 提交前 git hooks（husky + lint-staged）自动跑，不通过不让提交。
>
> 我们项目 `package.json` 里有 `lint / lint:fix / typecheck` 脚本，CI 流程必跑。"

### Q40：Git Flow

> "我们用简化的 Git Flow：
> - `master`：线上版本
> - `develop`：开发主干
> - `feature/xxx`：功能分支，从 develop 切，开发完 PR 回 develop
> - `release/xxx`：预发分支，用于灰度
> - `hotfix/xxx`：线上紧急修复，从 master 切
>
> 大多时候只在 feature 和 develop 之间切。"

### Q41：你们前后端怎么协作？

> "**接口驱动开发**：
> 1. 后端先定义 API（URL、入参、返回结构、字段含义）
> 2. 前后端共同评审接口定义（文档可能是 Swagger / Apifox / 飞书文档）
> 3. 后端按接口开发，前端同时用 Mock 数据开发
> 4. 联调阶段对字段、错误码、边界情况
> 5. 上线
>
> **关键点**：接口定好后改字段要双方确认。我们项目前端的 `src/apis/` 46 个模块对应后端的 Controller。"

---

## 十二、自我提升类（必有一问）

### Q42：怎么跟上前端技术的发展？

**答法**：

> "我是后端主力，前端跟进偏'实用主义'：
> 1. 看 **Vue 官方文档和 Release Notes**，了解重大变更（比如 setup 语法糖、响应式 API 优化）
> 2. 跟**团队前端同事交流**，他们实际用的新工具会带我一起学
> 3. 看 **ByteDance / Alibaba 的开源前端工具**（Arco / AntD / Taro），用他们的实战经验间接学
> 4. 做需求遇到不懂的就现查文档，不强求体系化学习
>
> 诚实说：我不会每月读前端公众号。重点是**我们项目用到的我吃透，没用到的知道概念**。"

### Q43：前端方向你最想深入的是什么？

> "如果让我选方向，会是 **前端工程化和性能优化**——这些和我后端经验有共通点：
> - **性能监控体系**：Aegis 埋点、LCP/FCP/TBT 指标体系
> - **构建优化**：Vite 插件原理、chunk 分包策略
> - **微前端架构**（multi-tenant 场景，跨团队协作大项目时有用）
>
> 这几块和我后端的'性能优化 + 架构设计'思路一致，可以把后端经验迁移过去。"

---

## 十三、实战问题（压力面）

### Q44："页面突然白屏了"怎么排查？

**按层次答**：

> "**5 步排查**：
> 1. **F12 看 Console**：JS 报错？未捕获 Promise？typo 导致的 undefined？
> 2. **看 Network**：静态资源加载失败？JS 文件 404？API 没返回？
> 3. **看 Elements**：DOM 有没有挂上去？是不是 Vue 组件没渲染？
> 4. **复现环境**：是特定浏览器 / 特定账号 / 特定数据？
> 5. **埋点数据**：Aegis 有没有上报错误？线上真实用户出现时能看到堆栈
>
> 最常见原因：① 某个接口返回了异常格式（前端解构崩了）；② 新发布的代码某处依赖缺失；③ 缓存问题（前端改了 URL 但用户缓存了旧 HTML）。"

### Q45："Vue 组件不更新" 怎么排查？

> "常见 4 大原因：
>
> 1. **响应式丢失**：`const data = { a: 1 }` 而不是 `ref` 或 `reactive`——数据改了但 Vue 不知道
> 2. **直接赋值数组/对象的索引/属性**：`arr[0] = x` 或 `obj.newProp = y`（Vue 2 有问题，Vue 3 已修复 Proxy 解决）
> 3. **computed 依赖错了**：依赖的 ref 没 `.value`，结果是个静态值
> 4. **keep-alive 缓存**：组件被缓存了，切回来时 onMounted 不再触发（应该用 onActivated）
>
> 我最近碰过的是 ③：在一个 ts 文件里导出 `const count = ref(0)`，另一个文件 import 后 `count` 不是 ref 而是 `Ref` 类型的对象，没加 `.value`。"

### Q46："权限不对" 怎么排查？

> "分层查：
> 1. **后端实际返回的 permissions 里有没有那个 identifier**（打印 user store）
> 2. **前端 v-hasPerm 的参数拼写**（`'contract:btn:create'` 还是 `'contract:create'`）
> 3. **超管 wildcard 有没有生效**（是不是 `*:*:*` 被过滤掉了）
> 4. **后端接口层有没有校验**（前端放过了但后端拦了）
>
> 我们遇过一次：新加的按钮 <code>v-hasPerm</code> 写对了，但后端菜单配置没给这个 identifier，结果后端返回的 permissions 里没有这串字符——一查后端菜单管理页面加一条就好了。**前后端协作中 50% 的权限问题是配置问题，不是代码问题**。"

---

## 🎯 面试的前端篇总结

### 最常被问的 5 个核心

1. **讲讲你用过的前端技术栈** → Vue 3 + Vite + TS + Pinia + Arco
2. **v-if vs v-show** / **ref vs reactive** → 基础题必背
3. **动态路由怎么做** → import.meta.glob + addRoute
4. **前端权限怎么做** → v-hasPerm + 超管 wildcard
5. **Vue 2 到 Vue 3 有什么新东西** → Composition API + Setup + TS 友好

### 安全降级话术

**问到你不懂的**：
> "这块我没深入研究过。我是后端，前端是辅助技能。如果需要我可以现场查文档，或者让前端同事一起看。"

**诚实比硬答重要**。面试官也是人，不会因为你承认前端不深入就扣分，但会因为你瞎编被扣分。

### 开场白定位

记住你的人设：
> "**后端主力，能读懂/改 bug/接新需求的 Vue 3 代码，不是前端岗位候选人，但全栈能力没问题。**"

把这句话变成肌肉记忆，任何问题都回到这个定位上。

---

> 📘 配合《comics-management 交互式代码课程》使用，代码细节都在那边。
> 这份是**口语化的问答册**，面试前一天过一遍记住答案。

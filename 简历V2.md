# 彭冲 · Java 后端工程师

**4.5 年经验** · 阅文集团 · Java 后端 · 现居上海
📧 1539112470@qq.com · 📱 17344035687 · 期望岗位：Java 后端开发（中厂自研）

---

## 关于我

4.5 年 Java 后端，擅长从业务场景抽象出可扩展的工程框架（策略 / 工厂 / 编排器），做过 N+1 优化、跨系统一致性、分布式事务补偿、批量任务稳定性治理等场景。追求"用简单方案解决复杂问题"，适合业务快速迭代 + 技术深度并重的团队。

---

## 技术栈

- **语言与框架**：Java 8/21、Spring Boot 2.3/2.7、MyBatis、Spring Security、Lombok
- **存储**：MySQL（分区表 / 组合索引 / 分库分表）、Redis（分布式锁 / 本地缓存）、腾讯云 COS
- **中间件**：TDMQ-CMQ、XXL-Job、七彩石配置中心
- **微服务**：TRPC、Polaris 服务注册、Peacock 脚手架
- **工程能力**：分布式事务补偿、N+1 优化、EasyExcel 流式处理、AES/HMAC 加解密、MDC 链路追踪

---

## 工作经历

**阅文集团 · 漫剧业务线** · 2023.03 - 至今 · Java 后端工程师

负责漫剧分销与签约平台核心后端，服务**万级 UP 主 + 千级机构 MCN**，月均签约 300+ 单，年结算金额千万级。独立 Owner 3 个核心模块（分销签约 / 结算分润 / IP 中台内容），深度对接集团 6+ 中台服务。

**北京宇信科技集团股份有限公司 上海分公司**（外派富邦华一银行）· 2021.09 - 2023.02 · Java 开发工程师

参与<strong>富邦华一银行普惠信贷管理系统</strong>开发，覆盖授信、客户管理、合同、贷后、还款等核心模块。技术栈 Spring Cloud + Feign + MyBatis + Redis + Liteflow + XXL-Job。
- **授信模块员工持股融资业务**：实现授信申请 → 押品（抵质押率 / 押品估值）→ 多岗位审批 → 合同额度校验 → 放款的完整链路
- **批量任务开发**：基于 XXL-Job 实现贷后还款定时扫描、利息计算、逾期判定
- **审批流编排**：基于 Liteflow 抽象多岗位审批节点，支持流程动态配置

---

## 项目一：阅文漫剧分销与签约平台（主力项目 · 3 模块 Owner）

**时间**：2023.03 - 至今 · **技术栈**：Spring Boot、MyBatis、TRPC、七彩石、腾讯云 COS

### 核心产出

**1. 签约框架抽象（策略 + 模板方法）**
- 基于 `ContractInvoker` + `CreateContractHandler` 封装签约标准流程（幂等 → 校验 → 创建 → 回调），泛型 `CreateContractContext` 自动装配个人 8 项 / 机构 15 项数据
- **新合同类型接入 5 天 → 1 天**，签约接口代码量 **-40%**，已支撑 5 种合同类型稳定运行

**2. 结算接口 N+1 优化**
- 定位结算列表循环调用 RPC 的 N+1 问题（单次 200 次 RPC），用方法内 HashMap + `computeIfAbsent` 做请求级缓存
- **RPC 200 次 → 10~20 次，接口响应 3~5s → 500ms**

**3. 新旧机构 ID 双向映射（跨系统兼容）**
- 阅文老分润系统千万级历史数据 + 几十个业务线在用，无法切换。设计 `organization_mapping` 表 + Service 层统一 ID 翻译（`newToOld` / `batchOldToNew`）
- **千级机构平滑过渡，零侵入新业务代码，结算零差错**

**4. 内容处理器工厂（工厂模式 + 本地缓存）**
- 基于 `ArtifactHandlerFactory` 统一成品处理接口（`create` / `bindCams` / `syncStatus`），List 注入 + Map 自动注册
- **新增内容形态 3 天 → 0.5 天**，支撑 4 种内容类型稳定运行

**5. 机构批量创建剧本（跨服务 ID 映射 + 幂等）**
- 五步流程：解析扩展字段 → 名称/书籍ID/作品类型三维度去重 → 分批调用内容中心（每批 50 条）→ 临时 ID → 正式 ID 反向映射 → 落库幂等
- **单次支撑 500 条批量上线，去重准确率 100%，跨服务 ID 映射零错乱**

### 线上问题治理（代表性事故）

- **合同号重复导致分账错乱**：SQL 多表联查定位脏数据 → 接口前置幂等 → DB 唯一索引 → 业务层关联计数。**上线后 3 个月零重复，修复 10+ 历史脏数据**
- **夜间批量定时任务失败**：分片（每批 50 条）+ 幂等标记 + 3 次重试 + 全链路日志。**周均 1~2 次告警 → 月均 0 次，DB 锁冲突 -80%**
- **多环境配置差异**：硬编码改七彩石动态管理。**发版数据对齐 2 小时 → 10 分钟**

---

## 项目二：漫剧运营后台 vega（辅助 · Spring Boot 2.7 + Java 21）

- **帖子批量 Excel 导入**：EasyExcel 流式解析，单条失败跳过不中断整批
- **合约 URL 存活监控**：XXL-Job 定时探活 + Redis 频控（1 小时最多 1 次告警）

## 项目三：创作者入驻端 nova（辅助 · Undertow + MyBatis-Plus）

- **机构创建 6 步 Orchestrator 编排**：参数校验 → OCR → 机审 → 落库 → 用户绑定 → 业务派发，策略 + 校验器链插拔
- **敏感数据透明加解密**：`AESStringTypeHandler` + DCL 单例从 Spring 拿密钥，DB 字段声明级自动加解密

---

## 教育背景

**安徽理工大学** · 机械设计制造及其自动化 · 本科 · 2017.09 - 2021.06

# 我真做过的需求(阅文漫剧分销平台)

> 全部基于 `/Users/pengchong/workspace/2026/` 里自己整理的真实材料。
> 每段都能讲 3-5 分钟,面试官追问扛得住。

---

## 需求 1 · 签约框架重构(5 种合同类型统一管控)

### 需求是什么(口语化)

分销签约最早只有"个人 UP 主签约"一种。后面业务逐个提:机构合作、补充协议、版权授权、委托制作。**每种合同流程都有个性化步骤,但共性也多** — 都要做幂等、参数校验、合同创建、后置回调。

早期写得散,**每加一种合同要改 3-5 处核心代码,开发周期 3-5 天**,还容易回归 bug。

### 卡点

**卡点 1:共性步骤散落**

幂等校验在 Controller,参数校验在 DTO,合同创建调合同中心,后置回调又散到各种 handler。每种合同类型都重复写一遍这 4 步。

**卡点 2:个性化步骤难抽**

个人签约要查实名认证、银行卡、人脸识别(8 项数据);机构签约要查企业资质、对公账户、法人身份证(15 项)。写成 if/else 一个方法 300 行。

**卡点 3:接入新合同改动面大**

每加一种要改 Service、Controller、Handler 3 处,review 时谁都不敢点"同意"。

### 方案

**策略模式 + 模板方法模式双剑合璧**。

- **策略**:`ContractInvoker` 定义"签约"动作,每种合同类型实现一个
- **模板方法**:`CreateContractHandler` 抽象基类封装标准流程(幂等 → 校验 → 创建 → 回调),`execute` 是 `final` 方法,子类只能覆写 `doCreate`

```java
public interface ContractInvoker {
    ContractType getType();
    ContractResult invoke(CreateContractContext ctx);
}

public abstract class CreateContractHandler {
    public final ContractResult execute(CreateContractContext ctx) {
        idempotentCheck(ctx);          // 共性:幂等(合同号 + 申请状态校验)
        validateParams(ctx);           // 共性:参数验证
        Contract c = doCreate(ctx);    // 个性:子类实现
        afterCreate(c, ctx);           // 共性:后置(调合同中心、发通知)
        return ContractResult.success(c);
    }
    protected abstract Contract doCreate(CreateContractContext ctx);
}

@Component
public class PersonalSignContractHandler extends CreateContractHandler
        implements ContractInvoker {
    public ContractType getType() { return ContractType.PERSONAL_SIGN; }

    @Override
    protected Contract doCreate(CreateContractContext ctx) {
        // 个人签约:查 UP 主实名、银行卡、人脸
        IdentityInfo identity = ctx.get(ContextKey.IDENTITY, IdentityInfo.class);
        BankCardInfo bankCard = ctx.get(ContextKey.BANK_CARD, BankCardInfo.class);
        // ... 调合同中心 createContract
    }
}
```

**`CreateContractContext` 泛型上下文容器**:根据主体类型(个人/机构)自动装配所需数据。个人装 8 项,机构装 15 项。

```java
public class CreateContractContext {
    private final Map<ContextKey, Object> data = new HashMap<>();

    public <T> T get(ContextKey key, Class<T> type) {
        return type.cast(data.get(key));
    }

    public static CreateContractContext forPersonal(Long guid) {
        CreateContractContext ctx = new CreateContractContext();
        ctx.put(ContextKey.IDENTITY, identityDriver.getByGuid(guid));
        ctx.put(ContextKey.BANK_CARD, bankCardDriver.getByGuid(guid));
        ctx.put(ContextKey.FACE_RECOG, faceRecogDriver.getByGuid(guid));
        // ... 8 项
        return ctx;
    }

    public static CreateContractContext forOrg(String orgId) {
        CreateContractContext ctx = new CreateContractContext();
        ctx.put(ContextKey.ORG_INFO, orgManager.getOrgInfo(orgId));
        ctx.put(ContextKey.COMPANY_CERT, companyCertDriver.getByOrgId(orgId));
        ctx.put(ContextKey.BANK_ACCOUNT, bankAccountDriver.getByOrgId(orgId));
        // ... 15 项
        return ctx;
    }
}
```

### 效果

- **新合同接入从 5 天 → 1 天**,核心代码零改动
- 已支撑 **5 种合同类型**:个人签约、机构合作、补充协议、版权授权、委托制作
- **签约接口代码量减少 40%**

### 涉及文件

- 合同核心模块(分销签约模块,我 Owner)
- `ContractInvoker` 策略接口、`CreateContractHandler` 模板基类
- `CreateContractContext` 泛型上下文容器

### 这题能展开聊的

1. **策略 + 模板方法一起用会不会过度设计?**
   - 单看任何一个都不够:纯策略会让每种合同类型重复"幂等 → 校验 → 创建 → 回调";纯模板方法又无法按类型路由。两者一起才干净
2. **上下文容器为什么用 Map 不用强类型对象?**
   - 强类型要为每种合同类型建 DTO,类爆炸。Map + 泛型 getter 兼顾类型安全和灵活性。代价是 key 打错了运行时才知道 — 我们所有 key 都放在 `ContextKey` enum 里,编译期能发现拼写错误
3. **合同中心是你写的吗?**
   - 不是。合同中心是集团公共中台,我只是对接方。我写的是业务层(分销签约)如何把业务参数转换成合同中心需要的格式

---

## 需求 2 · 合同"已过期"状态动态计算(零延迟零 Job)

### 需求是什么(口语化)

合同有 3 个展示状态:**已生效、已终止、已过期**。最早的实现:

- DB 存 3 种 status
- 定时任务每小时扫表,把"status = 已生效且 cooperation_end_time < NOW"的改成"已过期"

问题来了:
- **最长延迟 1 小时** — 用户看到合同明明过期了,状态还显示"已生效"
- **万级记录**,定时任务压力大
- **Job 挂了,状态就永远停在"已生效"**

### 卡点

**卡点 1:强一致性要求高**

前端筛选"已过期"要求 0 延迟,业务方根本不能接受"有时候准,有时候不准"。

**卡点 2:定时任务扫万级表**

锁冲突、长事务风险。越扫越慢。

**卡点 3:业务增加新状态(比如"临期",7 天内到期)也不想再加 Job**

### 方案

**"已过期"不存库,靠 `cooperation_end_time < NOW()` 实时动态计算**。

数据库只存两种基础状态:**已生效(1)、已终止(9)**。

**查询时 SQL 加 `CASE WHEN`**:

```sql
SELECT id, contract_number,
       CASE
           WHEN status = 1 AND cooperation_end_time < NOW() THEN 2   -- 已过期
           WHEN status = 1 THEN 1                                    -- 已生效
           WHEN status = 9 THEN 9                                    -- 已终止
       END AS status_view
FROM contract
```

**按"已过期"筛选时 Service 自动转 SQL 条件**:

```java
// 前端传 statusView = 2 (已过期)
if (statusView != null && statusView == 2) {
    query.setStatus(1);
    query.setCooperationEndBefore(LocalDateTime.now());
}
```

**索引配合**:`idx_status_end_time (status, cooperation_end_time)` 组合索引,WHERE `status=1 AND cooperation_end_time < NOW()` 最左前缀命中。

### 效果

- **状态延迟从最长 1 小时 → 0**(实时)
- **消除定时任务依赖**
- 万级合作记录状态查询实时准确

### 这题能展开聊的

1. **以后加"临期"状态怎么办?**
   - 同样套路:`CASE WHEN status = 1 AND cooperation_end_time BETWEEN NOW() AND DATE_ADD(NOW(), INTERVAL 7 DAY) THEN 3`。不改表
2. **CASE WHEN 多了会不会影响查询性能?**
   - 计算列不走索引,但是 WHERE 条件(`cooperation_end_time < NOW()`)走索引。SELECT 列的 CASE WHEN 只是结果集上的微小开销
3. **为什么"已终止"要存库?**
   - 它是**人工触发**的(单方违约、协商解约),不是时间到就自动变的。必须存库记录"谁什么时候为什么终止"

---

## 需求 3 · N+1 查询优化(方法内 HashMap 本地缓存)

### 需求是什么(口语化)

结算列表接口一次查 50-200 条,每条要显示"操作人名字"。老代码:

```java
list.stream().map(s -> {
    UserDTO u = userService.getByUserId(s.getOperatorId());   // RPC!
    vo.setOperatorName(u.getName());
});
```

for 循环每条调 RPC,**最差 200 次,接口 3-5 秒**,用户打开列表明显卡。

### 卡点

- 用户服务是 RPC(不是本地 DB),每次走网络
- **同批记录里很多 operatorId 重复**(运营操作就那几个人)
- 缓存不能太重 — 列表查询不值得上 Redis

### 方案

**方法内 HashMap 做轻量级缓存**,生命周期 = 一次请求:

```java
public List<SettlementVO> listSettlements(QueryParam param) {
    List<SettlementDO> list = mapper.list(param);

    // 方法内本地缓存
    Map<Long, UserDTO> userCache = new HashMap<>();

    return list.stream().map(s -> {
        SettlementVO vo = SettlementVO.from(s);
        // 命中就返,没命中调 RPC 并存 cache
        UserDTO operator = userCache.computeIfAbsent(
                s.getOperatorId(),
                uid -> userService.getByUserId(uid)
        );
        vo.setOperatorName(operator.getName());
        return vo;
    }).collect(toList());
}
```

核心在 `computeIfAbsent` — 同一个 operatorId 在列表里出现 N 次,RPC 只调一次。

**生产监控**:加了日志 `log.info("list size={}, cache hit rate={}", list.size(), hitRate)`,命中率常年 **85%+**。

### 效果

- **最差 RPC 从 200 次 → 10-20 次**(实际用户数)
- **接口响应 3-5 秒 → 500ms**
- N+1 彻底消除

### 这题能展开聊的

1. **为什么不用 Redis 缓存?**
   - ① QPS 不高(运营后台);② 用户信息会变,Redis 要做失效策略;③ 方法内 HashMap 自带"请求级一致性",不会出现"前后显示不同名字"
2. **批量接口 `batchGetByUserIds` 不是更好吗?**
   - 更好。实际两步:先 HashMap 去重收集 operatorId,再批量调一次。但如果用户服务没提供批量接口,HashMap 是最简单可行的方案
3. **并发安全吗?**
   - HashMap **方法内局部变量**,不跨线程,不需要 ConcurrentHashMap。这点 review 时要强调

---

## 需求 4 · organization_mapping 新旧机构 ID 双向转换

### 需求是什么(口语化)

阅文多业务线,有**老的分润系统**(上线好多年、存了好几年历史数据、上游几十个业务线在调用、千万级数据)和**新的机构主数据系统**。两套 ID 不一样。

分销结算需要同时适配:
- 前端用新 ID 查询
- 分润系统只认老 ID
- 返回给前端要翻译回新 ID

### 卡点

**卡点 1:老分润系统改不了**

上线多年,几十个上游调用,千万级数据。推动它改造代价太大,只能在新系统这边兼容。

**卡点 2:映射表怎么设计,既兼容历史又不污染新业务?**

**卡点 3:双向转换,哪里转、谁负责?**

不能在 Mapper 里转(跨服务混在一起);不能在 Controller 里转(业务耦合)。

### 方案

**单独一张映射表 + Service 层统一翻译**。

```sql
CREATE TABLE organization_mapping (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    org_id VARCHAR(64) NOT NULL COMMENT '【新系统】标准机构ID',
    old_system_id VARCHAR(64) NOT NULL COMMENT '【老系统/分润系统】旧机构ID',
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT NOW(),
    update_time DATETIME DEFAULT NOW(),
    UNIQUE KEY uk_org_id (org_id),
    UNIQUE KEY uk_old_system_id (old_system_id)
);
```

**核心字段只有 3 个必须**(org_id、old_system_id、status),其他扩展字段。

**两边都加唯一索引**,新老双向查询都是 O(1)。

**接口流转**(以周结算为例):

```java
public PagedData<WeekSettleVO> queryWeekSettle(WeekSettleReq req) {
    // 1. 前端传新 ID → 转老 ID
    String oldOrgId = mappingService.newToOld(req.getOrgId());

    // 2. RPC 调分润系统(只认老 ID)
    List<ProfitDTO> profits = profitProvider.queryManjuWeekSettle(
            oldOrgId, req.getStartDate(), req.getEndDate(),
            req.getPageNo(), req.getPageSize());

    // 3. 分润返的是老 ID → 批量反向转新 ID
    Set<String> oldIds = profits.stream()
            .map(ProfitDTO::getOrgId).collect(toSet());
    Map<String, String> oldToNew = mappingService.batchOldToNew(oldIds);

    // 4. 查新 ID 的机构名(走 OrgManager 聚合层)
    Map<String, String> orgNames = orgManager.batchGetOrgName(oldToNew.values());

    // 5. 组装 VO(带新 ID + 名字)
    return profits.stream().map(p -> {
        WeekSettleVO vo = WeekSettleVO.from(p);
        String newId = oldToNew.get(p.getOrgId());
        vo.setOrgId(newId);
        vo.setOrgName(orgNames.get(newId));
        return vo;
    }).collect(...);
}
```

**核心设计点**:
- **转换逻辑只在 Service 层**,Controller 收新 ID,Mapper/Provider 拿老 ID
- **提供单次 + 批量两个接口**,批量走 `IN` 查询减少 DB 调用
- **映射表不污染业务主数据**,删表也不影响新业务

### 效果

- **千级机构新旧 ID 平滑过渡**
- **零侵入新业务代码**
- **分润结算零差错**

### 这题能展开聊的

1. **为什么不在 DB 加视图做映射?**
   - 分润系统是另一个团队的服务,跨服务不在一个 DB,视图做不了
2. **新机构没有老 ID 怎么办?**
   - 新机构不会推到分润系统,所以也不需要老 ID。分销结算走的是"历史机构",它们必然有老 ID。如果要接入,由运维批量 INSERT 初始化
3. **映射表会不会越来越大?**
   - 千级机构,表本身就千条记录,不大。双唯一索引 O(1) 查询

---

## 需求 5 · ArtifactHandlerFactory 成品处理器工厂

### 需求是什么(口语化)

IP 中台要登记 3 种内容类型:**剧本、剧集、成品**。

- 每种类型的创建字段不一样(剧本有书籍 ID,剧集有集数)
- 调用的下游服务不一样(剧本调内容中心,剧集调短剧服务)
- CAMS 绑定参数不一样

老实现就是 `if (type == SCRIPT) ... else if (type == EPISODE) ...`,**加一种类型要改 4-6 处代码**。

### 卡点

**卡点 1:判断散落到处都是**

创建、查询、绑 CAMS、状态同步、查询详情,全都要类型判断。

**卡点 2:扩展成本高**

加第 4 种"成品"要改 Service、Mapper、Business 多处。

### 方案

**工厂模式 + 统一 Handler 接口 + 本地缓存优化实例复用**。

```java
public interface ArtifactHandler {
    ContentType getType();
    void create(CreateContext ctx);
    void bindCams(BindContext ctx);
    void syncStatus(SyncContext ctx);
}

@Component
public class ScriptArtifactHandler implements ArtifactHandler {
    public ContentType getType() { return ContentType.SCRIPT; }
    public void create(CreateContext ctx) { /* 剧本创建 */ }
    public void bindCams(BindContext ctx) { /* 剧本 CAMS 绑定 */ }
    public void syncStatus(SyncContext ctx) { /* 剧本状态同步 */ }
}

@Component
public class EpisodeArtifactHandler implements ArtifactHandler { ... }

@Component
public class ArtifactHandlerFactory {
    private final Map<ContentType, ArtifactHandler> handlerMap = new HashMap<>();

    @Autowired
    public ArtifactHandlerFactory(List<ArtifactHandler> all) {
        for (ArtifactHandler h : all) handlerMap.put(h.getType(), h);
    }

    @PostConstruct
    public void validate() {
        // 启动时校验:同一 ContentType 只能有一个 Handler
        Set<ContentType> seen = new HashSet<>();
        for (ArtifactHandler h : handlerMap.values()) {
            if (!seen.add(h.getType()))
                throw new IllegalStateException("重复的 ContentType: " + h.getType());
        }
    }

    public ArtifactHandler get(ContentType type) {
        ArtifactHandler h = handlerMap.get(type);
        if (h == null) throw new BusinessException("unsupported " + type);
        return h;
    }
}
```

业务代码完全不用判断类型:

```java
public void createArtifact(ContentType type, CreateContext ctx) {
    handlerFactory.get(type).create(ctx);
}
```

**加"成品"类型**:只要新建 `ProductArtifactHandler` + `@Component`,Spring 启动自动注册。**业务代码零改动**。

### 效果

- **新增内容形态开发周期从 3 天 → 0.5 天**
- **核心流程零改动**
- 已支撑 4 种内容类型

### 这题能展开聊的

1. **为什么用 List 注入而不是 Map?**
   - Spring 支持 `@Autowired Map<String, ArtifactHandler>`,但 key 是 Bean 名字不是业务枚举。我要的 key 是 `ContentType`,所以手动转
2. **两个 Handler 返同一 ContentType 会怎样?**
   - 启动时 `@PostConstruct` 校验就会抛异常,不等到运行时
3. **本地缓存优化实例复用是啥意思?**
   - 最早是 `applicationContext.getBean(ScriptArtifactHandler.class)` 每次从 Spring 容器取,其实是 O(1) 但有 Spring 内部查找。改成启动时装 Map,直接 HashMap.get(),更快

---

## 需求 6 · batchCreateContentScriptForOrg 批量创建剧本

### 需求是什么(口语化)

机构合作签完约后,**一次要批量上线 50-500 个剧本**。流程:

1. 解析前端 JSON(扩展字段有几十个)
2. 去重(不能重复创建)
3. 调内容中心拿正式 ID(我方只有临时编号)
4. ID 映射落库
5. 补充机构、合同、跟进人信息

### 卡点

**卡点 1:JSON 扩展字段复杂**,每种剧本不一样

**卡点 2:去重不是简单按 name**,按 **名称 + 书籍ID + 作品类型** 三维度联合去重

**卡点 3:内容中心单次上限 50 条**,500 条要分 10 批

**卡点 4:跨服务 ID 映射容易错乱** — 临时 ID ↔ 正式 ID

**卡点 5:落库还要幂等**,防重复提交

### 方案

**5 步流程**:

```java
public List<ArtifactScriptDO> batchCreateContentScriptForOrg(BatchCreateParam param) {
    // 步骤 1:解析 JSON 扩展字段
    List<CreateScriptWorkItem> workList = param.getWorks().stream()
            .map(this::parseExtraJson)
            .collect(toList());

    // 步骤 2:精准去重(name + cbid + workType)
    Map<String, CreateScriptWorkItem> uniqueMap = new LinkedHashMap<>();
    for (CreateScriptWorkItem w : workList) {
        String key = w.getScriptName() + "|" + w.getCbid() + "|" + w.getWorkType();
        uniqueMap.putIfAbsent(key, w);   // 先到先得
    }
    List<CreateScriptWorkItem> uniqList = new ArrayList<>(uniqueMap.values());

    // 步骤 3:分批调内容中心(单次 50 条)
    Map<Long, CreateScriptWorkItem> idMap = new HashMap<>();
    Lists.partition(uniqList, 50).forEach(batch -> {
        Map<Long, CreateScriptWorkItem> sub = batchDoCreateContentScript(
                batch, userDO, orgInfoDO, type);
        idMap.putAll(sub);
    });

    // 步骤 4:反向组装 ID 映射(已在步骤 3 里完成:正式ID → 剧本数据)

    // 步骤 5:本地落库 + 幂等(contract_number + apply_id 维度查已存在)
    return createArtifactScripts(idMap, orgInfoDO, contractInfo);
}

private Map<Long, CreateScriptWorkItem> batchDoCreateContentScript(
        List<CreateScriptWorkItem> workList, UserDO userDO,
        OrgInfoDO orgInfoDO, ContentTypeEnum type) {
    // 3.1 组装第三方请求参数
    List<AddComicScriptArg> argList = workList.stream().map(w -> {
        AddComicScriptArg arg = new AddComicScriptArg();
        arg.setScriptName(w.getScriptName());
        arg.setCbid(w.getCbid());
        arg.setOrgId(orgInfoDO.getOrgId());
        return arg;
    }).collect(toList());

    // 3.2 调内容中心 / 短剧服务
    Result<Map<Long, Long>> result = shortPlayProvider.batchAddScript(argList);
    if (!result.isSuccess()) {
        log.error("第三方创建失败: {}", result.getMessage());
        throw new BusinessException("剧本创建失败");
    }

    // 3.3 反转映射:临时ID→正式ID  →  正式ID→剧本数据
    Map<Long, Long> tempToFormal = result.getData();
    return reverseMap(tempToFormal, workList);
}

private Map<Long, CreateScriptWorkItem> reverseMap(
        Map<Long, Long> tempToFormal, List<CreateScriptWorkItem> workList) {
    Map<Long, CreateScriptWorkItem> tempToWork = workList.stream()
            .collect(toMap(CreateScriptWorkItem::getTempId, w -> w));

    Map<Long, CreateScriptWorkItem> result = new HashMap<>();
    for (Map.Entry<Long, Long> e : tempToFormal.entrySet()) {
        CreateScriptWorkItem w = tempToWork.get(e.getKey());
        if (w != null) result.put(e.getValue(), w);
    }
    return result;
}
```

### 效果

- 支撑机构合作场景批量上线
- **单次最高 500 条**
- **去重准确率 100%**,跨服务 ID 映射零错乱

### 这题能展开聊的

1. **为什么用 LinkedHashMap 去重?**
   - 保持输入顺序(HashMap/HashSet 不保证)。LinkedHashMap 保序且 `putIfAbsent` 自然完成"先到先得"
2. **分批 50 条为什么不并行?**
   - ① 内容中心有 QPS 限制,并行反而被限流;② ID 映射错误排查要串行日志;③ 500 条 10 批 × 200ms = 2s 可接受
3. **中途失败怎么办?**
   - 部分成功策略:失败的这批记录到 `failedList`,接口返 `{success, failed}`,运营前端看到哪些失败,点"重试失败的"再发一次。不整体回滚(内容中心 delete 接口未必幂等)
4. **反向组装为什么要两步?**
   - 内容中心返 `{临时ID: 正式ID}`,我要 `{正式ID: 剧本数据}` 用来落库。所以先 `workList` 构建 `{临时ID: 剧本数据}`,再遍历 `tempToFormal` 反转

---

## 治理 1 · 合同号重复导致分账统计错乱

### 问题现象

机构合作场景下出现**合同号重复、多剧本异常关联同一合同**。影响:
- `org_coop_apply` 表多条相同 `contract_number` 的记录
- 下游批量创建剧本时重复生成 `artifact_script`
- **分账结算时金额重复统计**
- 影响 **10+ 笔分账记录**

### 排查过程

**第一步:SQL 定位重复合同号**:

```sql
SELECT contract_number, COUNT(*) as cnt
FROM org_coop_apply
WHERE is_valid = 1
GROUP BY contract_number
HAVING COUNT(*) > 1;
```

**第二步:查合同号 - 申请 - 剧本的一对多关联**:

```sql
SELECT a.contract_number,
       COUNT(DISTINCT a.apply_id) as apply_cnt,
       COUNT(b.script_id) as script_cnt
FROM org_coop_apply a
LEFT JOIN artifact_script b ON a.apply_id = b.apply_id
WHERE a.is_valid = 1
GROUP BY a.contract_number
HAVING COUNT(DISTINCT a.apply_id) > 1;
```

`apply_cnt > 1` 就是异常。

### 修复(三层防御)

**第一层:Controller 前置幂等校验**:

```java
long existCount = orgCoopApplyMapper.countByContractNumberAndStatus(
        contractNumber,
        Arrays.asList(ApplyStatusEnum.PENDING.getCode(),
                      ApplyStatusEnum.PROCESSING.getCode()));
if (existCount > 0) {
    throw new BusinessException("合同号[" + contractNumber + "]已存在有效申请");
}
```

**第二层:DB 唯一索引**:

```sql
CREATE UNIQUE INDEX idx_contract_number ON org_coop_apply(contract_number);
```

**第三层:下游剧本创建幂等**:

```java
List<ArtifactScriptDO> existScripts = scriptDriver.getArtifactScriptsByApplyId(applyId);
if (!CollectionUtils.isEmpty(existScripts)) {
    Logs.access("applyId={} has already created scripts, skip", applyId);
    return existScripts;
}
```

**存量数据清洗**:

```sql
-- 保留最早有效申请,其余标记为无效
UPDATE org_coop_apply
SET is_valid = 0, update_time = NOW()
WHERE contract_number = 'xxx'
  AND apply_id NOT IN (SELECT MIN(apply_id) FROM org_coop_apply WHERE contract_number = 'xxx');

-- 删除重复剧本
DELETE FROM artifact_script
WHERE apply_id IN (SELECT apply_id FROM org_coop_apply
                   WHERE contract_number = 'xxx' AND is_valid = 0);
```

**结算 SQL 修正**:

```sql
-- 修正前:按合同号,会包含重复申请的剧本
SELECT SUM(settle_amount) FROM settlement_record WHERE contract_number = 'xxx';

-- 修正后:按有效申请
SELECT SUM(s.settle_amount)
FROM settlement_record s
JOIN org_coop_apply a ON s.apply_id = a.apply_id
WHERE a.contract_number = 'xxx' AND a.is_valid = 1;
```

### 效果

- **上线 3 个月零次重复创建**
- **分账统计准确率 100%**
- 修复 10+ 笔历史脏数据

### 这题能展开聊的

1. **为什么既要代码校验又要 DB 唯一索引?**
   - 两层防御。代码校验给友好错误提示;DB 索引防并发场景(两个请求同时过代码校验时都觉得"没重复",DB 兜底拦)
2. **为什么不加分布式锁?**
   - 合同号本身是幂等键,天然有唯一性。分布式锁适合"没有幂等键但要做幂等"的场景
3. **if 校验会不会把"新合同重新签"也拦了?**
   - 不会。只查 `status IN (PENDING, PROCESSING)` 的有效申请。已完成/已终止不算。所以"第一次签约失败撤回后重新发起"是允许的

---

## 治理 2 · 合同中台回调状态不同步

### 问题现象

合同中台回调"乙方已签署",但本地 `contract_status` 没变,页面一直"待签约"。

### 排查过程

**第一步:看中台回调日志** — 正常。

**第二步:看 DB 状态** — `contract_status` 没变,但 `update_time` 变了。

这说明代码跑过了,只是 status 那行没执行。

**第三步:看回调代码**:

```java
public void callback(ContractCallbackDTO dto) {
    String status = dto.getProcessStatus();
    if ("2".equals(status)) {
        contractDetail.setSignTime(LocalDateTime.now());
        contractDetail.setRemark("双方已签约");
        // ❌ 漏了这行:contractDetail.setContractStatus(2);
    } else if ("5".equals(status)) {
        contractDetail.setContractStatus(3);   // ✅ 这个分支写了
    }
    contractDetailDriver.updateById(contractDetail);
}
```

**根因**:某个分支漏写 `setContractStatus`。`update_time` 变是 MyBatis 自动更新。

### 修复

**直接补上缺失的 set**:

```java
if ("2".equals(status)) {
    contractDetail.setContractStatus(2);   // ✅ 补上
    contractDetail.setSignTime(LocalDateTime.now());
    contractDetail.setRemark("双方已签约");
}
```

**预防机制**:重构成枚举映射,避免以后再漏:

```java
private static final Map<String, Integer> STATUS_MAP = Map.of(
    "2", ContractStatus.FULLY_SIGNED.getCode(),
    "5", ContractStatus.PARTY_B_SIGNED.getCode(),
    "9", ContractStatus.TERMINATED.getCode()
);

public void callback(ContractCallbackDTO dto) {
    Integer targetStatus = STATUS_MAP.get(dto.getProcessStatus());
    if (targetStatus == null) {
        log.warn("unknown processStatus: {}", dto.getProcessStatus());
        return;
    }
    contractDetail.setContractStatus(targetStatus);
    contractDetailDriver.updateById(contractDetail);
}
```

### 这题能展开聊的

1. **回调接口有鉴权吗?**
   - 有,HMAC-SHA256 签名。中台发回调带 `X-Sign` Header,签名校验过才接受
2. **为什么不一开始就用 Map?**
   - 最初 if/else 更直观,加到 3 个状态够用。加到 5 个状态时发现太容易漏,才重构。典型"代码随业务演进"

---

## 治理 3 · 双合同场景展示状态错误

### 问题现象

有些作品同时签**版权合同 + 侵权合同**(版权方 + 实际分发方不是同一个)。**页面显示"已生效",但实际侵权合同还没签**,用户投诉。

### 排查

**老代码只看一个合同**:

```java
public WorkDisplayStatus getDisplayStatus(Long workId) {
    Contract coopContract = contractDriver.getByWorkId(workId);
    if (isSignFinish(coopContract.getStatus()))
        return WorkDisplayStatus.EFFECTIVE;
    return WorkDisplayStatus.WAIT_SIGN;
}
```

查数据发现有些作品关联了两个合同(`coop_contract` + `understand_contract`),老代码只看前者。

### 修复

```java
public WorkDisplayStatus getDisplayStatus(Long workId) {
    Contract coopContract = contractDriver.getByWorkId(workId);
    Contract understandContract = understandContractDriver.getByWorkId(workId);

    if (existTwoContract(workId)) {
        // 双合同:两个都完成才是已生效
        if (isSignFinish(coopContract.getStatus())
                && isSignFinish(understandContract.getStatus())) {
            return WorkDisplayStatus.EFFECTIVE;
        }
        return WorkDisplayStatus.WAIT_SIGN;
    }
    // 单合同按自己状态
    return isSignFinish(coopContract.getStatus())
            ? WorkDisplayStatus.EFFECTIVE
            : WorkDisplayStatus.WAIT_SIGN;
}
```

### 这题能展开聊的

1. **为什么不放到 DB view 里?**
   - ① 业务逻辑会变(未来可能三合同);② 前端多个入口要调,view 不方便做缓存
2. **双合同是谁签的?**
   - 版权合同是版权方(IP 原著作者)签;侵权合同是分发方(MCN、UP 主)签。两个角色各一份,都签完才合法分发

---

## 治理 4 · 批量定时任务稳定性

### 问题现象

夜间批量任务(额度释放、状态更新)**周均失败 1-2 次**,DB 锁冲突、超时。

### 方案

**1. 分片拆分**(每批 50 条):

```java
List<OrgCoopApplyDO> all = mapper.selectAll();
Lists.partition(all, 50).forEach(partition -> {
    mapper.batchUpdateStatus(partition, newStatus);
});
```

**2. 幂等标记**:

```sql
ALTER TABLE org_coop_apply ADD COLUMN update_task_id VARCHAR(64);
```

```java
if (StringUtils.isNotEmpty(apply.getUpdateTaskId())
        && apply.getUpdateTaskId().equals(taskId)) {
    continue;   // 已更新过
}
apply.setUpdateTaskId(taskId);
```

**3. 失败重试 + 告警**:

```java
int retry = 0;
while (retry < 3) {
    try {
        doUpdate(apply);
        break;
    } catch (Exception e) {
        retry++;
        if (retry == 3) {
            log.error("batch update failed, id={}", apply.getApplyId(), e);
            Alerter.alert("批量更新失败", "applyId=" + apply.getApplyId());
        } else {
            Thread.sleep(1000);
        }
    }
}
```

### 效果

- **任务失败告警从周均 1-2 次 → 月均 0 次**
- **数据库锁冲突减少 80%**

---

## 总结:这些能讲什么亮点?

| 需求/治理 | 关键词 | 量化 |
|---|---|---|
| 1 签约框架 | 策略 + 模板方法 + 泛型上下文 | 5 天 → 1 天 |
| 2 过期动态计算 | 消除定时任务 | 1 小时 → 0 |
| 3 N+1 优化 | 方法内 HashMap | 3-5s → 500ms |
| 4 新旧 ID 映射 | organization_mapping 双向转换 | 零侵入新业务 |
| 5 工厂模式 | ArtifactHandlerFactory | 3 天 → 0.5 天 |
| 6 batchCreate | 分批 + 去重 + 反向 ID 映射 | 单次 500 条 100% 准确 |
| 治理 1 合同号重复 | 三层防御 | 3 个月零次 |
| 治理 2 回调漏 set | 枚举 Map 预防 | 根因级修复 |
| 治理 3 双合同展示 | 多合同联动判断 | 业务合规 |
| 治理 4 批量任务 | 分片 + 幂等 + 重试 | 周均 1-2 次 → 月均 0 次 |

**面试顺序建议**:

1. 开场 1 分钟 — 先讲"阅文漫剧分销平台 + 万级 UP 主 + 月均 300 单 + 3 个核心模块"
2. 被问"最复杂" — 讲 1(签约框架)
3. 被问"最有成就感" — 讲 3(N+1 优化),数字漂亮
4. 被问"踩坑最深" — 讲治理 1(合同号重复三层防御)
5. 被问"有没有重构老代码" — 讲 5(工厂模式)
6. 被问"跨系统问题" — 讲 4(新旧 ID 映射)

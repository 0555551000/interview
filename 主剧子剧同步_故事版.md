# 主剧子剧分类同步 —— 唠嗑版

> 这个需求是 2026 年 4 月底做的。
> 看起来就是"加几行代码调个 RPC",连 PM 都说"这个半天搞完吧?"
> 结果做下来总共花了一天多,排查 bug 占了大头。
> 但**面试能讲的东西不少**:跨系统数据一致性、依赖升级兼容、异常分层保护,都在这里面。

---

## 一、需求来了:一个看起来很简单的小活

2026 年 4 月底,PM 找我:

> "彭哥,有个小 bug 要修。merchantry 那边反馈,**运营在 Vega 后台给一部剧配了分类,merchantry 只有主剧显示对,子剧还是'未分类'**。"

我:"子剧?哪来的子剧?"

她:"Vega 里存的都是**主剧**,内容中心那边每个主剧会派生出 N 个子剧给不同的分销商。比如《XX 仙尊》主剧 vdid=100,子剧有 101、102、103 分别给不同客户。merchantry 同步过去的数据是**所有 vdid(主剧+子剧)都有记录**。运营给 100 配了'沙雕漫',但同步只把 100 改了,101/102/103 还是默认 0。"

我:"所以现在 merchantry 查'分类=沙雕漫'的剧,能找到主剧找不到子剧?"

她:"对。但**真正分发给客户用的就是子剧**。分销商看到的都是'未分类',没法按分类检索。"

我:"明白了。那就在 Vega 同步 dramaCategory 到 merchantry 时,把所有子剧也一起同步对吧?"

她:"对。这事儿应该**半天就能搞定**。"

(又是一个"应该半天"。每次 PM 说这句话,都要做好心理建设。)

---

## 二、需求到底是啥(人话版)

### 2.1 业务数据流

三个系统的分工:

```
Vega(运营后台)
  ├─ 运营在这里给每个主剧配 dramaCategory
  └─ 存在 ArtifactVideoExtra.dramaCategory 字段

     ↓ HTTP 同步

merchantry(商户系统,就是 comic-drama-web)
  └─ video_sync_index.drama_category 字段
  └─ 分销商通过它过滤看到的剧

     ↑ 查询

内容中心(ShortPlayServer)
  └─ 主剧 + 所有子剧的真正源头
  └─ 提供 TRPC 接口让外部查子剧列表
```

### 2.2 现状

Vega 里有段代码 `syncDramaCategoryToComicDramaWeb`,在 `editVideo` 结束后调用,**只同步主剧一条**:

```java
// 现状
private void syncDramaCategoryToComicDramaWeb(VideoEditParam param) {
    if (param.getDramaCategory() == null) return;
    comicDramaWebManager.updateDramaCategory(param.getVdid(), param.getDramaCategory());
    // ↑ 只调一次,只同步主剧
}
```

### 2.3 要改成

- 同步主剧
- **调内容中心查所有子剧**
- 遍历子剧,每个都调一次 `updateDramaCategory`
- 失败不影响主业务(编辑视频)

听起来就是个 for 循环嘛。

---

## 三、我是怎么一步步做的

### 第一步:找内容中心要接口

Vega 已经在用 `ShortPlayQueryAPI` 查过漫剧了,先看里面有没有"按主剧查子剧"的方法:

```bash
# 在 IDEA 里用 ⌘+O 搜 ShortPlayQueryAPI
```

翻了一下现有方法:
- `queryVideoInfo(vdid)` — 单个查
- `batchQueryVideoInfo(vdids)` — 批量查,但要**提前知道**子剧 ID
- `batchQueryVideoBaseInfo(...)` — 同上

**没有"给我主剧 ID,返回所有子剧"的接口**。

找内容中心的同事:

> "哥,你们那边有没有接口,我给你一个主剧 vdid,你给我返回所有子剧?"

他:"有,`listVideoInfosByMvdid`,1.8.7 版本才有的。你们用的啥版本?"

我看了一眼 pom:

```xml
<ccshortplayserver-stub>1.7.7</ccshortplayserver-stub>
```

他:"升到 1.8.7 就有了。"

听起来简单。

### 第二步:升级依赖的惊魂一下午

改 pom,把 `1.7.7` 改成 `1.8.7`。

IDEA 刷新 Maven,编译——

```
BUILD FAILURE
Could not find artifact ccshortplayserver-stub:1.8.7
```

???

去 Nexus 一查,1.8.7 jar **存在**,但我本地 ~/.m2 没有。

```bash
mvn dependency:get -Dartifact=com.yuewen.nrzx.bizjava.ccshortplayserver.shortplayquery:ccshortplayserver-stub:1.8.7 -U
```

下载成功。再跑 `mvn compile`——

```
BUILD FAILURE (9 个错误)
NonStandardVideoService.java:1168: 找不到符号 getDramaCategory()
AcquisitionPostProcessService.java:78: 找不到符号 getArtifactVideosByNames(...)
VideoEditParam.java:126: 找不到符号 setDramaCategory(...)
... 6 个同类错误
```

我傻了:**我都没动这些文件,凭啥跟我报错?**

冷静一下,看错误——全部是**别人的代码**引用了 `peacock-driver-uploader` 里的方法,但这些方法在新的 jar 版本里**没了**。

换句话说:**我升级 ccshortplayserver 连累了 peacock 的编译**(虽然两个是不同的依赖,但 `peacock-driver-uploader` 也是 SNAPSHOT,同事升级的时候可能删除了某些方法)。

找同事 p_qlqliu:

> "哥,最近改过 ArtifactVideoExtra 吗?我这边 NonStandardVideoService 编译不过,说 `getDramaCategory` 找不到。"

他:"我改了,但我**本地能编过**啊。你拉最新代码了吗?"

我:"……还没。"

`git pull` 拉了最新主干,同事已经修了那 9 个文件。编译通过。

**教训**:升级任何依赖前,**先拉最新代码**。SNAPSHOT 版本的 jar 和同事的代码可能是**紧耦合**的。

### 第三步:写核心逻辑

终于能写代码了:

```java
private void syncDramaCategoryToComicDramaWeb(VideoEditParam param) {
    if (param.getDramaCategory() == null) return;
    Long mvdid = param.getVdid();
    Integer category = param.getDramaCategory();

    // 1. 同步主剧(保持原行为)
    syncOneDramaCategory(mvdid, category, "main");

    // 2. 查子剧
    try {
        List<Long> subVdids = listSubVdidsByMvdid(mvdid);
        if (subVdids.isEmpty()) {
            log.info("no sub drama for mvdid={}", mvdid);
            return;
        }
        log.info("found {} sub drama(s) for mvdid={}: {}", subVdids.size(), mvdid, subVdids);
        for (Long subVdid : subVdids) {
            syncOneDramaCategory(subVdid, category, "sub");
        }
    } catch (Exception e) {
        log.error("list sub drama exception, mvdid={}", mvdid, e);
    }
}

private void syncOneDramaCategory(Long vdid, Integer category, String kind) {
    try {
        Result<Void> rsp = comicDramaWebManager.updateDramaCategory(vdid, category);
        if (rsp.isFailure()) {
            log.error("{} failed, vdid={} code={} msg={}", kind, vdid, rsp.getCode(), rsp.getMessage());
        }
    } catch (Exception e) {
        log.error("{} exception, vdid={}", kind, vdid, e);
    }
}

private List<Long> listSubVdidsByMvdid(long mvdid) {
    ListVideoInfoByMvdidRequest req = ListVideoInfoByMvdidRequest.newBuilder()
            .setContext(Context.newBuilder().setSourceId(shortPlaySourceId).build())
            .setMvdid(mvdid).build();
    ListVideoInfoByMvdidResponse rsp =
            shortPlayQueryAPI.listVideoInfosByMvdid(new RpcClientContext(), req);
    if (rsp == null || rsp.getVideoList() == null) return Collections.emptyList();
    return rsp.getVideoList().stream()
            .map(ShortPlaySimpleInfo::getVdid)
            .filter(id -> id != mvdid)  // 内容中心一般会把主剧也返回,过滤掉
            .toList();
}
```

### 第四步:异常保护的三层设计

同事 CR 的时候问我:"**如果内容中心 TRPC 挂了,你这 editVideo 能正常提交吗?**"

我:"emmm……应该能吧,我外面加了 try-catch。"

他:"你再仔细看。"

我再看——啊,有漏洞:

```java
private void syncDramaCategoryToComicDramaWeb(VideoEditParam param) {
    ...
    try {
        List<Long> subVdids = listSubVdidsByMvdid(mvdid);  // ← 查子剧异常,这里能 catch
        for (Long subVdid : subVdids) {
            syncOneDramaCategory(subVdid, category, "sub");
        }
    } catch (Exception e) { ... }  // ← 整个循环挂了也 catch
    // 但 syncOneDramaCategory 自己有 try-catch 吗?
}
```

再看 `syncOneDramaCategory` 本身,我是有 try-catch 的。OK,两层保护。

但**外层的 editVideo 调用点**呢?如果 `syncDramaCategoryToComicDramaWeb` 抛了什么奇葩异常(比如 NPE),会炸到 editVideo 吗?

同事:"**关键路径(editVideo)不能被辅助同步搞挂**。你再加一层兜底。"

最终三层保护:

```
editVideo()
  ├─ 主流程(更新 Vega DB、推内容中心等,核心业务)
  └─ 同步 dramaCategory(辅助,失败不影响主流程)
        └─ try-catch #1: 整个方法外层兜底
             ├─ 同步主剧
             │   └─ try-catch #2: 单条失败不传播
             ├─ 查子剧
             │   └─ try-catch #3: 查失败不影响主剧同步
             └─ 遍历子剧同步
                 └─ try-catch #2 再跑一次: 一条子剧失败不影响其他
```

原则:**每一层独立 catch,各自降级,绝不把辅助逻辑的失败传染给主业务**。

### 第五步:测试的窘境

代码写完,想测一下——**发现没法测**。

为啥?

1. 我需要一个"**有子剧的主剧**",这样调 `listVideoInfosByMvdid` 才能返回非空
2. Vega 里所有 vdid 都是"主剧",但**不是每个主剧在内容中心都有子剧**(只有被实际分发过、分销商定制过的才会派生子剧)
3. test 环境里**真正有子剧的主剧只有个位数**

我问内容中心同事:

> "test 环境有哪几个 mvdid 带子剧?"

他:"我看看……(查了 10 分钟)……暂时没找到合适的,你要不自己造一个?"

我:"……算了,上线后看日志吧。"

这是个**无奈的选择**。理论上应该构造一个完整的测试 case,但成本太高(要在内容中心造子剧)。我退而求其次:

- **看日志验证代码被触发了**:发布后,任意编辑一个主剧 dramaCategory,日志里应该出现 `syncDramaCategoryToComicDramaWeb` 相关输出
- **"no sub drama"** 也算通过:证明代码跑到了这一步,只是这个主剧没子剧
- **"found N sub drama"** 是最好的结果:真遇到有子剧的剧就验证完整链路

---

## 四、几个教训

### 教训 1:升级依赖的"隐藏连锁反应"

`ccshortplayserver-stub` 是我主动升级的,但**它的升级间接导致其他 jar 的版本问题**——这种连锁反应在 SNAPSHOT 依赖的项目里很常见。

**反思**:
- 升级依赖前 **git pull 一下最新代码**,避免拉到"别人改到一半"的状态
- 升级后如果编译报"别处"的错,**先怀疑是不是别人也改了,我没同步**,不要只怀疑自己

### 教训 2:三层异常保护是值回票价的

写代码时我一开始只加了一层 try-catch,觉得够用了。CR 时被同事追问,才意识到**辅助功能失败不能影响主流程**这个原则。

后来想想,如果不是三层保护:
- TRPC 超时 → 可能抛 `RpcException` → editVideo 接口返回 500
- 用户看到"保存失败",重新点一次
- 但其实 Vega 本地 DB 已经更新了 → 出现 **"保存失败但其实成功了"** 的尴尬场景

**三层保护 = 三次降级机会**,用户永远看到"保存成功",辅助同步慢慢追赶。

### 教训 3:没有理想测试数据时的妥协

我最初想做端到端测试:找个主剧,调接口,验证 merchantry DB 里子剧的 drama_category 都更新了。

但 test 环境没有合适数据,构造成本又高。**我选了降级方案:看日志**。

这不是偷懒,是**务实**:
- 代码逻辑很简单(一个 RPC + for 循环),复杂度不高
- 主剧同步的老逻辑**没改**,回归测试风险低
- 子剧同步新增代码,日志里能看到被触发 + 无异常即可
- 真出问题立刻能通过日志定位,成本可接受

**反思**:不要追求 100% 的测试覆盖,**按 ROI 选最划算的验证方式**。

---

## 五、上线效果

- 代码部署 test 环境,编译通过,服务正常启动
- 老的主剧同步逻辑**不受影响**,回归 OK
- 新增子剧同步逻辑,等**有真实子剧数据**的运营操作触发验证
- merchantry 那边的同事说"等正式有数据过来我们看看,有问题再反馈"

---

## 六、面试怎么讲

### 开场(15 秒)
"2026 年 4 月做的跨系统数据一致性补丁。场景是 Vega 运营给主剧配'分类'字段,要同步到 merchantry,但之前只同步主剧不同步子剧,导致分销商看到的所有子剧都是'未分类'。我通过调内容中心新接口,实现主剧+所有子剧批量同步。"

### 被问"**为什么不直接加个定时任务定期全量同步?**"
"两个原因:
1. **时效性**:运营配完分类,分销商应该**立刻**能按分类过滤,不是等定时任务下一轮
2. **精准**:只有被变更的主剧需要同步子剧,全量扫表浪费

所以用'编辑触发同步'模式更合适。代价是代码复杂一点,有三层异常保护。"

### 被问"**升级依赖你踩过什么坑?**"
"踩过。我把 `ccshortplayserver-stub` 从 1.7.7 升到 1.8.7,本地编译突然炸了 9 个错,全是别人的代码。排查后发现是**同事改了 `peacock-driver-uploader` 这个 SNAPSHOT 依赖,我没拉最新代码**,自己的本地源码和本地 jar 版本对不齐。

教训是:**升级依赖前 git pull,升级后第一次编译就报错的看准来源——是自己的改动,还是别人的代码和新 jar 对不齐**。"

### 被问"**三层异常保护,是不是过度设计?**"
"不是过度。核心原则是**辅助功能不能影响主流程**。editVideo 是运营编辑视频的核心操作,如果因为'子剧同步失败'导致整个接口 500,用户会以为没保存,重新点一次——但其实已经保存了,就出现'看似失败实际成功'的诡异场景。

三层保护让同步失败**降级为后台日志**,不传染到用户界面。面试官往往在这种细节上看出你有没有踩过线上坑。"

### 被问"**如果 100 个子剧同步时挂了 50 个怎么办?**"
"当前方案:**剩下的继续同步,失败的打日志告警**。理由:
- 100 个里 50 失败,通常是 merchantry 短时抖动,**不会每次都 50%**
- 失败的子剧在**下次运营变更该主剧的 dramaCategory 时会自动重试**
- 如果要做更严谨的补偿,可以加个定时任务扫 `video_sync_index.drama_category` 和 Vega 的一致性

现在没做补偿任务是**业务接受度**问题——PM 说容忍度很高,目前这种最终一致性就够了。如果后面投诉多,就加一个每天扫一次的对账 Job。"

### 被问"**如果让你重做会怎么改?**"
"两点:
1. **不要依赖'编辑时触发'同步** — 如果一条主剧的子剧在 Vega 没编辑之前就进了 merchantry,它永远拿不到分类。应该加一个**定时对账 Job**,把漏掉的补上。
2. **加本地事务表 / outbox 模式** — 现在同步失败只记日志,失败追溯不方便。如果把每次同步动作记到一个事务表,Worker 异步拉取重试,可观测性会好很多。"

---

## 七、这个项目让我学到啥

1. **跨系统一致性不能只靠"编辑时同步"**:一定要有**对账兜底**,事件驱动 + 定时扫描双保险
2. **SNAPSHOT 依赖的陷阱**:版本号相同但内容可能每天变,升级前一定要拉最新代码
3. **异常保护的层级思维**:关键路径 / 辅助路径分开,辅助失败绝不影响关键
4. **测试的 ROI**:不是所有变更都值得端到端测试,**按改动的影响面 + 回归风险**决定测试粒度
5. **简单需求藏着的设计点**:一个"for 循环调 RPC"的小需求,能引出异常分层、依赖兼容、数据一致性这些话题

---

> 🔗 技术细节版见《剧集池管理_实现.md》(这个需求是剧集池管理的衍生子任务,实现细节整理在那里)
> 这份偏故事,面试前温一遍找找手感。

# 5 年 Java 面试 · 技术主题问答册

> 按**技术主题**组织,每道题先给"面试官可能怎么追问",再给"你可以怎么答"。
> 基于阅文漫剧分销与签约平台真实工作内容整理。

---

## 一、架构与分层

### Q1. 你项目里分 Controller / Business / Service / Manager / Accessor / Driver / Provider 这么多层,会不会过度设计?

**答**:分层按**关注点**划分,不是为了层数好看。

- **Controller**:接参、权限注解、统一响应
- **Business**:面向用例的流程编排,跨领域时才出现(比如 `BatchCreateContentScriptBusiness` 先调内容中心、再落库、再绑 CAMS)
- **Service**:单领域业务规则(`ContractService` 只管合同)
- **Manager**:数据聚合层,典型例子是 `OrgManager` — 封装 8 张表 + 3 个中台服务的查询,对外统一接口
- **Accessor/Driver**:数据访问,`Driver` 是阅文 peacock 脚手架的封装,负责持久化
- **Provider**:RPC 客户端代理,集中管 TRPC 调用(如 `ShortPlayProvider`、`YuewenProfitSeparatorClientApi`)

简单 CRUD 我会 Controller → Service → Mapper 三层。只有**流程复杂、有外部依赖、有聚合**时才引 Business/Manager。

### Q2. OrgManager 这种聚合层和 Service 的边界在哪?

**答**:Service 做**业务规则**(状态流转、校验),Manager 做**数据组装**(跨表、跨服务的查询拼装)。

比如"查机构完整信息"这个动作:
- `OrgManager.getFullOrgInfo(orgId)` — 聚合了 8 张表 + 3 个中台,返回打好包的 `OrgFullDTO`
- Service 层拿到 `OrgFullDTO` 做业务判断(状态、权限、场景过滤)

两层独立,Manager 可以被多个 Service 复用。

---

## 二、分布式锁与定时任务

### Q3. `VideoLibrarySyncJob` 每 5 分钟跑一次,多实例部署怎么防并发?

用 Redis 分布式锁:
```java
Result<RedisLock> lock = redisLockService.obtainLock("lock:syncVideoLibrary", 600, 0);
if (!lock.isSuccessful()) return;
try { ... } finally { redisLockService.unLock(lock.getValue()); }
```
要点:
1. **`SETNX EX` 原子写**,不能拆开
2. **TTL 给业务最长耗时的 2 倍**(我们 P99 3 分钟,TTL 定 600s)
3. **释放锁用 Lua 校验 value**(UUID),防止误释放别人的锁
4. 获取失败**直接跳过**不重试,5 分钟后会再来

### Q4. 业务超时锁被抢走,数据写两次怎么办?

- **幂等兜底**:`INSERT ... ON DUPLICATE KEY UPDATE`,同一 vdid 写两次结果一致
- **续期(Watchdog)**:像 Redisson 做看门狗,每 TTL/3 续期。我们 Job 耗时可控没上
- **业务可重入**:同一水位线跑多次结果一致

### Q5. XXL-Job 和 Spring @Scheduled 怎么选?

- **单机 + 本地锁**:@Scheduled + Redis 锁够用(我们分销平台的 `VideoLibrarySyncJob`、`AutoRenewalAuthorizationJob` 都是)
- **需要可视化调度 + 动态参数 + 分片执行**:上 XXL-Job(vega 运营后台用了 7 个 XxlJob)

XXL-Job 的核心收益是**调度配置从代码拿出来**,运营能自助改频率。

---

## 三、加解密与安全

### Q6. 数据库字段加密(手机号/邮箱)怎么业务无感?

用 MyBatis TypeHandler:
```java
public class AESStringTypeHandler extends BaseTypeHandler<String> {
    public void setNonNullParameter(PreparedStatement ps, int i, String v, ...) {
        ps.setString(i, CommonAES.encryptWithBase64(v));
    }
    public String getNullableResult(ResultSet rs, String col) {
        return CommonAES.decryptWithBase64(rs.getString(col));
    }
}
```
DO 字段上标下 `typeHandler=AESStringTypeHandler`,业务层完全无感。

### Q7. AES-CBC 固定 IV 不安全吧?

承认:固定 IV 同明文产生同密文,泄露模式。

改造方向:AES-GCM + 随机 IV(IV 和密文拼存)。迁移路径:
1. 新字段用 GCM + 随机 IV
2. 老字段双读(先 GCM 试,失败试 CBC)
3. Job 批量重新加密
4. 下线 CBC

**目前是技术债,没排上优先级**(业务压力大)。

### Q8. 内部接口 HMAC-SHA256 怎么防重放?

三件套:
- `X-Timestamp`:±5min 窗口
- `X-Nonce`:Redis `SETNX` 去重
- `X-Sign`:`HMAC-SHA256(Method + Path + Body, secret)`

关键:签名比较用 `MessageDigest.isEqual`,**不能用 `equals`** — 后者会短路,给时序攻击可乘之机。

### Q9. 登录密码怎么存?

PBKDF2 + 每用户独立随机 salt(16 字节)。表存 `salt` + `hashed_password` + `hash_version`。`hash_version` 给未来升级算法(比如 Argon2)留口子。

---

## 四、一致性 / 跨系统同步

### Q10. 分销平台和 vega / 内容中心怎么保证数据一致?

"定时同步 + 事件回调"的最终一致:

- **主链路**:`VideoLibrarySyncJob` 每 5 分钟从内容中心拉水位,写本地索引
- **实时链路**:vega 改了分类,HTTP 回调分销平台 `/api/internal/drama/category`(HMAC 签名),直接 UPDATE
- **补链**:定时任务的 `fixLoopStartTime` 参数支持从历史时间戳全量跑,兜底漏同步

### Q11. 内容中心 TRPC 挂了同步会怎样?

- 单条失败:`syncVideo` 里 try/catch,log 后跳过
- 批次超时:TRPC 客户端默认 500ms 超时重试 2 次
- 连续失败:熔断(sunwalker 框架自带) / 告警
- `Lists.partition` 分 30 条一批,单批失败不影响其它批

### Q12. Saga 分布式事务怎么用?

合同签署是典型场景:

1. 本地落 `contract` 表
2. 远程调电子签拿 `signUrl`
3. 回填 `sign_url`
4. 发 MQ 通知 CAMS 绑定

任一步失败:
- 第 2 步失败:回滚本地合同状态为 `OA_REJECTED`
- 第 3、4 步失败:靠 `ContractSignedJob` 定时扫 SIGNED 但 `cams_bound=false` 的合约做**补偿**

最终一致 + 可观测,不追求强一致。

### Q13. 合同号重复怎么防?

三层防御(我做过的真实案例):
1. **代码层**:前置 `countByContractNumberAndStatus` 查已存在的有效申请
2. **数据库层**:`contract_number` 加唯一索引
3. **下游幂等**:`createArtifactScriptsForOrg` 前先查 `getArtifactScriptsByApplyId` 防重复创建剧本

上线后 3 个月零次重复创建。

---

## 五、RPC / 微服务

### Q14. TRPC 对比 Dubbo / Spring Cloud 有啥特别?

- **协议层**:Protobuf + 多路复用 TCP,比 HTTP+JSON 快
- **服务发现**:Polaris(北极星),类似 Nacos 但和腾讯基础设施深度整合
- **治理能力**:超时、重试、熔断在 stub 层配,业务代码只 `@TRpcClient(id="xxx")`

我在分销平台写过 Facade 消费多个 TRPC 服务(合同中心、分润系统、内容中心 / ShortPlayProvider 等),统一做**错误码转换、参数 JSON 化、响应 parseObject**,隔离上下游。

### Q15. 一次 RPC 要传 500 个 vdid 怎么办?

包体有上限。`Lists.partition(vdidList, 50).forEach(batch -> rpc.batchQuery(batch))`。

- 分批大小按 stub 约定(内容中心是 50)
- 串行 vs 并行看延迟:每批 200ms,10 批串行 2 秒可接受就串行
- 并行用 `CompletableFuture` 控并发度,避免打挂下游

---

## 六、Redis / 缓存

### Q16. N+1 查询优化你们怎么做的?

结算列表的典型场景:50-200 条记录,每条要显示"操作人名字" — 最差 200 次 RPC,接口 3-5 秒。

**方法内 HashMap 本地缓存**:
```java
Map<Long, UserDTO> userCache = new HashMap<>();
list.stream().map(s -> {
    UserDTO operator = userCache.computeIfAbsent(
        s.getOperatorId(),
        uid -> userService.getByUserId(uid)   // 只执行一次
    );
    ...
});
```

**效果**:RPC 从 200 次降到实际用户数(10-20 次),**接口响应 3-5s → 500ms**。

### Q17. 为什么不用 Redis 缓存?

- QPS 不高(运营后台日几百次)
- 用户信息会变,Redis 要做失效策略
- 方法内 HashMap 自带"请求级一致性",同一请求里快照一致

如果 QPS 涨起来,再升 Caffeine + Redis 双层。

### Q18. 缓存穿透/击穿/雪崩怎么防?

- **穿透**:空值缓存 + 布隆过滤器
- **击穿**:Redis `SETNX` 互斥锁,只让一个线程回源
- **雪崩**:TTL 加随机抖动(`baseTTL + random(0, 300s)`)

---

## 七、消息队列 / 异步

### Q19. 文件任务怎么用 MQ 解耦?

上传 → 落 COS → 写 `file_upload_task` → 发 TDMQ 消息 → Worker 消费 → `ArtifactProcessorFactory.get(type).process(task)`。

- **解耦**:上传同步返回,处理慢慢做
- **削峰**:任务堆 MQ,Worker 池拉
- **重试**:MQ ack + `FileTaskJob` 定时扫失败任务

### Q20. MQ 消息怎么保证不重复消费?

- `file_task_id` 做业务幂等键,消费前查状态,已处理直接 ack
- 严格场景用 Redis `SETNX` 幂等 token
- DB 主键冲突也是幂等兜底

---

## 八、MyBatis / SQL 优化

### Q21. 自定义分页组件为什么要做?

后台 15+ 个查询接口都要"先 COUNT 后 LIST",重复代码 60%。自己封一个 MyBatis 插件处理动态条件拼接 + 双查询,改一处影响全局。

### Q22. `video_sync_index` 多条件查询怎么优化?

```sql
WHERE publish_status = ? AND drama_category = ? AND (vdid = ? OR name LIKE ?)
```

- 组合索引 `(publish_status, drama_category)` 匹配常用筛选
- `name LIKE '%x%'` 左模糊**不走索引**,业务限制只允许右模糊,或改 ES
- `vdid = ?` 走唯一索引

### Q23. organization_mapping 映射表怎么设计?

核心字段只要 3 个必须:
```sql
CREATE TABLE organization_mapping (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  org_id VARCHAR(64) NOT NULL COMMENT '新系统ID',
  old_system_id VARCHAR(64) NOT NULL COMMENT '老系统/分润系统ID',
  status TINYINT DEFAULT 1,
  create_time DATETIME DEFAULT NOW(),
  update_time DATETIME DEFAULT NOW(),
  UNIQUE KEY uk_org_id (org_id),
  UNIQUE KEY uk_old_system_id (old_system_id)
);
```

**两边都加唯一索引**,新老双向查询都 O(1)。

### Q24. DECIMAL 精度问题

早期 `DECIMAL(10,2)` 只能存到 `99,999,999.99` — 年结算千万级规模已经不够。
- `DECIMAL(12,2)` → 上限约 100 亿,够用但没余量
- 优化到 `DECIMAL(16,4)` → 够大 + 支持 4 位小数的分润比例计算

### Q25. 分区表的使用场景?

推广数据表按 `statis_day` 做 LIST 分区,每天一个分区:
- `WHERE statis_day = '2025-01-01'` 自动分区裁剪
- 过期数据 `DROP PARTITION` 秒删
- 避免亿级后性能崩溃

坑:分区键必须在主键里、跨分区反而慢、分区数上限 1024。

---

## 九、Spring / 并发 / JVM

### Q26. `TransmittableThreadLocal` 和 `InheritableThreadLocal` 区别?

- ThreadLocal:线程私有,线程池不传递
- InheritableThreadLocal:父线程 new 子线程时传递,线程池复用线程不行
- **TransmittableThreadLocal(阿里)**:线程池提交任务时捕获父 TTL,在子线程 restore,解决异步/Job 里拿不到 SessionContext 的问题

我们 `SessionContext` 就是用 TTL,保证 Controller → @Async → Job 链路上下文传下去。

### Q27. Java 21 在 vega / nova 用上了吗?

用了 21 但大部分代码还是 8 风格,主要为:
- **虚拟线程**(Loom):TRPC 调用、COS 上传压测评估中
- **Record**:简单 DTO 省 getter/setter
- **switch expression**:状态机代码更清爽
- 需要 `--add-opens` 处理 MyBatis 反射,pom 显式引 mybatis 3.5.13

### Q28. Undertow 配置的 io=10 worker=80 是什么意思?

- **IO 线程**:NIO select/读写,数量等于 CPU 核数
- **Worker 线程**:处理业务请求,阻塞 IO/DB/RPC 都占这里。80 是按 QPS×RT 算的
- **IO 线程绝不能阻塞**,业务全扔 worker

---

## 十、业务与设计模式

### Q29. 合同 5 种类型代码怎么组织?

**策略 + 模板方法**:
- `ContractInvoker` 统一接口,5 种类型各实现一个
- `CreateContractHandler` 抽象基类,`execute` 是 final 模板方法(幂等 → 校验 → 创建 → 回调)
- 子类只实现 `doCreate`,共性全在基类
- 入口 Service 按 `contract_type` 查 `Map<Type, Invoker>` 路由

**新增类型开发周期从 5 天 → 1 天**,核心代码零改动。

### Q30. 内容类型 4 种怎么组织?

**工厂模式 + 统一接口**:
```java
public interface ArtifactHandler {
    ContentType getType();
    void create(CreateContext ctx);
    void bindCams(BindContext ctx);
    void syncStatus(SyncContext ctx);
}

@Component
public class ArtifactHandlerFactory {
    private final Map<ContentType, ArtifactHandler> handlerMap = new HashMap<>();

    @Autowired
    public ArtifactHandlerFactory(List<ArtifactHandler> all) {
        for (ArtifactHandler h : all) handlerMap.put(h.getType(), h);
    }
}
```

新增"成品"类型只加 `ProductArtifactHandler` + `@Component`,Spring 启动自动注册,**业务代码零改动**。

### Q31. "已过期"状态怎么不用定时任务?

数据库只存基础状态(已生效 / 已终止),"已过期"靠 `cooperation_end_time < NOW()` **实时动态计算**。
- 查询时 SQL 加 `CASE WHEN cooperation_end_time < NOW() THEN 2 ELSE 1 END`
- 列表筛选"已过期" → Service 自动转成 `WHERE status = 1 AND cooperation_end_time < NOW()`

**状态延迟从最长 1 小时降到 0**,消除定时任务依赖。

### Q32. 机构创建编排怎么做?

管道思路,6 步:
```java
for (OrgValidator v : validators) v.validate(param);   // 校验
OcrResult ocr = ocrService.recognize(...);              // OCR
AuditResult audit = auditProcessor.audit(...);          // 机审
Organization org = orgManager.create(...);              // 落库
profileManager.bindUser(user, org);                     // 绑定
businessTypeHandler.handle(org);                        // 业务派发
```

- Validator 用 `@Order` 控优先级,加校验只加 `@Component`
- BusinessTypeHandler 工厂 + 策略,每种业务一个 Handler
- 每阶段失败转 `BusinessException`,全局异常处理统一错误码

---

## 十一、工程落地与软技能

### Q33. 你做过哪些性能优化?

具体数字:
- **N+1 → HashMap 缓存**:3-5s → 500ms
- **DECIMAL 精度从(10,2)到(16,4)**:消除金额溢出
- **分批 RPC(30条/批)**:避免超包体
- **分区表**:过期数据秒删
- **MDC 链路追踪**:排查从 30min 降到 10min
- **批量任务分片(50条/批)**:锁冲突 -80%

### Q34. 最有挑战的一次线上问题?

合同号重复的案例(真实):

> 机构合作场景出现**合同号重复**,影响 10+ 笔分账。
>
> 排查:
> 1. SQL 先定位有重复的 `contract_number`
> 2. 看是代码层没校验还是 DB 没唯一索引 — 都没有
> 3. 再看下游:`createArtifactScriptsForOrg` 里也没幂等
>
> 修复(三层防御):
> 1. Controller 里前置 `countByContractNumberAndStatus`
> 2. DB 加 `UNIQUE INDEX idx_contract_number`
> 3. 下游 `getArtifactScriptsByApplyId` 查已创建直接返
>
> 存量脏数据用 SQL 清洗:`UPDATE ... SET is_valid=0 WHERE apply_id NOT IN (SELECT MIN(apply_id) ...)`
>
> 效果:3 个月零次重复创建,分账准确率 100%。
>
> **教训**:**跨系统字段一定要代码 + DB 双防御**,单层任何一层都可能漏。

### Q35. 怎么和前端协作?

- 接口文档用 OpenAPI / Swagger(我们项目有 md 文档)
- 前端用 `vite-plugin-mock` 本地 mock,不依赖后端
- 错误码按模块分段,前端拦截 401 跳登录、403 提示权限
- 监控对齐:前端 Aegis + 后端 MDC 关联 traceId

---

## 十二、STAR 万能回答模板

- **S(Situation)**:哪个项目,哪个模块,规模多大(记住:万级 UP 主、千级 MCN、月均 300 单)
- **T(Task)**:要解决什么问题,量化越好(比如 P99 >3s、日均 10 万条)
- **A(Action)**:具体方案、技术选型、代码关键点
- **R(Result)**:数据对比、问题解决、经验沉淀

**示例**(N+1 优化):
- S:分销平台结算列表接口,一次查 50-200 条,每条要显示操作人名字
- T:RPC 最差 200 次,接口耗时 3-5s,用户投诉
- A:方法内 HashMap + `computeIfAbsent` 去重
- R:RPC 降到 10-20 次,响应 500ms,加日志监控命中率常年 85%+

---

## 附:面试反问清单(你主动问面试官)

- 团队现在 Java 版本多少?有升级 JDK 17/21 计划吗?
- 微服务治理用什么?服务注册是什么?
- 日均 QPS / P99 RT 是什么量级?
- 新人 onboarding 到能独立交付平均多久?
- Code review 流程?有没有架构评审?
- 技术栈更新频率,允许引入新库吗?

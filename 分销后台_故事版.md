# 分销后台 C 端作品管理 —— 唠嗑版

> 这个需求是 2026 年 1 月做的。
> 乍一看是"给后台加两条业务线 + 搞个下载功能"，非常普通。
> 但做进去发现——**流式打包 + 异步任务 + 权限分发 + 防盗版** 一套组合拳，
> 正好是面试官最爱问的话题，扎扎实实练了一波手。

---

## 一、需求起源：运营小张的求救

2026 年 1 月某个周四下午，运营小张在群里@我：

> "@彭哥，又有分销商要素材了，这是今天第 8 单。"

我点开一看——某分销商想要**《XX 仙帝》** 这部剧的前 50 集素材，用来剪切片投抖音。

我："你每次都手动发？"

她："**每次都手动发**。我这边从 COS 下 50 个视频，打包成 zip，上传到腾讯微云或者用企业微信传给对方。一单大概**半小时到一小时**，看网络。一个月大概 **100 单**。"

我："这得占你多少时间？"

她：："**两天**。整整两天就干这个。"

我："这事儿我能帮你解决。"

过了两天，PM 给了正式需求文档，标题叫《**分销后台（C 端）改造**》，核心内容：

1. 新增"**抖音端原生**"和"**快手端原生**"两条业务线（现在只有微信小程序一条）
2. **作品管理**页面：列表、筛选、排序、详情
3. **剧集详情 + 分页播放器**（每剧 100 多集，不能一次查完）
4. **批量下载**：让用户自己选集数下载，**最多 70%**
5. **任务中心**：追踪下载任务的状态

看到第 4 条时我笑了——小张的救命稻草在这。

---

## 二、需求到底是啥（人话版）

### 2.1 新增两条业务线

原来分销商登录我们后台，只能看到"微信小程序"的作品。现在要加"**抖音端原生**"和"**快手端原生**"——这两条业务线是我们最近新开的。

听起来"加两个菜单"就行对吧？但是**字段和规则是不一样的**：
- 微信小程序：按**剧集**分发，有独家授权/公共池
- 抖音原生：按**母子账号**分发，企业账号下子账号直接看全部
- 快手原生：和抖音类似但又有细微差别（比如主体只能是红袖）

**如果把三条线硬塞一个 Service 里**，`if (line == WX) ... else if (line == DY) ...` 代码马上就是屎山。

我的想法：**策略模式**。

每条业务线一个 Handler，共同的接口（列表、详情、下载权限），Controller 只做路由。新加业务线 = 加一个 Handler，主干不动。

### 2.2 分发漫剧分类

产品加了个字段："**分发漫剧分类**"，6 种枚举：沙雕漫、解说漫、AI真人、简笔画、动态漫、图漫。

这是给运营看的，用来在分销端标记这部剧**适合哪种分发**。

**坑点**：我们 DB 里原本已经有一个`category`字段（拟真/动画短剧/简易动画/...），**跟这个新字段含义部分重叠但不完全一样**。

比如：
- 原 `category=拟真` → 新 `distribute_category=ai真人`
- 原 `category=动画短剧` → 新 `distribute_category=动态漫`
- 原 `category=简易动画` → 新 `distribute_category=?`（运营要手动配，我们给不了默认）

**所以我的方案是**：
- **不改**原字段（上游同步还用它）
- **新加**一个字段
- **写刷数脚本**做历史数据默认映射
- **加个 manual 标记位**，运营手动改过的不被同步覆盖

这块踩的坑在下面"我是怎么一步步做的"部分会讲。

### 2.3 分页播放器

一部剧可能有 **100 多集**，后台要能看、能放、能下单集。

如果一次把 100 集全查出来，前端渲染卡不说，**SQL 本身就慢**（JOIN 了剧集元数据表，100 行每个查一次其他关联，非常容易 N+1）。

改成**50 集/页**。用户切换页的时候再查下一批。

### 2.4 批量下载

这是**整个需求的核心**。用户流程：

```
1. 进入剧集详情
2. 勾选要下载的集数（最多 70%）
3. 勾选 "我已阅读并同意法律声明"
4. 点"批量下载"
5. 跳转到任务中心，看任务进度
6. 任务完成 → 给一个下载链接（zip）
```

**为什么不能同步下载？**
一部剧 70 集，**每集 200MB，70 集就是 14GB**。
- 同步接口：**15 分钟打包 + 15 分钟用户等** = 超时稳稳的
- 内存：**一把加载 14GB 进堆** → **OOM**
- 网络：用户网络中断 = 前功尽弃

**所以必须异步**：

```
[用户] 点击"批量下载"
  ↓
[后端]
  ① 创建 download_task 记录（状态 PENDING）
  ② 发 MQ 消息
  ③ 立即返回 taskId
  ↓
[前端] 跳转任务中心
  ↓
[Worker] 异步处理
  ① 流式打包 zip
  ② 上传 COS
  ③ 更新 task 状态 DONE + zip URL
  ↓
[前端] 轮询任务状态，DONE 时显示下载按钮
```

### 2.5 任务中心

一个新菜单，展示用户的所有下载任务，实时更新进度。

---

## 三、我是怎么一步步做的

### 第一步：策略模式重构（日常炼功）

原代码里微信小程序线是**硬编码**的，`WorkController` 里直接写业务逻辑。我要做的第一件事是**把它拆出来**，让它只是"一种实现"，不是"唯一实现"。

定义接口：

```java
public interface WorkManageHandler {
    BizLine line();
    PageResult<WorkVO> listWorks(Long orgId, WorkQueryDTO q);
    DramaDetailVO detail(Long orgId, Long dramaId);
    boolean canDownload(Long orgId, Long dramaId);
}
```

然后写三个实现：
- `WxMiniWorkHandler`（把原代码迁进来）
- `DouyinNativeWorkHandler`（新写）
- `KuaishouNativeWorkHandler`（新写）

Controller 变瘦：

```java
@GetMapping("/works")
public Result<?> list(@RequestParam BizLine line, @RequestParam Long orgId, WorkQueryDTO q) {
    return Result.ok(handlerFactory.get(line).listWorks(orgId, q));
}
```

CR 的时候同事小李（刚入职 1 年）问我："**为啥不直接写 `if-else`？看起来更简单。**"

我跟他聊了一下午："**简单是一时的。等三条业务线跑一年之后，每条都加了一堆特殊逻辑，`if-else` 的分支会爆炸到 500 行。而且不同业务的改动会互相影响——你改抖音的代码改出了微信小程序的 bug，这种事防不住。Handler 模式把不同业务的代码隔离开，一条线坏了不影响另一条。**"

他听完恍然大悟：原来"设计模式"不是为了炫技，是为了**给未来的自己留活路**。

### 第二步：分类映射踩坑

分发漫剧分类映射这块，我第一版没做 `manual` 标记。

效果：运营小张周一早上改了 20 个剧集的分发分类，下班前我们和内容中心的同步 Job 跑了一遍——**全给覆盖回去了**。

周二早上小张骂娘："**@彭哥 我改的都没了？！**"

我："……（心虚）稍等，我查下。"

一看同步代码——里面用的是 `UPDATE drama SET category = ?, distribute_category = ?` 全量覆盖。没考虑"用户手动改过的字段"这个场景。

立马改：
- 加 `distribute_category_manual` 字段（0/1）
- 运营改过 → 置 1
- 同步 Job 跳过 `manual=1` 的行
- 从操作日志里 diff 出被覆盖的 20 条记录，手动补回来

小张："这回对了。"

复盘：**历史数据迁移 + 用户手动操作，这两件事必须从设计阶段就想清楚**。我当时只想着"映射一下",忘了"如果用户改完又被覆盖呢？"

### 第三步：流式打包（最硬核的一块）

这块我之前也没做过，是临时学的。

**挑战**：一个 zip 包可能 14GB，怎么打？

#### 最 naive 的版本（错误示范）

```java
// ❌ OOM 版本
ByteArrayOutputStream baos = new ByteArrayOutputStream();
ZipOutputStream zos = new ZipOutputStream(baos);
for (String key : keys) {
    byte[] data = cosClient.download(key);  // 把视频加载进内存
    zos.putNextEntry(new ZipEntry(...));
    zos.write(data);                        // 写进 zip
}
byte[] zip = baos.toByteArray();            // 14GB 全进堆 → OOM
cosClient.upload(outputKey, zip);
```

这玩意儿 200MB 的单集视频都顶不住，别说 14GB 包。

#### 正确版本（流式）

```java
Path tmpZip = Files.createTempFile("dl-", ".zip");
try (OutputStream fos = Files.newOutputStream(tmpZip);
     ZipOutputStream zos = new ZipOutputStream(new BufferedOutputStream(fos))) {

    for (int i = 0; i < keys.size(); i++) {
        zos.putNextEntry(new ZipEntry(String.format("%02d.mp4", i + 1)));
        try (InputStream in = cosClient.getObjectStream(keys.get(i))) {
            IOUtils.copy(in, zos);  // 8KB buffer 循环 read/write
        }
        zos.closeEntry();
        taskService.updateProgress(taskId, i + 1, keys.size());
    }
}
// 本地 zip 完成后，分片上传到 COS
cosClient.uploadMultipart(outputKey, tmpZip);
```

关键变化：
1. **本地文件**而不是 ByteArrayOutputStream → 磁盘 14GB 没压力，堆内存只用 8KB
2. **`IOUtils.copy`** 底层是 8KB buffer 循环，**从 COS 流过来一小段就写一小段 zip**，恒定内存
3. **分片上传 COS**：不用 `putObject`（整包上传容易超时），用 `initiateMultipartUpload` + 多个 `uploadPart`，每片 10MB，失败可重传

测试时我故意打了一个 14GB 的包，监控看**堆内存占用始终在 50MB 以内**，打包完成后一次性上传 COS。

心里美滋滋：**OOM 的恐惧终于解除了**。

### 第四步：任务中心的几个小设计

#### 任务表

```sql
CREATE TABLE download_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    org_id BIGINT NOT NULL,
    task_name VARCHAR(200),           -- 剧名 + 集数范围
    drama_id BIGINT NOT NULL,
    episodes JSON NOT NULL,           -- 选中的集数 [1,2,3,...]
    status TINYINT NOT NULL,          -- 0 待执行 1 执行中 2 完成 3 失败
    progress INT DEFAULT 0,
    total INT DEFAULT 0,
    cos_url VARCHAR(500),
    error_msg VARCHAR(1000),
    retry_count INT DEFAULT 0,
    created_at DATETIME,
    updated_at DATETIME,
    INDEX idx_org_status (org_id, status),
    INDEX idx_created (created_at)
);
```

用 JSON 存选中集数列表，一个字段搞定。

#### 重试机制

下载失败分两类：
1. **可重试**（COS 网络超时、临时 500）→ **指数退避重试 3 次**
2. **不可重试**（权限、参数错误）→ **直接失败**

```java
try {
    zipBuilder.buildZip(msg.getTaskId(), ...);
    taskService.markDone(msg.getTaskId());
} catch (RetryableException e) {
    int retry = taskService.incrRetry(msg.getTaskId());
    if (retry > 3) {
        taskService.markFailed(msg.getTaskId(), e.getMessage());
        alertService.notify("下载任务失败 taskId=" + msg.getTaskId());
    } else {
        long delaySec = (long) Math.pow(2, retry) * 60;  // 1, 2, 4 分钟
        mqProducer.sendDelayed(msg, Duration.ofSeconds(delaySec));
    }
} catch (Exception e) {
    taskService.markFailed(msg.getTaskId(), e.getMessage());
}
```

指数退避的用意是：第一次失败可能是偶发，**立刻重试没意义**；等一分钟，网络恢复了再试；还失败再等两分钟……给系统一点"缓一缓"的时间。

#### 前端轮询

前端每 5 秒轮询一次 `/task/{taskId}/status`。

我一开始直接 `SELECT * FROM download_task WHERE id=?`。

压测发现：**100 个用户同时打开任务中心** = **每秒 20 次 DB 查询**，上生产的话这肯定不够。

优化：**Redis 缓存状态**
```java
// Worker 更新进度时同时写 Redis
redisTemplate.opsForValue().set("task:" + taskId, JSON.toJSONString(task), Duration.ofDays(1));

// 查询接口优先读 Redis
TaskVO cached = redisTemplate.opsForValue().get("task:" + taskId);
if (cached != null) return cached;
// miss 再查 DB
return taskMapper.selectById(taskId);
```

Redis 命中率 **95%+**，DB 压力立马下来。

### 第五步：防霸占

测试同学小陆很皮，**一小时发了 50 个下载任务**，把 Worker 全占满了，其他用户任务排队 30 分钟没人管。

她给我发消息："**你的 Worker 会不会被一个用户霸占？**"

我："……会。"

解决：
- DB 层：每个用户同时 RUNNING 任务数 **≤ 2**
- MQ 消费：按 `org_id` hash 到不同 Worker，**防止一个 org 霸占一个 Worker**

她："**这才合理**。"

---

## 四、三周下来的几个教训

### 教训 1：OOM 不是段子，是真事

流式 I/O 这种"基础概念"，我之前一直**以为懂了**。真到写 14GB 打包时才发现：
- 什么时候用 `ByteArrayOutputStream`？→ **小数据**（< 10MB）
- 什么时候用本地文件临时存？→ **大文件**
- 什么时候用流式 `IOUtils.copy`？→ **需要恒定内存**

这些不是"可选知识"，是**每个后端都应该有的直觉**。

### 教训 2：设计模式是真能救命

策略模式重构花了 1 天，感觉"没什么产出"。
但**后来加快手原生**那条业务线时，我只改了一个 Handler 文件，**主干一行都没动**。

这就是"写一次的难，省未来的 10 次"。

### 教训 3：并发度要从用户维度看

测试小陆的暴力攻击让我意识到——**系统吞吐量 ≠ 单用户体验**。

5 个 Worker 的吞吐是 5 个并发任务，但如果一个用户能占 4 个 Worker，其他用户看到的就是"一直在排队"。

**保证公平性**是工业级系统的基本素养。

### 教训 4：产品说"10 天"你要想"那 11 天咋办"

结算项目里 PM 说"10 天没确认就自动确认"。下载这里 PM 说"打包失败重试 3 次"。
**用户永远会做超出你预期的事**。加一层兜底，该告警告警、该失败失败，但**不要让系统卡死**。

---

## 五、上线之后的效果

- 运营小张的工作：从**每月 100 单，每单半小时，共 50 小时** → **每月 10 单，每单 10 分钟，共 1.7 小时**。**节省 48 小时/月**。
- 流式打包：任何体积的 zip，**堆内存 < 50MB**
- 任务中心：轮询 DB 压力降 95%（Redis 缓存）
- 下载任务成功率：**99.3%**（失败主要是用户主动取消）
- 业务同学说"感觉后台终于好用了，不用每次都找运营"

---

## 六、面试怎么讲

### 开场（15 秒）
"这是我 2026 年初做的分销后台改造，从单业务线扩到三条，加了异步批量下载和任务中心。核心技术点是**流式 zip 打包** 和 **策略模式业务线扩展**。"

### 被问"**大文件怎么处理？**"
"三层处理：
1. **流式读**：从 COS 用 `InputStream` 拉
2. **流式写**：`ZipOutputStream` + `IOUtils.copy` 8KB buffer
3. **流式传**：COS 分片上传，每片 10MB

整个打包 14GB 包，堆内存始终 < 50MB。"

### 被问"**策略模式为啥不用继承？**"
"组合优于继承。每条业务线字段规则差异大，继承树会很深。
用 `@Component` + 接口注入，**新加 Handler 不改老代码**，符合开闭原则。
而且单测好写——每个 Handler 独立测。"

### 被问"**任务失败怎么处理？**"
"分两类：
- **可重试**（网络、COS 临时失败）→ **指数退避 3 次**（1/2/4 分钟）
- **不可重试**（参数、权限）→ 直接失败 + 告警

重试 3 次还失败触发人工介入，防止无限循环。"

### 被问"**如果一个用户狂发任务霸占 Worker？**"
"两层保护：
1. DB 层限每用户同时 RUNNING **≤ 2 个**，超了提交接口直接拒
2. MQ 层按 `org_id` hash 路由到不同 Worker，单一 org 最多占用其中一个 Worker"

---

> 🔗 技术细节版见《分销后台_实现.md》
> 这份偏故事，面试前温一遍找找手感。

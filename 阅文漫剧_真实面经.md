# 阅文漫剧项目面经(唠嗑版)

> 这份是当**和朋友聊天**那种感觉写的。不是给 HR 看的,是给你自己面试前过一遍。
> 照着这个状态讲,不是背简历,是真在讲工作。

---

## 一、这个项目到底在干嘛(1 分钟能讲清)

阅文漫剧分销平台,就是**让 UP 主和 MCN 机构 跟平台签约,把自己的漫剧作品分发到抖音、快手、小程序那些渠道,然后和平台分账**。

说白了就是阅文里面一个**业务中台**,做 3 件事:

1. **签合同** — 个人来签、机构来签、补协议签,各种合同类型
2. **算钱** — 每月结算,算谁该分多少,跟分润系统对账
3. **管内容** — 剧本/剧集/成品,登记到 CAMS(集团财务系统)做分账

规模的话:

- **万级** UP 主(万多是真实的,不吹亿级那种)
- **千级** 机构 MCN
- **月均 300 来单**签约
- **年结算千万级**
- 手上管**上万部**漫剧内容

技术栈就是**阅文集团那套** — Peacock 脚手架(本质 Spring Boot 封装)、TRPC(≈Dubbo)、Polaris(≈Nacos)、MySQL、Redis。对外对接**6 个集团中台** — 合同中心、分润系统、机构主数据、敏感数据中心、文件服务、内容中心,**全部是调用方,不是我们写的**。

我在里面干**2 年**,Owner 了 **分销签约** + **结算分润** 两个模块,**IP 中台** 是核心开发者但 Owner 是另一个同事。

**讲到这里大概 1 分钟,够开场介绍**。

---

## 二、签约框架这块我最得意(3 分钟)

### 为啥要改

我刚接手时,合同类型就**个人签约**一种。然后业务三个月提一种新合同:

- "要加机构合作了"
- "再加个补充协议"
- "还有版权授权"
- "对了还有委托制作"

每种合同都要:**查信息 → 校验 → 调合同中心 → 通知下游**。老代码就一个 `ContractService.create` 方法,里面 `if (type == PERSONAL) ... else if ...`,**300 行嵌套**。

加第四种合同那次我真崩了。因为要改的地方**散布在 5 处**:Service、Validator、各种 Helper。我改了 4 处,漏了一处,上线后没发邮件,运营投诉。

**那时候就下定决心,下次加合同之前我要重构**。

### 我怎么重构的

核心是**策略 + 模板方法 + 泛型上下文**组合。讲起来比较绕,但其实不难:

**策略 `ContractInvoker`** — 每种合同类型一个实现类,按 type 路由:

```java
public interface ContractInvoker {
    ContractType getType();
    ContractResult invoke(CreateContractContext ctx);
}
```

**模板方法 `CreateContractHandler`** — 共性流程放基类,`execute` 用 `final` 锁死不让子类改:

```java
public abstract class CreateContractHandler implements ContractInvoker {
    public final ContractResult invoke(CreateContractContext ctx) {
        idempotentCheck(ctx);      // 所有合同通用
        validateParams(ctx);        // 所有合同通用
        Contract c = doCreate(ctx); // 这个让子类实现
        afterCreate(c, ctx);        // 所有合同通用
        return ContractResult.success(c);
    }
    protected abstract Contract doCreate(CreateContractContext ctx);
}
```

**上下文 `CreateContractContext`** — 个人装 8 样数据,机构装 15 样,用 Map + enum key 组织:

```java
public class CreateContractContext {
    private final Map<ContextKey, Object> data = new HashMap<>();

    public <T> T get(ContextKey key, Class<T> type) {
        return type.cast(data.get(key));
    }

    public static CreateContractContext forPersonal(Long guid) {
        // 查实名、银行卡、人脸识别...8 项
    }

    public static CreateContractContext forOrg(String orgId) {
        // 查企业资质、对公账户、法人身份证...15 项
    }
}
```

**效果挺爽的**:

- 新合同接入**从 5 天降到 1 天**
- 签约接口代码量**少了 40%**
- 后面"委托制作"新同事 1 天就接完了,没出问题

### 面试官会挖哪些点

**"策略+模板方法不会过度设计吗?"**

我的答法:**两个模式解决不同问题**。策略是"按类型路由",模板方法是"共性流程复用"。只用任何一个都不够 — 只用策略,每种都要重复写 4 步;只用模板方法,没法按类型路由。两个一起才干净。

**"用 Map 做上下文不会丢类型安全吗?"**

部分丢,但有补偿。`ContextKey` 是 enum,编译期就能发现 key 拼错。`get(key, Class)` 传 Class 做强转,放错类型立刻 ClassCastException。如果做成强类型要为每种合同建 `PersonalContext/OrgContext/...`,类爆炸。**tradeoff 是 Map 省代码,类型安全有但不极致**。

**"合同中心是你写的吗?"**

不是。**合同中心是集团公共中台,几十个业务线在用,跨部门**。我做的是业务层 — 对接它、管我们这边的状态、流程编排。

---

## 三、结算那块 N+1 优化(讲起来最爽的故事)

这个故事短,但讲起来特别有意思,**量化漂亮**。

### 事情是这样的

结算列表接口,就是运营看每月结算单那个列表。一次查 50-200 条,每条要显示**操作人姓名**。

老代码:

```java
list.stream().map(s -> {
    UserDTO u = userService.getByUserId(s.getOperatorId());
    vo.setOperatorName(u.getName());
    ...
});
```

for 循环调用户服务 RPC。**200 条 = 200 次 RPC,每次 20ms,接口 3-5 秒**。

运营投诉:"你们这个列表打开要转菊花好久,卡死了"。

### 我怎么优化的

这里有个关键动作:**我没急着改代码,先去看数据**。

导出了一周的操作日志,发现一个规律:**200 条记录里 operatorId 只有 5-15 个不同值**(就那么几个运营在操作),重复度 85% 以上。

这数据意味着**方法内缓存就够用**,不用上 Redis。

代码改动就 5 行:

```java
Map<Long, UserDTO> userCache = new HashMap<>();
list.stream().map(s -> {
    UserDTO operator = userCache.computeIfAbsent(
        s.getOperatorId(),
        uid -> userService.getByUserId(uid)
    );
    vo.setOperatorName(operator.getName());
});
```

`computeIfAbsent` 的意思:**同 key 只执行一次 lambda,第二次开始从 Map 取**。

**效果**:

- RPC 从 **200 次降到 10-20 次**(实际用户数)
- 接口响应 **3-5s → 500ms**
- 加了日志监控命中率,生产常年 **85% 以上**

### 面试官喜欢挖的点

**"为啥不用 Redis?"**

三个原因:
1. QPS 不高,B 端后台日均几百次调用,上 Redis 性价比低
2. 用户信息会变,Redis 要搞失效策略
3. 方法内 HashMap 自带"请求级一致性",同一请求里名字一致,请求完自动释放

未来 QPS 涨了,再升级 Caffeine + Redis 两级缓存。

**"为啥不用批量接口 `batchGetByUserIds`?"**

**如果用户服务有这接口,肯定更好**。实际是他们没有,跨团队沟通加接口成本太高。HashMap 是"没有批量接口"场景的次优解。

> **这里面试官如果追问"那你怎么不推动他们加",就是在挖你的主动性**。
> 答:"这是我当时的一个糟糕决策。HashMap 方案是临时救急,但我没同时推动他们加批量接口,结果后面我们组遇到类似场景就全复用 HashMap,技术债越滚越大。正确做法应该是短期 HashMap 救急 + 同步推动批量接口 + 长期替换。这教训让我后面做事都会想'短期解 + 长期解并行'。"

**"HashMap 线程安全吗?"**

**安全**,不用 ConcurrentHashMap。**方法内局部变量不跨线程**。这点 review 时要强调,避免有人误改。

**"你怎么知道 operatorId 重复度高?凭感觉吗?"**

**凭数据**。导出一周的操作日志,按 operatorId 分组看分布。如果分布不集中(200 条 180 个不同用户),HashMap 就没意义了,要换方案。

---

## 四、新旧机构 ID 那张映射表(最朴素但最有价值)

### 背景

阅文多业务线,历史上有两套机构 ID:

- **老的**:分润系统上线多年,存了好几年结算数据,**几十个业务线在调**,千万级数据 — **改不动**
- **新的**:最近搞的机构主数据系统,ID 格式规范 — **不能被污染**

我们分销结算既要新 ID 查询、又要调分润系统(老 ID),返回给前端还要翻译回新 ID。

### 我做的方案

**一张映射表解决**:

```sql
CREATE TABLE organization_mapping (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    org_id VARCHAR(64) NOT NULL COMMENT '新系统ID',
    old_system_id VARCHAR(64) NOT NULL COMMENT '老系统ID',
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT NOW(),
    UNIQUE KEY uk_org_id (org_id),
    UNIQUE KEY uk_old_system_id (old_system_id)
);
```

**就 3 个必填字段:新 ID、老 ID、status。两边都加唯一索引,双向查询 O(1)**。

Service 层统一翻译:

```java
public PagedData<WeekSettleVO> queryWeekSettle(WeekSettleReq req) {
    // 1. 新 ID → 老 ID
    String oldOrgId = mappingService.newToOld(req.getOrgId());

    // 2. 调分润系统(只认老 ID)
    List<ProfitDTO> profits = profitProvider.queryManjuWeekSettle(oldOrgId, ...);

    // 3. 老 ID 列表 → 批量反向转新 ID
    Set<String> oldIds = profits.stream().map(...).collect(toSet());
    Map<String, String> oldToNew = mappingService.batchOldToNew(oldIds);

    // 4. 查新 ID 对应机构名
    Map<String, String> orgNames = orgManager.batchGetOrgName(oldToNew.values());

    // 5. 组装 VO 给前端
    return profits.stream().map(p -> {...}).collect(...);
}
```

### 面试官容易挖的坑

**"这算什么技术亮点啊,就是张中间表"**

我会这样答:**"您说得对,技术实现不复杂。但我想表达的是决策的价值"**。面对老系统改不动 + 新系统不能污染的两难,我**选择引入中间层而不是改任一边**。我见过太多项目选错了,要么硬改老系统导致几十个业务线炸,要么把老 ID 硬塞进新系统主表污染数据。

真正的亮点不是"建映射表",是**看清业务权衡,选最务实的方案**。

**"为什么不在 DB 做视图?"**

分润系统是另一个团队的服务,它的 DB 不在我这边,视图做不了。

**"新机构没老 ID 怎么办?"**

新机构在新系统创建时**不往分润系统推**(分润系统已经封存不接新数据),所以不需要老 ID。分销结算的对象是"历史签约机构",它们必然有老 ID。

---

## 五、工厂模式搞成品处理器(写得最爽的一次)

### 背景

IP 中台要登记 3 种内容类型:**剧本、剧集、成品**。每种类型:

- 创建字段不一样(剧本有书籍 ID,剧集有集数)
- 调的下游服务不一样
- CAMS 绑定参数不一样

老代码:`if (type == SCRIPT) ... else if (type == EPISODE) ...`,**每新增一种要改 4-6 处代码**。

### 重构

一个接口:

```java
public interface ArtifactHandler {
    ContentType getType();
    void create(CreateContext ctx);
    void bindCams(BindContext ctx);
    void syncStatus(SyncContext ctx);
}
```

每种类型一个实现:

```java
@Component
public class ScriptArtifactHandler implements ArtifactHandler {
    public ContentType getType() { return ContentType.SCRIPT; }
    public void create(CreateContext ctx) { /* 剧本创建 */ }
    public void bindCams(BindContext ctx) { /* 剧本 CAMS 绑定 */ }
    public void syncStatus(SyncContext ctx) { /* 剧本状态同步 */ }
}

@Component
public class EpisodeArtifactHandler implements ArtifactHandler { ... }
```

工厂自动注册:

```java
@Component
public class ArtifactHandlerFactory {
    private final Map<ContentType, ArtifactHandler> handlerMap = new HashMap<>();

    @Autowired
    public ArtifactHandlerFactory(List<ArtifactHandler> all) {
        for (ArtifactHandler h : all) handlerMap.put(h.getType(), h);
    }

    @PostConstruct
    public void validate() {
        Set<ContentType> seen = new HashSet<>();
        for (ArtifactHandler h : handlerMap.values()) {
            if (!seen.add(h.getType()))
                throw new IllegalStateException("重复的 ContentType: " + h.getType());
        }
    }

    public ArtifactHandler get(ContentType type) { ... }
}
```

**Spring 启动时 List 注入所有实现,按 type 自动装 Map**。加新类型零改动业务代码,就加一个 `@Component` 的 Handler 类。

**效果**:

- 新增内容形态:**3 天降到 0.5 天**
- 后来真的加了第 4 种"非标剧集",半天接完

### 面试官喜欢挖的点

**"为啥用 List 注入不用 @Autowired Map?"**

Spring 支持 `@Autowired Map<String, ArtifactHandler>`,但 key 是 Bean 名字不是我要的 ContentType。所以必须手动转。

**"两个 Handler 返相同 ContentType 会怎样?"**

启动时 `@PostConstruct` 就炸。我特意做了校验,不等运行时才发现。

---

## 六、合同号重复这个线上 bug(讲得最有故事感)

### 事情是这样的

某天运营反馈:"结算对账对不上,7 月少了 10 多笔分账,合计 3 万块"。

我一查,合同表里**同一个合同号出现了多条记录**,每条下面都挂了剧本,每次算分账都重复统计。

SQL 一拉:

```sql
SELECT contract_number, COUNT(*)
FROM org_coop_apply
WHERE is_valid = 1
GROUP BY contract_number
HAVING COUNT(*) > 1;
```

**30 多个合同号重复**。

### 为啥会这样

看代码,合同创建这块:

```java
// 先 SELECT 判断是否存在
long count = mapper.countByContractNumber(contractNumber);
if (count > 0) throw new BizException("已存在");
// 再 INSERT
mapper.insert(apply);
```

**SELECT 和 INSERT 不原子**。用户双击提交、网络重试、前端没防抖 — 两个请求同时过 SELECT 判空,都觉得"没重复",都 INSERT,DB 也没唯一索引,全进去了。

这是经典的并发穿透。

### 我是怎么修的

**三层防御**:

**第一层**:DB 加唯一索引

```sql
CREATE UNIQUE INDEX idx_contract_number ON org_coop_apply(contract_number);
```

**第二层**:代码保留 SELECT 校验(给友好错误提示)

```java
long count = mapper.countByContractNumberAndStatus(
    contractNumber,
    Arrays.asList(PENDING, PROCESSING)
);
if (count > 0) throw new BizException("合同号 xxx 已存在有效申请");
```

**第三层**:下游剧本创建也做幂等

```java
List<ArtifactScriptDO> exist = scriptDriver.getByApplyId(applyId);
if (!CollectionUtils.isEmpty(exist)) return exist;  // 已创建过,返老数据
```

### 存量数据怎么清

写 SQL 清脏:

```sql
-- 保留最早一条有效,其它标为无效
UPDATE org_coop_apply
SET is_valid = 0
WHERE contract_number = 'xxx'
  AND apply_id NOT IN (
    SELECT min_apply_id FROM (
        SELECT MIN(apply_id) AS min_apply_id
        FROM org_coop_apply
        WHERE contract_number = 'xxx'
    ) tmp
  );

-- 删无效申请关联的剧本
DELETE FROM artifact_script
WHERE apply_id IN (SELECT apply_id FROM org_coop_apply
                   WHERE contract_number = 'xxx' AND is_valid = 0);
```

**结算 SQL 改写** — 之前按合同号 SUM,改成 JOIN 有效 apply 统计:

```sql
-- 修前
SELECT SUM(settle_amount) FROM settlement_record WHERE contract_number = 'xxx';

-- 修后
SELECT SUM(s.settle_amount)
FROM settlement_record s
JOIN org_coop_apply a ON s.apply_id = a.apply_id
WHERE a.contract_number = 'xxx' AND a.is_valid = 1;
```

**上线后 3 个月零次复发**,修复了 10 多笔历史脏数据。

### 面试官爱挖的点

**"为啥既要代码校验又要 DB 唯一索引?"**

两层防御解决不同问题:
- **代码校验**:给友好错误提示
- **DB 索引**:防并发(两个请求同时过代码校验都觉得没重复,DB 兜底拦)

少一层都有漏洞。

**"为啥不加分布式锁?"**

合同号本身是**用户输入的天然幂等键**,DB 唯一索引就是最天然的分布式锁。加 Redis 锁反而:
- 多一个依赖,Redis 挂了锁就没了
- 每请求 setnx + del 有性能开销
- 锁超时释放失败有新风险

**"3 个月零复发能说明彻底解决了吗?"**

(这题要诚实)**其实不能**。更严谨说法是"符合预期"。如果要严谨结论得 6-12 个月数据 + 按层统计拦截数。我简历里写"彻底解决"是带了点主观色彩。但能说明**三层防御至少有一层在起作用** — 代码层拦截日志每月 5-10 次,证明方案有效。

---

## 七、还有几个治理故事(选讲)

### 1. 合同中台回调没同步状态

中台回调"乙方已签署",我们本地 `contract_status` 没更新,页面一直显示"待签约"。排查发现回调代码某个分支**漏写了 `setContractStatus`**,只 set 了时间和备注。

改了之后重构成枚举 Map 映射,再也不怕漏:

```java
private static final Map<String, Integer> STATUS_MAP = Map.of(
    "2", ContractStatus.FULLY_SIGNED.getCode(),
    "5", ContractStatus.PARTY_B_SIGNED.getCode(),
    "9", ContractStatus.TERMINATED.getCode()
);
```

### 2. 双合同展示状态错

有些作品同时签**版权合同 + 侵权合同**(版权方 + 分发方不是同一个)。老代码只判断一个合同,页面误显示"已生效"。

修复:加 `existTwoContract()` 判断,双合同场景要两份都签完才是 EFFECTIVE。

### 3. 批量定时任务稳定性

夜间批量任务**周均失败 1-2 次**,DB 锁冲突。改成:
- 每批 50 条分片
- `update_task_id` 幂等标记
- 失败重试 3 次 + 告警

**周均 1-2 次 → 月均 0 次**,锁冲突减 80%。

---

## 八、合同"已过期"状态动态计算(面试我最爱讲的 case)

### 为啥讲这个

因为这个案例**能体现工程师思维**:**不是所有状态都该存 DB,有些该算**。

### 老方案

合同有 3 个状态:已生效、已终止、已过期。最早 DB 存 3 种 status,定时任务每小时扫,把"已生效且 `cooperation_end_time < NOW`"的改成"已过期"。

坑:
1. **延迟最长 1 小时**,用户看到过期的合同还显示"已生效"
2. **万级合同扫表**,锁冲突风险
3. **Job 挂了状态永远错**(真挂过一次,内存 OOM 凌晨没人管)

### 新方案

**区分事实和推导**:

- 已生效、已终止 = **事实**(某时点发生了动作)→ 存 DB
- 已过期 = **推导值**(时间到自动变)→ **不存 DB,SQL 动态算**

```sql
SELECT
  id,
  CASE
    WHEN status = 1 AND cooperation_end_time < NOW() THEN 2    -- 已过期
    WHEN status = 1 THEN 1                                      -- 已生效
    WHEN status = 9 THEN 9                                      -- 已终止
  END AS status_view
FROM contract;
```

**DB 只存 status = 1 或 9**。"已过期"完全由 SQL 算出来。

前端筛"已过期"?Service 自动转成 `WHERE status = 1 AND cooperation_end_time < NOW()`。

**索引配合**:`idx_status_end_time(status, cooperation_end_time)` 组合索引走最左前缀。

**效果**:**延迟 1 小时 → 0,消除 Job 依赖**。

### 面试官会挖的点

**"以后加'临期'(7 天内到期)状态怎么办?"**

同样套路,改 SQL:

```sql
WHEN status = 1 AND cooperation_end_time BETWEEN NOW()
    AND DATE_ADD(NOW(), INTERVAL 7 DAY) THEN 3
```

**不改表,不加 Job**。动态计算的核心优势。

**"已终止为啥存库?"**

它是**人工触发**的(运营点"终止合同"),有发生时间点(谁什么时候为什么终止)。必须存库。**事实存库,推导值动态算** — 这是我的设计原则。

---

## 九、量化数据汇总(面试要脱口而出)

| 指标 | 数据 |
|---|---|
| 合同类型接入 | 5 天 → 1 天 |
| 过期状态延迟 | 1 小时 → 0 |
| N+1 优化 | 3-5s → 500ms |
| 签约接口代码量 | -40% |
| 后台查询代码量 | -60% |
| 内容形态接入 | 3 天 → 0.5 天 |
| 批量最大 | 500 条单次 |
| 合同号重复 | 3 个月零复发 |
| 批量任务稳定性 | 周 1-2 次 → 月 0 次 |
| DB 锁冲突 | -80% |

规模数字:

- 万级 UP 主 + 千级 MCN
- 月均 300+ 单
- 年结算千万级
- 管理万部漫剧
- 对接 6+ 中台

---

## 十、面试时怎么组合讲

### 开场 1 分钟:第一部分"项目讲清楚"的内容

### 被问最复杂的:讲签约框架(第二部分)

### 被问最得意的:讲 N+1 优化(第三部分,数字漂亮)

### 被问解决过的线上问题:讲合同号重复(第六部分,三层防御)

### 被问架构思维:讲已过期状态动态计算(第八部分)

### 被问设计模式:讲工厂模式(第五部分)

### 被问跨系统问题:讲新旧 ID 映射(第四部分)

---

## 避坑清单

- ❌ 说"合同中心是我做的" — 是集团中台,对接方
- ❌ 说"QPS 几十万" — B 端后台,QPS 几十而已
- ❌ 说"Kafka/RocketMQ" — 我们用 TDMQ/CMQ
- ❌ 说"Spring Cloud" — 用 TRPC + Polaris
- ❌ 说"Nacos" — 我们用 Apollo/Rainbow
- ❌ 吹"所有线上问题都是我修的" — 说"治理过其中 4 个典型"
- ❌ 吹"彻底解决" — 说"3 个月零复发,符合预期"

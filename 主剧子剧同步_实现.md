# 主剧子剧分类同步(跨系统一致性补丁)

> 2026 Q2 做的一个衍生需求,是"剧集池管理"项目的配套优化。
> 表面是"for 循环调几次 RPC",但涉及:**跨系统数据一致性 + SNAPSHOT 依赖升级 + 异常分层保护 + 测试 ROI 权衡**。
> 我是这个需求的**主力**,1 天代码 + 半天联调。

---

## 一、一句话讲清楚是什么

Vega 运营给主剧配"分发分类"字段(dramaCategory),要同步到 merchantry 的 `video_sync_index.drama_category`。**原来只同步主剧一条,新需求是同步主剧 + 所有子剧**。

子剧由**内容中心**(ShortPlayServer)维护,Vega 要调它的 TRPC 接口 `listVideoInfosByMvdid` 拿子剧列表。

---

## 二、需求背景(面试开场)

### 业务诉求
- **运营配置位置**:Vega 后台(编辑剧集时选分类)
- **实际使用位置**:merchantry(分销商查看时按分类过滤)
- **痛点**:之前只同步主剧,**分销商看到的所有子剧都是"未分类"**,无法检索

### 技术挑战
- **跨三系统**:Vega(写入方)↔ 内容中心(子剧信息)↔ merchantry(目标存储),任何一个抖动都影响同步
- **SNAPSHOT 依赖升级**:`ccshortplayserver-stub` 要从 1.7.7 升到 1.8.7(新接口在新版本才有)
- **不能阻塞主业务**:editVideo 是运营核心操作,同步失败绝不能让编辑接口 500
- **没有现成测试数据**:test 环境几乎没有"带子剧的主剧",端到端验证困难

### 我的定位
后端主力 + 单人负责。协作:
- 👆 **内容中心**(索要新接口 + 确认调用方式)
- 👇 **merchantry**(接收端同学对账验证)
- ↔️ **产品**(明确"全部子剧都要同步"的业务规则)

---

## 三、核心设计思路

### 3.1 同步时机:编辑触发 vs 定时补偿

**方案 A**:在 `editVideo()` 结束后触发同步(事件驱动)
**方案 B**:定时任务扫 Vega 的 dramaCategory → 同步到 merchantry(轮询)

| 维度 | A 事件驱动 | B 定时任务 |
|---|---|---|
| 时效性 | 实时 | 滞后(分钟级) |
| 资源占用 | 低(有变更才跑) | 高(每次全量扫) |
| 对丢失/补偿的容忍度 | 差(一次失败就丢) | 好(下次继续补) |

**选 A**,但承认它的弱点:**没有补偿机制**。

权衡依据:
- 运营对时效性敏感(配完分类希望立刻生效)
- 业务规模小,失败可人工触发(重新保存一次视频即可触发)
- **未来可以加 B 作为对账兜底**(outbox 模式,不在本次范围)

### 3.2 异常保护分层

**核心原则**:**辅助功能不能影响主流程**。editVideo 是关键路径,dramaCategory 同步是辅助路径,辅助挂了主路径必须能继续。

分层:

```
editVideo() [关键路径]
    │
    ├─ 主流程: 更新 Vega DB + 推内容中心
    │
    └─ syncDramaCategoryToComicDramaWeb() [辅助路径]
        │
        ├─ [Layer 1] 整个方法内部 try-catch
        │       任何未预期异常都吞掉,不向上传播
        │
        ├─ 1. syncOneDramaCategory(mvdid)
        │       └─ [Layer 2] 单条同步 try-catch
        │               一条失败不影响其他
        │
        ├─ 2. listSubVdidsByMvdid()
        │       └─ [Layer 3] 查子剧 try-catch
        │               TRPC 异常时返回空列表,不影响主剧同步
        │
        └─ 3. for each subVdid: syncOneDramaCategory(sub)
                └─ [Layer 2 再跑] 每个子剧独立保护
```

三层保护保证:无论 TRPC 超时、merchantry 返回业务失败、还是内容中心挂了,editVideo **永远返回成功**,辅助同步的失败只进日志。

### 3.3 子剧过滤策略

调 `listVideoInfosByMvdid(mvdid)` 时,内容中心的返回里**一般会包含主剧本身**。如果不过滤,会出现:

- 主剧 100 先同步一次 → merchantry UPDATE vdid=100
- 查子剧列表返回 [100, 101, 102, 103]
- 遍历又 UPDATE vdid=100 一次(冗余)

所以用 `filter(id -> id != mvdid)` 过滤掉主剧自身。

---

## 四、实现细节

### 4.1 pom 升级

**改动 1 行**:

```xml
<!-- solution_vega/pom.xml -->
<dependency>
    <groupId>com.yuewen.nrzx.bizjava.ccshortplayserver.shortplayquery</groupId>
    <artifactId>ccshortplayserver-stub</artifactId>
    <version>1.8.7</version>  <!-- 原 1.7.7 -->
</dependency>
```

升级注意点:
- `mvn -U` 强制拉取最新 SNAPSHOT(老版本会被 Maven 缓存,不 `-U` 拉不到新包)
- `git pull` 同步最新代码(SNAPSHOT 依赖和项目代码可能紧耦合,同事改了方法你没拉就编不过)

### 4.2 新增方法:listSubVdidsByMvdid

```java
/**
 * 根据主剧 mvdid 调内容中心拉所有子剧 vdid。
 *
 * 对应 TRPC 接口: ShortPlayQueryAPI#listVideoInfosByMvdid(1.8.7+ 才有)
 * 返回的 videoList 一般包含主剧本身,此处过滤掉,只返子剧。
 *
 * @param mvdid 主剧 vdid
 * @return 子剧 vdid 列表(不含主剧);接口返回空/null 时返回空集合
 */
private List<Long> listSubVdidsByMvdid(long mvdid) {
    ListVideoInfoByMvdidRequest request = ListVideoInfoByMvdidRequest.newBuilder()
            .setContext(Context.newBuilder().setSourceId(shortPlaySourceId).build())
            .setMvdid(mvdid)
            .build();

    ListVideoInfoByMvdidResponse response =
            shortPlayQueryAPI.listVideoInfosByMvdid(new RpcClientContext(), request);

    if (response == null || response.getVideoList() == null) {
        return Collections.emptyList();
    }
    return response.getVideoList().stream()
            .map(ShortPlaySimpleInfo::getVdid)
            .filter(id -> id != mvdid)  // 过滤主剧自身
            .toList();
}
```

**为什么这段代码很小**:因为 Vega 现有代码已经注入好 `shortPlayQueryAPI`(老接口也在用),我只需调一个新方法就行。**站在前人铺好的路上**。

### 4.3 核心同步逻辑改造

**原实现(只同步主剧)**:

```java
private void syncDramaCategoryToComicDramaWeb(VideoEditParam param) {
    if (param == null || param.getDramaCategory() == null || param.getVdid() == null) return;
    try {
        Result<Void> rsp = comicDramaWebManager.updateDramaCategory(
                param.getVdid(), param.getDramaCategory());
        if (rsp.isFailure()) {
            log.error("sync failed, code={} msg={}", rsp.getCode(), rsp.getMessage());
        }
    } catch (Exception e) {
        log.error("sync exception", e);
    }
}
```

**新实现(主剧 + 所有子剧)**:

```java
private void syncDramaCategoryToComicDramaWeb(VideoEditParam param) {
    if (param == null || param.getDramaCategory() == null || param.getVdid() == null) return;

    Long mvdid = param.getVdid();
    Integer category = param.getDramaCategory();

    // 1. 先同步主剧
    syncOneDramaCategory(mvdid, category, "main");

    // 2. 查所有子剧(查失败不影响主剧同步)
    List<Long> subVdids;
    try {
        subVdids = listSubVdidsByMvdid(mvdid);
    } catch (Exception e) {
        log.error("list sub drama failed, mvdid={}", mvdid, e);
        return;
    }

    if (subVdids.isEmpty()) {
        log.info("no sub drama for mvdid={}", mvdid);
        return;
    }
    log.info("found {} sub drama(s) for mvdid={}: {}", subVdids.size(), mvdid, subVdids);

    // 3. 遍历同步子剧(单条独立保护)
    for (Long subVdid : subVdids) {
        syncOneDramaCategory(subVdid, category, "sub");
    }
}

/**
 * 统一的单条同步方法,吞掉所有异常只打日志。
 * 用 kind 参数区分"main"和"sub",便于日志检索。
 */
private void syncOneDramaCategory(Long vdid, Integer category, String kind) {
    try {
        Result<Void> rsp = comicDramaWebManager.updateDramaCategory(vdid, category);
        if (rsp.isFailure()) {
            log.error("[{}] sync failed, vdid={} code={} msg={}",
                    kind, vdid, rsp.getCode(), rsp.getMessage());
        }
    } catch (Exception e) {
        log.error("[{}] sync exception, vdid={}", kind, vdid, e);
    }
}
```

### 4.4 调用点保持不变

`VideoService.editVideo()` 的调用方式**零改动**:

```java
public void editVideo(VideoEditParam param) {
    ArtifactVideoDO artifactVideoDO = videoManager.getArtifactVideoByVdidIfNullThrow(param.getVdid());
    // ... 主流程
    videoDriver.updateArtifactVideo(editDO);

    // 同步漫剧分类(辅助路径,失败仅记录日志,不阻塞主流程)
    syncDramaCategoryToComicDramaWeb(param);
}
```

设计意图:**新增能力对调用者透明**。调用方不需要关心"是同步主剧还是同步子剧",统一塞进 `syncDramaCategoryToComicDramaWeb` 方法内部。

---

## 五、几个关键设计权衡

### 权衡 1:查询失败时的降级策略

`listVideoInfosByMvdid` 返回 null / 异常时怎么办?

**选项 A**:重抛异常,让主剧同步也失败(严格一致性)
**选项 B**:返回空列表,主剧同步继续(弱一致性)

**选了 B**。理由:
- 主剧同步是**上游用户已经期待的**(它是原有行为)
- 子剧同步是**本次新增**,失败只是"新功能没生效",不应该让老功能也挂
- 业务容忍度:运营下次保存还会重试

### 权衡 2:异步 vs 同步

当前实现是**同步**的:editVideo 返回前,所有子剧同步完成(或失败)。

**要不要改异步(消息队列)?**

反对改异步的理由:
- 子剧数量在个位数到几十,**总耗时 100ms 内可控**
- 改异步要引入 MQ 依赖 + 消费者 + 幂等 + 死信队列,**复杂度暴增**
- 当前"异常保护 + 日志告警"已经够用

支持改异步的理由:
- 彻底解耦,editVideo 响应时间恒定
- 失败自动重试,有天然补偿

**决策**:暂不改,业务量增长到一定规模再重构。**过早优化是万恶之源**。

### 权衡 3:要不要加定时对账 Job

场景:某个主剧的子剧**在运营编辑之前就进 merchantry** → 它永远不会被同步到(因为编辑事件只覆盖未来)。

**现状**:没有对账 Job,靠运营重新保存一次视频来补同步。
**理想**:定时扫 Vega 的 `ArtifactVideoExtra.dramaCategory` 和 merchantry 的 `video_sync_index.drama_category`,diff 后补同步。

当前不做的原因:
- 业务量小,人工触发可接受
- 对账 Job 设计要考虑扫描策略、并发、锁、幂等,工作量不小
- **先观察线上数据不一致的频率**,再决定要不要投入

---

## 六、踩过的坑

### 坑 1:升级 SNAPSHOT 依赖的连锁反应

升 `ccshortplayserver-stub: 1.7.7 → 1.8.7` 时,本地编译挂了 **9 个错**。错误都在**别人的代码里**:

```
NonStandardVideoService.java:1168
  找不到符号: 方法 getDramaCategory()
  位置: 类 ArtifactVideoExtra

AcquisitionPostProcessService.java:78
  找不到符号: 方法 getArtifactVideosByNames(...)
  位置: ArtifactVideoDriver
```

我既没改这些文件,也没改 `peacock-driver-uploader` 依赖版本,凭什么挂我?

原因:`peacock-driver-uploader` 是 SNAPSHOT 依赖,**每天版本都可能变**。同事改了这个 jar 里某些方法(删除/重命名),代码改动推了 master,但 jar 更新后我本地代码没拉,**本地代码依赖的旧方法在新 jar 里已经没了**。

**定位步骤**:
1. 第一反应:是不是我升 ccshortplayserver 引起的? → 回滚 ccshortplayserver 测试 → 还是挂
2. 分析报错:涉及的类(`ArtifactVideoExtra` / `ArtifactVideoDriver`)都不是 ccshortplayserver 里的 → 不是我的锅
3. `git log --oneline origin/master..HEAD` → 看自己本地领先/落后多少 commit → 发现落后 1 个
4. `git pull --rebase origin master` → 同事修复代码到位 → 编译通过

**教训**:
- **升级依赖前先 `git pull`**
- **升级后第一次编译报错**,要区分"我改的锅"和"别人改了没同步"

### 坑 2:日志在 error.log 不在 warn.log

我写的日志是 `log.error(...)`,以为会打到 `warn.log`。结果找了半天找不到,最后在 `/usr/local/trpc/log/error.log` 才找到。

项目的日志分级文件约定:
- `access.log`:请求记录
- `warn.log`:只收 WARN 级别
- `error.log`:收 ERROR 级别 + 异常栈
- `debug.log`:全量(包括 DEBUG)

**反思**:排查之前先用 `grep -l "找的关键词" /usr/local/trpc/log/*.log` 确定在哪个文件,不要只看一个。

### 坑 3:测试数据难构造,最后靠日志兜

test 环境真正有子剧的主剧只有个位数。我找内容中心同事要 vdid,他查了 10 分钟说"暂时没找到合适的"。

我的降级验证方案:

```bash
# 发布后,在 test 容器里
tail -F /usr/local/trpc/log/error.log | grep -E "syncDramaCategory|sub drama|mvdid"

# 另一个 terminal,触发任意 mvdid 的 editVideo
curl -X POST .../api/v1/video/edit -d '{"vdid":X, "dramaCategory":1, ...}'
```

日志会打三种结果之一:
- `found N sub drama(s) for mvdid=X: [...]` → 完美,真有子剧
- `no sub drama for mvdid=X` → 代码通了,这个主剧没子剧
- `list sub drama exception` → TRPC 调用失败

**反思**:不是所有代码都值得构造完整 E2E 测试。简单逻辑 + 低回归风险 = 可以只靠日志 + 真实流量验证。

---

## 七、上线观察指标

### 日志关键词

```
syncDramaCategoryToComicDramaWeb  # 触发入口
found N sub drama(s)              # 正常找到子剧
no sub drama                       # 该主剧没子剧(正常)
list sub drama exception           # 查子剧失败(告警)
[main] sync failed                 # 主剧同步失败(告警)
[sub] sync failed                  # 某个子剧同步失败(告警)
```

### 对账 SQL(手动)

```sql
-- 在 merchantry 库,随机抽查几个主剧 + 子剧的 drama_category
SELECT vdid, name, drama_category, update_time
FROM video_sync_index
WHERE vdid IN (主剧vdid, 子剧vdid1, 子剧vdid2, ...)
ORDER BY vdid;

-- 期望:drama_category 值一致
-- 如果不一致:查看 error.log 对应时间点的同步日志
```

---

## 八、面试可能被问的问题

### Q: 为什么不用消息队列做异步同步?

A: 子剧数量可控(几到几十),同步总耗时 100ms 内。引入 MQ 会增加:依赖、消费者、幂等控制、死信处理,**复杂度和收益不匹配**。当前"同步 + 异常保护 + 日志告警"足够。

业务量或子剧数涨 10x 后(比如单主剧带 500+ 子剧),再重构成异步。

### Q: 如果某个子剧同步失败,你怎么保证最终一致性?

A: 当前没有自动补偿,靠**运营重新保存一次视频**触发重试(editVideo 会重新跑一遍同步)。

如果业务要求更高,可以加:
1. **对账定时任务**:每天扫 Vega.extra.dramaCategory vs merchantry.video_sync_index.drama_category,diff 后补齐
2. **Outbox 模式**:把同步动作记到本地事务表,Worker 异步拉取重试

现阶段没做,因为业务容忍度高 + 失败率低。

### Q: 升级依赖踩的坑,你学到什么?

A: SNAPSHOT 依赖是"活的 jar",**内容每天可能变**。升级前必须:
1. **git pull 最新代码**(避免本地代码和新 jar 不配套)
2. **mvn -U** 强刷依赖(避免本地缓存老版本)
3. **第一次编译报错**时,分辨是**自己改的锅**还是**别人改了没同步**

### Q: 三层异常保护是不是过度设计?

A: 不是。**辅助功能永远不能影响主业务**。

举例:editVideo 是运营的核心操作,保存失败用户会重试。如果因为 merchantry 抖动导致 editVideo 500,用户体验就是"我保存失败了!"——但其实 Vega 本地已经保存成功,只是同步挂了。**用户误判会带来更大问题**(重复操作、联系客服)。

三层保护让同步失败**降级到日志告警**,用户永远看到"保存成功"。这是工程稳健性的体现。

### Q: 如果让你从零重做,会怎么改?

A: 三点:
1. **从一开始就做对账 Job**:不光依赖事件驱动同步,定期扫描补漏
2. **引入 outbox 模式**:写一张本地事务表,Worker 异步消费 + 重试
3. **接口幂等**:merchantry 的 `updateDramaCategory` 接口要天然幂等(相同 vdid + category 调 N 次结果一致),目前没明确验证

现在这套方案是**够用但不完美**,符合"先跑起来再优化"的工程节奏。

### Q: 子剧查询接口为什么要过滤掉主剧自身?

A: 内容中心的 `listVideoInfosByMvdid` 接口**约定里会把主剧也返回**(属于"所有相关视频"列表)。如果不过滤:

- 主剧在 `syncOneDramaCategory(mvdid)` 里同步一次
- 查子剧列表返回 `[mvdid, sub1, sub2]`
- 遍历时又对 mvdid 调用一次 `syncOneDramaCategory`(多余)

过滤掉主剧是**避免重复同步**,减少对 merchantry 的无效调用。

---

## 九、代码文件清单

- `solution_vega/pom.xml`:依赖版本 1.7.7 → 1.8.7
- `solution_vega/src/main/java/com/yuewen/openplatform/vega/service/VideoService.java`:
  - 改:`syncDramaCategoryToComicDramaWeb` 拆成主 + 子剧两段
  - 新增:`syncOneDramaCategory` 私有方法
  - 新增:`listSubVdidsByMvdid` 私有方法
  - 新增:3 个相关 import

**改动共 1 个文件 + 1 行 pom**,约 70 行代码增量。体现"**用最小改动实现新能力**"。

---

> 🔗 业务背景 + 踩坑故事见《主剧子剧同步_故事版.md》
> 这份偏技术细节,面试被追问时翻出来照着讲。

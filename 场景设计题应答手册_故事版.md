# 场景设计题应答手册（用自己项目反客为主）

> 中厂自研岗 3~4 轮面试，几乎每轮都有一道 **"如果让你设计 XX 你会怎么做"**。
> 这题没有标准答案，考的是**框架感 + 落地经验 + 踩坑思维**。
> 这份文档的目标：**无论面试官抛什么场景题，都能把话题拉回到你做过的 5 个项目里，用真实经验作答**。

---

## 目录

1. 通用应答框架（7 步法）
2. 5 个高频场景题 × 用我自己项目作答
   - 设计分销系统
   - 设计权限系统
   - 设计跨系统数据同步
   - 设计高并发结算系统
   - 设计 Webhook 接收 + 异步任务
3. 场景题里的"标准坑"清单
4. 被问到不熟悉的领域怎么办
5. 速记卡

---

## 一、通用应答框架（7 步法）

面试官抛题后**别急着答**。先花 30 秒思考，然后按这 7 步走，每步 1~2 分钟，**全程 10~15 分钟**。

### 步骤 1：澄清需求（**别跳过！**）

面试官的题**永远是模糊的**。直接开始答 = 答偏。

反问 2~3 个问题：
- **规模**：多少用户 / 多少 QPS / 多少数据量？
- **场景**：是 toB 还是 toC？读多还是写多？实时还是离线？
- **边界**：要不要考虑多机房？要不要全球化？

**示例**：
> 面试官："设计一个分销系统。"
> 你："好的，我问几个问题先对齐一下——
> 1. 这个分销系统的量级大概多少？分销商几千还是几十万？
> 2. 核心是结算分账，还是商品推广链路，还是两者都要？
> 3. 有没有跨国需求？"

**加分点**：**问题本身显示你懂这个领域**。

---

### 步骤 2：容量估算（30 秒快算）

按澄清后的数字心算一下：
- QPS = 日活 × 人均请求 / 86400
- 存储 = 日增数据量 × 保留天数
- 写比读 = 评估读写放大

**示例**：
> "假设分销商 10 万，日活 30%，每人每天 100 次操作 → 日请求 3000 万 → 平均 QPS 350，峰值 × 3 = 1000 QPS。
> 数据量：订单表每天 100 万条，存 2 年 = 7 亿条 → 需要分库分表。"

**加分点**：**数字要说出口**，不算纯"感觉"。

---

### 步骤 3：核心模型（表 / 接口）

画 3~5 个核心实体 + 1~2 个核心接口。
**不要一上来就画架构图**，先把领域模型讲清楚。

**示例**（分销）：
- 核心表：`distributor`（分销商）、`order`（订单）、`settlement`（结算单）
- 核心接口：
  - `POST /order/create` — 创建分销订单
  - `POST /settlement/generate` — 月度生成结算

---

### 步骤 4：整体架构（分层）

接入层 → 业务层 → 数据层，画 3 层。
**标清楚用什么中间件**：Nginx / API 网关 / Redis / MQ / MySQL / ES。

**示例**：
```
接入层:  Nginx → API 网关(鉴权/限流)
业务层:  订单服务 / 结算服务 / 分销商服务
数据层:  MySQL(主从) / Redis(缓存/锁) / RocketMQ(异步) / ES(查询)
```

---

### 步骤 5：关键链路深挖（**主战场**）

挑 **1~2 个核心链路**讲透。面试官最想听这段。

每条链路按：**请求入口 → 数据流 → 存储写入 → 异步处理 → 响应返回**

**示例**（结算生成链路）：
> "月初 00:00 定时触发 → 扫描全量分销商（分片 + 分布式锁）→ 单个分销商的订单走流式查询 ResultHandler → 按规则计算佣金 → 写结算表 → 发 MQ 通知分销商 → 分销商点导出走异步下载任务中心。"

---

### 步骤 6：扩展性 + 高可用 + 监控

把"这个系统挂了怎么办"讲清楚：
- **扩展性**：分库分表怎么拆？加机器能不能水平扩？
- **高可用**：MySQL 主从、Redis 哨兵 / 集群、MQ 多副本
- **监控**：关键指标（QPS / 延迟 / 错误率 / 业务指标）、告警阈值
- **降级 / 限流**：下游挂了怎么降级、突发流量怎么限流

---

### 步骤 7：踩过的坑（**杀手锏**）

**这是反客为主的关键**。把话题拉到你真实做过的事：
> "这个我们以前踩过坑——当时我们 XX 系统出现 XX 问题，后来是用 XX 办法解决的。这次设计我会提前规避。"

→ **面试官瞬间从"考题模式"切到"聊项目模式"**，你占主动。

---

## 二、5 个高频场景题 × 用我自己项目作答

### 场景题 1：设计一个分销系统

**面试官抛题**："设计一个类似淘宝联盟的分销系统，支持分销商推广商品和结算。"

**你的独家优势**：**你本来就做过分销后台 5 年**，直接讲。

#### 答题脚本

**第 1 步 · 澄清**：
> "好，我先问两个问题：
> 1. 分销商规模多大？C 端还是 B 端为主？
> 2. 结算是按订单实时还是按月周期？"

（面试官回答后）继续：

**第 2 步 · 背景 & 定位**：
> "我在阅文就做了 5 年分销后台，所以这个场景对我比较熟。我们那边的模型是：**10 万 +  B 端分销商、按月周结**，我就按这个量级来讲，不合适的地方再调整。"

→ **第一句就把面试官镇住了**：你不是临时想，你真做过。

**第 3 步 · 核心模型**：
> "核心三张表：
> - `distributor`：分销商基础信息 + 分佣比例 + 结算账号
> - `order`：分销订单，冗余 `distributor_id` 和 `commission_amount` 避免关联查询
> - `settlement`：月度结算单，唯一索引 `(distributor_id, period)` 防重复
>
> 核心接口只有两条链路：
> - 推广链路：分销商拿推广链接 → 带 `distributor_id` 跳转商品页 → 下单时写入 order
> - 结算链路：月初定时 job → 扫订单 → 生成 settlement → 分销商导出 / 查看"

**第 4 步 · 关键链路**（挑结算链路深讲）：
> "结算这块我踩过坑，详细讲下：
>
> **难点 1：数据量大**。我们当时一个月订单几千万，`select * from order where distributor_id=X` 直接 toList 会 OOM。
> **解法**：MyBatis 的 `ResultHandler` 流式处理，一行一行处理，内存稳定在 200M 以内。
>
> **难点 2：定时任务重复执行**。Spring `@Scheduled` 在 4 台 pod 上会跑 4 次。我们当时出过生产事故，结算单重复生成。
> **解法**：Redis 分布式锁 + DB 层 `(distributor_id, period)` 唯一索引双保险。
>
> **难点 3：导出超大 Excel**。财务要导 120 万条，页面转圈 2 分钟然后 500。
> **解法**：改异步，扔到任务中心，后台流式写到对象存储，发站内信给下载。"

→ **三个难点都是真实踩坑**，面试官听得眉飞色舞。

**第 5 步 · 扩展性**：
> "订单表量大，按 `distributor_id % 16` 分库，查单分销商的数据直接路由到单库。
> 跨分销商的全局查询（比如运营后台）走 ES 异构存储，Canal 监听 MySQL binlog 同步。"

**第 6 步 · 监控 & 踩坑**：
> "监控重点：
> - 定时 job 成功率（跑没跑、跑了多久）
> - 结算金额突变告警（环比涨跌 30% 告警）
> - 分销商投诉率（结算金额对不上）
>
> 最容易错的点：**分佣比例调整的时间点**。我们老系统是"改了立刻生效"，结果月中改比例会把前半月的订单按新比例算。后来改成**按订单创建时间锁定当时的比例**。"

→ 这种细节是书本上没有的，是真干过才知道的。

---

### 场景题 2：设计一个权限系统

**面试官抛题**："设计一个 RBAC 权限系统，支持按钮级权限控制。"

**你的独家优势**：**Vega 剧集池你就做过权限 + 按钮级**。

#### 答题脚本

**澄清**：
> "问两点：
> 1. 多少用户 / 多少角色 / 多少按钮级资源？
> 2. 是单系统还是要做集中式权限中心管多个业务系统？"

**背景**：
> "Vega 剧集池我做过这块，我们是 **300+ 运营用户、20 多个角色、按钮级资源 500+**，单系统内部权限。按这个量级讲。"

**核心模型**（经典 RBAC）：
> ```
> user ─── user_role ─── role ─── role_permission ─── permission
> ```
> - `permission` 表细化到按钮级：`{resource: '剧集池', action: '删除', resourceId: '按钮id'}`
> - 菜单树和权限绑定：一个菜单节点关联多个 permission
> - 数据权限（比如"只看自己部门的剧"）用 `data_scope` 字段 + 代码里拼 SQL `where` 条件

**关键链路**：
> "**登录 → 获取用户全量权限 → 缓存到 Redis**。前端每个按钮渲染时判断有没有这个 permission key。
>
> 后端接口也要二次校验（前端隐藏按钮 ≠ 安全），每个 `@PostMapping` 上加 `@RequirePermission('xxx')` 注解，AOP 拦截。"

**踩过的坑**：
> "Vega 有个坑：**权限配置是用 YAML 放在七彩石里的**（`permission.init` 那个 34KB YAML）。改一次要发布一次。
>
> 后来改成**DB 存权限 + Redis 缓存 + 七彩石只存'初始化种子'**。运营加角色、调权限不用发布代码。
>
> 另一个坑：**数据权限容易写漏**。我们统一封装了 `DataScopeAspect`，所有 DAO 查询自动拼 `where department_id in (...)`，避免开发漏写。"

---

### 场景题 3：设计跨系统数据同步

**面试官抛题**："A 系统有主数据，B/C/D 系统要同步，怎么保证最终一致性？"

**你的独家优势**：**主剧子剧同步 = 这题的标准答案**。

#### 答题脚本

**澄清**：
> "问下：
> 1. 对延迟的容忍度？秒级 / 分钟级 / 小时级？
> 2. 下游系统多少个？有没有失败重试 SLA？"

**背景**：
> "我做过一个类似的：**阅文主剧子剧同步**。主剧在 A 系统（IP 平台）、子剧在 B 系统（漫剧），B 要实时拿到 A 的变更，延迟容忍分钟级。"

**方案对比**（显示你的视野）：
> "跨系统同步有 3 种方案，各有适用：
>
> **方案 A：双写**。A 系统写完 DB 直接调 B 的接口。
> → 最简单，但 A 要感知 B 的存在，耦合深；B 挂了 A 要重试。
>
> **方案 B：MQ 解耦**。A 写完发 MQ，B 消费。
> → 解耦好，但 MQ 可能丢、顺序问题。
>
> **方案 C：binlog + CDC**（Canal / Debezium）。直接订阅 A 的 DB 变更。
> → 最解耦，但同步的是表结构，业务语义丢了。
>
> **我们选的是 B + 可靠消息**：A 写 DB + 发 MQ 用本地消息表保证原子（事务消息），B 消费带幂等。"

**关键链路**：
> "**A 侧（生产者）**：
> 1. 本地事务：写业务表 + 写消息表（同一事务）
> 2. 异步 job 扫描消息表，发 MQ
> 3. 发送成功标记消息表为已发送
>
> **B 侧（消费者）**：
> 1. 幂等消费：用消息 ID + Redis SETNX 去重
> 2. 失败重试：默认 3 次，最终失败进死信队列
> 3. 死信队列告警，人工介入
>
> **一致性对账**：
> - T+1 跑对账任务，对比 A 和 B 的关键字段
> - 差异的条目自动补推 MQ"

**踩过的坑**：
> "主剧子剧同步踩过一个经典问题：**顺序问题**。
> 运营对同一部剧连续改 3 次字段，如果 MQ 乱序消费，最终状态可能是中间值不是最新值。
>
> 解法：**按 drama_id 做 MQ 分区 key**（RocketMQ 的 MessageQueueSelector），同一部剧的消息打到同一个队列，严格顺序。
>
> 另一个坑：**幂等要用业务 ID 不是消息 ID**。消息 ID 重发会变，但同一业务事件的业务 ID 不变，用业务 ID 去重才真正幂等。"

---

### 场景题 4：设计高并发结算系统

**面试官抛题**："双 11 当天有 1000 万笔订单要结算，怎么设计？"

**你的独家优势**：**C 端结算 + 周结你就做过**，量级对齐一下。

#### 答题脚本

**澄清**：
> "问下：
> 1. 1000 万笔是实时结算还是批量？峰值 QPS 大概多少？
> 2. 结算结果要实时给用户看，还是 T+1 给够？"

**背景**：
> "我做过的 C 端分销结算是**月周结，不是实时的**。但量级类似：单月千万订单级别。如果要求实时结算，架构会不一样，我先按批量讲，再聊实时场景的改造点。"

**批量方案**（按你做过的讲）：
> "**核心思路：分片 + 流式 + 异步**。
>
> 1. **任务分片**：把 1000 万订单按 `distributor_id` 分成 100 片，每片 10 万
> 2. **分布式执行**：用 XXL-Job 分片任务，10 个 worker 并行跑
> 3. **流式查询**：每个 worker 用 `ResultHandler` 流式读 DB，边读边算边写
> 4. **批量写**：每 1000 条结算单 batch insert 一次，不要一条一条写
> 5. **限流**：控制 DB 写入 QPS 不超过 5000，避免把 DB 压挂
> 6. **失败重试**：单片失败不影响全局，失败片进重试队列"

**实时场景改造**（加分项）：
> "如果要求实时结算（下单即出佣金），改造点：
>
> 1. 下单 MQ → 结算服务消费 → 实时计算佣金 → 写 Redis 累计 + 异步落 DB
> 2. Redis 用 Hash 结构：`settlement:{distributor_id}:{period}` → `{total_amount, order_count}`
> 3. 查询走 Redis，DB 做 T+1 对账
> 4. 关键点：**Redis 要做高可用**（Cluster + 持久化），否则 Redis 挂了数据丢"

**踩过的坑**：
> "我真实踩过的：
>
> **坑 1：资金计算精度**。用 `double` 算佣金，1000 万次乘法累积误差能到几百块。全部改 `BigDecimal`，`setScale(2, RoundingMode.HALF_UP)`。
>
> **坑 2：结算 job 跑一半挂了**。最早是整体事务，挂了回滚全部，重跑要几小时。后来改成**按分销商粒度事务**，每个分销商独立事务，进度记录在 DB，挂了从断点续跑。
>
> **坑 3：结算金额对账**。结算出来的总金额跟订单流水对不上，运营质问。后来加了**三方对账**：订单流水总和 = 结算金额总和 = 支付流水总和，三方对齐才允许发放。"

---

### 场景题 5：设计 Webhook 接收 + 异步任务系统

**面试官抛题**："设计一个接收第三方 Webhook 并异步处理的系统。"

**你的独家优势**：**视频号加热 + 异步下载任务中心**你都做过。

#### 答题脚本

**澄清**：
> "问下：
> 1. Webhook 的 QPS？峰值多高？
> 2. 处理时长？毫秒级还是分钟级？
> 3. 失败重试要求？"

**背景**：
> "我做过两个类似系统：
> - **视频号加热链接**：接微信视频号 Webhook，量不大但要求不丢
> - **分销 C 端异步下载**：用户点导出 → 异步生成 Excel → 通知下载
> 核心思路是一样的：**接入层快速响应 + 业务层异步处理 + 状态可查**。"

**核心模型**：
> ```
> task: {id, type, payload, status, retry_count, result, created_at, finished_at}
> status: pending / running / success / failed / retrying
> ```

**关键链路**：
> "**接入层**：
> 1. 收到 Webhook → **立即写入 task 表（pending）+ 返回 200 OK**（不处理业务，5ms 内响应）
> 2. 鉴权：校验 Webhook 来源签名（HMAC-SHA256）
> 3. 幂等：Webhook 会重试，用外部的 `event_id` 做唯一索引去重
>
> **业务层（异步）**：
> 1. MQ 消费或定时 job 扫 pending 任务
> 2. 处理完改状态 + 写 result
> 3. 失败按指数退避重试（1min / 5min / 30min），最多 5 次
> 4. 超过 5 次进死信，告警人工介入
>
> **查询层**：
> 用户查任务状态 → 查 task 表 → 返回进度 / 结果链接"

**踩过的坑**：
> "**坑 1：Webhook 响应慢导致对方重试风暴**。最早业务逻辑写在接收接口里，处理 2 秒才返回，微信那边 1 秒就超时重试，我们这边收到大量重复 Webhook，订单变成 N 倍。
> 解法：**接收 = 落库 + 200 OK**，业务异步处理，接收响应控制在 100ms 以内。
>
> **坑 2：任务堆积**。异步下载高峰期任务堆积到 10 万，用户等 30 分钟。
> 解法：
> - 任务按**优先级分队列**（VIP 用户优先）
> - 按任务类型**水平扩 consumer**
> - 前端加**预估等待时间**告诉用户（别让用户一直刷）
>
> **坑 3：任务状态漂移**。consumer 处理中挂了，状态卡在 running。
> 解法：加**超时判定**，running 超过 N 分钟自动改 failed，释放给其他 consumer 重试。"

---

## 三、场景题里的"标准坑"清单

不管问啥场景题，这些坑要能随口举出例子：

| 坑 | 啥时候出现 | 标准解法 |
|---|---|---|
| **幂等** | 任何写接口 | 业务 ID 唯一索引 / Redis SETNX / 状态机 |
| **缓存击穿** | 热 key 查询 | 互斥重建 / 永不过期 + 后台刷新 |
| **缓存穿透** | 查不存在的 key | 布隆过滤器 / 空值缓存 |
| **缓存雪崩** | 大量 key 同时过期 | TTL 加随机抖动 |
| **消息丢失** | MQ 生产/消费 | 事务消息 / 本地消息表 / 手动 ACK |
| **消息重复** | MQ 重试 | 消费者幂等 / 去重表 |
| **消息乱序** | 多队列消费 | 按 key 分区 / 单队列顺序消费 |
| **分布式锁丢锁** | 长耗时任务 | 看门狗续期 / Redisson |
| **分布式事务** | 跨服务写 | 本地消息表 / TCC / Saga |
| **分库分表查全量** | 运营后台查询 | 异构 ES / 宽表聚合 |
| **定时任务多实例重复** | K8s 多 pod | Redis 锁 / XXL-Job 分片 |
| **大 key / 热 key** | 运营活动热点 | 拆分 / 本地缓存兜底 |
| **深分页** | `limit 10000, 20` | 延迟关联 / 游标分页 |
| **OOM 导出** | 全量数据导出 | 流式 ResultHandler / 异步任务 |
| **事务超时** | 长事务 | 拆小事务 / 异步 |
| **连接池打满** | 慢 SQL / 下游慢 | 超时设短 / 熔断 / 扩池 |

---

## 四、被问到不熟悉的领域怎么办

### 场景：面试官问"设计一个 IM 系统"，你没做过

**错误做法**：硬编，越编越错。

**正确做法**（三段式）：

**第 1 段 · 诚实 + 迁移**：
> "IM 我没实际做过，但场景上跟我做的 XX 有相似的地方（说一个你熟的场景）。我按我的理解先讲一下框架，不对的地方您指正。"

**第 2 段 · 用你懂的技术讲框架**：
> "IM 本质是**长连接 + 消息转发 + 消息持久化**。
> - 长连接我想到的是 WebSocket / TCP，服务端用 Netty
> - 消息转发是经典的发布订阅，我们业务场景用 MQ，IM 估计也是 MQ + 路由
> - 消息持久化分在线消息和离线消息，离线要存 DB
>
> 这是我能想到的大框架，细节上像单聊、群聊、在线状态、消息顺序这些我没做过，您能跟我讲讲你们系统里这块是怎么做的吗？"

**第 3 段 · 反向学习**：
> 面试官讲完后：
> "明白了，跟我之前理解的 XX 有点类似（对齐到你熟悉的）。如果让我参与这个系统，我觉得第一步需要先摸清 XX 这块，然后才能做细节设计。"

**为啥这招有效**：
- **诚实不会扣分**（硬编才扣大分）
- **把你会的迁移过来**显示你有工程能力
- **主动让面试官讲**，把他带到他擅长的区域，他会讲得开心，面试体验就好

---

## 五、速记卡

- **7 步法**：澄清 → 估算 → 模型 → 架构 → 关键链路 → 扩展性 → 踩坑
- **核心原则**：**把任何题都拉回到你做过的 5 个项目**（分销 / 结算 / 权限 / 同步 / Webhook）
- **反客为主**：第 1 步澄清之后说"我做过类似的，按这个量级讲"，气场立刻变
- **踩坑清单**：幂等 / 缓存三坑 / 消息三问题 / 分布式锁 / 分库分表 / 深分页 / OOM
- **不熟的领域**：诚实 + 迁移已有经验 + 反向让面试官讲
- **最关键一句话**：**"这个我们以前踩过坑——..."** 一说出这句，你就从被考核者变成分享者

---

## 六、对照表：面试官的题 → 我用哪个项目作答

| 面试官问 | 关键词 | 用这个项目作答 |
|---|---|---|
| 分销 / 电商联盟 / 推广 | `分销 / 佣金 / 推广` | 分销后台 + C 端结算 |
| RBAC / 权限 / 后台管理 | `权限 / 角色 / 按钮` | Vega 剧集池 |
| 数据同步 / 跨系统一致性 | `同步 / 一致性 / 异构` | 主剧子剧同步 |
| 高并发结算 / 账单 / 对账 | `结算 / 对账 / 金额` | C 端结算优化 |
| Webhook / 异步任务 / 任务中心 | `回调 / 异步 / 任务` | 视频号加热 + 异步下载 |
| 配置中心设计 | `动态配置 / 热生效` | 七彩石使用经验 |
| 发布流程 / CI/CD | `发版 / 灰度 / 回滚` | 上线打包全流程 |
| 故障应对 / 监控告警 | `监控 / 告警 / 降级` | 故障复盘经验 7 类 |

→ **几乎所有场景题都能映射到你已有的项目**。这就是 5 年积累的价值。

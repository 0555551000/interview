# 跨系统 HTTP 调用 307 重定向排查 —— 唠嗑版

> 这是 2026 年 5 月初联调阶段的一次 bug 排查经历。
> 接口返回 succeed=5、failed=0,看起来一切正常,但下游数据库一行都没更新。
> 整个排查过程从"为什么 dryRun 都对了真跑还不行"开始,
> 一路扒到 OkHttp 的默认行为、Nginx 强制 https、配置中心覆盖,
> 最后在七彩石上改一行配置秒级修复。
> **面试时是讲"bug 排查方法论"和"工程效率取舍"的好素材**。

---

## 一、需求背景:存量数据回填

> 跟前一个《主剧子剧同步》是同一个需求的延续。

主剧子剧同步那个需求,我做完后产品 PM 说:

> "彭哥,新增加的剧分类同步没问题。但**测试库里有一批历史剧已经设了分类**,商户端那边显示还是 `--`,因为这批历史剧从来没经过新的'编辑'流程,根本没触发同步。能不能加个回填接口,把这批存量数据一次性刷过去?"

我:"行,加个 `/api/v1/video/backfillDramaCategory` 接口,扫 vega 的 `artifact_video` 表,挑出 `extra.dramaCategory` 有值的剧,逐条复用现有同步逻辑,支持 dryRun 先估算数量,真跑可以指定 vdid 局部回填。"

PM:"完美。"

代码不复杂,主要是分页扫表、调内部签名 API、处理失败列表。一上午写完。

---

## 二、自测阶段:接口对了,数据没动

代码部署到 testvega 后,在浏览器 Console 里调接口:

```javascript
// dryRun 看看能扫到几条
fetch('/api/v1/video/backfillDramaCategory', {
  method:'POST',
  headers:{'Content-Type':'application/json'},
  body:JSON.stringify({dryRun:true})
}).then(r=>r.json()).then(console.log)

// 返回:
{
  "code": 0,
  "data": {
    "dryRun": true,
    "total": 5,
    "succeed": 5,
    "failed": 0,
    "durationMs": 171
  }
}
```

5 条候选,完美。继续真跑:

```javascript
fetch('/api/v1/video/backfillDramaCategory', {
  method:'POST',
  headers:{'Content-Type':'application/json'},
  body:JSON.stringify({})
}).then(r=>r.json()).then(console.log)

// 返回:
{
  "data": {
    "dryRun": false,
    "total": 5,
    "succeed": 5,    // ← 5 条全部成功
    "failed": 0,     // ← 零失败
    "durationMs": 423
  }
}
```

接口告诉我"5 条全部同步成功了"。

---

## 三、问题来了:商户端啥也没变

我登录 `testnova-merchant.yuewen.com/library`,刷新页面看那 5 条剧的"漫剧分发类别"列。

**全是 `--`**。

一条都没变。

我心里"咯噔"一下:**接口返回 succeed=5,但下游显示零变化**。

这是最难排查的那种 bug—— 不报错、不抛异常、统计数据都对,**但实际什么都没发生**。

---

## 四、排查方法论:从最外层一层层剥

我的习惯是**从最外层往里追,每一层都要找到证据**,不靠猜。

### 第 1 层:接口返回是不是真的?

`succeed=5` 是接口自己统计的,会不会是统计逻辑写错了,实际全失败但都被算成成功?

我去 vega 容器(`testvega` 后端机器)看代码里的同步函数 `syncDramaCategoryToComicDramaWeb`:

```java
private void syncOneDramaCategory(Long vdid, Integer dramaCategory, String kind) {
    try {
        Result<Void> rsp = comicDramaWebManager.updateDramaCategory(vdid, dramaCategory);
        if (rsp.isFailure()) {
            Logs.error("[syncDramaCategoryToComicDramaWeb] {} failed, vdid={} ... code={} msg={}",
                    kind, vdid, dramaCategory, rsp.getCode(), rsp.getMessage());
        }
    } catch (Exception e) {
        Logs.error("[syncDramaCategoryToComicDramaWeb] {} exception, ... err={}", ...);
    }
}
```

注意这里是 **try-catch 吞异常**——失败只打日志,不影响外层统计。

也就是说:**接口的 succeed=5 只能说明"调用没抛异常",不能说明"商户端真的写入了"**。

OK,先去看日志。

### 第 2 层:vega 端的日志

进到 vega 容器,搜关键字:

```bash
grep "syncDramaCategoryToComicDramaWeb" /usr/local/trpc/log/error.log | tail -20
```

**一搜一大把红字**:

```
ERROR [syncDramaCategoryToComicDramaWeb] main failed, vdid=35680737004634401 dramaCategory=3 
       code=-40003 msg=comicDramaWeb updateDramaCategory exception: transform response failed
ERROR [syncDramaCategoryToComicDramaWeb] list sub drama exception, mvdid=35680737004634401, 
       err=not find func:/trpc.ywnrzx.ccshortplayserver.ShortPlayQuery/ListVideoInfosByMvdid
```

5 条主剧同步**全失败了**,但接口还是返回 succeed=5。看到两个不同的错误:

1. **HTTP 调用商户端**:`-40003 transform response failed`
2. **TRPC 调内容中心**:`not find func ListVideoInfosByMvdid`

这就是**异常分层吞掉了真实状态**——上层只看到"没抛异常",下层日志里全是失败。

> 这是个**设计问题**,后面我会改:让 backfill 接口返回里区分"调用成功"和"实际写入成功"。

### 第 3 层:`transform response failed` 是什么?

这是 vega 调商户端 HTTP 时的报错。我去看抛异常的源码:

```java
// HttpClientInvoker.java
ResponseSpec resp = HttpRequest.post(url)
        .bodyJson(jsonData)
        .customClient(client)
        .execute()
        .onException((r, e) -> Logs.access("post [{}] failed, ex is {}", ...))
        .onFailure(responseSpec -> Logs.access("post [{}] failed, code is {}", url, responseSpec.statusCode()));
return resp.bodyObject(clazz)
        .getOrElseThrow(() -> new IllegalStateException("transform response failed"));
```

关键:`onFailure` 在 HTTP 状态码非 2xx 时触发,但**只打了一行日志,然后 bodyObject 就失败了**——它压根没解析响应体,直接抛"transform response failed"。

也就是说:**响应是收到了,但状态码不是 200,具体多少没在异常里带出来**。

继续 grep `onFailure` 的日志:

```bash
grep "post.*failed.*code" /usr/local/trpc/log/access.log | tail -5

# 输出:
post [http://testnova-merchant.yuewen.com/api/internal/drama/category] failed, code is 307
post [http://testnova-merchant.yuewen.com/api/internal/drama/category] failed, code is 307
post [http://testnova-merchant.yuewen.com/api/internal/drama/category] failed, code is 307
...
```

**307 重定向**!

并且注意 URL—— **`http://`** 开头。

### 第 4 层:307 + http:// 是什么意思?

**307 Temporary Redirect** 是 HTTP 协议里的临时重定向状态码。

我直接 curl 一下确认:

```bash
curl -I http://testnova-merchant.yuewen.com/api/internal/drama/category

# 输出:
HTTP/1.1 307 Temporary Redirect
Location: https://testnova-merchant.yuewen.com/api/internal/drama/category
```

商户端 nginx 配置了 **HTTP 强制跳 HTTPS**(很常见的安全策略)。所有 `http://` 请求一律 307 重定向到 `https://`。

那为什么 vega 没跟随重定向?

### 第 5 层:OkHttp 的默认行为

vega 底层用 OkHttp 发 HTTP 请求。OkHttp 关于重定向有几个**容易踩的默认行为**:

1. **GET / HEAD 默认会跟随重定向**(包括 301/302/307/308)
2. **POST 在某些版本和某些状态码下不会跟随**——尤其是 **307 和 308** 要求"必须保留方法"
3. **跨协议(http→https)时**,即使能重定向,**也可能被 OkHttp 的 `followSslRedirects` 控制**;如果客户端代码没显式开启,某些场景就直接失败

vega 的 OkHttp 客户端配置里没启用跨协议重定向,所以 POST 收到 307 直接挂在 `onFailure`,响应体都没读就 fail 了。

### 第 6 层:为什么 vega baseUrl 写成 http://?

去看 vega 的配置文件:

```yaml
# vega/src/main/resources/application.yml
comicDramaWeb:
  baseUrl: http://testnova-merchant.yuewen.com   # ← 这里就是错的
  internalSecret: xxx
```

**根因找到了**:vega 项目代码里 baseUrl 默认值写成了 `http://`,而商户端 nginx 强制 https。

---

## 五、修复:不发版,直接改配置

到这里其实最简单的修复是:

```yaml
# 改成 https
comicDramaWeb:
  baseUrl: https://testnova-merchant.yuewen.com
```

但**不需要改代码再发版**——我们用了七彩石(腾讯内部配置中心),线上配置可以覆盖代码里的默认值。

我去七彩石平台 `ywos.solution_vega.config` 分组,新增一条配置:

| Key | Value | Type |
|---|---|---|
| `comicDramaWeb.baseUrl` | `https://testnova-merchant.yuewen.com` | STRING |

发布到 test 环境。vega 这边监听了配置变更,**秒级生效,不用重启**。

重新跑 backfill 接口,再去看 vega access.log:

```bash
grep "comicDramaWeb.updateDramaCategory" /usr/local/trpc/log/access.log | tail -3

# 输出:
[comicDramaWeb.updateDramaCategory] url=https://testnova-merchant.yuewen.com/api/internal/drama/category 
  vdid=34523432804136701 dramaCategory=2 rsp={"code":0,"msg":"成功",...}
```

返回 `code=0` 成功!

去商户端的数据库验证:

```sql
SELECT vdid, drama_category, name FROM video_sync_index 
WHERE vdid = 34523432804136701;

# 之前: drama_category = 0
# 现在: drama_category = 2
```

商户端 `/library` 页面刷新后,"我咔咔"那条剧的"漫剧分发类别"显示**"解说漫"**(对应 dramaCategory=2)。

**链路通了**。

---

## 六、整个排查的方法论复盘

我把这次排查抽象成一个流程,以后跨系统调用出问题都能照着搬:

### 1. **不信接口的 succeed**,要看下游真实状态

接口返回的成功/失败统计,本质是"调用没异常",不代表"业务真的生效"。
**要么验下游日志,要么验下游数据库**。

### 2. **从最外层异常 message 开始倒推**

`transform response failed` 这种泛泛的错误,要去**源码里找抛它的地方**,看上下文(状态码、URL、headers)。

### 3. **关注每一层的"默认值"**

OkHttp 默认 POST 不跟随 307、Nginx 默认强制 https、`onFailure` 默认只打日志不抛异常—— 这些**默认行为是 bug 温床**,排查时要逐个核对。

### 4. **用日志关键字和数据库直查交叉印证**

不能只看一边。我这次:
- vega 容器看 `error.log`(发现 `transform response failed`)
- vega 容器看 `access.log`(发现 `code is 307`)
- 商户端容器看 `access.log`(发现 InternalAuthFilter 日志没有 → 请求根本没到业务层)
- 商户端 MySQL 直查 `video_sync_index`(印证 `drama_category=0`)

**4 条证据交叉**,根因不会跑。

### 5. **能改配置就别改代码**

这次修复方案我有两个选项:

| 方案 | 操作 | 生效时间 | 风险 |
|---|---|---|---|
| A. 改配置 | 七彩石新增覆盖 | 秒级 | 极低,可一键回滚 |
| B. 改代码 | 改 yml + push + CI + 发版 | 小时级 | 需要走完整流程 |

**生产环境的配置变更比代码变更更可控**——可灰度、可回滚、可追溯历史版本。
代码层面的默认值留给开发环境就够了。

### 6. **修完别忘了反思设计**

这次 bug 暴露了 backfill 接口的设计问题:**统计数据看不出真实状态**。

我后续的优化想法:

```java
// 在 BackfillDramaCategoryResult 里加一个区分维度
public class BackfillDramaCategoryResult {
    private int total;              // 候选数
    private int callSucceed;        // 调用没抛异常的(原 succeed)
    private int actuallyWritten;    // 下游真实写入成功的(新增)
    private int callFailed;         // 调用直接失败
    private int writeFailed;        // 调用成功但下游写入失败
    private List<Long> failedVdids;
}
```

**让"调用成功"和"真实写入成功"分开**,以后这种"接口对了下游没动"的 bug 一眼能看出来。

---

## 七、面试怎么讲

### 开场(15 秒)

"2026 年 5 月联调一个跨系统数据回填接口时,接口返回 succeed=5 全部成功,但下游数据库一行都没更新。
我从最外层的接口返回开始一层层倒推,经过 6 层排查,最终定位到是 vega 项目里 baseUrl 默认值写成了 `http://`,
而商户端 nginx 强制 HTTPS 重定向 307,vega 用的 OkHttp 默认不跟随 POST 重定向。
最后没改代码,直接在七彩石覆盖了一条配置,秒级修复。"

### 被问"**你怎么定位到是 baseUrl 配置错的?**"

"我有一套**层层倒推**的习惯:

1. 先怀疑接口返回的 `succeed=5` 是不是真的 → 看代码发现 try-catch 吞了下层异常
2. 去 vega 错误日志找下层报错 → 发现 `transform response failed`
3. 去源码找谁抛这个异常 → OkHttp 的 `bodyObject().getOrElseThrow()`
4. 看相邻的 `onFailure` 日志 → 找到 `code is 307`
5. curl 验证 → 确认是 nginx 强制 https 重定向
6. 去 vega 的 application.yml → 看到 baseUrl 是 `http://` 开头

每一步都要拿到证据,不靠猜。"

### 被问"**为什么 OkHttp POST 不跟随 307?**"

"HTTP 协议规定 **307 Temporary Redirect 必须保留请求方法**——不能像 302 那样把 POST 改成 GET。OkHttp 的实现里,跨协议(http→https)的 POST 重定向有额外的安全限制,
默认 `followSslRedirects` 行为对 POST 比较保守。

但说实话,**没必要 100% 解释清楚 OkHttp 的源码**——面试官想看的是你**遇到这种问题会去查、会去验证**,而不是死背协议细节。"

### 被问"**为什么不直接在代码里改 http→https?**"

"两个原因:

1. **生效速度**:改代码要走 git push、CI 构建、CD 部署,在我们这边一套流程半小时起步;改七彩石配置秒级生效,出问题一键回滚

2. **环境隔离**:不同环境的 baseUrl 本来就该不一样(生产 / 预发 / 测试 / 本地),代码里写死任何一个都不合理。**配置中心是生产实践里管理这种环境差异的标准做法**。

代码里的默认值我后来改成了空字符串 + 启动校验:配置中心没读到 baseUrl 直接 fail-fast,避免开发同学凭代码里的默认值误判。"

### 被问"**你说接口的 succeed 不可信,那应该怎么设计?**"

"backfill 接口的统计数据应该**分维度**:

- `total` 候选数
- `callSucceed` 调用没抛异常
- `actuallyWritten` 下游真实写入成功
- `callFailed` 调用层失败
- `writeFailed` 调用成功但写入失败(返回 -20002 等业务码)

让调用方一眼能区分'调用通了没'和'写入成功没',不要用一个 succeed 把两件事混在一起。

我在这次 bug 之后准备给接口加上这个改动。"

### 被问"**类似的坑你还见过吗?**"

"见过两个类似的:

1. **TRPC 接口客户端 SDK 升级了,服务端没升级** → 报 `not find func`,跟这次的 not find func 是同一个项目里另一个伴生的坑。我后来定了个规则:**升级 RPC SDK 前,先去看服务端是否已经部署对应版本**。

2. **跨域 cookie 没带** → 浏览器 fetch 请求没加 `credentials: 'include'`,后端报 401。这种坑也是源于"默认值不友好"。

总结一下:**默认值是 bug 的温床**,凡是 HTTP / RPC / 序列化框架,都要警惕它的隐式默认行为。"

---

## 八、这个 bug 让我学到啥

1. **跨系统调用的协议层细节很重要**:HTTP 状态码、重定向语义、TLS、签名校验,任何一层默认值都可能咬人

2. **`succeed` 数据可能撒谎**:接口的统计永远只反映"它自己看到的状态",真实状态要去下游验

3. **配置中心是生产环境必备**:不仅是为了多环境管理,更是为了**线上紧急修复时不必走发版流程**

4. **"读源码"是排查的基本功**:`transform response failed` 这种泛泛的异常 message,只有看源码才知道它是怎么来的

5. **bug 修完要反思设计**:每次踩坑都是设计反思的机会,这次的 backfill 接口我会加 `actuallyWritten` 字段,让以后的人不会再被同样问题坑

---

## 附录:排查命令清单(下次直接抄)

### 容器内查日志

```bash
# 找 vega 容器(后端)
ssh <vega-host>

# 看错误日志
grep "syncDramaCategoryToComicDramaWeb" /usr/local/trpc/log/error.log | tail -20
grep "comicDramaWeb.updateDramaCategory" /usr/local/trpc/log/access.log | tail -10
grep "post.*failed.*code" /usr/local/trpc/log/access.log | tail -5

# 看 TRPC 调用错误
grep "not find func\|TRpcException" /usr/local/trpc/log/error.log | tail -10
```

### 验证 Nginx 重定向

```bash
# 直接 curl 看响应头
curl -I http://testnova-merchant.yuewen.com/api/internal/drama/category

# 期望看到:
# HTTP/1.1 307 Temporary Redirect
# Location: https://...
```

### 数据库直查

```bash
mysql -h <db-host> -P 3306 -u <user> -p<password> <database> \
    -e "SELECT vdid, drama_category, name FROM video_sync_index WHERE vdid IN (...);"
```

### 七彩石覆盖配置

```
1. 打开 https://rainbow.woa.com
2. 进入 ywos.solution_vega.config 分组
3. 添加配置项 key=comicDramaWeb.baseUrl, value=https://xxx, type=STRING
4. 发布到 test 环境
5. vega 监听变更,秒级生效(无需重启)
```

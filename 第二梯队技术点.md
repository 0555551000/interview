# 第二梯队技术点(代码里挖的日常硬活)

> **用法说明**
>
> 这份不是"亮点大合集",是**日常代码里藏的那些"看起来平淡但真做过的人才会处理"的细节**。
> 15 个点全都来自 comic-drama-web 真实代码库,有具体文件路径可追溯。
>
> **面试场景**:
> - 主线亮点讲完了,面试官问"还有别的吗?" → 从这里抛
> - 面试官追问某个技术细节(如 "你们签名怎么防重放") → 这里有具体实现
> - 被问"你日常都在做什么" → 这些就是日常的体现
>
> 每个点都有 **20-30 秒口述模板**,开口即用。

---

## 一、安全 / 签名类(5 个)

### 1. 常量时间签名比较防时序侧信道

**场景**:内部服务 HMAC 签名校验。

**坑**:普通 `String.equals` 遇到第一个不同字符就 return,攻击者能通过响应时间差异一位位试出正确签名。

**解法**:用 XOR 操作**强制遍历完整串**。

```java
// InternalAuthFilter.java 第 133-142 行
private static boolean constantTimeEquals(String a, String b) {
    if (a == null || b == null || a.length() != b.length()) return false;
    int diff = 0;
    for (int i = 0; i < a.length(); i++) {
        diff |= a.charAt(i) ^ b.charAt(i);  // 无分支,永远遍历完
    }
    return diff == 0;
}
```

**📁 文件**:`src/main/java/.../web/filter/InternalAuthFilter.java`

**🎤 面试话术(20 秒)**:

> 我们内部签名校验里用了常量时间比较 — 因为普通 equals 会在第一个不同字符短路返回,攻击者可以通过响应时间差异猜出签名前缀。我们改成 XOR 操作,无论签名对不对,都要遍历完整个字符串。虽然我们是内网,理论上没这风险,但安全这种事宁可过度防御。

---

### 2. 可重复读的 Request Body 包装器

**场景**:Filter 验证签名要读 body,后面 Controller `@RequestBody` 也要读。

**坑**:Servlet 规范规定 body 只能读一次。

**解法**:`HttpServletRequestWrapper` 把 body 缓存成 byte 数组,每次读新建 `ByteArrayInputStream`。

```java
private static class CachedBodyRequestWrapper extends HttpServletRequestWrapper {
    private final byte[] body;

    CachedBodyRequestWrapper(HttpServletRequest request) throws IOException {
        super(request);
        this.body = StreamUtils.copyToByteArray(request.getInputStream());
    }

    @Override
    public ServletInputStream getInputStream() {
        ByteArrayInputStream bais = new ByteArrayInputStream(body);
        return new ServletInputStream() {
            public int read() { return bais.read(); }
            public boolean isFinished() { return bais.available() == 0; }
            public boolean isReady() { return true; }
            public void setReadListener(ReadListener l) {}
        };
    }
}
```

**📁 文件**:`InternalAuthFilter.java` 第 148-193 行

**🎤 面试话术**:

> Servlet 的 InputStream 只能读一次,但我们 Filter 里要读 body 算签名,Controller 的 @RequestBody 也要读一次。我写了个 HttpServletRequestWrapper,第一次读的时候把 body 缓存成 byte 数组,然后每次 getInputStream 都返回一个新的 ByteArrayInputStream 从这个数组读,这样可以无限次读。Spring 内部也有类似的 ContentCachingRequestWrapper,思路一样。

---

### 3. HMAC 签名的查询参数规范化(TreeMap + URL 编码)

**场景**:多个服务共同校验同一个请求签名。

**坑 1**:HashMap 迭代顺序随机,两端拼出来的字符串不一样,签名对不上。

**坑 2**:`URLEncoder.encode` 默认把空格编码为 `+`,HTTP 标准期望 `%20`。

**解法**:`TreeMap` 强制字典序 + `replace("+", "%20")`。

```java
public static String canonicalQuery(Map<String, String> params) {
    if (params == null || params.isEmpty()) return "";
    TreeMap<String, String> sorted = new TreeMap<>(params);  // 字典序
    return sorted.entrySet().stream()
            .map(e -> e.getKey() + "=" + urlEncode(StringUtils.defaultString(e.getValue())))
            .collect(Collectors.joining("&"));
}

private static String urlEncode(String s) {
    return URLEncoder.encode(s, StandardCharsets.UTF_8).replace("+", "%20");  // 关键
}
```

**📁 文件**:`util/InternalSignUtil.java` 第 77-85 行

**🎤 面试话术**:

> 跨系统签名有两个坑:一是 HashMap 顺序不定,两端生成的签名串不一样,所以我们用 TreeMap 强制字典序排序;二是 URLEncoder 默认把空格编码为加号,但 HTTP 标准通常用 %20,我们显式做了替换。这种细节不做的话联调半天找不到原因。

---

### 4. AES 加密带时间戳的时效性设计

**场景**:生成短期有效的操作码 / 分享链接,需要自动过期。

**解法**:加密时把 `userId|timestamp` 一起加密,解密时校验时间戳是否超 10 分钟。

```java
public static String encrypt(Long userId) {
    long timestamp = System.currentTimeMillis();
    String plainText = userId + "|" + timestamp;   // 明文带时间戳一起加密
    // AES 加密返回 Base64
}

public static Long decrypt(String token) {
    // AES 解密
    String[] parts = plain.split("\\|");
    long userId = Long.parseLong(parts[0]);
    long timestamp = Long.parseLong(parts[1]);
    if (System.currentTimeMillis() - timestamp > EXPIRY_TIME_MS) {
        return null;  // 10 分钟过期
    }
    return userId;
}
```

**📁 文件**:`util/MerchantUserIdCryptoUtil.java` 第 37-110 行

**🎤 面试话术**:

> 这个工具类有个巧思:加密前把用户 ID 和当前时间戳拼在一起再加密,解密时不仅恢复 ID,还要校验时间戳超没超 10 分钟。即使密文泄露,也只能用 10 分钟。不用额外存数据库或 Redis,密文自带"保质期"。典型用于临时操作码、分享链接这种短期场景。

---

### 5. 扫描 @SkipLogin 注解的启动时实现

**场景**:部分接口(注册、健康检查)不需要登录,用注解标记。

**难点**:Spring 给 Controller 生成 CGLib 代理,直接 `bean.getClass()` 拿到的是代理类,**找不到原始注解**。

**解法**:`AopUtils.getTargetClass(bean)` 拿原始类,再反射扫方法。

```java
@Service
public class SkipLoginPathScanner implements ApplicationContextAware {
    private final Set<String> skipLoginPaths = new HashSet<>();

    @PostConstruct
    public void scan() {
        String[] controllers = applicationContext.getBeanNamesForAnnotation(Controller.class);
        for (String name : controllers) {
            Object bean = applicationContext.getBean(name);
            Class<?> beanClass = AopUtils.getTargetClass(bean);  // 关键:不是 bean.getClass()!
            String baseUrl = getBaseUrl(beanClass);
            Arrays.stream(beanClass.getMethods())
                .filter(m -> m.isAnnotationPresent(SkipLogin.class))
                .forEach(m -> skipLoginPaths.add(normalizeUrl(baseUrl, extractMethodUrl(m))));
        }
    }
}
```

**📁 文件**:`web/SkipLoginPathScanner.java`

**🎤 面试话术**:

> 我们用 @SkipLogin 注解标记免登录接口,启动时扫所有 Controller 把路径收集到 HashSet,Filter 里查这个集合 O(1) 判断。有个坑:Spring 会给 Controller 生成 CGLib 代理,如果直接 bean.getClass() 拿到的是代理类,找不到原始的 @SkipLogin 注解。必须用 AopUtils.getTargetClass(bean) 拿原始类。这个细节我踩过一次才明白。

---

## 二、并发 / 上下文类(3 个)

### 6. TransmittableThreadLocal + 离线上下文

**场景**:Controller 里用 SessionContext 拿用户信息,但异步任务 / 定时 Job 里没登录态,拿不到会 NPE。

**两个设计**:
1. 用阿里 **TransmittableThreadLocal** 代替普通 ThreadLocal — 线程池提交任务时自动透传父线程上下文
2. 提供 **`createOfflineContext()`** — Job 启动时创建一个虚拟用户(`userId = -1`),所有 getXxx 都能工作

```java
private static final ThreadLocal<SessionContext> SESSION_THREAD_LOCAL
        = new TransmittableThreadLocal<>();

// Job 用这个
public static SessionContext createOfflineContext() {
    SessionContext ctx = new SessionContext();
    ctx.sessionId = FastUUID.nextId();
    ctx.user = User.builder().userId(-1L).email("").build();
    ctx.request = Request.builder().requestIp("127.0.0.1").build();
    SESSION_THREAD_LOCAL.set(ctx);
    return ctx;
}
```

**📁 文件**:`helper/SessionContext.java` 第 41、53-60 行

**🎤 面试话术**:

> ThreadLocal 在线程池场景会丢失上下文,我们用阿里的 TransmittableThreadLocal(TTL),它会在线程池提交任务时自动把父线程的上下文拷贝过去。另外对定时 Job 这种没有真实用户的场景,我们提供了 createOfflineContext 方法,创建一个 userId = -1 的虚拟上下文,这样 Job 里调 SessionContext.getUser() 就不会 NPE,而且日志里能明显区分"这是 Job 操作,不是用户操作"。

---

### 7. Redis 锁模板方法(Supplier + try-finally)

**场景**:定时任务多机部署防并发,需要 Redis 分布式锁。

**坑**:手动 try-catch-finally 释放锁到处写,总有人忘。

**解法**:封装模板方法,使用方只传 lambda 业务逻辑:

```java
public <T> Result<T> handleLock(String lockKey, Supplier<Result<T>> supplier) {
    Result<RedisLock> lockResult = obtainLock(lockKey);
    RedisLock lock = lockResult.getValue();
    if (!lockResult.isSuccessful() || lock == null) {
        return BusinessCode.LOCK_TOO_FAST_ERROR.toResult();
    }
    try {
        return supplier.get();   // 业务逻辑
    } finally {
        unLock(lock);            // 一定释放
    }
}

// 使用方:
redisLockService.handleLock(lockKey, () -> {
    doSync();
    return BusinessCode.SUCCESS.toResult();
});
```

**📁 文件**:`service/RedisLockService.java` 第 98-111 行

**🎤 面试话术**:

> 分布式锁最容易忘释放。我封装了个模板方法,内部处理获锁和 finally 释放,使用方只需要传一个 lambda 表达式写业务逻辑。即使业务抛异常 finally 也会释放,调用方零风险。比让每个人手写 try-catch-finally 清爽多了。这是典型的"模板方法模式" + Java 8 Supplier 接口。

---

### 8. 异步线程池的 ThreadFactory + CallerRunsPolicy

**场景**:IO 密集型异步任务的线程池。

**两个细节**:
1. **自定义 ThreadFactory** 给线程起有意义的名字(`async-io-pool-1`) — 生产排查线程堆栈时秒定位
2. **用 CallerRunsPolicy** 而不是默认的 AbortPolicy — 队列满时让调用线程同步执行,**宁可阻塞也不丢任务**

```java
this.ioIntensiveExecutor = new ThreadPoolExecutor(
    corePoolSize, corePoolSize,
    KEEP_ALIVE_SECONDS, TimeUnit.SECONDS,
    new LinkedBlockingQueue<>(QUEUE_CAPACITY),
    new ThreadFactory() {
        private final AtomicInteger threadNumber = new AtomicInteger(1);
        public Thread newThread(Runnable r) {
            Thread t = new Thread(r, "async-io-pool-" + threadNumber.getAndIncrement());
            t.setDaemon(true);
            return t;
        }
    },
    new ThreadPoolExecutor.CallerRunsPolicy()   // 队列满让调用线程自己跑
);
```

**📁 文件**:`util/AsyncTaskExecutorUtil.java` 第 30-49 行

**🎤 面试话术**:

> 配线程池我有两个习惯:一是自定义 ThreadFactory 给线程起有意义的名字,比如 `async-io-pool-1`,生产环境看 jstack 线程堆栈时能立刻定位;二是拒绝策略选 CallerRunsPolicy 而不是默认的 AbortPolicy — 队列满了让提交任务的线程自己跑,宁可阻塞也不丢任务。这个权衡对业务类任务很重要,金融结算、订单创建这种场景丢任务后果严重。

---

## 三、权限 / 业务类(2 个)

### 9. 四级层级权限的动态级别计算

**场景**:公司(coid) → 业务线(coopStatus) → 产品(productId) → 租户(tenantId) 四级权限。

**难点**:每个请求的 header 中这四个值组合不同,要判断用户在哪一级,后面查权限表用这个级别过滤数据。

```java
private static LevelTypeEnum computeLevel(long coid, int coopStatus, long productId, long tenantId) {
    if (coid > 0 && coopStatus > 0 && productId > 0 && tenantId > 0)
        return LevelTypeEnum.TENANT;
    else if (coid > 0 && coopStatus > 0 && productId > 0)
        return LevelTypeEnum.PRODUCT;
    else if (coid > 0 && coopStatus > 0)
        return LevelTypeEnum.COOPSTATUS;
    else if (coid >= 0)       // coid = 0 是系统管理员
        return LevelTypeEnum.COID;
    else
        throw new IllegalArgumentException("workSpace header is invalid");
}
```

**📁 文件**:`helper/SessionContext.java` 第 209-227 行

**🎤 面试话术**:

> 我们权限是四层嵌套结构:公司→业务线→产品→租户。每个请求的 header 里可能传不同组合的值。我写了一个级别计算函数,从细到粗判断:如果四个维度都有值,用户是租户级;少一个就是产品级,以此类推。特殊细节是 coid=0 代表"系统管理员"(跨所有公司),所以用 >= 0 而不是 > 0。这个级别后面用来查权限表过滤数据。

---

### 10. Lombok @Delegate 简化对象委托

**场景**:SessionContext 内部有 User、WorkSpace、Authority 等多个子对象,外层要暴露它们的方法。

**笨办法**:手写 `getUserId(){ return user.getUserId(); }` 一堆代理方法。

**优雅办法**:`@Delegate` 注解,Lombok 编译时自动生成所有公共方法的代理。

```java
public final class SessionContext {
    @Delegate private User user;
    @Delegate private WorkSpace workSpace;
    @Delegate private Request request;
    @Delegate private Authority authority;
    // 外层直接用 sessionContext.getUserId() 会自动转到 user.getUserId()
}
```

**📁 文件**:`helper/SessionContext.java` 第 28-38 行

**🎤 面试话术**:

> SessionContext 内部聚合了 User、WorkSpace 等几个子对象,外层调用要暴露所有子对象的方法。我没手写 50 多个 getXXX 代理方法,直接用 Lombok 的 @Delegate 注解。Lombok 编译期自动生成所有公共方法的代理,`sessionContext.getUserId()` 自动转发到 `user.getUserId()`。省代码又类型安全。

---

## 四、工程细节类(5 个)

### 11. ConcurrentHashMap + computeIfAbsent 做 enum 查询缓存

**场景**:经常按 code 查错误码对应的 enum 值。

**笨办法**:每次遍历 `ErrorCode.values()`。

**优雅办法**:用 Map 缓存,`computeIfAbsent` 原子"检查-插入"避免重复计算。

```java
private static Map<Integer, ErrorCode> innerMap = new HashMap<>();

public static ErrorCode get(int code) {
    return innerMap.computeIfAbsent(code, key -> {
        for (ErrorCode errorCode : ErrorCode.values()) {
            if (errorCode.getCode() == key) return errorCode;
        }
        return null;
    });
}
```

**📁 文件**:`constant/ErrorCode.java` 第 102-111 行

**🎤 面试话术**:

> 错误码枚举很多,每次按 code 查要遍历所有 values 值。我用 Map 做缓存,关键是用 computeIfAbsent 而不是先 check 再 put — 这是一个原子操作,即使多线程同时查同一个 code,也只会计算一次。小细节但对并发场景很重要。

---

### 12. 时间戳长度智能识别(秒级 vs 毫秒级)

**场景**:业务各处时间戳格式混乱,有秒级(10 位)也有毫秒级(13 位)。

**解法**:用字符串长度自动判断并智能转换。

```java
public static boolean isSecsStamp(long timestamp) {
    return String.valueOf(timestamp).length() == 10;
}

public static long appendMillisOrRaw(long timestamp) {
    return isSecsStamp(timestamp) ? TimeUnit.SECONDS.toMillis(timestamp) : timestamp;
}

public static long trimMillisOrRaw(long timestamp) {
    return isSecsStamp(timestamp) ? timestamp : TimeUnit.MILLISECONDS.toSeconds(timestamp);
}
```

**📁 文件**:`util/TimeUtil.java` 第 124-130 行

**🎤 面试话术**:

> 业务场景时间戳格式混乱,有秒级有毫秒级。我写了两个智能转换方法,`appendMillisOrRaw` 保证返回毫秒,`trimMillisOrRaw` 保证返回秒,用字符串长度(10 位 vs 13 位)自动判断输入格式,已经是目标格式就不转。调用方不用纠结输入,框架自动处理。

---

### 13. 腾讯广告 XML 同步的完整链路(本地 → COS → CDN)

**场景**:定时 Job 生成腾讯广告商品 XML,上传 COS 后要刷新 CDN。

**完整链路**:
1. 生成 XML 写本地文件
2. 上传 COS(从 URL 反推 filePath)
3. 刷新 CDN(不刷新前端看到旧数据)
4. `finally` 清理本地文件

```java
private String uploadXml(File file) {
    try (InputStream inputStream = Files.newInputStream(file.toPath())) {
        String currentFileUrl = hotConfig.getCurrentAdqGoodsFile();
        // 从 URL 反推 COS filePath
        String filePath = currentFileUrl.substring(currentFileUrl.indexOf(".com/") + 5);

        // 1. 上传 COS
        COSUtil.putObject(DEFAULT_REGION, DEFAULT_BUCKET, filePath, inputStream);

        // 2. 刷新 CDN
        String url = String.format(CDN_TEMPLATE, DEFAULT_BUCKET, filePath);
        COSUtil.refreshCDN(Collections.singletonList(url));
        return url;
    } finally {
        deleteLocalFile(file);  // 清理本地,避免磁盘满
    }
}
```

**📁 文件**:`job/AdqGoodsSyncJob.java` 第 150-280 行

**🎤 面试话术**:

> 腾讯广告 ADQ 需要定时刷新商品 XML。我们的做法是生成到本地 → 上传腾讯云 COS → 刷新 CDN → 清理本地。关键细节:上传后一定要刷新 CDN,不然前端拿到的还是旧版本;finally 块一定要清理本地文件,不然日积月累磁盘满了直接服务挂。涉及多个系统的流程,每一步错误处理和资源清理都要想清楚。

---

### 14. 微信套餐告警的累计值差异计算

**场景**:微信小程序流量套餐,需要每天统计**新增消费**并告警。

**难点**:微信接口只返回"本月累计消费",不返回"今天新增消费"。

**解法**:Redis 存每天的累计值,今天累计 - 昨天累计 = 今日新增。处理累计回溯的边界情况(`Math.max(0, ...)`)。

```java
// 今天的累计
long totalUsed = current.getTotalUsed();

// 从 Redis 读昨天累计
String yKey = buildRedisKey(miniAppId, type, LocalDate.now().minusDays(1));
long yesterdayCumulative = Long.parseLong(
    cacheRedisTemplate.execute(yKey, jedis -> jedis.get(yKey)) != null
        ? cacheRedisTemplate.execute(yKey, jedis -> jedis.get(yKey))
        : "0"
);

// 新增 = 今天累计 - 昨天累计,负数按 0 处理(运营可能回溯修正数据)
long actualNew = totalUsed >= yesterdayCumulative ? (totalUsed - yesterdayCumulative) : 0L;

// 记录今天累计,30 天过期
String tKey = buildRedisKey(miniAppId, type, LocalDate.now());
cacheRedisTemplate.execute(tKey, jedis ->
    jedis.setex(tKey, 30 * 24 * 3600, String.valueOf(totalUsed)));
```

**📁 文件**:`job/WxPackageAlertJob.java` 第 96-120 行

**🎤 面试话术**:

> 微信接口只给本月累计消费,但我们要告警的是"今天新增"。我用 Redis 记录每天的累计值(key 带日期),今天统计时用今天累计减昨天累计就是新增。边界情况是可能运营会回溯修正数据导致"今天累计"比"昨天累计"小,我用 `Math.max(0, ...)` 兜底避免负数告警。Redis 设 30 天过期,避免数据无限累积。

---

### 15. Spring MVC 接口的前置校验完整性

**场景**:`/api/internal/*` 内部接口的参数校验。

**逐层校验策略**:
1. Filter 层:HMAC 签名、时间戳窗口、Nonce 去重
2. Controller 层:基本参数校验(非空、范围)
3. Service 层:业务规则校验(数据存在性、状态合法性)

```java
// Controller 层:只做基础参数校验
@PostMapping("/drama/category")
public Response<Map<String, Object>> updateDramaCategory(@RequestBody UpdateDramaCategoryReq req) {
    if (req == null) return fail(-20001, "请求体不能为空");
    if (req.getVdid() <= 0) return fail(-20001, "vdid 必须 > 0");
    if (req.getDramaCategory() < 0 || req.getDramaCategory() > 6)
        return fail(-20001, "dramaCategory 取值 0-6");

    // 到 Service 层做业务校验
    boolean ok = contentLibraryService.updateDramaCategory(req.getVdid(), req.getDramaCategory());
    if (!ok) return fail(-20002, "本地无同步记录,忽略更新");
    return ok(Map.of("vdid", req.getVdid(), "dramaCategory", req.getDramaCategory()));
}
```

**🎤 面试话术**:

> 我们的参数校验分三层:Filter 层做身份相关(签名、时间戳、Nonce),Controller 层做基础参数校验(非空、范围),Service 层做业务校验(数据存在性、状态合法性)。分层的好处是每层只关注自己的职责,问题定位快。错误码也分段:-20001 是参数错,-20002 是业务错,-20003 是系统错,看错误码就知道往哪层查。

---

## 使用场景矩阵

### 场景 A:主线讲完,面试官说"还有别的吗?"

抛 **2 个短的**(每个 20 秒):

- "对了,我们的签名验证里用了常量时间比较防时序攻击,这细节挺有意思..."(第 1 个点)
- "还有我用 Lombok 的 @Delegate 简化了 SessionContext 的对象委托,比手写 50 个代理方法舒服多了..."(第 10 个点)

### 场景 B:面试官追问某个细节

- "你们签名怎么防重放?" → 第 1、3 点(常量时间 + 规范化查询)
- "异步上下文怎么传?" → 第 6 点(TTL + 离线上下文)
- "权限怎么过滤数据?" → 第 9 点(四级层级)
- "线程池怎么配?" → 第 8 点(ThreadFactory + CallerRunsPolicy)

### 场景 C:被问"你日常都在做什么"

**抛 3 个**,展示不只是做亮点,也会处理这些琐碎:

- 错误码 Map 缓存(第 11 个)
- 时间戳智能识别(第 12 个)
- 模板方法 + Supplier 简化 Redis 锁使用(第 7 个)

说话模板:

> "日常工作里除了做架构方案,很多时间在处理这种'看起来平淡但背后有坑'的细节。比如 [随便挑一个点],一开始简单做可能就踩坑,处理好了看不出来但线上稳定。"

---

## 讲故事的套路

**每个点都按这个结构讲**:

1. **场景**(10 秒):业务背景
2. **坑**(10 秒):简单做法会出什么问题
3. **解法**(10 秒):我们怎么做的
4. **收尾**(可选):"这个思路在其它场景也能用"

**万能模板**:

> "我们 [业务场景] 的时候,一开始是这么做的 [简单方案]。后来发现 [具体问题 / 坑],比如 [举例]。所以我改成 [具体方案]。这个思路在 [其它场景] 也通用。"

---

## 最后提醒

**别一口气全讲** — 这 15 个点是弹药库,**按需抛出**。一次面试能讲 2-3 个就够了。

**选适合语境的** — 面试官聊到安全就抛签名类的(1-5),聊到并发就抛并发类的(6-8),聊到权限就抛权限类的(9),剩下的(10-15)是工程细节展示"多想一步"的习惯。

**最重要的话术**:

> "这些都是日常,不是架构决策。但这些细节多想一步,生产环境就少一次半夜起床。"

说这句话的时候,你就赢了 — 面试官知道你是真的在一线干过。

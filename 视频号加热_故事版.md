# 视频号加热链接(Webhook 接收服务)—— 唠嗑版

> 朋友问我"最近做啥奇葩需求了？"，我说"接微信推给我的数据呗。"
> 他："这不就是写个接口吗？"
> 我："就是写个接口，但是坑巨多。"
> 这份文档就是把"为啥这么简单的需求搞了两周"的故事讲清楚。

---

## 一、需求来了：一个看起来很简单的活

2026 年 2 月，PM 小苏来找我："彭哥，接个需求，**视频号加热数据**。"

我："啥是加热？"

她："就是你在视频号给自己的短剧投广告，微信后台给你推曝光、扣费、广告付费这些数据。原来是运营每天去后台下载 Excel，再整理进系统，**T+1 滞后**，代投那边看不到实时数据，投放效果判断慢。"

我："那用他们的 API 查呗？"

她："**他们不提供查询接口**，只有**推送**——你给他们一个 HTTPS 链接，他们把数据推给你。"

我："OK，那就是写个 Webhook 接收接口。"

她："对。但是有几个硬性要求……"（她翻文档）

1. **HTTPS**，HTTP 不行
2. **3 秒内必须返回 200**
3. **域名必须备案**，不能用 IP
4. **必须 POST**

我看了一眼："这就是 Webhook 标配啊。"

她："对对对，就是看着简单。你估个工期？"

我想了想："3 天吧，一天接口，一天测试，一天联调。"

（后来我才知道，这个估时错得有多离谱。最后做了**两周**。）

---

## 二、文档看完之后才发现：事情没那么简单

回去翻微信给的技术文档，仔细读到以下几个点：

### 坑点 1：3 秒是硬约束

文档原话："**如果 3 秒内未收到 HTTP 200 响应，我方将判定该链接异常，降低推送频率，严重时暂停推送。**"

我第一反应："那我就在接口里做完所有事儿呗。反正量也不大，能多慢？"

然后我做了个心算：
- Nginx 层 TLS 握手 + 转发：**~5ms**
- Spring MVC 反序列化 JSON：**~2ms**
- Redis 查去重：**~1-2ms**（正常情况）
- MySQL 写明细：**~5ms**（正常情况）
- 返回 200：**~1ms**

正常情况：**15ms 左右**，完全没问题。

但是——

- Redis 偶发抖动：**20ms+**
- MySQL 慢查询或锁等：**500ms+**
- JVM Full GC：**200-500ms**（每小时可能发生 1 次）
- 高峰期线程池满：**几秒的排队**

**最坏情况可能达到 2-3 秒**，贴在 3 秒红线上写代码，**上线之后一出问题就雪崩**。

我立刻改主意："**不行，接收和处理必须解耦。接收只做'收下 + 立刻回 200'，处理全部异步。**"

### 坑点 2：金额单位是微分

文档里一行字："`action_value` 是金额，单位是 10⁻⁶ 元。"

我：10 的负 6 次方？那**实际金额 = action_value / 1,000,000**？

对。也就是 `1,234,567` 这样的数字，其实代表 **1.234567 元**。

我的第一反应是：`double amount = value / 1000000.0;`

然后我想起**老周以前讲过金额永远别用 double**，容易丢精度。于是改成：

```java
BigDecimal amt = BigDecimal.valueOf(value)
    .divide(BigDecimal.valueOf(1_000_000), 2, RoundingMode.HALF_UP);
```

入库时按"元"精度 2 位存。

**看起来没问题对吧？**

但是我仔细想了下：用户一天可能有几十万条事件，每条都 round 到 2 位小数，累加求和后**尾数误差会越来越大**。财务对账时发现差了几分钱、几毛钱，够他们追着我骂一周。

最后方案：**全程用 long 存微分，展示层才换算**。

```java
// DB 字段
@Column long action_value_micro;

// 聚合 SQL:精确求和
SELECT SUM(action_value_micro) FROM event WHERE ...;

// 最后一步展示层换算（只做一次 round）
public String toYuan(long micro) {
    return new BigDecimal(micro)
        .divide(BigDecimal.valueOf(1_000_000), 6, HALF_UP)
        .setScale(2, HALF_UP)
        .toPlainString();
}
```

财务对账：**一分钱不差**。（这块 PM 后来跟财务李姐汇报时，李姐给了我一个大拇指）

### 坑点 3：微信会重推

文档里另一句话："**为保证数据可靠性，我方在未收到有效响应时会重推。**"

也就是**至少一次语义**（at-least-once），**不保证不重复**。

如果我没做去重：
- 同一曝光事件进库 2 次 → **曝光数虚高**
- 同一扣费事件入库 2 次 → **钱算重了**
- 代投方看到虚高数据 → **决策做错**

必须有强去重。好在每条消息都有 `unique_id`。

### 坑点 4：去重怎么做

第一想法：**Redis SETNX** + DB 唯一索引。

但是 Redis SETNX 每条都要一次网络 IO，高峰期又贴近 3 秒红线。而且我一天几十万条事件，**Redis key 数量爆炸**。

后来查资料，发现 Redis 4.0+ 的 **`RedisBloom` 模块**支持布隆过滤器。

想法升级：
- **第一层 Bloom Filter**：99% 的"一定没重复"可以不用精确查
- **第二层 SETNX**：疑似重复才走精确判断
- **第三层 DB 唯一索引**：兜底，Redis 挂了也不会重复入库

三层去重，完美。（详见实现版）

### 坑点 5：多客户

文档没写，但 PM 说："**目前只有瓯源 1 家代投方，以后会有多家。**"

也就是说我设计的时候要考虑：
- 每家配的监测链接必须不同
- 每家看到的数据不能交叉
- **新增一家客户代码不能重写**

这个如果一开始不做，以后加第二家客户就要重构。我就决定一步到位。

---

## 三、方案设计（我是怎么想的）

### 3.1 第一版设计：接收 → 入队 → 异步处理

```
[微信]
  ↓ HTTPS POST
[Nginx]（TLS 终止）
  ↓
[Spring Boot 接口]  只做 3 件事
  ① 最简校验（签名、客户）
  ② 原始字节进内存队列
  ③ 立即 return 200
  ↓（队列）
[后台线程 Consumer]
  ① 解析 JSON
  ② 三层去重
  ③ 发 TDMQ
  ↓
[Worker]
  ① 落 MySQL 明细（对账用）
  ② 更新聚合表（看板用）
  ③ 更新 Redis ZSet（实时大盘用）
  ↓
[看板 + 企微推送]
```

### 3.2 内存队列选什么？

最开始我用的是 `LinkedBlockingQueue`——Java 标准库，写起来方便。

但老周 CR 时问我："**你这个 `offer` 入队会阻塞吗？**"

我："不会，我用的是 `offer(e, timeout, TimeUnit)`。"

他："那你超时设置了多少？"

我："50ms。"

他："Webhook 接收是关键路径，50ms 的阻塞你扛得住吗？万一消费变慢导致队列一直满，每个请求都阻塞 50ms，你接口 P99 就炸了。"

我："……有道理。那用什么？"

他："**Disruptor** 看过吗？LMAX 那个。"

我："听说过，没用过。"

他："去试试，**无锁环形队列，入队是纳秒级**。我们组另一个项目用它接日志，百万 TPS 都没问题。"

周末我研究了下 Disruptor，核心是 **RingBuffer + CAS**，生产者消费者都是无锁的，吞吐比 `LinkedBlockingQueue` 高一个数量级。

改完之后：
- 入队 P99：**< 500μs**（之前 50ms 阻塞）
- 队列满时策略：`offer()` 返回 false，**落磁盘兜底**（不阻塞接口）

### 3.3 磁盘兜底

队列满了怎么办？常规做法有三种：
1. 阻塞等待 → 接口超时
2. 丢弃 → 数据丢失
3. **落磁盘** → 性能好、不丢数据

我选的是 3：

```java
if (!ringBuffer.offer(rawEvent)) {
    fallbackFileWriter.append(rawEvent);  // 顺序追加写磁盘
}
```

磁盘文件有个后台 Job 每分钟扫一次，把数据补录进 MQ。

**磁盘顺序写有多快？** SSD 顺序写 500MB/s 起步，比任何 MQ 都快。

老周看完："**这个设计不错，打了个安全网。**"

（实际上线后，**磁盘兜底从来没触发过**——我的量级下根本用不上。但它是一道"就算量涨 10 倍也能扛"的保险。）

### 3.4 多客户怎么做

两个方案：

**方案 A**：每个客户一个独立 URL
```
https://our.com/webhook/oyuan
https://our.com/webhook/kehu2
```

**方案 B**：同一个 URL + 客户 key 参数
```
https://our.com/webhook?c=oyuan&sig=xxx
```

方案 A 每加一家客户要改 Controller，麻烦。方案 B 只改数据库配置。

我选 B。加了个 `client_config` 表：

```sql
CREATE TABLE wechat_heat_client (
    id BIGINT PRIMARY KEY,
    client_key VARCHAR(32) UNIQUE,
    client_name VARCHAR(100),
    secret_key VARCHAR(128),    -- 签名用
    enabled TINYINT DEFAULT 1,
    shadow TINYINT DEFAULT 0,   -- 影子模式,只接收不入库
    wecom_webhook_url VARCHAR(500)
);
```

新加一家客户 = 插一行数据。**代码不改**。

**灰度开关 `shadow`** 也是临时加的——预研阶段接收新客户时，我希望"只接收、不入业务库、留审计"，万一协议对不齐可以回滚。后来真用上了一次，救了我一次。

---

## 四、上线那天：被微信限流的 30 分钟

上线当天下午 2 点，灰度 10% → 观察 30 分钟 → 全量。

全量**半小时**后，监控告警响了：**Webhook P99 响应 1.8 秒**。

我："……黄了。" （贴着 3 秒了）

一边看日志一边想办法：发现**大部分请求都在 Redis 等锁**。因为我第一版的去重代码是这样的：

```java
// ❌ 第一版:接口里直接做 SETNX
Boolean ok = redisTemplate.opsForValue().setIfAbsent(
    "heat:dup:" + uniqueId, "1", Duration.ofHours(48));
```

每个请求都走 Redis 一次网络。**高峰期 Redis 偶发抖动到 300ms**，P99 就飙了。

然后更糟：3 点钟微信推送明显**变稀疏**——"怎么感觉推送频率降了一半？"

去控制台一查，微信后台显示："**监测链接响应异常，已临时降低推送频率至原频率 30%**"。

完蛋，被限流了。

立马应急：
1. **运维同学**帮忙把 Nginx `proxy_read_timeout` 从 2s 调到 5s
2. **我**立刻把接口内的 Redis 操作全部挪到后台 Consumer（按前面设计的那样，之前偷懒没做）
3. 接口只保留最核心的"入队 + 返回 200"

改完重新发布，**15 分钟**后 P99 从 1.8s 降到 **40ms**。

观察 1 小时，微信推送频率**自动恢复**。

晚上复盘，我在群里发："**今天下午被微信降频 30 分钟，教训：不要贴着 SLA 红线写代码，一次抖动就可能触发降级。以后所有涉及第三方回调的接口，响应时间留 10x buffer。**"

老周回："**好教训，赞。**"

---

## 五、其他几个小坑

### 坑：unique_id 偶发为空

上线第二周，发现有重复数据进库——诡异的是，我明明做了三层去重。

一查日志，发现有些事件 `unique_id` 是 null，三层全失效。

我猜是微信在某些特殊场景下（比如补推）没带 unique_id。

修复方式：**自己造一个代理 ID**

```java
String dedupKey = Optional.ofNullable(event.getUniqueId())
    .orElseGet(() -> md5(event.getActionTs() + "-"
                          + event.getDramaId() + "-"
                          + event.getActionType() + "-"
                          + event.getActionValue()));
```

四个字段拼起来 MD5，足够唯一。

### 坑：IP 白名单

我一开始想"微信推送的 IP 肯定是固定段"，搞个白名单提升安全性。

结果去找 IP，**文档里没给**。

问客服："**动态分配的，会变。**"

那白名单咋搞？

我的做法：
- 上线后**收集实际推送的 IP 段**
- 加到 Nginx 做**软白名单**（不在白名单里**只告警不拒绝**）
- 每季度人工 review 一次，加新 IP

**为啥"软"白名单**？因为万一微信突然换 IP 段了，我不想因为白名单拒绝导致数据丢失。**宁可多收假数据（靠签名拦截），不能错杀真数据**。

---

## 六、上线两个月，业务数据

- 推送频率：日常约 **30-50 TPS**，大促当天**峰值 ~500 TPS**
- P99 响应：**30-50ms** 日常，峰值下 **<100ms**（3 秒红线远远没压力）
- 数据滞后：从**T+1 日**（运营手动导）降到**1 分钟内**（实时接收）
- 运营效率：原来每天花 2 小时整理 Excel，现在**完全自动化**
- 客户满意度：代投方瓯源的人说"终于能看到实时数据了"，发了**锦旗**（集团产品给挂在茶水间）

---

## 七、面试怎么讲

### 开场（15 秒）

"这是我 2026 年初做的视频号加热数据接收服务。本质是一个面向微信的 Webhook 接收站——我方开接口，微信把加热的曝光/扣费/广告付费数据实时推过来。"

### 如果被问"**简单需求而已吧？**"（压力面）

"表面简单，但有**四个技术挑战**：
1. 微信**3 秒硬超时**，贴着这个线不能写代码
2. 微信**不保证不重复**，需要强去重
3. 金额是**10⁻⁶ 元微分**，精度要求严
4. 多客户设计，**新增 0 改代码**"

### 如果被问"**3 秒超时怎么设计？**"

"接收和处理彻底解耦：
- **Disruptor 环形队列**做接收，入队纳秒级
- **本地磁盘兜底**，队列满了也不丢
- Redis、DB 等有网络抖动风险的操作**全部异步**
- 接口只做最简校验 + 入队 + return 200"

### 如果被问"**当前 TPS 不高啊，为什么搞这么重？**"

"坦白讲，日常流量只有几十 TPS，确实**同步方案也能跑**。
但有两个考虑：
1. 微信 3 秒是**硬约束**，一次抖动就触发降频——**上线当天我真踩过**
2. 产品规划以后会有**N 家客户**，今年为未来 10x 流量预留，比明年推翻重写成本低得多"

**这个答案比死扛'性能好'更真实**，也是大牛思维。

---

## 八、这个项目让我学到啥

1. **小需求不一定简单**：看起来是"写个接口"，实际涉及性能、幂等、金额精度、多租户五个维度
2. **贴着 SLA 写代码是自杀**：任何第三方回调接口，响应时间留 10x buffer
3. **金额永远用 long**：不是 double，甚至不是 BigDecimal（除非精度需求明确）
4. **设计要想到未来 5-10x**：不过度但要留空间，多花半天设计省一个月重构
5. **上线要观察一小时**：别发完就下班，那天幸亏我在工位，不然被降频一晚上第二天就要写故障复盘

---

> 🔗 技术细节版见《视频号加热链接_实现.md》
> 面试前 5 分钟看这个，心里就有底了。

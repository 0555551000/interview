# 剧集池管理 —— 唠嗑版

> 这个需求是 2026 年 4 月底做的。
> 看起来就是"加几张表 + 写几个 CRUD 接口",CRUD 项目做过一万个对吧?
> 但做进去才发现:**数据模型抉择 + 权限体系 + 发布链路 + Jackson 序列化**,
> 一套组合拳打下来,**后端该踩的坑一次性全踩了**。

---

## 一、需求起源:商业模式升级

2026 年 4 月中,产品同学拿着 PRD 找我:

> "我们现在所有漫剧对所有签约客户都开放,没法做'独家授权'的玩法。运营想给分销商一段**独占期**——'你先投 3 天,卖得好再释放给别人',相当于让分销商愿意花大价钱拿独家。现在没法做,他们商务那边谈得很别扭。"

我点了下需求文档,标题叫《剧集池管理》,核心:

1. **公共池**:一个剧被放进去,**所有分配了该链路的客户**都能看到、都能分发
2. **独家池**:一个剧被放进去,**只有指定的那一个分销商**能看到
3. **保护期**:独家有 1/3/7/14 天的保护期,到期后可"释放到公共池"
4. **业务链路**:区分"抖音端原生"、"快手端原生"、"微信小程序免费"三条线

面试常听的"商业模式抽象落到技术上",就是这种需求。

---

## 二、需求到底是啥(人话版)

### 2.1 "池"的含义

剧集就像**超市货架上的货**:

- **公共池** = 放在开放货架上,所有客户都能拿
- **独家池** = 给某客户锁在柜子里,钥匙只有他有

运营的工作就是把每个剧"放进哪个池 / 给谁独家 / 多久后释放"。

### 2.2 数据模型的抉择 —— 单表还是两张表?

我纠结了半天:

**方案 A:两张表**
```
vega_public_pool:    (id, link_type, comic_id, grant_type, ...)
vega_exclusive_pool: (id, link_type, comic_id, distributor_id, exclusive_period, ...)
```

**方案 B:单表 pool_type 区分**
```
vega_drama_pool:  (id, pool_type, link_type, comic_id, ...)
                     ↑ 1=公共池 2=独家池
```

第一反应是 A,因为"两种业务含义不同"。但又想到一个关键场景:**从独家释放到公共池**。

- A 方案:`DELETE FROM exclusive_pool WHERE xxx; INSERT INTO public_pool ...`,两个事务,容易半拉子状态
- B 方案:`UPDATE drama_pool SET exclusive_period = 99 WHERE xxx`,一条 SQL 原子完成

最终选 B。

教训:**不要只看"当前两种实体长得像不像",还要看"它们之间会不会流转"**。会流转的实体,倾向于放同一张表。

### 2.3 "默认全部"字段的来源

一开始漏了个关键字段。产品说:

- **普通 / 首发**:不选分销商,默认"所有该链路的客户"都能看
- **联合首发**:运营勾选具体 2-3 个分销商

我最初的设计:只用一张关联表 `vega_drama_pool_distributor(pool_id, distributor_id)`。"普通"的话就 INSERT 一条针对每个客户的记录。

产品看完:"那**快手端原生有 200 个客户**,你每次加一个普通剧,都要 INSERT 200 条?万一我加错一个要回滚?"

我:"……有道理。"

改方案:

```sql
ALTER TABLE vega_drama_pool ADD distributor_scope TINYINT;
-- 1 = "默认全部该链路客户"(不写关联表)
-- 0 = "具体分销商"(去查关联表)
```

这样普通/首发剧**一条记录搞定**,查询时判断 `distributor_scope=1` 直接返回"全部",避免污染关联表。

---

## 三、我是怎么一步步做的

### 第一步:建表 + 基础 CRUD(顺利)

两张表:
- `vega_drama_pool` 主表
- `vega_drama_pool_distributor` 关联表(只在联合首发/独家时用)

10 个接口:
- 公共池分页/批量添加/编辑/上下架/加入独家
- 独家池分页/批量添加/编辑
- 辅助:漫剧识别/按链路查客户

代码写完、编译过,本地 mvn 打包 OK。心里美滋滋,以为半天就能搞定。

**然后部署就开始了。**

### 第二步:权限体系的三方拉扯(第一个下午炸了)

原本以为加接口就行,直到同事提醒我:"Vega 有菜单权限,你加的接口要在 Rainbow 配置里对应新增菜单节点,不然前端看不到按钮。"

我:"啥是 Rainbow?"

同事:"配置中心。我们 vega_permission 表不是手工维护的,是启动时读 Rainbow 里一个叫 `permission.init` 的 YAML,清空重建。你想加菜单,就在那 YAML 里加节点,然后调 `/DataInit/permission` 重建。"

我心里一咯噔——**"清空重建"这种操作,YAML 写错一行整个后台权限就崩了**。

果然后面就栽在 YAML 缩进上了。

**坑 1**:YAML 缩进错了一个空格,init 接口返回一堆:

```
Cannot create property=children for JavaBean=PermissionNode
Unable to find property 'type' on class: java.lang.String
in 'string', line 256, column 17
```

第一眼看懵,第二眼开始看缩进层级表……发现是 `- ADMIN # 管理员` 和后面的 `children:` 层级对不齐,导致 `- type: 3` 被当成了 `ADMIN` 字符串的属性。

改好后我记了个表:

```
6   空格: 剧集池管理 "- type: 2"
8   空格: title / path / identifier / roles / children
10  空格: 抖音原生 "- type: 2"(是剧集池 children 的项)
12  空格: 抖音原生的 title / children
14  空格: 按钮 "- type: 3"
...
```

**教训**:YAML 是**缩进敏感的语言**,任何复制粘贴都要检查空格。工具界的毒瘤之一。

**坑 2**:init 报错后,`vega_permission` 表的状态是什么?

代码看过:`clearPermission()` 和 YAML 解析在同一个事务里,YAML 解析失败在 `clearPermission()` 之前,**表没被清,还能救**。

但如果"YAML 格式正确但 roles 写错"呢?可能会走到 clearPermission,然后 insert 失败——这就是"**半拉子状态**",整个后台权限表变成"只有目录没有按钮"。

所以之后我每次发布 YAML,都会先在本地 Python 校验一次:

```bash
python3 -c "import yaml; yaml.safe_load(open('permission.init.yaml'))"
```

语法不对直接报行号。

### 第三步:发布链路的惊魂一下午(500 错误的锅)

代码上了 test,前端调接口报 **500 内部异常**。

打开浏览器 F12 一看,response body 只有:

```json
{"timestamp":"2026-04-30T08:51:25.469+
```

**然后就没了**。body 被切断!

这时候我的心理活动:
- 是网关问题吗?
- 是代码问题吗?
- 是我新写的代码不对?
- 还是 Jackson 的锅?

先看后端日志 `warn.log`,**没有任何相关异常**。

然后 access.log、debug.log,都没发现。

我就在那儿抓瞎。

最后试了下 `error.log`:

```bash
grep -A 20 "found RuntimeException" /usr/local/trpc/log/error.log | tail -50
```

看到了:

```
org.springframework.http.converter.HttpMessageConversionException:
Type definition error: [simple type, class java.time.LocalDateTime];
Java 8 date/time type `java.time.LocalDateTime` not supported by default:
add Module "com.fasterxml.jackson.datatype:jackson-datatype-jsr310" to enable handling
(through reference chain:
  Response["data"]->PagedResult["list"]->ArrayList[0]->PublicPoolItemVO["publicPoolUpdateTime"])
```

**Jackson 不认识 `LocalDateTime`**!

我第一反应:"那我给字段加 `@JsonFormat(pattern="yyyy-MM-dd HH:mm:ss")`,Jackson 就知道怎么格式化了对吧?"

改好,发布,调用……**还是同样的错**。

再仔细看文档,恍然大悟:

> **`@JsonFormat` 只能指定格式,不能让 Jackson 识别类型**。Jackson 要识别 `LocalDateTime`,必须显式注册 `jackson-datatype-jsr310` 模块。

但项目没注册这个模块,我的选项:

1. **全局注册 jsr310**:改 Jackson 全局配置,**影响老接口**,不敢动
2. **VO 里用 `String` 代替 `LocalDateTime`**:改动范围小,绝对不影响别人

我选了方案 2:

```java
// 改前
private LocalDateTime publicPoolUpdateTime;

// 改后
private String publicPoolUpdateTime;  // 格式:yyyy-MM-dd HH:mm:ss

// Service 里格式化
.publicPoolUpdateTime(fmtDateTime(r.getPublicPoolUpdateTime()))
```

加个工具方法:

```java
private static final DateTimeFormatter DATETIME_FMT =
    DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");

private static String fmtDateTime(LocalDateTime t) {
    return t == null ? null : t.format(DATETIME_FMT);
}
```

发布 → 接口通了,返回:

```json
{
  "code": 0,
  "data": {
    "list": [{
      "id": 1,
      "comicId": "DY001",
      "publicPoolUpdateTime": "2026-04-30 14:05:14",
      ...
    }],
    "total": 3
  }
}
```

**这个坑花了 4 小时**,其中 3 小时在找 `error.log` 和试错。

---

## 四、几个教训

### 教训 1:`error.log` 才是"真日志"

老项目里,warn / debug / access / error 好几套日志文件。我之前习惯看 warn.log,结果**关键的异常栈都在 error.log**。

**反思**:下次遇到"接口 500、业务日志没异常"的诡异现象,**先看 error.log**,别在 warn 里找。

### 教训 2:Jackson + Java 8 时间类型是老项目通病

这是**几乎每个老 Java 后端都会踩的坑**。原因是:

- Jackson 2.x 默认不认 `LocalDateTime`(JSR-310 类型)
- 必须注册 `jackson-datatype-jsr310` 模块
- 很多老项目为了**不影响现有接口**,就没注册

所以新接口用 LocalDateTime 就会炸。

**反思**:新接口的 VO 字段类型要**先看老项目怎么写的**。如果老项目用 `Date` 或 `String`,你就用 `Date` 或 `String`,别上来就 `LocalDateTime`。

`@JsonFormat` 注解是**治不了这个 bug 的**——它只负责格式化,不负责类型识别。

### 教训 3:发布链路要摸清楚

老项目的发布可能涉及好几个工具:

- `publish.sh` + `dtools dpatch`:上传补丁 jar,快速发布
- Sumeru **发布**按钮:构建新镜像,正式发布
- Rainbow 配置发布:改配置,不碰代码

**这三个是完全独立的**。你改代码只走 `publish.sh`,Rainbow 配置不会自动同步;你发 Rainbow 配置,代码也不会自动重新部署。

**反思**:每个项目第一次上线时,**画一张"改哪种东西走哪种发布"的流程图**贴在工位上。

### 教训 4:业务异常要区分 code

我代码里抛的 `IllegalStateException("独家池已存在该漫剧")`,被全局异常处理器兜底成 `{"code":9999,"message":"内部异常"}`。

前端看到 9999 一脸懵:"我就想加入独家啊,为啥是'内部异常'?"

**反思**:业务类异常(重复、不存在、无权限等)应该走**自定义的业务异常**(比如 `BusinessException`),返回**可识别的 code**,让前端能提示用户"该漫剧已独家,请直接编辑"。

不能用 `IllegalStateException` 或 `RuntimeException` 这种通用异常,容易被当成系统故障。

---

## 五、上线之后的效果

- **10 个接口全部通了**,前端联调顺利
- **YAML 权限配置**生效,菜单/按钮按角色显隐
- **独家授权商业模式**上线,销售同学:"终于有东西可以谈了"
- **我自己**:从"不知道怎么看日志"到"直接 `grep 'found RuntimeException' error.log`",一秒定位

---

## 六、面试怎么讲

### 开场(15 秒)
"2026 年 4 月做的剧集池管理。业务上,把'所有剧对所有客户开放'升级为'可公共 / 可独家,带保护期'的商业模式。技术上是 **10 个接口的 CRUD + 权限体系 + 跨系统同步**,从设计到上线踩了不少坑。"

### 被问"**单表还是两张表?**"
"用了单表 + `pool_type` 字段区分。原因:两种池之间会相互流转(比如'释放到公共池'),如果是两张表,流转要跨表 DELETE + INSERT,破坏原子性。单表 UPDATE 一下字段就行,更稳。
代价是部分字段冗余(比如 `grant_type` 只在公共池用),但业务量不大,换简洁性划算。"

### 被问"**LocalDateTime 序列化失败怎么处理的?**"
"一开始以为加 `@JsonFormat` 就行,后来发现 Jackson 默认**不认识** `LocalDateTime` 这个类型,必须注册 `jsr310` 模块才行。
`@JsonFormat` 只能指定格式,不能解决类型识别。
我选了**改 VO 用 String**,不动全局 Jackson 配置——改动面可控,绝对不影响老接口。"

### 被问"**权限怎么做的?**"
"项目用了 Rainbow 配置中心,`permission.init` YAML 描述整个菜单和按钮权限。启动时或调 `/DataInit/permission` 触发 `PermissionsInitService` 读 YAML,清空 `vega_permission` 重建。
接口代码**不用写** `@PreAuthorize`,权限由前端根据 Rainbow 配置决定菜单/按钮显隐。
这种'配置驱动权限'好处:权限改动不用发版,改 Rainbow YAML 就行。代价是 YAML 语法错会导致重建失败——我因为缩进错了一个空格,排查了半小时。"

### 被问"**你发现响应被截断怎么 debug 的?**"
"分 3 步:
1. **看后端 `error.log`**(不是 warn.log!),搜 `found RuntimeException`
2. 找到完整异常栈 `Jackson LocalDateTime not supported`
3. 定位到 VO 某个字段,改类型

关键教训:**异常日志可能在你不熟悉的 log 文件里**,别只看一个文件就下结论。"

### 被问"**如果让你重做会怎么改?**"
"三点:
1. 自定义 `BusinessException` + ErrorCode,让'独家池已存在'这类业务错误返回明确 code,不要被兜底成 9999
2. Rainbow YAML 加 **CI 校验**:提交前跑一次 YAML lint + 解析,防止缩进错误上生产
3. 时间字段**统一规范**:项目层面决定'全 Date / 全 LocalDateTime + 注册 jsr310 / 全 String',别同一个项目三种风格并存"

---

> 🔗 技术细节版见《剧集池管理_实现.md》
> 这份偏故事,面试前温一遍找找手感。

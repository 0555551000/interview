<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>富邦华一银行普惠信贷管理系统 · 项目详情</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }

:root {
    --bg: #0d1117;
    --surface: #161b22;
    --surface-2: #1f2630;
    --text: #e6edf3;
    --text-dim: #8b95a7;
    --primary: #f0b429;       /* 金融金 */
    --primary-soft: #d4a017;
    --accent: #ff7b72;
    --success: #56d364;
    --warning: #e3b341;
    --info: #79c0ff;
    --border: #30363d;
    --gold-grad: linear-gradient(135deg, #f0b429 0%, #d4a017 100%);
}

html { scroll-behavior: smooth; }

body {
    font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif;
    background: var(--bg);
    color: var(--text);
    line-height: 1.7;
    overflow-x: hidden;
}

/* 背景 */
.bg-animation {
    position: fixed;
    inset: 0;
    z-index: -1;
    background:
        radial-gradient(circle at 20% 30%, rgba(240, 180, 41, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 70%, rgba(212, 160, 23, 0.06) 0%, transparent 50%),
        var(--bg);
}

/* 导航 */
nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    background: rgba(13, 17, 23, 0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    z-index: 100;
    padding: 16px 0;
}

nav .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

nav .logo {
    font-weight: 700;
    font-size: 18px;
    background: var(--gold-grad);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 32px;
}

nav a {
    color: var(--text-dim);
    text-decoration: none;
    font-size: 14px;
    transition: color 0.2s;
}

nav a:hover { color: var(--primary); }

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
}

section {
    padding: 100px 0 60px;
}

/* Hero */
#hero {
    padding-top: 140px;
    min-height: 90vh;
    display: flex;
    align-items: center;
}

.hero-content { animation: fadeInUp 1s ease-out; }

.hero-tag {
    display: inline-block;
    padding: 6px 14px;
    background: rgba(240, 180, 41, 0.12);
    color: var(--primary);
    border-radius: 100px;
    font-size: 13px;
    margin-bottom: 20px;
    border: 1px solid rgba(240, 180, 41, 0.3);
}

.hero-title {
    font-size: 52px;
    font-weight: 700;
    margin-bottom: 16px;
    background: linear-gradient(135deg, #fff 0%, #f0b429 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1.2;
}

.hero-subtitle {
    font-size: 20px;
    color: var(--text-dim);
    margin-bottom: 16px;
    font-weight: 400;
}

.hero-meta {
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
    margin-top: 12px;
    margin-bottom: 32px;
}

.hero-meta .meta-item {
    display: flex;
    align-items: center;
    gap: 6px;
    color: var(--text-dim);
    font-size: 14px;
}

.hero-meta .meta-item::before {
    content: "■";
    color: var(--primary);
    font-size: 8px;
}

.hero-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-top: 36px;
}

.stat-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 22px;
    transition: all 0.3s;
}

.stat-card:hover {
    transform: translateY(-4px);
    border-color: var(--primary);
    box-shadow: 0 10px 40px rgba(240, 180, 41, 0.15);
}

.stat-number {
    font-size: 28px;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 6px;
    font-variant-numeric: tabular-nums;
}

.stat-label {
    color: var(--text-dim);
    font-size: 13px;
}

/* Section header */
.section-header {
    margin-bottom: 40px;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s;
}

.section-header.visible {
    opacity: 1;
    transform: translateY(0);
}

.section-number {
    color: var(--primary);
    font-family: "SF Mono", Menlo, monospace;
    font-size: 14px;
    margin-bottom: 8px;
}

.section-title {
    font-size: 32px;
    font-weight: 700;
    margin-bottom: 12px;
}

.section-desc {
    color: var(--text-dim);
    font-size: 15px;
    max-width: 700px;
}

/* 项目总览 */
.project-overview {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 36px;
    margin-bottom: 40px;
}

.project-overview h3 {
    font-size: 22px;
    margin-bottom: 16px;
    color: var(--text);
}

.project-overview p {
    color: var(--text-dim);
    margin-bottom: 12px;
    font-size: 14.5px;
}

.project-overview .gold {
    color: var(--primary);
    font-weight: 600;
}

/* 业务流程图 */
.flow-diagram {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 12px;
    margin-top: 24px;
    position: relative;
}

.flow-step {
    background: var(--surface-2);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 16px 12px;
    text-align: center;
    position: relative;
    transition: all 0.3s;
}

.flow-step:hover {
    border-color: var(--primary);
    transform: translateY(-2px);
}

.flow-step .step-num {
    color: var(--primary);
    font-family: "SF Mono", monospace;
    font-size: 11px;
    margin-bottom: 6px;
}

.flow-step .step-name {
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 4px;
}

.flow-step .step-desc {
    font-size: 11px;
    color: var(--text-dim);
}

.flow-step:not(:last-child)::after {
    content: "→";
    position: absolute;
    right: -10px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--primary);
    font-size: 16px;
    z-index: 1;
}

/* 模块卡片 */
.modules-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-top: 32px;
}

.module-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 26px;
    transition: all 0.3s;
}

.module-card:hover {
    border-color: var(--primary);
    transform: translateY(-4px);
    box-shadow: 0 10px 30px rgba(240, 180, 41, 0.1);
}

.module-card .module-icon {
    font-size: 24px;
    margin-bottom: 12px;
}

.module-card h4 {
    font-size: 18px;
    margin-bottom: 10px;
    color: var(--text);
}

.module-card p {
    color: var(--text-dim);
    font-size: 13.5px;
    line-height: 1.65;
}

.module-card .tag {
    display: inline-block;
    padding: 2px 8px;
    background: rgba(240, 180, 41, 0.12);
    color: var(--primary);
    border-radius: 4px;
    font-size: 11px;
    margin-right: 6px;
    margin-top: 8px;
}

/* 核心产出 */
.highlights-timeline {
    position: relative;
    padding-left: 40px;
    margin-top: 32px;
}

.highlights-timeline::before {
    content: "";
    position: absolute;
    left: 15px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(180deg, var(--primary), transparent);
}

.highlight-item {
    position: relative;
    margin-bottom: 28px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 22px 26px;
    transition: all 0.3s;
}

.highlight-item:hover {
    border-color: var(--primary);
    transform: translateX(4px);
}

.highlight-item::before {
    content: "";
    position: absolute;
    left: -32px;
    top: 24px;
    width: 14px;
    height: 14px;
    background: var(--primary);
    border-radius: 50%;
    box-shadow: 0 0 0 4px rgba(240, 180, 41, 0.2);
}

.highlight-item h4 {
    font-size: 17px;
    margin-bottom: 12px;
    color: var(--text);
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
}

.highlight-item h4 .badge {
    font-size: 11px;
    padding: 2px 8px;
    background: rgba(240, 180, 41, 0.12);
    color: var(--primary);
    border-radius: 4px;
    font-weight: 500;
}

.highlight-block {
    margin: 10px 0;
}

.highlight-block .label {
    color: var(--primary);
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 4px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.highlight-block .content {
    color: var(--text-dim);
    font-size: 14px;
    line-height: 1.7;
}

.highlight-block .content code {
    background: var(--surface-2);
    color: var(--info);
    padding: 1px 6px;
    border-radius: 3px;
    font-family: "SF Mono", Menlo, monospace;
    font-size: 12.5px;
}

.highlight-block .content strong {
    color: var(--primary);
    font-weight: 600;
}

/* 业务术语解释 */
.glossary-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-top: 32px;
}

.glossary-item {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 20px;
    transition: all 0.3s;
}

.glossary-item:hover {
    border-color: var(--primary);
}

.glossary-item .term {
    font-size: 16px;
    font-weight: 600;
    color: var(--primary);
    margin-bottom: 8px;
}

.glossary-item .def {
    color: var(--text-dim);
    font-size: 13.5px;
    line-height: 1.6;
}

/* 技术栈 */
.tech-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 32px;
}

.tech-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 18px;
    text-align: center;
    transition: all 0.3s;
}

.tech-card:hover {
    border-color: var(--primary);
    transform: translateY(-2px);
}

.tech-card .tech-name {
    font-size: 15px;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 4px;
}

.tech-card .tech-role {
    color: var(--text-dim);
    font-size: 12px;
}

/* 工作内容 */
.work-summary {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-top: 32px;
}

.work-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 24px;
    transition: all 0.3s;
}

.work-card:hover { border-color: var(--primary); }

.work-card h4 {
    font-size: 16px;
    color: var(--primary);
    margin-bottom: 12px;
}

.work-card ul {
    list-style: none;
    padding: 0;
}

.work-card li {
    color: var(--text-dim);
    font-size: 13.5px;
    padding-left: 16px;
    position: relative;
    margin-bottom: 6px;
    line-height: 1.6;
}

.work-card li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--primary);
}

/* footer */
footer {
    padding: 40px 0;
    text-align: center;
    color: var(--text-dim);
    font-size: 13px;
    border-top: 1px solid var(--border);
    margin-top: 40px;
}

footer a {
    color: var(--primary);
    text-decoration: none;
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
}

/* 响应式 */
@media (max-width: 768px) {
    nav ul { display: none; }
    .hero-title { font-size: 36px; }
    .hero-subtitle { font-size: 16px; }
    .hero-stats, .modules-grid, .tech-grid { grid-template-columns: repeat(2, 1fr); }
    .flow-diagram { grid-template-columns: repeat(2, 1fr); }
    .flow-step:not(:last-child)::after { display: none; }
    .glossary-grid, .work-summary { grid-template-columns: 1fr; }
    .section-title { font-size: 26px; }
}
</style>
</head>
<body>

<div class="bg-animation"></div>

<nav>
    <div class="container">
        <div class="logo">PC · 富邦华一</div>
        <ul>
            <li><a href="#hero">概览</a></li>
            <li><a href="#project">项目</a></li>
            <li><a href="#modules">模块</a></li>
            <li><a href="#highlights">核心产出</a></li>
            <li><a href="#glossary">业务术语</a></li>
            <li><a href="#tech">技术栈</a></li>
        </ul>
    </div>
</nav>

<!-- Hero -->
<section id="hero">
    <div class="container">
        <div class="hero-content">
            <span class="hero-tag">Java 后端开发 · 银行金融系统 · 1.5 年</span>
            <h1 class="hero-title">富邦华一银行<br>普惠信贷管理系统</h1>
            <p class="hero-subtitle">覆盖授信 · 客户 · 合同 · 贷后 · 还款的核心业务系统</p>
            <div class="hero-meta">
                <span class="meta-item">2021.09 - 2023.02 · 1 年 6 个月</span>
                <span class="meta-item">宇信科技上海分公司 · 外派</span>
                <span class="meta-item">Java 开发工程师</span>
            </div>
            <div class="hero-stats">
                <div class="stat-card">
                    <div class="stat-number">5</div>
                    <div class="stat-label">核心业务模块</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">8+</div>
                    <div class="stat-label">岗位审批流</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">100%</div>
                    <div class="stat-label">资金流水准确率</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">7×24</div>
                    <div class="stat-label">系统可用性要求</div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- 项目总览 -->
<section id="project">
    <div class="container">
        <div class="section-header">
            <div class="section-number">/ 01</div>
            <h2 class="section-title">项目背景</h2>
            <p class="section-desc">富邦华一银行（台资银行，富邦金控大陆子行）的普惠信贷条线核心业务系统，承载小微企业、个人经营贷的全生命周期管理</p>
        </div>

        <div class="project-overview">
            <h3>业务定位</h3>
            <p>
                普惠信贷是银行服务<span class="gold">小微企业、个体工商户、农户</span>的政策性贷款业务。本系统是富邦华一银行普惠条线的<span class="gold">核心业务支撑系统</span>，覆盖客户从申请、审批、放款到还款、贷后管理的<span class="gold">完整生命周期</span>。
            </p>
            <p>
                系统对接行内多个外围系统（核心账务系统、客户信息系统、信审系统、押品估值系统），是银行内部<span class="gold">资金流转的关键节点</span>，对一致性、审计追溯、合规性要求极高。
            </p>

            <h3 style="margin-top:28px;">核心业务流程（信贷生命周期）</h3>
            <div class="flow-diagram">
                <div class="flow-step">
                    <div class="step-num">01</div>
                    <div class="step-name">客户准入</div>
                    <div class="step-desc">资质核查 · KYC</div>
                </div>
                <div class="flow-step">
                    <div class="step-num">02</div>
                    <div class="step-name">授信申请</div>
                    <div class="step-desc">押品评估 · 多岗审批</div>
                </div>
                <div class="flow-step">
                    <div class="step-num">03</div>
                    <div class="step-name">合同签订</div>
                    <div class="step-desc">额度引用 · 文本生成</div>
                </div>
                <div class="flow-step">
                    <div class="step-num">04</div>
                    <div class="step-name">放款</div>
                    <div class="step-desc">核心账务 · 资金划付</div>
                </div>
                <div class="flow-step">
                    <div class="step-num">05</div>
                    <div class="step-name">贷后还款</div>
                    <div class="step-desc">利息计算 · 逾期判定</div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- 核心模块 -->
<section id="modules">
    <div class="container">
        <div class="section-header">
            <div class="section-number">/ 02</div>
            <h2 class="section-title">核心业务模块</h2>
            <p class="section-desc">5 大核心模块串联起信贷业务的完整闭环，每个模块对应一个特定的业务领域</p>
        </div>

        <div class="modules-grid">
            <div class="module-card">
                <div class="module-icon">🏢</div>
                <h4>授信管理模块</h4>
                <p>对私 / 对公客户授信申请、押品录入与估值、抵质押率计算、多岗位审批流。每笔授信必须配套抵质押物或担保品，审批通过后方可关联合同。</p>
                <span class="tag">押品估值</span>
                <span class="tag">抵质押率</span>
                <span class="tag">多岗审批</span>
            </div>
            <div class="module-card">
                <div class="module-icon">👥</div>
                <h4>客户管理模块</h4>
                <p>对私客户（个人经营贷）+ 对公客户（小微企业）双轨客户体系。整合 KYC、征信查询、关系图谱（关联人/担保人）、客户分级。</p>
                <span class="tag">KYC</span>
                <span class="tag">征信</span>
                <span class="tag">关系图谱</span>
            </div>
            <div class="module-card">
                <div class="module-icon">📄</div>
                <h4>合同管理模块</h4>
                <p>合同生成必须引用授信和额度，合同金额必须在授信总额范围内。多岗位审批（客户经理/风控/法务/支行长）通过后才能放款。</p>
                <span class="tag">额度校验</span>
                <span class="tag">文本生成</span>
                <span class="tag">流程编排</span>
            </div>
            <div class="module-card">
                <div class="module-icon">📊</div>
                <h4>贷后管理模块</h4>
                <p>贷款发放后的全生命周期监控：分期还款计划、利息计提、逾期判定、催收提醒、五级分类（正常/关注/次级/可疑/损失）。</p>
                <span class="tag">利息计算</span>
                <span class="tag">逾期判定</span>
                <span class="tag">五级分类</span>
            </div>
            <div class="module-card">
                <div class="module-icon">💰</div>
                <h4>还款管理模块</h4>
                <p>支持等额本息、等额本金、按月付息到期还本等多种还款方式。对接核心账务系统完成扣款、记账、利息分摊、罚息计算。</p>
                <span class="tag">还款计划</span>
                <span class="tag">罚息计算</span>
                <span class="tag">账务对接</span>
            </div>
            <div class="module-card">
                <div class="module-icon">⚙️</div>
                <h4>批量任务调度</h4>
                <p>基于 XXL-Job 实现日终批量：还款计划生成、利息计提、逾期扫描、五级分类调整、报表生成。每天凌晨跑批，结果对接报表系统。</p>
                <span class="tag">XXL-Job</span>
                <span class="tag">日终跑批</span>
                <span class="tag">幂等保障</span>
            </div>
        </div>
    </div>
</section>

<!-- 核心产出 -->
<section id="highlights">
    <div class="container">
        <div class="section-header">
            <div class="section-number">/ 03</div>
            <h2 class="section-title">个人核心产出</h2>
            <p class="section-desc">在团队中的具体贡献：聚焦授信、批量任务、流程编排三个方向</p>
        </div>

        <div class="highlights-timeline">

            <div class="highlight-item">
                <h4>1. 授信模块 · 员工持股融资业务开发 <span class="badge">核心需求</span></h4>
                <div class="highlight-block">
                    <div class="label">业务场景</div>
                    <div class="content">
                        员工持股融资是银行普惠信贷下的<strong>专项产品</strong>，针对企业员工通过股权激励持股的<strong>融资场景</strong>。区别于普通经营贷的核心点：押品是<strong>员工持有的股权</strong>，估值需对接行内股权估值系统，且授信审批要叠加企业关联方风险评估。
                    </div>
                </div>
                <div class="highlight-block">
                    <div class="label">我做了什么</div>
                    <div class="content">
                        从需求评审 → 设计 → 编码 → 上线<strong>全程负责该业务</strong>：
                        实现授信申请录入 → 股权押品估值对接 → 抵质押率计算（股权类押品折扣率与不动产不同）→ 多岗位审批流（客户经理 → 风险审查 → 信贷审批委员会）→ 合同额度联动校验 → 放款指令下发的<strong>完整链路</strong>。
                    </div>
                </div>
                <div class="highlight-block">
                    <div class="label">技术亮点</div>
                    <div class="content">
                        基于 <code>Liteflow</code> 抽象审批流的节点定义（<code>NodeComponent</code>），不同岗位的审批逻辑通过 SPI 注册；
                        使用 <code>策略模式</code> 区分股权 / 不动产 / 应收账款等不同押品类型的估值算法；
                        授信额度的<strong>幂等校验</strong>用 Redis 分布式锁 + DB 唯一索引双保险，防止并发申请导致超额。
                    </div>
                </div>
            </div>

            <div class="highlight-item">
                <h4>2. 数据库设计 + 多环境配置管理 <span class="badge">基础工程</span></h4>
                <div class="highlight-block">
                    <div class="label">背景</div>
                    <div class="content">
                        新业务模块上线需要从零设计表结构，且要适配行内 SIT / UAT / 生产<strong>三套环境</strong>，每套环境的核心账务系统地址、对接外围系统的密钥都不同。
                    </div>
                </div>
                <div class="highlight-block">
                    <div class="label">我做了什么</div>
                    <div class="content">
                        <strong>表结构设计</strong>：员工持股融资业务的核心 8 张表设计（授信主表、押品明细、审批流水、额度变更日志、合同关联、还款计划、流水流水、操作审计）。所有金额字段统一用 <code>DECIMAL(18, 4)</code>，日期统一 UTC 存储；
                        <strong>索引规划</strong>：按查询频次设计组合索引，授信查询走 <code>(customer_id, status, apply_date)</code>，审批走 <code>(approval_node, status)</code>；
                        <strong>配置管理</strong>：所有环境差异化配置接入 <code>Apollo 配置中心</code>，按 namespace 隔离环境，敏感配置（数据库密码、对接密钥）走 Apollo 加密。
                    </div>
                </div>
            </div>

            <div class="highlight-item">
                <h4>3. 批量任务开发（贷后还款定时扫描） <span class="badge">日终批跑</span></h4>
                <div class="highlight-block">
                    <div class="label">背景</div>
                    <div class="content">
                        银行系统每天有<strong>固定的日终批跑窗口</strong>（通常凌晨 0:30~5:00），完成所有贷款的利息计提、还款计划更新、逾期判定、五级分类调整。批跑任务挂了会直接影响第二天的报表准确性，被监管追责。
                    </div>
                </div>
                <div class="highlight-block">
                    <div class="label">我做了什么</div>
                    <div class="content">
                        基于 <code>XXL-Job</code> 实现 <strong>3 个核心批跑任务</strong>：<br>
                        <strong>① 还款计划生成</strong>：扫描当天放款的合同，按"等额本息/等额本金/按月付息"等不同还款方式生成完整还款计划，BigDecimal 精确计算到分；<br>
                        <strong>② 逾期判定 + 利息计算</strong>：扫描所有未结清合同的还款计划，超过应还日期未还的判定为逾期，按逾期天数计算罚息（罚息利率 = 合同利率 × 1.5）；<br>
                        <strong>③ 五级分类调整</strong>：根据逾期天数自动调整贷款分类（90 天内"关注"、90~180 天"次级"、180~360 天"可疑"、360+ 天"损失"），变更需写审计日志。
                    </div>
                </div>
                <div class="highlight-block">
                    <div class="label">技术细节</div>
                    <div class="content">
                        <strong>分片 + 幂等</strong>：XXL-Job 分片配置 8 片并行，按 <code>contract_id % 8</code> 分配到不同 worker；任务执行前查 <code>batch_run_log</code> 表的 <code>(task_name, business_date)</code>，已完成则跳过，确保<strong>幂等</strong>。<br>
                        <strong>金额精度</strong>：所有利息计算用 <code>BigDecimal.setScale(2, RoundingMode.HALF_UP)</code>，禁止 double，避免一个月 10 万笔贷款累积出几百块的误差。<br>
                        <strong>异常恢复</strong>：单个 contract 失败不中断整体批跑，错误进 <code>batch_error_log</code>，凌晨 5 点跑完后人工介入处理失败条目。
                    </div>
                </div>
            </div>

            <div class="highlight-item">
                <h4>4. 接口开发 · 多岗位审批流编排 <span class="badge">流程引擎</span></h4>
                <div class="highlight-block">
                    <div class="label">业务场景</div>
                    <div class="content">
                        银行的审批流复杂度远超一般业务系统：一笔授信可能需要经过 <strong>客户经理 → 支行风控 → 总行风险审查 → 信贷审批委员会 → 副行长</strong> 5~8 个节点。每个节点的审批人随<strong>授信金额、产品类型、客户级别</strong>动态变化。
                    </div>
                </div>
                <div class="highlight-block">
                    <div class="label">我做了什么</div>
                    <div class="content">
                        基于 <code>Liteflow</code> 抽象审批流：<br>
                        <strong>节点定义</strong>：每个审批节点封装为 <code>NodeComponent</code>，节点内部封装"通过 / 拒绝 / 退回 / 加签"4 种行为；<br>
                        <strong>流程配置</strong>：审批流定义存 Apollo（XML 格式），<strong>调整审批顺序不需要发版</strong>；<br>
                        <strong>动态分派</strong>：基于规则引擎（QLExpress）实现"金额 < 100w 跳过总行审批"等动态分派；<br>
                        <strong>审计追溯</strong>：每个节点的审批结果、审批人、审批时间、备注全部入 <code>approval_history</code> 表，<strong>满足银监会审计要求</strong>。
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<!-- 业务术语 -->
<section id="glossary">
    <div class="container">
        <div class="section-header">
            <div class="section-number">/ 04</div>
            <h2 class="section-title">银行业务术语速查</h2>
            <p class="section-desc">面试官如果是非金融背景，可以快速给他解释这些术语</p>
        </div>

        <div class="glossary-grid">
            <div class="glossary-item">
                <div class="term">授信</div>
                <div class="def">银行同意给客户的最高可借款额度。"授信 100 万"≠"借了 100 万"，是个上限。比如信用卡的额度就是一种授信。</div>
            </div>
            <div class="glossary-item">
                <div class="term">押品 / 抵质押物</div>
                <div class="def">客户拿来抵押的资产，比如房产、股权、应收账款。银行评估其价值，按一定折扣率给出可贷金额。</div>
            </div>
            <div class="glossary-item">
                <div class="term">抵质押率</div>
                <div class="def">押品价值 × 抵质押率 = 可贷金额。不动产通常 70%，股权 30~50%，应收账款 60~80%。等于银行的"风险折扣"。</div>
            </div>
            <div class="glossary-item">
                <div class="term">五级分类</div>
                <div class="def">监管要求把贷款按风险分为：正常 / 关注 / 次级 / 可疑 / 损失。后三类合称"不良贷款"，是银行考核的关键指标。</div>
            </div>
            <div class="glossary-item">
                <div class="term">等额本息 / 等额本金</div>
                <div class="def">两种还款方式。等额本息每月还的总额相同（房贷常用）；等额本金每月还的本金相同，利息递减。</div>
            </div>
            <div class="glossary-item">
                <div class="term">日终跑批</div>
                <div class="def">银行核心系统在每天营业结束后（通常 0:30~5:00）执行的批量任务窗口，完成账务结转、利息计提、报表生成等。</div>
            </div>
            <div class="glossary-item">
                <div class="term">普惠金融</div>
                <div class="def">国家政策导向的金融服务，主要服务小微企业、农户、个体工商户。银行有专门的普惠条线 KPI。</div>
            </div>
            <div class="glossary-item">
                <div class="term">对私 / 对公</div>
                <div class="def">对私 = 给个人客户的业务（个人经营贷、按揭）；对公 = 给企业客户的业务（流动资金贷款、项目贷款）。</div>
            </div>
        </div>
    </div>
</section>

<!-- 工作内容 -->
<section id="work">
    <div class="container">
        <div class="section-header">
            <div class="section-number">/ 05</div>
            <h2 class="section-title">日常工作内容</h2>
            <p class="section-desc">外派模式下的工作日常 · 接需求 · 写代码 · 上线 · 运维</p>
        </div>

        <div class="work-summary">
            <div class="work-card">
                <h4>需求与设计</h4>
                <ul>
                    <li>参与业务方需求评审（业务方 = 银行普惠条线产品经理）</li>
                    <li>输出概要设计、详细设计文档（行内有强制模板，需归档审计）</li>
                    <li>数据库表结构设计 + 索引规划评审</li>
                    <li>关键接口的时序图、状态机设计</li>
                </ul>
            </div>
            <div class="work-card">
                <h4>编码与测试</h4>
                <ul>
                    <li>员工持股融资业务模块开发（独立负责）</li>
                    <li>多岗位审批流的 Liteflow 节点编排</li>
                    <li>XXL-Job 批量任务编写、本地调试</li>
                    <li>单元测试、SIT 自测、UAT 配合测试</li>
                </ul>
            </div>
            <div class="work-card">
                <h4>上线与运维</h4>
                <ul>
                    <li>SIT → UAT → 生产 三环境逐级上线</li>
                    <li>Apollo 配置变更与发版协调</li>
                    <li>日终批跑监控（凌晨值班）</li>
                    <li>线上问题排查（日志、SQL 审计、堆栈分析）</li>
                </ul>
            </div>
            <div class="work-card">
                <h4>跨团队协作</h4>
                <ul>
                    <li>对接核心账务系统（行内）</li>
                    <li>对接押品估值系统（行内 + 第三方）</li>
                    <li>与风控团队联调审批规则</li>
                    <li>与运维团队配合做数据库变更</li>
                </ul>
            </div>
        </div>
    </div>
</section>

<!-- 技术栈 -->
<section id="tech">
    <div class="container">
        <div class="section-header">
            <div class="section-number">/ 06</div>
            <h2 class="section-title">技术栈</h2>
            <p class="section-desc">银行项目典型技术选型 · 重稳定、重审计、重一致性</p>
        </div>

        <div class="tech-grid">
            <div class="tech-card">
                <div class="tech-name">Spring Cloud</div>
                <div class="tech-role">微服务框架</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">Feign</div>
                <div class="tech-role">声明式 RPC</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">MySQL</div>
                <div class="tech-role">主从 · 分区表</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">MyBatis</div>
                <div class="tech-role">ORM 框架</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">Redis</div>
                <div class="tech-role">分布式锁 · 缓存</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">Liteflow</div>
                <div class="tech-role">流程编排引擎</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">XXL-Job</div>
                <div class="tech-role">分布式调度</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">Apollo</div>
                <div class="tech-role">配置中心</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">QLExpress</div>
                <div class="tech-role">规则引擎</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">BigDecimal</div>
                <div class="tech-role">资金精度计算</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">SPI</div>
                <div class="tech-role">扩展点机制</div>
            </div>
            <div class="tech-card">
                <div class="tech-name">单元测试</div>
                <div class="tech-role">JUnit · Mockito</div>
            </div>
        </div>
    </div>
</section>

<footer>
    <div class="container">
        <p>彭冲 · Java 后端工程师 · 富邦华一银行普惠信贷管理系统项目详情</p>
        <p style="margin-top:8px;">📧 1539112470@qq.com · 📱 17344035687</p>
    </div>
</footer>

<script>
// 懒加载动画 - 所有需要 fade-in 的元素监听
const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('visible');
        }
    });
}, { threshold: 0.1 });

document.querySelectorAll('.section-header, .module-card, .highlight-item, .stat-card, .glossary-item, .tech-card, .work-card').forEach(el => {
    observer.observe(el);
});

// 平滑滚动
document.querySelectorAll('nav a').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
    });
});
</script>

</body>
</html>

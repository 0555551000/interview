# C 端结算优化(周结 + 发票核验)—— 唠嗑版

> 如果面试官问"你最近在忙啥？"，我脑子里第一个蹦出来的就是这个。
> 2026 年 3 月开的头，整整忙了三周，从白天扯皮到晚上敲代码。
> 这份是**我私下跟朋友聊项目那种感觉**的版本，不是写给 HR 看的。

---

## 一、故事的开始：那个下午

2026 年 2 月底的某个周五下午，产品小王拉了个会，就叫"**C 端结算优化需求对齐**"。我点进会议室看到财务、计费、大数据的人都来了，心里咯噔一下——"这事儿肯定不简单"。

小王开场说："财务这边反馈，现在分销商月结对账周期太长，账对不平要等一个月后才发现，搞不好追着追着数据都找不全了。另外抖音那边本来就是**周级出账**——7 号、14 号、21 号、28 号——我们月结一次，钱在账上压两三周。业务和财务一起提的，想改周结。"

我当时脑子里 3 个反应：
1. **一条业务流从月到周，周期变短 4 倍**，所有跟时间相关的逻辑都要动
2. 顺带还要加**发票核验**，这意味着不只是前端改个显示，整个**状态机**要重做
3. 财务、计费、大数据三方接口我都要对……**三周能搞定吗？**

小王继续说："还有个事儿，**结算单 Tab 要拆三层**，原来一个扁平列表用户找不到东西；然后 B 端的下载页面也要加分销商视角，运营要能按周下。"

我默默在本子上画了个图：

```
月结 → 周结            (周期重做)
发票核验 (加状态机)    (状态流转 + 对接财务)
三层 Tab 重构           (前端 + 后端分层)
B 端下载页              (新增页面)
数据爬取                (大数据协作 —— 这个是救命稻草)
```

会议末尾我提了一嘴："**数据爬取这块，运营每周手动导 Excel 再上传，3 人天/月的重复劳动，能不能大数据同学帮我们爬一下？**"

大数据的小钟眼睛一亮："行啊，我们有抖音管家的 cookie 池，你们把字段规范给我，我每天推 MQ 给你们。"

——这一刻我松了口气，**至少爬取这块不是我一个人扛**。

---

## 二、需求到底是啥（人话版）

简单说就一句话：**把分销商的结算从月结改成周结，顺便把发票核验流程搞起来，顺便把页面重构一下，顺便把 B 端的下载功能加上。**

听起来像俄罗斯套娃。

### 2.1 分销商周结

原来是：一个月给你出一张账单，里面包含这个月所有的推广收入，你对账确认 → 上传发票 → 财务打款。**流程 30-45 天**。

改完是：**每周一张结算单，一个月出 4 张（R1-R4），月末还有一张"每月返点单"，合计 5 张**。每张独立走流程。

为啥是 R1-R4？因为和**抖音对齐**——抖音后台本来就是周结账，账单日是 7、14、21、28 号，我们跟上节奏就行，不用再自己算。

**但这里有个坑我一开始没想到**：一个月有时候有 5 周（跨月周），这个周算在哪个月？比如 1 月 29 号到 2 月 4 号这一周，算 1 月 R5 还是 2 月 R1？

我去问财务的李姐：
- 我："跨月周怎么算？"
- 她："按**周日所在的月**。"
- 我："周日？不是周一？"
- 她："我们一直这么算的。"
- 我：（OK，我按 ISO 标准写好的代码白写了）"……行，我改。"

回去把代码改了，加了 **20 多个单元测试**把所有边界 case 都覆盖了（跨年、闰年、月末周、当月 5 周），生怕再漏。

### 2.2 发票核验（这是最硬核的一块）

原来发票流程是这样的：
1. 用户上传发票图片
2. 状态改成"已上传"
3. 然后…就没有然后了，财务在线下对，对完口头告诉运营
4. 运营在系统里手动点"已打款"

问题大了去了：
- **没人知道发票到底是对的还是错的**
- 如果财务发现发票主体写错了，要**微信上私聊运营**重新让用户传
- 系统里状态和实际对不上，每个月都出**错账 3-5 起**

产品这次要求：加一个**发票核验状态机**，从"待确认"一路走到"已打款"，每一步都能看到，出错了能回退。

我去找计费的赵哥对接发票核验接口，第一次对话让我印象深刻：

- 我："你们核验接口响应多久？"
- 赵："P99……差不多 8 秒吧。"
- 我："（深呼吸）8 秒？那我同步调的话我接口不就卡死了？"
- 赵："你异步调呗。"
- 我："那成功回调走哪？"
- 赵："没回调，你自己轮询。"
- 我："……（内心 OS：合着甩给我了）"

所以我设计了 **TDMQ 异步消息 + 延迟消息轮询** 的方案（详见下面实现章节），这个设计后来 CR 时老周（我们组 TL）拍了下桌子说："**这个设计漂亮，就这么干。**" 嘿嘿。

### 2.3 三层 Tab 重构

产品画了个图：

```
结算单
├── 推广收入 （按周结）
│   ├── 抖音渠道
│   │   ├── 正常周（R1/R2/R3/R4）
│   │   └── 每月返点（RM）
│   └── 快手渠道
│       └── 正常月
└── 内容收入（月结）
```

原来所有结算单**堆一个列表**，一个机构如果同时是分销商 + 内容创作者，结算单混在一起，用户经常找错。

重构后按"收入类型 → 渠道 → 周期"分三层，用户一目了然。

### 2.4 B 端下载页加分销商视角

运营小张给我私聊："彭哥，现在我每周都要手动从用户那把发票收过来，整理好发给财务，能不能在我的 B 端页面直接按周下载？"

我："这个活儿你干多久了？"
小张："**一周 8 小时**。"
我："行，给你加。"

### 2.5 数据爬取

运营还要每周从抖音后台手动导 Excel，大数据小钟接了这个活，我负责消费端。

---

## 三、我是怎么一步步做的

### 第一周：方案设计 + 扯皮

**周一**：我把整个需求拆成 5 个模块，画了个总体架构图发在群里。

**周二**：和计费赵哥 + 财务李姐一起开会对接口。这个会开了**整整 3 小时**，反复确认字段：
- "结算周怎么表示？"—— `2026-02-R1` 这种格式
- "主体怎么区分？"—— 红袖和宏文两个法人主体，DB 加个 `entity` 字段
- "发票核验失败的错误码枚举？"—— 财务列了 8 种错误原因

**周三下午**：产品小王又来了："**分销商如果 10 天没确认结算单怎么办？**"

我楞了一下："嗯……一般不会吧？"

她："万一呢？线上总有各种情况，比如机构已经跑路了、账号丢了、人忙忘了。财务的钱压在那边不结算也不行。"

我："那就做一个自动确认兜底呗。XXL-Job 每天扫一次，超过 10 天的自动改成'已确认'，发通知出去。"

她："对对对，就这个意思。"

（后来上线后**真的有 3 家机构是 10 天没点的**，自动确认救了命，不然 PM 又得被财务追着问）

**周四**：和前端小俊对页面。他一开始想搞"一套页面通过参数切换两种模式"，我说："B 端分销商和内容创作者字段差很多，硬塞一起代码会很乱。**我们做两个独立组件，顶部筛选器切换**，后端 Service 也拆，维护清晰。" 他想了想说："行。"

**周五**：整个方案文档写完，老周（TL）CR 了一下，给了 3 条意见：
1. 状态机要做得**更严格**，不能用 `UPDATE` 直接改状态
2. 发票核验的**重试要有上限**，不能无限轮询
3. 要加**对账 Job**，每月核对和计费侧总金额

全部 Get，进入第二周。

---

### 第二周：疯狂写代码

**周一**：数据模型改造。老表 `settle_bill` 加字段：

```sql
ALTER TABLE settle_bill ADD COLUMN settle_period VARCHAR(16) COMMENT '结算周期 2026-02-R1';
ALTER TABLE settle_bill ADD COLUMN period_type TINYINT COMMENT '1周结 2月结 3返点';
ALTER TABLE settle_bill ADD COLUMN revenue_week VARCHAR(16) COMMENT '收入产生周';
ALTER TABLE settle_bill ADD COLUMN entity VARCHAR(32) COMMENT '结算主体 红袖/宏文';
CREATE INDEX idx_org_period ON settle_bill(org_id, settle_period);
```

字段都加完，写了刷数脚本把历史数据补上 `period_type=2`（月结）。

**周二**：写周期计算工具类 `SettlePeriodUtil`。这里跟财务李姐的"周日所在月"规则较劲了一下午——自测了跨年的 case（2025-12-29 周一 到 2026-01-04 周日），按李姐的规则应该是 `2026-01-R1`，但我第一版写成了 `2025-12-R5`。调了俩小时才对。

**周三-周四**：核心——**发票核验状态机**。

我用了**枚举 + 静态流转 Map**，所有合法流转一张表一目了然：

```java
public enum SettleStatus {
    TO_CONFIRM(0),       // 待确认
    UNSETTLED(1),        // 未结算
    VERIFYING(2),        // 核验中
    INVOICE_RECEIVED(3), // 已收票
    PAID(4),             // 已打款
    INVOICE_ERROR(5);    // 发票错误

    private static final Map<SettleStatus, Set<SettleStatus>> ALLOW = Map.of(
        TO_CONFIRM,       Set.of(UNSETTLED),
        UNSETTLED,        Set.of(VERIFYING),
        VERIFYING,        Set.of(INVOICE_RECEIVED, INVOICE_ERROR),
        INVOICE_ERROR,    Set.of(UNSETTLED),      // 允许重新上传
        INVOICE_RECEIVED, Set.of(PAID),
        PAID,             Set.of()                // 终态
    );

    public boolean canTransitTo(SettleStatus next) {
        return ALLOW.getOrDefault(this, Set.of()).contains(next);
    }
}
```

**为什么用这个方式？**

我之前在另一个项目见过用 if-else 写状态流转的代码，长什么样？

```java
if (bill.getStatus() == 1) {
    if (next == 2) { ... }
    else if (next == 5) { throw ... }
    ...
} else if (bill.getStatus() == 2) {
    if (next == 3) { ... }
    ...
}
```

500 行，改一个规则一身汗。用枚举 + 流转 Map，**改一行配置全局生效**，CR 时谁都看得懂。

**周五**：异步发票核验流程。流程设计：

```
[用户] 上传发票图片
   ↓ 前端 form-data
[我方接口]
   1. 存 COS，拿到 fileUrl
   2. 落 invoice_upload 表，状态"核验中"
   3. 发 TDMQ 消息 InvoiceVerifyMsg { billId, fileUrl }
   4. 立即返回 200 给用户（"已提交核验，请稍候"）
   ↓
[Consumer]
   1. 调财务核验接口 verifyInvoice(fileUrl, billId)
   2. 成功 → 状态机 VERIFYING → INVOICE_RECEIVED
   3. 失败 → 状态机 VERIFYING → INVOICE_ERROR + 记录 error_reason
   4. 超时 → TDMQ 延迟消息重试，30s 后再试，最多 10 次
```

**核心代码**：

```java
@TDMQConsumer(topic = "invoice-verify", maxRedelivery = 10)
public void handle(InvoiceVerifyMsg msg) {
    try {
        VerifyResult r = financeClient.verify(msg.getFileUrl(), msg.getBillId());
        if (r.isOk()) {
            settleStatusService.transit(msg.getBillId(), INVOICE_RECEIVED, "SYS");
        } else {
            settleStatusService.transit(msg.getBillId(), INVOICE_ERROR, "SYS");
            invoiceLogMapper.insertError(msg.getBillId(), r.getErrorReason());
        }
    } catch (TimeoutException | IOException e) {
        // 触发 MQ 重试
        throw new RetryableException(e);
    }
}
```

---

### 第三周：联调 + 上线 + 救火

**周一**：和财务接口联调。出的第一个问题：**"接口要求我把发票 base64 塞请求体"**。

我打了个电话："哥，这发票图片一张 2MB，base64 后 3MB，你请求体放得下吗？你们 Nginx 限制多少？"

对方："默认 2MB……"

我："那 base64 这条路肯定走不通。**我把 COS URL 给你，你自己下载核验**。"

对方："行，你给我开 COS 预签名 URL 权限。"

后来接口改成了传 URL，稳定多了。

**周二**：和前端联调三层 Tab。一个坑是**Tab 切换时查询参数不重置**——切到"快手"发现搜索的还是抖音的筛选条件。我让前端在切 Tab 时 `reset(queryDTO)`。

**周三**：UAT 测试。测试同学小陆给我提了 8 个 bug，其中最坑的一个：

> **"状态机被绕过"**

她在自己的账号里直接 SQL `UPDATE settle_bill SET status = 3 WHERE id = xxx`，绕过了状态机。

我："……测试你在线上这么干？"

她："我在测试环境干的。但你想啊，如果开发里有人手抖呢？"

我："有道理。"

当天加了 **MyBatis 拦截器**，所有对 `settle_bill.status` 字段的 UPDATE 语句（不是通过 `SettleStatusService`）都抛异常。CR 时组里炸锅："这拦截器搞得好！"

```java
@Intercepts(@Signature(type = Executor.class, method = "update", ...))
public class SettleStatusUpdateBlocker implements Interceptor {
    @Override
    public Object intercept(Invocation inv) throws Throwable {
        MappedStatement ms = (MappedStatement) inv.getArgs()[0];
        if (ms.getSqlCommandType() == SqlCommandType.UPDATE) {
            String sql = ms.getBoundSql(inv.getArgs()[1]).getSql();
            if (sql.contains("settle_bill") && sql.contains("status")) {
                // 判断调用栈里有没有 SettleStatusService
                if (!isCalledFromStatusService()) {
                    throw new IllegalStateException(
                        "禁止直接更新 settle_bill.status,请使用 SettleStatusService"
                    );
                }
            }
        }
        return inv.proceed();
    }
}
```

**周四上线**。灰度 10% 流量，观察 2 小时没问题，全量。

**周五下午**：某个机构用户反馈"我上传了发票，但状态一直是'核验中'，没变"。

我看日志——财务接口**返回了 504 超时**，但我们的重试机制是"延迟 30 秒"，所以用户还没看到状态变化。

改：
- 前端加个"刷新"按钮，用户急可以手动刷
- 延迟消息从 30s 改成首次 10s（快速感知），后续按 30s、60s、120s 指数退避
- 状态显示更友好："核验中（通常 2 分钟内完成，稍候可刷新查看）"

用户安抚下来了。

---

## 四、三周下来，我学到了啥

### 学到 1：对接就是谈判，不是索取

一开始我以为对接就是"我要什么参数、你给我什么接口"，实际不是。赵哥想让我塞 base64，我如果不推，就要在前端加一堆压缩逻辑。**站住自己的技术立场**是后端工程师的基本素养。

### 学到 2：业务规则必须文档化 + 测试覆盖

"跨月周按周日月份算"这种规则，如果**没有白纸黑字写下来、没有单测**，过半年换个开发接手就又会踩坑。我写完后在 Confluence 发了个文档链接给整组。

### 学到 3：状态机是个好东西

以前觉得设计模式是"面试才用"的东西。这次实打实感受到了威力——
- 新加一个状态：改 1 行
- 审查合法流转：看 1 张表
- 发票被打回再重传：走 `INVOICE_ERROR → UNSETTLED` 一条边，不改其他代码

用好了之后再也不想写 if-else 状态判断。

### 学到 4：兜底兜底兜底

这项目最让我自豪的不是设计多漂亮，而是**每个环节都想好了 plan B**：
- 财务接口超时 → MQ 重试
- 重试 10 次还失败 → 告警给运营
- 用户 10 天不确认 → XXL-Job 兜底
- 状态机被越权改 → MyBatis 拦截器兜底
- 计费数据和我方对不上 → 每月对账 Job

老周说过一句话："**5 年的工程师和 3 年的区别，就在于他想到的 edge case 是 3 年工程师的 3 倍。**" 共勉。

---

## 五、上线三个月后的数据（可以吹一吹）

- 分销商回款周期从**平均 35 天降到 14 天**（业务同学给财务发了感谢邮件）
- 运营结算相关工作**从 3 人天/月降到 0.5 人天/月**
- **错账从每月 3-5 起降到 0**（上线 3 个月内）
- 头部机构"掌玩"的人给我们组发了**锦旗**（真事，挂在墙上）

---

## 六、面试怎么讲（一句话开场）

> "C 端结算优化，我 2026 年初做的一个**主力项目**。分销商月结改周结，加了发票核验状态机，重构了结算单层级。我对接财务、计费、大数据，三周上线。最有技术含量的是**发票核验的异步状态机**和**MyBatis 拦截器防越权改状态**。上线后回款从 35 天缩到 14 天，错账降到 0。"

**如果面试官追问"讲讲状态机设计"**：
打开文档第三章、跳到"发票核验状态机"那段开始讲。有代码、有理由、有踩坑、有复盘——**三分钟能讲完一个有血有肉的故事**。

这才是面试应该的样子。

---

> 🔗 技术细节版见《C端结算优化_实现.md》，这里只写故事和心路。
> 两份配合着看，面试前一个月刷完整版、面试前一小时看这个唠嗑版热身。

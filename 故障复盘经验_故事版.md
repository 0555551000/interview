# 故障复盘经验（阅文漫剧分销 · 亲历版）

> 写给自己的话：面试官问"你印象最深的一次线上事故"，脑子一片空白是大忌。
> 这份文档把近两年我真正踩过的坑按类型排好，每个都按 **现象 → 定位 → 根因 → 止血 → 长期修复 → 复盘收获** 来讲。
> 工具链按实际：**GitLab + 七彩石配置中心 + 123 平台容器 + Peacock 脚手架 + TRPC + Polaris**。

---

## 目录

1. 七彩石配置类型写错导致服务启动失败
2. 主剧子剧同步下游超时雪崩
3. C 端结算导出 OOM（大分页 + 一次性 toList）
4. Redis 缓存击穿 → DB 连接池打满
5. 灰度发布时新老节点配置不一致
6. 定时任务重复执行（多实例 + 无分布式锁）
7. 日志刷爆磁盘导致容器被 OOMKilled
8. 面试万能模板 · 怎么讲一次事故

---

## 1. 七彩石配置类型写错导致服务启动失败

### 现象
- 周四下午灰度发布 Vega 剧集池服务，123 平台上第一台节点起来后 **健康检查一直 503**，5 分钟后被自动下线。
- 老节点还在扛流量，**用户无感**，但灰度卡住不能继续。

### 定位过程
1. 123 平台点进容器 → `tail -f /data/log/vega/app.log`
2. 启动日志里一堆 `Failed to bind properties under 'permission.init'` 报错，类型转换异常。
3. 去七彩石查 `ywos.solution_vega.config` 树，`permission.init` 这个 key 前一天被人 **从 YAML 改成了 STRING**（粘贴时类型选错）。
4. Peacock 启动时用 `@ConfigurationProperties` 绑定这个 key，拿到一坨字符串当对象解析，直接炸。

### 根因
七彩石的配置是**带类型的**，YAML / STRING / JSON 不是装饰，SDK 按类型反序列化。改配置时如果只顾内容、没盯类型切换下拉框，线上就会炸。

### 止血
- 回滚七彩石到上一版本（幸好有历史版本 + 一键回滚）。
- 重新触发灰度，5 分钟恢复。

### 长期修复
- 在 Peacock 的启动阶段加了一个 **fail-fast 校验**：关键配置 key 启动时必须能正确绑定，绑定失败直接拒绝提供服务（让它在灰度第一台就暴露，而不是带病运行）。
- 在组内文档里加了 checklist：**七彩石改配置必须同时确认"类型"和"内容"**。
- 新增 key 时跨环境（测试/预发/生产）要同步，类型不能写偏。

### 复盘收获
- 配置中心不是"简单的 KV"，**类型即契约**。
- 启动期 fail-fast 比运行期慢慢飘错好。
- 回滚优先于定位，**先止血再查根因**。

---

## 2. 主剧子剧同步下游超时雪崩

### 现象
- 某天晚上 20:00 监控告警：`sync-drama-job` 任务队列堆积，Polaris 看到下游权限服务 P99 从 80ms 涨到 5s，RPC 超时比例 30%。
- 我们这边线程池排队，最后 **Peacock 默认的 TRPC 线程池打满**，连普通查询接口也开始超时。

### 定位过程
1. 看 Grafana：下游权限服务的 QPS 也没涨多少，但 P99 起飞。
2. 查七彩石，发现我们配的 `trpc.client.permission.timeout=5000ms`，**一旦下游慢，我们等 5 秒才失败**，线程被挂住。
3. 再看下游自己的日志：他们那台 MySQL 慢了，索引失效，单 SQL 2~3 秒。
4. 同一时间我们这边定时任务批量同步 8000 条子剧数据，**并发 50** 打过去，直接把他们压垮，自己也雪崩。

### 根因
- 下游慢是**导火索**。
- 真正的**放大器**是我们自己：**超时设得太长** + **批量并发没有保护** + **没有熔断降级**。

### 止血
- 临时把 `sync-drama-job` 的并发从 50 降到 5，堆积慢慢消化。
- 让下游先加索引。

### 长期修复
- 超时重调：下游正常 P99 是 80ms，我们超时设 800ms（10 倍余量），不再给 5 秒。
- 加 **Polaris 熔断**：错误率 > 50% 自动熔断 10s，半开探测。
- 批量任务加**信号量限流**：最多 10 个并发打下游。
- 定时任务 **分片执行**：原来一次处理 8000 条，改成 100 条一片、片间 sleep 200ms。
- 把"下游慢时我们的降级行为"写成一个明确的代码路径：同步任务失败就丢到延迟队列，2 分钟后重试，最多 3 次。

### 复盘收获
- **雪崩不是下游的锅，是自己的保护层太薄。**
- 超时时间 ≠ 兜底时间，**超时时间要接近正常 P99 × 10**，不是凭感觉填 5000。
- 定时批处理必须有**限流 + 分片**，不能一把梭。

---

## 3. C 端结算导出 OOM（大分页 + 一次性 toList）

### 现象
- 财务周一早上点导出 11 月全量分销结算明细，页面转圈转了 2 分钟，然后报错 500。
- 同时那个 Pod 在 123 平台上直接被 **OOMKilled**，自动重启了一次。

### 定位过程
1. 看日志，`java.lang.OutOfMemoryError: Java heap space`。
2. 123 平台调 heap dump：`jmap -dump:format=b,file=/data/heap.hprof <pid>`，下载回本地 MAT 分析。
3. 支配树根节点 60% 内存都是一个 `ArrayList<SettlementRecord>`，里面 120 万条。
4. Git blame 看代码，上周刚上的导出功能：`mapper.selectByMonth(month)` 一次 toList，然后循环拼 Excel。
5. 11 月数据激增（业务侧加了新分销渠道），记录量从 30 万 → 120 万。

### 根因
- **全量 toList 把 DB 一次性装进堆**。
- 没做**流式 / 分页 / 异步**。

### 止血
- 临时在接口层加"时间范围不能超过 7 天"的校验，让财务先分段导。
- 重启服务。

### 长期修复
- 改成**异步下载**：点"导出"→ 提交任务到任务中心 → 后台流式读 DB（MyBatis `ResultHandler` 一行一行处理）→ 写到对象存储 → 发站内信给财务下载。
- DB 侧配合加了 `(month, channel_id)` 联合索引，避免全表扫。
- 容器 heap 从 2G 升到 4G（治标）；更重要的是**代码层面永远不再有 toList 全量**。
- 加了 **Arthas 命令清单**，下次再遇到堆问题 30 秒能开 dump（之前找不到命令花了十几分钟）。

### 复盘收获
- **所有"导出"功能默认都该是异步的**，同步导出只在"数据量有硬上限"时才能用。
- 内存问题不能靠堆大小硬扛，堆越大 GC 停顿越长。
- MyBatis 的 `ResultHandler` / JDBC 的 `fetchSize` 要成为本能。

---

## 4. Redis 缓存击穿 → DB 连接池打满

### 现象
- 某个热剧刚上线，首页推荐位突然变"抢手"，5 分钟内 QPS 从 200 涨到 3000。
- Vega 服务的 HikariCP 打满（`maximumPoolSize=20`），接口普遍 2 秒起步，部分超时。

### 定位过程
1. 先看接口监控：主要打在 `/drama/detail` 这一个接口上。
2. 看代码：缓存 key 是 `drama:detail:{id}`，TTL 10 分钟，典型"旁路缓存"。
3. 查 Redis 监控：那个热剧的 key **在流量涨的前一秒刚好过期**，3000 QPS 同时穿透到 DB。
4. DB 侧没挂，但我们的连接池太小，排队。

### 根因
经典**缓存击穿**：热 key 过期瞬间所有请求同时回源。

### 止血
- 手动把那个剧的缓存预热回去（直接 `SET` 回 Redis）。
- 临时把连接池从 20 调到 40，先扛住。

### 长期修复
- 代码层加 **互斥重建**：回源前 `SETNX lock:drama:{id}` 抢锁，抢到的才查 DB，其他线程轮询等缓存。
- 热 key **过期时间加随机值**（10 分钟 ± 1 分钟），避免同批 key 一起过期。
- 热榜剧（运营配置）走**永不过期 + 后台刷新**策略，彻底不靠 TTL。
- 连接池大小按**(核心数 × 2) + 磁盘数**公式重新评估，不是拍脑袋。
- 加了监控：缓存命中率低于 95% 告警。

### 复盘收获
- "**缓存**"不是银弹，它有自己的一套故障模型：穿透 / 击穿 / 雪崩。
- **缓存过期时间加抖动**是最便宜的保护。
- 连接池小不是配置问题，是**对容量的认知问题**。

---

## 5. 灰度发布时新老节点配置不一致

### 现象
- 某次升级，新版本 Vega 加了 `feature.enableNewContract=true` 这个开关。
- 七彩石上我只在**生产环境**加了这个 key。
- 123 平台灰度第 1 台，拉的就是生产配置，正常。
- 但**老的 9 台**启动时拉配置的时刻 `feature.enableNewContract` 还不存在（SDK 缓存 + 默认值 false），走老逻辑。
- 结果：同一个合同，在新节点走新签约流程生成新字段，老节点走老流程不写新字段。数据**一半新一半旧**。

### 定位过程
1. QA 反馈"有些合同详情页崩，有些好的"。
2. 抓了崩的那几条，发现都是新字段为空 → 前端解析崩。
3. 去 Grafana 看接入层负载均衡，发现这些坏单子**集中在老 pod 的 IP 上**。
4. 登老 pod：`cat /data/peacock/config/cache/ywos.solution_vega.config.json | grep enableNewContract` → 不存在。
5. 新 pod 里有。**同一代码、不同配置**。

### 根因
- 上线顺序错了。**"加新配置开关" 必须先于 "发新代码"**。
- 且配置要**三个环境都先加**（哪怕默认 false），不能只加生产。

### 止血
- 七彩石把 `feature.enableNewContract` 补到所有环境，默认 false。
- 全量重启，让所有节点拉到同一份配置。
- 然后再开 true。

### 长期修复
- 在 Peacock 里封装了 `@FeatureFlag("feature.enableNewContract")` 注解，**读取时如果 key 不存在抛异常**（而不是默认 false 静默通过），强制迫使开发人必须先加配置。
- Checklist：**先改配置（全环境 default false）→ 发代码 → 改配置为 true** 三段式。
- 灰度期间的"混合状态"要在设计时就想清楚：新字段老节点写不出来的话，读端要能容忍 null。

### 复盘收获
- **灰度发布的本质是"多版本共存"**，不是"慢慢替换"。任何跨版本的差异都要在设计上兼容。
- **配置先行**，永远。
- 单机配置缓存 + 多节点的状态漂移，是一类很难一眼看出来的 bug。

---

## 6. 定时任务重复执行（多实例 + 无分布式锁）

### 现象
- 每月 1 号 00:00 的结算任务，11 月那次**把所有分销商的结算单重复生成了一遍**（同一笔结算插了 2 行），财务打电话过来问。

### 定位过程
1. 看 DB，确实每条 settlement 都有两条记录，`created_at` 只差几百毫秒。
2. 看日志，两台 pod 在 00:00:00 几乎同时打印了 "开始生成结算"。
3. 代码用的是 Spring 的 `@Scheduled(cron = "0 0 0 1 * ?")`。每个 pod 都跑了。
4. 我们当时是 **4 台 pod**。按理说应该重复 4 次，但 Peacock 的 TRPC 注册时间 + pod 启动时差让其中 2 台错过了秒级触发。

### 根因
- `@Scheduled` 是**进程级**的，不会自动单例。
- 当时代码里**没有分布式锁**，认为"多 pod 反正都会跑，幂等就行"——但 insert 的代码没做幂等。

### 止血
- 连夜写脚本删重复的 settlement（按 `(distributor_id, month)` 保留 `id` 最小的那一条，其他的软删）。
- 人工补发了少数对不齐的发票。

### 长期修复
- 所有 `@Scheduled` 任务前面套一层 **Redis 分布式锁**（`SET NX PX ttl`），抢到锁的才执行。
- 锁 TTL 设得比任务最长执行时间稍大，并加**看门狗续期**（防止 STW 长 GC 丢锁）。
- 关键业务操作（生成结算单、发券、退款）必须在 DB 层加**唯一约束**兜底（`UNIQUE(distributor_id, month)`），不能只靠代码层控制。
- 长远：迁移到 **XXL-Job** 这类调度中心，由调度中心保证单点分派（这个还在 backlog）。

### 复盘收获
- **多副本是默认**，任何"我只有一台"的假设在云原生下都是错的。
- **唯一约束是 DB 给你的免费保险**，能加就加。
- 代码幂等性是正交的安全网，不能用来替代锁。

---

## 7. 日志刷爆磁盘导致容器被 OOMKilled

### 现象
- 周日凌晨 3 点，4 台 pod **同时**被 123 平台杀掉重启。监控显示 `disk usage 100%`。
- 服务自动拉起后恢复，但日志文件丢了，复盘增加难度。

### 定位过程
1. 重启前的最后几分钟日志保留在 SLS 上（我们把 log 接到了 SLS）。
2. 看 QPS：没异常。
3. 看日志关键字频率：`DEBUG ... Polaris lookup` 这行一秒打了 50 万次。
4. 翻 GitLab：周五那天我合了个 MR，里面把一个 TRPC client 的日志级别从 INFO 改成 DEBUG 用于排查另一个问题——**忘记改回来了**。
5. 周末流量虽然低，但 DEBUG 级别的 Polaris 健康探测日志 1000 QPS，3 天打满 20G 磁盘。

### 根因
- **调试日志合入生产**。
- 日志滚动策略配置松：`logback` 的 `maxHistory=30`、`totalSizeCap=10GB` 被 DEBUG 的量级击穿。

### 止血
- 把日志级别改回 INFO，七彩石一推、JVM 动态 reload。
- 清理老日志。

### 长期修复
- GitLab MR 模板加一项：**"是否修改过日志级别？如有，已改回？"**（复选框）。
- CI 加了一个静态扫描规则：`logback-spring.xml` 里 `<root level="DEBUG">` 或特定 package `DEBUG` 直接拒绝合入 master。
- logback 加 **`totalSizeCap=5GB` + `maxFileSize=500MB`**，硬上限别太宽松。
- 监控新增：**磁盘使用率 > 80% 告警**（之前只监控 CPU / 内存）。

### 复盘收获
- **日志是流量放大器**，DEBUG 在低 QPS 下不明显，量级会欺骗人。
- 事故的根因经常是**"上个改动忘了收尾"**，MR checklist 比技术方案更省事。
- 磁盘也是资源，监控不能只盯 CPU / 内存。

---

## 8. 面试万能模板 · 怎么讲一次事故

面试官问："聊聊你印象最深的一次线上故障？"
别慌，按 **6 段式** 讲，每段 30~60 秒，全程不超过 4 分钟：

1. **背景一句话**："我们有个 XX 服务，日常 QPS 大概几百，部署在腾讯内部的 123 容器平台上。"
2. **现象**："某天几点，告警响了，具体表现是 XXX。"
3. **定位路径**："我先看的是监控 / 日志 / trace，看到 XXX 信号，然后我去 XXX 查，发现 XXX。"
  - 这里要体现**排障顺序**：监控 → 日志 → DB / 下游 → 代码。
4. **根因**："真正的原因是 XXX。其实不是 XXX，而是我们自己 XXX。"
  - 体现**自我反思**：不要甩锅给下游。
5. **止血 + 长期修复**："当时先 XXX 恢复服务，后面我们做了 A / B / C 三件事。"
  - 止血和长期修复**分开讲**，面试官会记住"这人懂事故分级"。
6. **复盘收获（一句话升华）**："这件事让我意识到 XXX。"
  - 例如：配置即契约 / 多副本是默认 / 日志是流量放大器。

### 怎么选故事
| 问题方向 | 选哪段 |
|---|---|
| 配置中心 / 发版类 | #1（七彩石类型写错）、#5（配置先行） |
| 雪崩 / 容量类 | #2（下游超时）、#4（缓存击穿） |
| 内存 / GC 类 | #3（导出 OOM） |
| 并发 / 幂等类 | #6（定时任务重复） |
| 细节 / 运维类 | #7（日志撑爆磁盘） |

### 面试官可能追问
- **"为什么不直接扩容 / 加机器？"** → 答：治标不治本，容量问题背后通常是代码/配置问题；我们这次是先止血再修根因。
- **"你们有没有演练过故障？"** → 答：目前是被动复盘，团队在推混沌工程，还没大规模落地（诚实）。
- **"这次事故持续多久？"** → 有数字就报数字：止血 5 分钟，彻底修复 3 天。

---

## 附：排障手册（登容器常用命令）

> 在 123 平台点进 pod，一个 terminal 开着，这些命令按顺序敲。

```bash
# 1. 看服务是否还活着
curl -s localhost:8080/actuator/health | head

# 2. 看最近报错
tail -n 500 /data/log/vega/app.log | grep -i error | tail -20

# 3. 看七彩石 SDK 当前缓存的配置（确认配置到没到）
cat /data/peacock/config/cache/ywos.solution_vega.config.json | python3 -m json.tool | head

# 4. JVM 状态
jps                                   # 找 pid
jstat -gc <pid> 1s 5                  # GC 频率
jstack <pid> > /tmp/jstack.$(date +%s) # 线程栈
jmap -histo:live <pid> | head -30     # 对象占比

# 5. 看下游
curl -w "\n%{time_total}s\n" http://下游服务/health

# 6. 紧急 dump
jmap -dump:format=b,file=/data/heap.hprof <pid>
# 然后到 123 平台把 /data/heap.hprof 下载回本地
```

---

## 一句话总结

**所有故障都可以写成一个三角形：**
- **外部刺激**（流量涨、下游慢、发版）
- **系统脆弱点**（无熔断、无限流、无锁、配置不对齐）
- **放大器**（重试风暴、日志风暴、toList 全量）

面试时要让面试官看到：你能**分清这三者**，知道该修哪一个，且记得下一次怎么防。
